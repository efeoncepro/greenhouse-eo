import ThemePreviewMockupView from '@views/greenhouse/admin/theme-preview/mockup/ThemePreviewMockupView'

export const dynamic = 'force-dynamic'

export const metadata = {
  title: 'Theme vivo — semánticos AXIS (mockup)'
}

const Page = () => <ThemePreviewMockupView />

export default Page
