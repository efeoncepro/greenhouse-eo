import AxisPaletteMockupView from '@views/greenhouse/admin/axis-palette/mockup/AxisPaletteMockupView'

export const dynamic = 'force-dynamic'

export const metadata = {
  title: 'Paleta AXIS — completa (mockup)'
}

const Page = () => <AxisPaletteMockupView />

export default Page
