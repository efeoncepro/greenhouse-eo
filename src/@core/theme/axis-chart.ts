/** Greenhouse adapter: chart values are portable AXIS data, not product-owned literals. */
import { axisChart } from '@efeoncepro/axis-tokens'

export const axisChartCategorical = axisChart.categorical
export const axisChartCategoricalDark = axisChart.categoricalDark
export const axisChartDirectional = axisChart.directional
export const axisChartDirectionalDark = axisChart.directionalDark
