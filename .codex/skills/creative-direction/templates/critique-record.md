# Registro de juicio — <nombre del trabajo>

> Plantilla del módulo `05_THE_BAR.md`. Una sesión de juicio = un registro. Los dos veredictos se escriben **separados** y nunca se promedian. Al cierre, el campo "Qué se aprende" alimenta `references/CASE_LEDGER.md`.

| Campo | Valor |
|---|---|
| Fecha | `YYYY-MM-DD` |
| Cliente | `Efeonce (interno)` \| `<nombre del cliente>` |
| Trabajo juzgado | Campaña / pieza / sistema — describir qué se miró, no el nombre interno |
| Piezas revisadas | Formatos y canales concretos (ej. film 30", KV, 4 adaptaciones social, audio 15") |
| Etapa | `concepto` \| `ruta` \| `preproducción` \| `ejecución` \| `final` |
| Quién juzga | Nombre y rol |

---

## 1. Plataforma contra la que se juzga

- **Problema de negocio:**
- **Idea central (una línea):**
- **Rol de la marca:**
- **Activos distintivos comprometidos:**
- **Efecto esperado (qué se mueve, cuánto, en qué plazo):**
- **Audiencia real (a quién le llega distraído y desinteresado):**

> Si algún campo está vacío, no hay sesión de juicio: hay que volver al módulo 04 antes de opinar.

---

## 2. Veredicto de efectividad

**Veredicto:** `sí funciona` \| `funciona con condiciones` \| `no funciona`

**Razón (en términos de negocio, defendible ante el cliente):**

| Dimensión | Estado | Nota |
|---|---|---|
| Vínculo de marca (tapando el logo) | `ok` \| `falla` | |
| Activos distintivos (construye vs. rota) | `ok` \| `falla` | |
| Efecto de negocio nombrado | `ok` \| `falla` | |
| Resistencia a baja atención | `ok` \| `falla` | |
| Emoción vs. solo informar | `ok` \| `falla` | |
| Situación de compra | `ok` \| `falla` | |
| Claridad del mensaje central | `ok` \| `falla` | |
| ¿Es *dull*? | `no` \| `sí` | Si es sí: el costo es desperdicio, no daño |

---

## 3. Veredicto de craft

**Veredicto:** `bien hecho` \| `correcto pero genérico` \| `mal ejecutado`

**Razón (oficio, no gusto):**

| Dimensión | Estado | Nota |
|---|---|---|
| Originalidad | `ok` \| `falla` | |
| ¿La idea sobrevivió la traducción? | `sí` \| `no` | |
| Ejecución | `ok` \| `falla` | |
| Propiedad (sin logo, ¿es de esta marca?) | `ok` \| `falla` | |
| Densidad | `ok` \| `falla` | |
| Unidad entre oficios | `ok` \| `falla` | |

---

## 4. Cuadrante y peldaño

- **Cuadrante:** `alto craft · alta efectividad` \| `alto craft · baja efectividad` \| `bajo craft · alta efectividad` \| `bajo craft · baja efectividad`
- **Tratamiento que corresponde al cuadrante (§2 del módulo):**
- **Peldaño de la Ladder — aspira / llega hoy:** `1 Influential Idea` … `6 Enduring Icon`
- **La brecha entre ambos, en una línea:**

---

## 5. Hallazgos

**Qué funciona (nombrado, para proteger en la siguiente ronda):**

**Qué falla (con el eje declarado: efectividad o craft):**

**Qué haría falta (condición de aprobación, no la solución):**

### Derivado a las skills de oficio

| Hallazgo (valor de oficio = se deriva, no se resuelve acá) | Eje | Skill dueña | Bloqueante |
|---|---|---|---|
| | `craft` | `design-studio` \| `motion-design-studio` \| `audio-studio` \| `copywriting` \| `social-media-studio` | `sí` \| `no` |

---

## 6. Decisión

- **Decisión:** `va` \| `va con ajustes` \| `vuelve` \| `se mata`
- **Quién decidió:** `director creativo (Efeonce interno)` \| `cliente (<nombre>)`
- **Si el cliente decidió en contra de la recomendación** — recomendación dada, su razón y el riesgo previsto:
- **Plazo y próximo hito:**

---

## 7. Qué se aprende de esto *(obligatorio)*

> Una o dos líneas transferibles a otro trabajo. No un resumen de la sesión: el patrón. Esto se vuelca a `references/CASE_LEDGER.md` — es la memoria del oficio, y sin ella el mismo juicio hay que volver a tenerlo el mes que viene.
