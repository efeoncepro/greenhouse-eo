---
paths:
  - "services/auth-server/**"
  - "src/lib/auth-server/**"
  - "scripts/auth-server/**"
---

# Auth server propio de Efeonce (TASK-1828 / TASK-1829 / TASK-1830 / EPIC-044) — invariantes (auto-load por path)

Carga **`docs/architecture/EFEONCE_NATIVE_AUTHORIZATION_SERVER_DECISION_V1.md`** (ADR nativo, Accepted 2026-09-03) + contrato OAuth **`docs/architecture/EFEONCE_AUTH_SERVER_OAUTH_CONTRACT_V1.md`** (TASK-1829) + runbook **`docs/operations/runbooks/auth-server.md`** + **`docs/architecture/agent-invariants/IDENTITY_WORKFORCE_AGENT_INVARIANTS.md` → §"Auth server propio"**. Contexto: el emisor `auth.efeonce.org` YA existe como runtime (`services/auth-server`, `node:http`, Cloud Run `us-east4`, single service staging+prod; **en producción desde 2026-09-04** por el release `9100bbd2765d`, revisión `auth-server-00005-pk8`; handler testeable en `app.ts`, cableado en `server.ts`) y es el único emisor de tokens de Efeonce. Sirve `/healthz`, `/readyz` (503 con `AUTH_SERVER_ENABLED=false`; ambos reportan `oauth:<bool>`) y `/.well-known/jwks.json` (llaves `active` + `retiring`, `max-age 300`); y, detrás de **`AUTH_SERVER_OAUTH_ENABLED`** (default `false`; TASK-1829 code complete en `develop` 2026-09-04, rollout pendiente), la superficie OAuth: metadata RFC 8414/OIDC, `/oauth/register` (DCR compat, sólo públicos), `/oauth/authorize` + `/oauth/consent` (PKCE S256; `login_required` hasta TASK-1830), `/oauth/token` (code + refresh rotativo; JWT ES256 15 min con `gv`), `/oauth/revoke`, `/oauth/introspect` — todo 404 con el flag OFF. Dominio `src/lib/auth-server/oauth/**`; tablas `greenhouse_auth.{oauth_clients,cimd_cache,authorization_codes,refresh_tokens,access_tokens,client_consents,oauth_audit_events}`; capabilities `identity.auth_client.register` / `identity.auth_consent.revoke` (módulo `organization`, grant `EFEONCE_ADMIN`) detrás de `POST /api/admin/auth-server/{oauth-clients,consents/revoke}` + CLI `pnpm auth-server:register-client`. Cualquier `Host` fuera del allowlist → 421; errores sanitizados; `captureWithDomain('identity', component=auth-server)`. La llave de firma `auth-server-es256` (EC P-256) vive en Cloud KMS HSM `us-east4/auth-server`; PostgreSQL (`greenhouse_auth.signing_keys` con ≤1 `active` por índice parcial + `signing_key_events` append-only) guarda sólo el JWK PÚBLICO (sin `d`) y el ciclo de vida. Front door: segundo host del LB del gateway MCP (`efeonce-mcp/infra/terraform`, `enable_auth_host`), misma policy Cloud Armor. **TASK-1830** agrega la autenticación de PERSONAS de clientes externos (Efeonce ID) detrás de **`AUTH_SERVER_PERSON_AUTH_ENABLED`** (default `false` en `deploy.sh`; hoy apagado en todos los entornos): magic link por correo (Resend, `emailType` `auth_server_magic_link` con kill-switch en `greenhouse_notifications.email_type_config`), passkeys WebAuthn, TOTP para step-up, sesión por cookie `__Host-efeonce_auth` (sliding 12 h / absoluta 7 d) y landing de invitación `/i/<token>`. Dominio `src/lib/auth-server/persons/**`; 8 tablas nuevas `greenhouse_auth.{sessions,magic_link_tokens,auth_rate_limits,person_auth_attempts,passkey_credentials,passkey_challenges,totp_enrollments,totp_backup_codes}`; capability `identity.auth_person.revoke` (EFEONCE_ADMIN) detrás de `POST /api/admin/auth-server/persons/revoke` (corte de emergencia, idempotente, `reason` ≥10). Recuperación = re-invitación (`reissue`), NUNCA self-service. Las respuestas del magic link son idénticas exista o no el correo (anti-enumeración con piso de latencia), así que un envío roto NO lo reporta nadie. Señales `auth.person.{magic_link_rate_limited,passkey_counter_regression,session_without_link}` (steady 0). Gate: `pnpm auth-server:person-auth:smoke` (PG real + KMS real). Fuera de alcance acá: gateway multi-issuer (1831), canaries (1832), pentest/rotación programada (1833), convergencia del login del portal (1834).

Reglas duras:

1. **NUNCA** exportar, copiar ni loggear la llave privada: no sale de KMS (el adapter firma vía API con CRC32C, DER→JOSE, `kid` RFC 7638 y verificación local obligatoria de cada firma). **NUNCA** material privado en PG, env vars ni Secret Manager.
2. **NUNCA** firmar con una llave que no esté `active`. `retiring` sólo se publica en el JWKS para que los tokens en vuelo sigan verificando.
3. **SIEMPRE** registrar una versión KMS nueva con `pnpm auth-server:rotate-key` (`--status | --register <version> | --retire <kid> [--force]`), NUNCA con `INSERT` a mano. Requiere proxy PG (`GREENHOUSE_POSTGRES_HOST=127.0.0.1`, `GREENHOUSE_POSTGRES_PORT=15432`, `GREENHOUSE_POSTGRES_SSL=false`, `GREENHOUSE_POSTGRES_INSTANCE_CONNECTION_NAME=""`) + `AUTH_SERVER_KMS_KEY`.
4. **SIEMPRE** retirar la versión anterior tras la ventana de solapamiento (≥1 h): `--retire <kid>` + `gcloud kms keys versions disable`. Un `retiring` eterno es superficie extra, no seguridad (hoy: v2 `active`, v1 `retiring` con retiro pendiente).
5. **NUNCA** `gcloud run services update --update-env-vars` a mano en `auth-server`: el SoT es `services/auth-server/deploy.sh` (`--set-env-vars` destructivo: `AUTH_SERVER_ENABLED` —default `true` desde 2026-09-04—, `AUTH_SERVER_ISSUER`, `AUTH_SERVER_ALLOWED_HOSTS`, `AUTH_SERVER_KMS_KEY`, `AUTH_SERVER_OAUTH_ENABLED` —default `false`—, `AUTH_SERVER_ENVIRONMENT_ID`, `AUTH_SERVER_MCP_AUDIENCE`); deploy sólo por `auth-server-deploy.yml` (`RELEASE_DEPLOY_WORKFLOWS`).
6. **NUNCA** editar `managed.domains` del cert del gateway para agregar el host (es ForceNew: re-provisiona `mcp.efeonce.org`). `auth.efeonce.org` tiene host rule + backend `efeonce-auth-server-backend` + cert `efeonce-auth-server-cert` propios.
7. **NUNCA** compartir `NEXTAUTH_SECRET` ni cookies del portal con el emisor: el login de Greenhouse NO cambia. IAM mínimo: runtime `auth-server@efeonce-group` = `cloudkms.signerVerifier` sólo sobre la llave + `cloudsql.client`; deployer `github-actions-deployer@` = `iam.serviceAccountUser` sobre `auth-server@` + `cloudkms.viewer` sobre la llave. Nada a nivel proyecto.
8. `/readyz` 503 con el flag OFF no es fallo. Señales (`src/lib/reliability/queries/auth-server-signals.ts`): `auth.issuer.jwks_unreachable` (runtime; reader en Vercel — `AUTH_SERVER_JWKS_URL` declarada en Production y staging desde 2026-09-04; `not_configured` sólo si falta), `auth.signing_keys.lifecycle` (data_quality) y las tres OAuth `auth.oauth.{code_reuse_detected,refresh_reuse_detected,cimd_rejected}` (incident, 24 h sobre `oauth_audit_events`, steady 0).
9. **NUNCA** prender `AUTH_SERVER_OAUTH_ENABLED` sin la fila del emisor en `greenhouse_core.external_identity_environments` en `active` ni sin validar la metadata publicada (`issuer` idéntico al origen). La fila `efeonce-auth` (`issuer_url=https://auth.efeonce.org`, `issuer_class=external` —inmutable—, `audience=https://mcp.efeonce.org/mcp`) **YA EXISTE en `draft`** (registrada 2026-09-04 por `pnpm auth-server:register-issuer-environment`, que llama al command `upsertExternalIdentityEnvironment` de TASK-1631: tx + audit + outbox; NUNCA SQL); en `draft` el resolver responde `environment_inactive` y todo `token` sería `access_denied`. Pasarla a `active` = mismo CLI con `--status active`, en el mismo momento del flip del flag. Prender = cambiar el default en `deploy.sh` + deploy por workflow; rollback = flag `false`.
10. **NUNCA** consumir un code, rotar un refresh, registrar un cliente o tocar `client_consents` fuera de los commands/store atómicos de `src/lib/auth-server/oauth/**` (`SELECT … FOR UPDATE`; reuso ⇒ revoca la familia + señal); **NUNCA** emitir un access token sin consent `active` para **cada** scope ni sin membership `bound` (`gv` fresco; escritura exige `step_up`); PKCE sólo `S256`; `localhost` por nombre sólo como alias loopback de clientes **públicos**; CIMD siempre con guard anti-SSRF y cache ≤ 24 h; nunca tokens/codes/secrets/IP/`sub` en claro (sólo hashes; audit append-only).
11. **NUNCA** editar `src/lib/auth-server/oauth/pages/efeonce-isotipo.generated.ts` a mano: se regenera desde el SSOT `public/branding/SVG/isotipo-full-efeonce.svg` con `pnpm auth-server:brand-assets:generate` (test de drift `brand-assets.test.ts`). Gates al tocar OAuth: `pnpm vitest run src/lib/auth-server` + `pnpm auth-server:oauth-store:smoke` (PG real).
12. CLIs del dominio (`scripts/auth-server/*.ts`): `pnpm auth-server:rotate-key` (llaves), `pnpm auth-server:register-client` (cliente confidencial), `pnpm auth-server:oauth-store:smoke` (store OAuth contra PG real), `pnpm auth-server:person-auth:smoke` (store de personas contra PG real + llave KMS real: triggers, append-only, tope de passkeys, BYTEA/BIGINT — lo que los mocks NO ejercitan), `pnpm auth-server:register-issuer-environment` (environment del emisor, `--status draft|active`) — los cinco exigen `.env.local` + proxy PG (`pnpm pg:connect`); `pnpm auth-server:brand-assets:generate` no. **NUNCA** reemplazarlos por SQL ni por `gcloud` a mano.
13. **TASK-1830 — frontera y ledger.** **NUNCA** escribir fuera de `greenhouse_auth` desde `src/lib/auth-server/**`: la única escritura legítima en `greenhouse_core` —ligar la persona— se delega al command `acceptExternalInvitation` de TASK-1631, que la ejecuta con su transacción, audit y outbox (guard `boundary-domain.test.ts`, que escanea el SQL literal del dominio; declarar toda tabla nueva ahí en el mismo PR). **NUNCA** mandar los intentos de persona a `greenhouse_serving.auth_attempts`: ese ledger es del PORTAL y no admite este runtime sin romperlo (CHECK cerrados de NextAuth en `provider`/`stage` —sin passkey ni TOTP—, `user_id_resolved` del espacio `client_users`, GRANT de INSERT sólo a `greenhouse_runtime` mientras el emisor conecta como `greenhouse_app`). El ledger del emisor es `greenhouse_auth.person_auth_attempts`, append-only por trigger y con `subject_hash` indexado.
14. **TASK-1830 — hashing.** **NUNCA** bcrypt para un bearer del emisor: todos usan `sha256Hex` + `safeEquals`, igual que codes/refresh/access. Sobre 256 bits de entropía un KDF lento no agrega resistencia y sí 300-800 ms de CPU de un solo hilo en un endpoint NO autenticado (amplificador de DoS en la puerta de entrada); el shim de `bcryptjs` del Dockerfile **se queda**. Corolario: un código de respaldo se hace LARGO (~127 bits) para no necesitarlo, no corto y luego protegido. Única excepción: el secreto TOTP, que es simétrico y va cifrado con la llave KMS **simétrica** `auth-server-totp-envelope` (HSM, `ENCRYPT_DECRYPT`, rotación 90 d, env `AUTH_SERVER_TOTP_KMS_KEY`) con AAD `<environment>|<subject>` — moverlo de fila lo vuelve indescifrable. `auth-server-es256` es EC de firma y **no puede cifrar**.
15. **TASK-1830 — `amr` y step-up.** **NUNCA** derivar `amr` de lo que declare el cliente: `uv` sale del flag REAL de la aserción WebAuthn y `totp` de una verificación real. `authLevel = 'step_up'` exige factor fuerte **y** reciente (<10 min) — en el lane ecosystem el actor es la máquina, así que es el único gate de la cadena que depende de QUIÉN es la persona; no existe «recordar este dispositivo». **NUNCA** pasarle a `verifyAuthenticationResponse` el contador almacenado: LANZA al retroceder y la credencial clonada quedaría VIVA sin disparar la señal; se le pasa `counter: 0` y la política se aplica sobre datos ya verificados. **NUNCA** consumir un magic link por GET (los escáneres de correo lo quemarían) ni pedir `allowCredentials` antes de autenticar por passkey (sería el oráculo de existencia que la anti-enumeración evita).
