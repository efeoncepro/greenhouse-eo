/** Focused CMS-logo release. Requires a fresh read-only hash inventory; excludes the shared loader. */
const fs=require('node:fs'),path=require('node:path'),crypto=require('node:crypto'),{execFileSync}=require('node:child_process');
const root=path.resolve('../efeonce-public-site-runtime/wp-content/plugins/eo-elementor-widgets'),baseline=JSON.parse(fs.readFileSync(process.argv[2],'utf8'));
const files=['includes/content-marketing/schemas/editorial.json','includes/content-marketing/templates/editorial.html','assets/css/content-marketing-host.css','assets/js/content-marketing.js',...['wordpress.png','webflow.svg','drupal.svg','modyo.svg'].map(x=>'assets/img/content-marketing/cms/'+x)];
const out=path.resolve('tmp/content-marketing-cms/release');fs.mkdirSync(out,{recursive:true});const manifest={contract:'content-marketing-elementor-release.v1',purpose:'Native CMS brand group requested by operator',files:files.map(p=>{if(!Object.hasOwn(baseline.files,p))throw Error('Missing baseline: '+p);return{path:p,sha256:crypto.createHash('sha256').update(fs.readFileSync(path.join(root,p))).digest('hex'),previousSha256:baseline.files[p]};})};
const zip=path.join(out,'package.zip');if(fs.existsSync(zip))fs.unlinkSync(zip);execFileSync('zip',['-q',zip,...files],{cwd:root});fs.writeFileSync(path.join(out,'manifest.json'),JSON.stringify(manifest,null,2)+'\n');console.log(JSON.stringify({files:files.length,out}));
