# Greenhouse — Feature Flag State Ledger (env-var flags)

> **Tipo de documento:** Ledger operativo vivo (SSOT del ESTADO de los env-var flags)
> **Creado:** 2026-06-18 por Claude (TASK-1079 follow-up)
> **Última actualización:** 2026-07-05 (TASK-1327 Brand Visibility live + TASK-1341 DataForSEO AIO worker guard pendiente)
>
> **Delta 2026-07-05 (TASK-1341 — DataForSEO AIO drift en `ops-worker`):** el lead magnet público `/brand-visibility` ya ejecuta submit→run→report, pero Google AI Overview quedó degradando en producción como `skipped:missing_secret` en observaciones recientes porque el runtime que ejecuta los runs async es Cloud Run `ops-worker` y la revisión detectó falta de `DATAFORSEO_API_LOGIN` en la revisión efectiva. Vercel env puede estar correcto y aun así ser inerte para runs async. **Estado operativo:** el modo público `light` solicita `google_ai_overview`; si el worker no tiene login + `DATAFORSEO_API_PASSWORD_SECRET_REF`, el informe queda `partial` de forma honesta. **Follow-up formal:** `docs/tasks/to-do/TASK-1341-dataforseo-aio-runtime-config-guard.md` agrega guard/preflight + smoke provider-scoped para aceptar sólo `succeeded` o `skipped:no_ai_overview_block`, nunca `missing_secret`. Revert <5 min: apagar `GROWTH_AI_VISIBILITY_GOOGLE_AIO_ENABLED` o corregir y redeployar `ops-worker`.
>
> **Delta 2026-07-04 (TASK-1333 — root cause categoría/sentiment `unknown` = `LLM_EXTRACTION` OFF en el worker):** el `categoryTaxonomySummary.status=unknown` (y sentiment `unknown`) en TODOS los runs reales tenía como causa raíz un **drift worker-vs-Vercel**: `GROWTH_AI_VISIBILITY_LLM_EXTRACTION_ENABLED` estaba **ON en Vercel** (prod/staging/dev, flip 06-30) pero **`false` en el `ops-worker`** — que es donde ejecutan los runs async (`ASYNC_EXECUTION` ON). La extracción de prosa (que puebla `categoryAssociations` + `sentimentLabel` + `messageDriftClaims` + `brandRank`) sólo corre si el flag está en el runtime que ejecuta el run → con el worker OFF, los 12 runs quedaron con `category_associations` vacío y sentiment `unknown`. Taxonomy/mapper/builder ya funcionaban; **cero código a tocar** (el arch review lo predijo). **Fix (2026-07-04):** `gcloud run services update ops-worker --update-env-vars` prendió los 3 GROWTH que estaban OFF — `LLM_EXTRACTION_ENABLED=true`, `OPERATOR_SEND_ENABLED=true`, `CATEGORY_GUARD_ENABLED=true` (revs `00454-9lb` → `00455-8f7`). **Verificado:** run real con extracción ON → categoría canónica poblada (`sector:banking_insurance`, `category:digital_agency`) + sentiment (`neutral`/`positive`), vs 0 antes. Credencial presente (`ANTHROPIC_API_KEY_SECRET_REF`). **Persistencia (decisión del operador 2026-07-04 "hagamos todo lo necesario"):** `services/ops-worker/deploy.sh` persiste los **3** flags en ON en ambos bloques (staging + prod): `LLM_EXTRACTION` (`:-true`, era el único del grader-stack olvidado en `:-false`), `OPERATOR_SEND` (`true` en ambos bloques) y `CATEGORY_GUARD` (`true` en ambos bloques). El gate original de OPERATOR_SEND/CATEGORY_GUARD (falso-0 para marcas no-agencia) quedó cerrado con **EPIC-021** (motor brand-aware live, ISSUE-110 resuelto). ⚠️ **RIESGO RESIDUAL de CATEGORY_GUARD ON en prod:** una marca cuya categoría no resuelva ni con el grounded read queda **bloqueada** (`aeo_category_unresolved`) — revert = flag a false; señal `growth.ai_visibility.profile_category_unresolved`. Toma efecto en el **próximo deploy del worker** (push develop). **Regla reforzada:** los flags que gatean el path async del grader (extracción/probes/providers/guard) DEBEN estar ON en el `ops-worker`, no sólo en Vercel; el valor de Vercel es inerte para los runs async.
>
> **Delta 2026-07-01 (TASK-1246 — reconciliación de readiness + sign-off legal):** re-verificados los **valores reales** de los flags del lead magnet en Production vía `vercel env pull` (no solo presencia): `GROWTH_AI_VISIBILITY_PUBLIC_INTAKE_ENABLED`, `_GRADER_ENABLED`, `_ASYNC_EXECUTION_ENABLED`, `GROWTH_FORMS_PUBLIC_API/SERVER_VALIDATION/PII_ENCRYPTION/EMAIL_VERIFICATION_ENABLED` y `GROWTH_GRADER_INTAKE_ON_FORMS_ENGINE_ENABLED` = **`true` confirmado** (las filas por-fila que aún dicen "prod: OFF" quedan superseded por esta Delta + el Delta 06-30). **Sign-off legal del consent/aviso de privacidad (Ley 21.719) = CONFIRMADO por el operador** (prerequisito abierto de la fila `PUBLIC_INTAKE` → cerrado). **Pendiente real (no greenhouse-eo o cross-task):** rotar `TURNSTILE_SECRET` (operador, secret fresco de Cloudflare), smoke E2E de la cara pública (repo `efeonce-web`), y cierre formal de TASK-1253/1255 (backfill PII legacy + job de retención). Mitigante (verificado en código): el grader self-serve (`fdef-ai-visibility-grader`) NO está embebido en ninguna superficie pública (no hay page en greenhouse-eo; `/aeo-2/` es el form comercial `efeonce-aeo-diagnostic`→HubSpot, que NO corre el grader; no existe en efeonce-web) → 0 tráfico self-serve real. El render efeonce-web del ADR 06-28 está sin construir/irreal.
>
> **Delta 2026-07-02 (AEO reference conversion copy + TASK-1318 full-name split):** la landing pública `/aeo-2/` mantiene el renderer `<greenhouse-form form-key="b120566a-dd1a-43c8-956a-4e0121e805b8" surface="fhsf-efeonce-aeo-diagnostic" locale="es-CL" color-scheme="light" appearance="bare">` contra `https://greenhouse.efeoncepro.com/growth-forms/renderer-latest.js`. AEO v8 `fver-38d38bbc-6a32-4e2c-bbd7-c0f0fc728c63` declara `style_variant=diagnostic_premium`, `security.captcha`, CTA `Empezar con mi diagnóstico →`, field visible `fullName` label `Nombre completo`, `autocomplete=name`, `validation_schema.namePolicy.split_full_name`, placeholders `Selecciona tu país` / `Selecciona un rango` y destination mapping server-side `firstName -> firstname` / `lastName -> lastname` sin `fullName` hacia HubSpot. El runtime que deriva los nombres está released en production desde `398b7f1b` (orchestrator `28608774955`); `activate-aeo-reference-copy --apply` ahora bloquea si `origin/main` no trae ese runtime salvo override explícito. Elementor `convers` usa el copy de referencia (`Descubre en qué nivel estás hoy — y empieza a subir.`); backup `_gh_backup_before_aeo_conversion_reference_copy_20260702T093257Z`, Kinsta purge, `heroans` estable. Gate live: `pnpm public-website:verify-aeo-live-contract`.
>
> **Delta 2026-06-30 (parity Growth/AEO a staging + development — "prendamos en todas partes donde corresponda", EPIC-021):** se alineó el set Growth/AEO de los `non-production` con prod. **staging (custom env = develop, el que usan):** estaba 27/32; se agregaron los **5 del motor EPIC-021 que faltaban** — `GROWTH_AI_VISIBILITY_OPERATOR_SEND_ENABLED`, `CATEGORY_GUARD_ENABLED`, `LLM_EXTRACTION_ENABLED`, `PROSE_EXTRACTION_SHADOW_ENABLED`, `REGRADE_ENABLED` → **32/32** (perplexity/google_aio ya estaban pre-existentes en staging). Esto corrige el `operator_send_enabled=false` que el doc de TASK-1291 veía en staging. **Redeploy staging `greenhouse-bt9fvga8d`** (los env vars no se activan en caliente). **development:** estaba 13/30; se completaron los 17 restantes → **30/30**. **Hygiene call (riesgo de creds expuestas):** `GROWTH_AI_VISIBILITY_PERPLEXITY_ENABLED` y `GOOGLE_AIO_ENABLED` se **dejaron OFF en development** (NO se replicaron) hasta la rotación de credenciales (TASK-1293); el grader degrada por-provider con gracia. En staging ya estaban (pre-existentes), no se removieron. Verdad live: `vercel env ls staging|development`.
>
> **Delta 2026-06-30 (segundo batch — "prende todas las que faltan", autorización explícita + riesgos aceptados):** redeploy prod `greenhouse-psl47zug8`. Prendidos en Production:
> - **Producto/UX/search:** `KNOWLEDGE_SEARCH_HYBRID_ENABLED`, `KNOWLEDGE_SEARCH_RERANK_ENABLED`, `NEXA_INTERACTION_LANE_ENABLED` (+`NEXT_PUBLIC_*` — ⚠️ necesita BUILD FRESCO para surfacear client-side, el redeploy reusa build), `NEXA_FORCE_KNOWLEDGE_RETRIEVAL_ENABLED`, `NEXA_KNOWLEDGE_SYNTHESIS_BRIEF_ENABLED`, `NEXA_ANSWERS_CANVAS_LENS_ENABLED`, `HOME_V2_ENABLED` (cambia el home de todos), `NEXA_QUOTE_AUTHOR_ACTION_ENABLED` (write gobernado).
> - **Kortex bridge (autorización explícita, incl. cross-platform execution):** `GREENHOUSE_SISTER_PLATFORM_OAUTH_ENABLED`, `KORTEX_COMMAND_ADAPTER/ADMIN/LIVE_EXECUTE_ENABLED`, `KORTEX_GITHUB_COMMANDS/WORKFLOW_DISPATCH_ENABLED`. (El auto-mode classifier frenó el primer batch masivo por `LIVE_EXECUTE`/`WORKFLOW_DISPATCH`; el operador re-autorizó explícito.)
> - **Comparison-table:** `PUBLIC_SITE_COMPARISON_TABLE_WRITES_ENABLED` ON + 4 vars del bridge WordPress replicadas de staging a prod (USERNAME/BASE_URL=efeoncepro.com/2 SECRET_REF; ambos secrets existen en Secret Manager). Writes draft-only al sitio público live; WP `writes_enabled` ya estaba ON (staging usaba el mismo sitio).
> - **⚠️ ALTO RIESGO (operador asumió):** `PPM_POSITION_ENABLED` ON → **publica la línea PPM del F29 con tasa placeholder 0.25% SIN validar contable** (número fiscal posiblemente errado como oficial). `ATTRIBUTABLE_LATENESS_OTD_ENABLED` ON (ops-worker `00432-9nf` + Vercel) → **afecta el bono de payroll** sin el shadow ≥30d + sign-off HR (TASK-1170). `NOTION_OTD/RPA/FTR_WRITEBACK_ENABLED` (+`_EFEONCE`/`_SKY`) + `NOTION_DUE_DATE_CAPTURE_ENABLED` ON (ops-worker `00432-9nf` + Vercel) → **escribe a los Notion de clientes** (Efeonce+Sky); el OTD requiere la property `[GH] OTD` creada en Notion o **da error client-facing**. Flags del worker son **efímeros** (deploy.sh declara defaults; próximo push develop los resetea).
>
> **Delta 2026-06-29 (release develop→main `056c2dde8`, EPIC-020/021 + finance/Q2C):** se prendieron en Production **`COMMERCIAL_Q2C_CANONICAL_CLOSE_ENABLED`** (cierre canónico Q2C) y **`RETENTION_POSITION_ENABLED`** (línea retenciones F29) con sign-off del operador (migraciones ya en la DB compartida `greenhouse-pg-dev`).
>
> **Delta 2026-06-30 (flip masivo Growth/AEO en Production, autorización explícita del operador + RIESGO ACEPTADO):** a pedido del operador se prendieron en **Vercel Production** TODOS los flags del AI Visibility Grader / lead magnet EPIC-020/021 + motor Growth Forms (29 flags: `GROWTH_AI_VISIBILITY_*` GRADER/OPENAI/ANTHROPIC/PERPLEXITY/GEMINI/GOOGLE_AIO/LLM_EXTRACTION/ASYNC/PROBES/AGENTIC/ENTITY/FIX_IT/PUBLIC_INTAKE/REPORT_EMAIL/LEAD_HANDOFF/OPERATOR_SEND/PORTAL_RUN/TRIAL/REGRADE/ARCHETYPE_PROMPTS/PROMPT_AUTHORING/BRAND_INTELLIGENCE + `GROWTH_GRADER_INTAKE_ON_FORMS_ENGINE_ENABLED` + `GROWTH_FORMS_PUBLIC_API/CATALOG/SERVER_VALIDATION/EMAIL_VERIFICATION/PII_ENCRYPTION` + `GROWTH_SEARCH_CONSOLE_ENABLED`). Redeploy prod `greenhouse-ic8cg4ery`. Verdad live: `vercel env ls` (los snapshots por-fila abajo quedan stale — esta Delta + `vercel env ls` mandan).
> - **⚠️ RIESGO DE SEGURIDAD ACEPTADO POR EL OPERADOR:** `GROWTH_AI_VISIBILITY_PERPLEXITY_ENABLED` y `GROWTH_AI_VISIBILITY_GOOGLE_AIO_ENABLED` quedaron ON en prod usando credenciales **expuestas en chat/captura** (`greenhouse-perplexity-api-key` + password DataForSEO). El operador autorizó explícitamente correr con creds comprometidas. **Pendiente: rotar ambas credenciales.** Revert <5 min: `vercel env rm` de esos 2 flags.
> - **CATEGORY_GUARD + PROSE_SHADOW también prendidos (2026-06-30, a pedido del operador pese a la advertencia):** `GROWTH_AI_VISIBILITY_CATEGORY_GUARD_ENABLED` ON en Vercel prod **y** en el ops-worker (rev `00431-m5l`, vía `gcloud --update-env-vars`; ⚠️ EFÍMERO — el `deploy.sh` lo declara `false` por defecto, el próximo push develop lo resetea; persistir = editar `deploy.sh`). El guard ON **bloquea runs con categoría no resuelta** (~13% cobertura determinista). `GROWTH_AI_VISIBILITY_PROSE_EXTRACTION_SHADOW_ENABLED` ON en Vercel prod (eval-only, no toca runs).
> - **Provisioning resuelto:** (1) ~~`TURNSTILE_SECRET` no está en prod~~ **RESUELTO 2026-06-30:** secret key del widget Cloudflare (modo Invisible) cargado en Vercel prod + redeploy `greenhouse-btvf0v9r6`; el intake público sale del fail-closed. ⚠️ **La secret key quedó EXPUESTA en captura de chat → ROTAR** (el operador confirmó que rota después). Site key `0x4AAAAAADqwX2R7v-k9pItv` (pública) vive en el frontend del sitio público efeoncepro.com. (2) ~~la property HubSpot `aeo_check_result` sin provisionar~~ **RESUELTO 2026-06-30:** gcloud reautenticado (camino canónico `gcloud auth login` + `application-default login`); el dry-run de `provision-ai-visibility-hubspot-properties.ts` confirma que `companies.aeo_check_result` + las 8 `ai_visibility_*` + grupo `aeo` **ya existen** en el portal HubSpot prod (48713323) → operator-send NO dará 400. (3) DUAL-LOCATION: el `ops-worker` compartido ya tiene los flags Growth ON (config staging), así que el lado worker queda cubierto.
> - **Growth Forms público AEO conectado (2026-06-30; renderer live 2026-07-01; full-name split 2026-07-02):** se creó y publicó el form gobernado `efeonce-aeo-diagnostic` + surface `fhsf-efeonce-aeo-diagnostic`, destination HubSpot secure-submit al form `AEO - Lead Form` (`8649e76c-8b01-41f3-9b0c-5713d7b4dba6`). Vercel prod agrega CORS en `GET/POST/OPTIONS` para `efeoncepro.com`; smoke live: `GET` contrato 200 con ACAO, `POST` sin token 403 `captcha_failed/missing_token` con ACAO. Desde TASK-1294 el renderer `<greenhouse-form>` soporta Turnstile invisible y emite `captchaToken`; desde TASK-1298 la landing `/aeo-2/` usa ese renderer live por `form-key`; desde TASK-1318 usa v8 `fver-38d38bbc-6a32-4e2c-bbd7-c0f0fc728c63` con `style_variant=diagnostic_premium` y split server-side de `Nombre completo`.
> - **`PUBLIC_SITE_COMPARISON_TABLE_WRITES_ENABLED` sigue OFF en prod**: requiere bridge WordPress en prod (`PUBLIC_WEBSITE_WORDPRESS_*` solo en staging) + `writes_enabled=true` en efeoncepro.com.
> - Nota: el `ops-worker` (servicio compartido) conserva GIT_SHA `83dd8d663` (tree-idéntico al release; orquestador hizo skip code-identical) — estado steady del servicio compartido, no drift de código.
>
> **Delta 2026-06-22 (TASK-1210, release develop→main `3a39c68ba`, sign-off CEO):** los 8 flags MXN+CLF — `FINANCE_CORE_MXN_ENABLED`, `NUBOX_EXPORT_FOREIGN_CURRENCY_ENABLED`, `FINANCE_MXN_PAYMENT_ORDERS_ENABLED`, `FINANCE_MULTI_CURRENCY_REPORTING_ENABLED`, `FINANCE_CORE_CLF_INDEXED_ENABLED`, `FINANCE_CLF_INCOME_PROJECTION_ENABLED`, `FINANCE_CLF_OBLIGATIONS_ENABLED`, `FINANCE_CLF_REPORTING_ENABLED` — pasaron a **ON en producción**: Vercel prod (agregados + redeploy `greenhouse-midjr78bo`) + ops-worker Cloud Run (persistente via `services/ops-worker/deploy.sh`, `:-true`). Los `*_BACKFILL_APPLY_ENABLED` (gates de script) siguen OFF. Verdad live: `vercel env ls` + `gcloud run services describe ops-worker`.
> **Doc relacionado:** [GREENHOUSE_FEATURE_FLAGS_ROLLOUT_PLATFORM_V1.md](../architecture/GREENHOUSE_FEATURE_FLAGS_ROLLOUT_PLATFORM_V1.md) (los flags PG declarativos — mecanismo distinto, ver abajo)

## Por qué existe este doc

El desarrollo agéntico deja **deuda cognitiva**: una feature se implementa detrás de un flag `*_ENABLED` default-OFF, pasa todos los gates, se mergea… y **el flag nunca se prende** en staging/prod porque nadie lo registró en un lugar encontrable. Este ledger es ese lugar: un vistazo único de **qué flags están prendidos dónde** y **qué queda pendiente de prender**.

Es **encontrable** desde: `CLAUDE.md` (Runtime Rollout Completion Gate), `AGENTS.md`, y el índice de `docs/operations/`.

## Dos mecanismos de flags (no confundir)

| Mecanismo | Dónde vive el estado | Cómo se prende | Gobierna |
|---|---|---|---|
| **Env-var flags** (`*_ENABLED`) — **este doc** | Vercel env vars (por environment) + `.env.local` | `vercel env add` + **redeploy** | Casi todo: features de runtime, integraciones, rollouts graduales sin tabla |
| **PG rollout flags** (`home_rollout_flags`) | PostgreSQL `greenhouse_serving.home_rollout_flags` | Admin endpoint, **sin redeploy** (resolver con cache 30s) | Variantes de shell/home + kill-switches per-block con scope `user>role>tenant>global` |

> Este ledger cubre **los env-var flags**. Para los PG flags, ver el platform doc. Un mismo dominio puede usar ambos.

## ⚠️ Reglas duras

- **NUNCA** declarar un env-var flag nuevo (`*_ENABLED`) sin agregar una fila a **§ Inventario** y, si queda code-complete pero sin prender, a **§ Pendientes de acción** — en el mismo PR. **Enforcement mecánico:** `pnpm docs:closure-check` corre `feature-flags-audit --strict` y **falla (exit 1) si hay un flag en código sin registrar acá** → ningún cierre/closure pasa con un flag sin documentar.
- **NUNCA** considerar un flag "rolled out" hasta verlo en el environment correcto (`vercel env ls`) **+ redeploy aplicado**. `code complete` ≠ `operationally complete` (ver Runtime Rollout Completion Gate en `CLAUDE.md`).
- **NUNCA prender un flag sólo en Vercel.** Un `*_ENABLED` se lee en **cada runtime que ejecuta el código gateado**, y Greenhouse tiene 5 runtimes con env vars independientes: **Vercel** (app Next.js) y 4 servicios **Cloud Run** (`ops-worker`, `commercial-cost-worker`, `ico-batch-worker`, `hubspot-greenhouse-integration`). Prenderlo en uno NO lo prende en los otros. **SIEMPRE** ejecutar el paso 0 de §`Cómo prender un env-var flag` (mapear runtimes ANTES de prender) y declarar en la fila del ledger **en qué runtime(s) vive**. Un flag que gatea una **projection reactiva / consumer / cron** corre en el **ops-worker**, NO en Vercel — prenderlo en Vercel no hace absolutamente nada. Caso fuente 2026-07-09: `GROWTH_EBOOK_EMAIL_DELIVERY_ENABLED` (email de entrega del ebook) vive sólo en el `ops-worker`; el runbook de este doc sólo enseñaba `vercel env add`, y prenderlo ahí habría dejado el email muerto con la success card prometiéndoselo al usuario.
- **NUNCA prender un flag de Cloud Run sólo con `gcloud run services update --update-env-vars`.** Los `deploy.sh` de los workers usan **`--set-env-vars` (destructivo)**: reescriben el set completo, así que una var agregada out-of-band **desaparece en el siguiente deploy, en silencio**. El SoT es **`services/<worker>/deploy.sh`** (declarar el flag ahí) + aplicar en vivo para efecto inmediato. **Los dos pasos, siempre.** Caso fuente 2026-07-10: el flag del ebook se prendió en la revisión `00470`, la `00473` lo borró, y el consumer reactivo registró `skip: flag OFF` — el ledger decía ON y la realidad era OFF. Un flag "prendido" que no está declarado en `deploy.sh` es una bomba de tiempo, no un rollout.
- **SIEMPRE** que prendas/apagues un flag en un environment, actualizá la **§ Snapshot** (con fecha) y, si cerró un pendiente, sacá la fila de **§ Pendientes de acción**.
- **NUNCA** confíes en este doc como verdad live para una decisión crítica — la **verdad live es `vercel env ls`**. Este doc es el ledger humano (intención + pendientes + último snapshot conocido).
- Para flags `NEXT_PUBLIC_*`: se hornean en el bundle **en build time** → prenderlos requiere un **build fresco** (push o redeploy con build cache desmarcado), no un redeploy que reusa build.

---

## § Pendientes de acción (la parte que se olvida)

> Flags **code-complete** esperando un flip. Esta es la cola anti-deuda-cognitiva. Sacá la fila cuando el flip esté aplicado + verificado.

| Flag | Owner | Estado actual | Acción pendiente | Nota |
|---|---|---|---|---|
| `HIRING_PUBLIC_APPLICATIONS_ENABLED` | TASK-1367/TASK-354 | **staging + Production: env vars SET 2026-07-09** (`HIRING_PUBLIC_APPLICATIONS_ENABLED=true` + `NEXT_PUBLIC_TURNSTILE_SITE_KEY`; `TURNSTILE_SECRET` presente en ambos). **Production build/release completo inicial** en `433cfa2b0fd3` (`production-release.yml` run `28991488376` success); **release acoplado del fix robusto de inferencia/responsabilidades** en `915be02a86abfd49c71365af8a647f9fdfa35207` (PR #151, manifest `915be02a86ab-7c6aa11e-b9c1-4990-8086-cdfacb3a763b`, runtime verde; `ops-worker` residual documentado como label drift si diff runtime vacío). **Opening real publicado:** `EO-OPN-0009` (`Account Manager / Especialista en Marketing`) creado vía writers canónicos el 2026-07-09. | (1) smoke público con browser/Turnstile: submit → `hiring_application` creada + dedupe genérico; (2) tras smoke, sacar esta fila de pendientes y dejar solo § Snapshot. | Gatea el endpoint público de postulación. Default OFF → **404 invisible** (no revela que existe). Anti-abuse: Turnstile + rate-limit (5/email/día, 20/IP/día) vía `hiring_application_intake_events`. Respuestas siempre genéricas (duplicado → mismo `accepted` 202). La careers UI (TASK-354) es el consumer. |
| `ASSET_MALWARE_SCAN_ENABLED` | TASK-1362 | **Código LIVE en producción desde el release `4e7e9093d` (2026-07-10)**; el flag sigue **OFF/default en todos los environments** (verdad live: `vercel env ls`) — **por diseño**, no es un pendiente de rollout. El escáner **`structural` corre SIEMPRE**, flag o no: es lo que hoy impide que un binario disfrazado de PDF entre al bucket privado. Adapter `clamav-http` **code-complete, NUNCA ejercitado contra un servicio real**. | **Owner de la decisión: `TASK-1378`** (`docs/tasks/to-do/TASK-1378-clamav-malware-scanner-provisioning-decision.md`). (1) Decidir si se provisiona ClamAV. **ClamAV es GPL (gratis); lo que se paga es Cloud Run**, en la misma cuenta GCP `efeonce-group`. Siempre-caliente (`min-instances=1`, ~2 GiB para la base de firmas; no escala a cero sin romper el submit público) ≈ **USD 19-25/mes**, contra **USD 7,32/30d** que cuesta hoy TODO Cloud Run del proyecto. Scanners de terceros por API descartados: sacarían el CV del candidato del perímetro (Ley 21.719). (2) Si se provisiona: desplegar el servicio, `vercel env add ASSET_MALWARE_SCAN_ENABLED Production` + `ASSET_MALWARE_SCAN_ENDPOINT` + redeploy; **ejercitar el flujo real** (PDF limpio → `attached`; EICAR → `infected` + `quarantined`) — que la env var exista NO prueba que el consumer funcione. | **Runtime: Vercel** (el upload público de CV corre en el route handler de Next; ningún Cloud Run sube assets de usuario). Gatea SÓLO el adapter de firmas. **Fail-closed**: flag ON sin `ASSET_MALWARE_SCAN_ENDPOINT` emite veredicto `error` (bloqueante), nunca degrada en silencio a "sin antivirus". |
| `HIRING_ASSESSMENT_AI_ENABLED` | TASK-1361 | OFF/default en todos los environments (verdad live: `vercel env ls`) · **code-complete + gated** | (1) correr el eval baseline REAL `pnpm tsx --require ./scripts/lib/server-only-shim.cjs scripts/hiring/assessment-scoring-eval.ts` (requiere `greenhouse-anthropic-api-key` + ADC) y verificar MAE/tolerancia/correlación verdes vs el dataset curado; (2) `vercel env add HIRING_ASSESSMENT_AI_ENABLED staging` + redeploy → habilita los propose paths (generar preguntas / sugerir puntaje); (3) prod tras sign-off HR/Legal (hiring-AI = alto riesgo EU AI Act). | Gatea SOLO los propose paths (`proposeQuestionsForCompetency` / `proposeScoreForResponse`). El confirm/reject de propuestas existentes NO se gatea (un humano siempre drena la cola). La IA PROPONE evidencia; el humano confirma vía `confirmAiProposal` — el LLM nunca escribe banco/score. Default OFF → los propose devuelven 409 `assessment_ai_disabled`. |
| `GROWTH_EBOOK_EMAIL_DELIVERY_ENABLED` | TASK-1375 | **ops-worker (prod): ON — 2026-07-10**, revisión `ops-worker-00474-jrf` + **declarado en `services/ops-worker/deploy.sh`** (SoT). ⚠️ El primer flip (2026-07-09, rev `00470-898`) se hizo sólo con `--update-env-vars` y la rev `00473` lo BORRÓ (`deploy.sh` usa `--set-env-vars` destructivo): el consumer reactivo registró `skip: flag OFF` en la primera submission real y el email nunca salió. El código de la projection + el template entraron con el release `41aefb457`. Base URL: NO se setea env → `publicBaseUrl()` cae al default correcto `https://greenhouse.efeoncepro.com`. Infra de email confirmada en el worker (`RESEND_API_KEY` + `RESEND_API_KEY_SECRET_REF` + `EMAIL_FROM`), misma maquinaria ya probada en prod por `GROWTH_AI_VISIBILITY_REPORT_EMAIL_ENABLED`. | (1) **smoke humano pendiente**: submit real en `think.efeoncepro.com/web-agentica` con correo corporativo → verificar que llega el email con el link gated. NO es automatizable: el Turnstile invisible del form bloquea browsers automatizados (comportamiento correcto), así que el submit aceptado sólo lo produce una persona. (2) Tras el smoke, sacar esta fila de pendientes y dejar sólo § Snapshot. | Email de respaldo del ebook lead magnet (consumer reactivo `growth_ebook_delivery`, drenado por `ops-reactive-growth`). **Flujo crítico**: la success card le promete el email al usuario, por eso NO debe quedar OFF en prod. **Genérico por ebook** (lee el contenido del `success_behavior` del form). El email lleva el LINK gated, NUNCA adjunta el PDF (9 MB). |
| `PPM_POSITION_ENABLED` | TASK-1189 | OFF/default en todos los environments (verdad live: `vercel env ls`) · **code-complete + shadow** | (1) **captura de la tasa PPM real** del contribuyente con el contador (hoy seed placeholder 0.25% en `ppm_rate_config`) + **validación contable** de la cifra shadow (19 períodos, ej. 2026-06 = 14.500 CLP); (2) flip del flag; (3) redeploy ops-worker si se agrega materialización reactiva. | Línea PPM del F29 (child A de TASK-1186). Default OFF → el endpoint `GET /api/finance/ppm/monthly-position` marca `enabled:false` (shadow). La tasa vive en la SSOT `ppm_rate_config` (actualizable sin deploy); el flag gatea la exposición "oficial". |
| `RETENTION_POSITION_ENABLED` | TASK-1188 | **staging: ON** (2026-06-20, toma efecto en el redeploy del push) · prod: OFF | (1) **prod queda gated en validación contable** de la cifra de retenciones (2026-05 = 242.623 CLP, 2026-06 = 138.646 CLP) vs el F29 real; (2) tras sign-off, `vercel env add RETENTION_POSITION_ENABLED Production` + redeploy; (3) redeploy del ops-worker si se agrega materialización reactiva. | Línea de retenciones del F29 (child B de TASK-1186). Default OFF → el endpoint `GET /api/finance/retention/monthly-position` marca `enabled:false` (shadow), nadie trata la cifra como oficial. La data shadow existe en `retention_monthly_positions` (materializada manualmente). |
| `NEXA_INTERACTION_LANE_ENABLED` + `NEXT_PUBLIC_NEXA_INTERACTION_LANE_ENABLED` | TASK-1079 | **staging:** env vars SET (2026-06-18), **redeploy pendiente** · prod: OFF | (1) redeploy de staging con **build fresco** (NEXT_PUBLIC se hornea en build) → habilita "Lateral" en el menú de modo de Nexa. (2) prod = decisión del operador tras sign-off | Lane sidecar de Nexa (concepto C). Default-safe: solo habilita la opción, el default sigue siendo el flotante. |
| `NOTION_DUE_DATE_CAPTURE_ENABLED` (M0) + `ATTRIBUTABLE_LATENESS_OTD_ENABLED` (M2) | TASK-921/922 (consumido por TASK-1169 → TASK-1170) | OFF/default por environment (verdad live: `vercel env ls`) | **NO bloquea TASK-1169** (su materializador/reconciliación/signal corren sin flag, shadow). Pero el OTD-imputable member×month tiene cobertura de freeze casi nula sobre la cohorte productiva hasta que estos flags estén ON + se backfillee el M2 shadow sobre la cohorte. Prender + backfill es prerequisito del reloj ≥30d y del cutover **TASK-1170**, no de esta task. | Dependencia de rollout documentada en ADR `GREENHOUSE_ATTRIBUTABLE_LATENESS_V1` §16.11 + Handoff. El flip productivo del bono es TASK-1170 (gateado, ≥30d shadow + sign-off). |

| `NOTION_OTD_WRITEBACK_ENABLED` + per-cliente `_EFEONCE` / `_SKY` | TASK-927 | OFF/default en todos los environments (verdad live: `vercel env ls`) · **code complete, rollout pendiente** | Writeback `[GH] OTD` a Notion (display-only, client-facing). Activación gateada al operador: (1) crear la propiedad `[GH] OTD` (select, read-only) en Notion Efeonce + Sky; (2) redeploy del ops-worker (registra el Cloud Scheduler `ops-otd-writeback` + endpoint `/otd/writeback`); (3) flip del flag per-cliente con el gate `delivery.attributable_lateness.shadow_terminal_open` en steady=0. | Display-only, NUNCA toca el bono. Default OFF → cero writes hasta activar. El batch es no-op con flag OFF aunque el job exista. Spec: TASK-927 + OTD_V1 §Delta 2026-06-20. |

| `FINANCE_CORE_CLF_INDEXED_ENABLED` + `FINANCE_CLF_INCOME_PROJECTION_ENABLED` + `FINANCE_CLF_OBLIGATIONS_ENABLED` + `FINANCE_CLF_REPORTING_ENABLED` + `FINANCE_CLF_BACKFILL_APPLY_ENABLED` | TASK-995 | OFF/default en todos los environments (verdad live: `vercel env ls`) · **code-complete + gated** | (1) `FINANCE_CORE_CLF_INDEXED_ENABLED=true` (master) + `FINANCE_CLF_INCOME_PROJECTION_ENABLED=true` en staging → habilita la rama CLF de los materializers (cotización/HES/**OC de cliente en UF** → income CLP + plano native UF + snapshot CLF→CLP); (2) validar con una OC de cliente en UF real → income materializado + los 4 signals `finance.uf.rate_freshness`/`finance.indexed_unit.*` en steady; (3) prod tras sign-off Finance. `FINANCE_CLF_OBLIGATIONS_ENABLED`/`FINANCE_CLF_REPORTING_ENABLED`/`FINANCE_CLF_BACKFILL_APPLY_ENABLED` se prenden cuando se cableen sus consumers (obligations CLF / readers / backfill — diferidos por anti-drift). | CLF/UF como unidad indexada nativa (ADR `GREENHOUSE_CLF_INDEXED_FINANCE_CORE_V1`). Default OFF → CLF sigue pricing/quote-only; el camino CLP/USD/MXN es bit-for-bit. Las OC son las **recibidas de clientes** (lado income/AR), no compras a proveedores. |

| `NEXA_QUOTE_AUTHOR_ACTION_ENABLED` | TASK-1212 | OFF/default en todos los environments (verdad live: `vercel env ls`) · **code-complete + gated** | (1) además del master `NEXA_ACTION_RUNTIME_ENABLED` (ya ON en staging/prod), prender `NEXA_QUOTE_AUTHOR_ACTION_ENABLED=true` en staging → habilita la governed action `author_quote` (Nexa puede crear/emitir una cotización con confirmación humana); (2) ejercer el loop `propose → confirm → execute` con una cotización real en staging + verificar quotation/líneas/outbox; (3) prod tras sign-off del operador. | Governed action de autoría/emisión de cotización (write gobernado interno). Default OFF → el resolver devuelve gap honesto `runtime_disabled` y el confirm rechaza; la mutación SIEMPRE ocurre en el confirm humano y el command `submitQuoteFromBuilder` re-enforza capability `commercial.quotation` + precio del engine. |

| `COMMERCIAL_Q2C_CANONICAL_CLOSE_ENABLED` + `COMMERCIAL_Q2C_CONTRACT_ONLY_ENABLED` | TASK-1206 | **`COMMERCIAL_Q2C_CANONICAL_CLOSE_ENABLED` staging: ON (2026-06-22, smoke HTTP PASS)** · prod: OFF · `COMMERCIAL_Q2C_CONTRACT_ONLY_ENABLED`: OFF en todos | (1) ~~staging flip + smoke~~ **HECHO 2026-06-22**: flag ON en staging + redeploy `greenhouse-jfz70d2gr`; smoke HTTP sobre fixture → `convert-to-invoice` delegó en `closeQuoteToCash` (201 con operationId/finalState canónicos), income + contrato + audit Q2C + outbox completo; 2.º POST → MISMO incomeId (anti doble-AR confirmado, 1 income en PG). (2) **prod pendiente**: tras sign-off Commercial/Finance, aplicar la migración `20260621222152560` en la base de prod (vía release control plane develop→main) + `vercel env add ... Production` + redeploy + smoke prod. `COMMERCIAL_Q2C_CONTRACT_ONLY_ENABLED` solo si el operador habilita `contract_only` (deal suspendido sin AR + SLA) — política aparte con sign-off. | Cierre canónico Quote-to-Cash (TASK-1206). En staging el path visible ya usa el comando canónico (añade audit Q2C + idempotencia; las conversiones que antes hacía el legacy ahora pasan por `closeQuoteToCash`). `contract_only` NUNCA es cierre terminal: audit `status='suspended'` + signal `contract_only_sla_breach`. |

| `GROWTH_AI_VISIBILITY_ARCHETYPE_PROMPTS_ENABLED` + `GROWTH_AI_VISIBILITY_PROMPT_AUTHORING_ENABLED` (Vercel **+ ops-worker**) | TASK-1290 | **staging: ON (2026-06-29, Vercel env staging + deploy.sh)** · prod: OFF | Prompts por arquetipo (categoría × `business_model` × buyer-intent) + autoría LLM del set. `ARCHETYPE_PROMPTS`: con ON `buildExecuteInput`/`enqueueGraderRun` resuelven el baseline determinista del arquetipo del perfil (`resolveArchetypeBaselinePack`: consumo/B2B-saas/retail/marketplace/público; `unknown`→genérico NUNCA agencia) o el set congelado `active` autorado; agencia = pack v1 bit-for-bit. `PROMPT_AUTHORING` (gateado además por `isGraderEnabled`): habilita `authorPromptSet` (LLM, manual via command — NO auto-run). **⚠️ DUAL-LOCATION** (run `light` inline Vercel + `full` async ops-worker; el switch vive en `buildExecuteInput`/`enqueueGraderRun`, común a ambos). Los tags del pack VIAJAN con el run (Slice 0) → el scorer mide con el framing del arquetipo (sin esto, ids nuevos colapsan el score). **Verificado E2E live (2026-06-29, gemini, staging DB):** SKY → autoría LLM (15 prompts consumo) → set active → run `EO-GRUN-00031` `succeeded` (provenance `prompt_set_id` registrado, prompts "Santiago-Calama"/"Sky vs LATAM") → **score 36.1 ≠ 0** (vs el 0 artificial del pack agencia); dimensiones tag-dependientes con evidencia. El set autorado de verificación se limpió → SKY queda en el baseline determinista de consumo hasta el review operador. **Pendiente prod:** review del copy autorado (TASK-1291) + eval golden-set multi-arquetipo (TASK-1292) → release control plane EPIC-021. | staging ON → SKY (consumo) medible ≠ 0 con prompts reales. Prod OFF → pack agencia idéntico (bit-for-bit). Revert (<5 min): flags a false. |
| `GROWTH_AI_VISIBILITY_CATEGORY_GUARD_ENABLED` (Vercel **+ ops-worker**) | TASK-1288 | **OFF en todos los environments** · code-complete | Guard de categoría no resuelta (ISSUE-110): cuando ON, un run cuyo perfil tenga `category_node_id = unknown`/baja confianza se bloquea (`aeo_category_unresolved`) en vez de generar prompts basura. **⚠️ DUAL-LOCATION** (el run corre en Vercel `light` inline y ops-worker `full` async — el guard vive en `buildExecuteInput`, común a ambos). **Pendiente:** prender SÓLO tras (1) Slice 4 (brand_intelligence grounded read) + (2) backfill verificado con cobertura alta (hoy 13% determinista — la mayoría free-text/null se resuelve con el grounded read) — sino bloquearía el lead magnet con muchos `unknown` legacy. Pre-check limpio en portal/operador (`category_unresolved` blocked) + backstop universal en `buildExecuteInput`. Signal `growth.ai_visibility.profile_category_unresolved` (org-linked unresolved, steady 0). | Default OFF → ningún run se bloquea por categoría (comportamiento idéntico). ON → run/envío con categoría no resuelta bloqueado → confirmación humana (TASK-1291). Revert (<5 min): flag a false. |
| `GROWTH_AI_VISIBILITY_BRAND_INTELLIGENCE_ENABLED` (Vercel **+ ops-worker**) | TASK-1288 | **staging: ON (2026-06-29, deploy.sh + Vercel)** · prod: OFF | Lectura grounded `brand_intelligence` (LLM sobre contenido legible del sitio + entity → snapshot estructurado que TASK-1289/1290 consumen). Gateado además por `isGraderEnabled`. **Nota:** ningún path runtime invoca aún el grounded read automáticamente — se ejecuta vía `scripts/growth/backfill-canonical-category.ts --grounded` (ya corrido contra `greenhouse-pg-dev`/staging: SKY→sector:passenger_airlines, Berel→manufacturing, Banco de Chile corregido→finance). Costo: gemini-flash-lite ~$0.0004/marca, 1×/marca/versión cacheado. **Pendiente prod:** release control plane + EPIC-021. | Default OFF → la resolución de categoría cae al prior determinista (HubSpot map + alias); cero costo LLM. ON → lectura grounded autoritativa (cascada con confianza). Revert (<5 min): flag a false. |
| `GROWTH_AI_VISIBILITY_GRADER_ENABLED` (master) + `GROWTH_AI_VISIBILITY_OPENAI_ENABLED` / `_ANTHROPIC_ENABLED` / `_PERPLEXITY_ENABLED` / `_GEMINI_ENABLED` / `_GOOGLE_AIO_ENABLED` | TASK-1226/TASK-1265 | **staging: GRADER + OPENAI + ANTHROPIC + PERPLEXITY + GEMINI + GOOGLE_AIO ON en ops-worker** (`ops-worker-00418-2m6`) · prod: OFF | ~~(1) secrets OpenAI/Anthropic~~ HECHO (ya existían del spike 1228, grant secretAccessor a greenhouse-portal@ verificado) · ~~(2) flags staging + smoke real~~ HECHO (smoke real local OpenAI 6/6 + Anthropic verificados; flags `staging` ON). **(3) prod pendiente (FOLLOW-UP pesado):** tras sign-off, migración `greenhouse_growth` + capabilities seed a prod vía release control plane develop→main + `vercel env add GROWTH_AI_VISIBILITY_* Production` + redeploy + smoke prod. ~~(4) Perplexity/Gemini: cuando se provisionen `greenhouse-perplexity-api-key`/`greenhouse-gemini-api-key`~~ HECHO (Gemini TASK-1233; Perplexity 2026-06-27: secret `greenhouse-perplexity-api-key` v1 + grant secretAccessor a greenhouse-portal@ + flag staging ON + smoke real local source=secret_manager). **Drift corregido 2026-06-29:** `ops-worker-00417-m86` tenia `GROWTH_AI_VISIBILITY_PERPLEXITY_ENABLED=false`; se hizo `gcloud run services update ops-worker --update-env-vars=GROWTH_AI_VISIBILITY_PERPLEXITY_ENABLED=true` → revision `ops-worker-00418-2m6` sirve 100% traffic con Perplexity ON, y `services/ops-worker/deploy.sh` queda persistido staging ON / prod OFF para futuros redeploys. Pendiente recomendado: smoke async low-volume post-flip. ~~**(5) Google AIO/TASK-1265:** aplicar migration provider CHECK, deploy y prender `GOOGLE_AIO` solo en staging~~ **HECHO (2026-06-28):** migración CHECK aplicada (dev/staging comparten `greenhouse-pg-dev`); flag staging ON en Vercel **y** en ops-worker (staging async → el run ejecuta en el worker, no en Vercel). ops-worker `deploy.sh` con branch staging ON / prod OFF + `DATAFORSEO_API_PASSWORD_SECRET_REF` + login vía GH secret `DATAFORSEO_API_LOGIN`; rev viva actual `ops-worker-00418-2m6`. **Smoke real verde end-to-end:** run `grun-61d1c683…` (solo `google_ai_overview`) drenado por el worker → observation `succeeded`, 27 citas, `$0.004` en `provider_observations`. Usable en los **3 endpoints** (public/client-portal/operator) por construcción (run-engine resuelve providers desde `policy.eligibleProviders`; los 3 modos incluyen el provider). **(5b) prod pendiente (gated TASK-1246/EPIC-020):** sign-off + migración/capabilities a prod + flag prod + **rotar el password DataForSEO** (expuesto en provisión). ⚠️ Llave Perplexity y password DataForSEO fueron expuestos en chat/captura → rotar antes de prod. | AI Visibility Grader (dominio growth.ai_visibility). Default OFF → cada adapter resuelve skip controlado (`grader_disabled`/`provider_disabled`/`missing_secret`), cero llamadas, cero costo. `light` excluye Anthropic+web_search por costo (§5 calibración) pero incluye Google AI Overview detrás de flag propio y costo por request DataForSEO. Runs manuales y recurrentes usan el provider set efectivo del environment que ejecuta (worker para async). |

| `GROWTH_AI_VISIBILITY_PUBLIC_INTAKE_ENABLED` (Vercel) | TASK-1240 | **staging: ON (2026-06-25, redeploy `greenhouse-h26jgk4dc` + smoke real verde)** · **prod: ON (2026-06-30 flip masivo Growth/AEO, redeploy `greenhouse-ic8cg4ery`; ver Delta top) — valor real `true` re-verificado 2026-07-01 vía `vercel env pull`** | (1) ~~**sign-off legal del consent / aviso de privacidad** (Ley 21.719/GDPR) antes de prender EN PROD~~ **RESUELTO 2026-07-01 (TASK-1246): el operador confirmó que el consentimiento + aviso de privacidad están revisados/aprobados.** (2) secret captcha `TURNSTILE_SECRET`: en **staging** = test secret Cloudflare always-pass (`1x0000…AA`); en **prod** = secret real (Secret Manager, SET 2026-06-30) — ⚠️ **expuesto en chat → rotación pendiente (operador)**. (3) `vercel env add ... staging`+prod HECHO + redeploy. (4) smoke real staging HECHO: POST con captcha → 202 + submission + run encolado (`EO-GRUN-00012`). (5) prod flip HECHO (ad-hoc 2026-06-30, no vía release control plane). **Mitigante de exposición (verificado en código 2026-07-01): el grader self-serve (form `fdef-ai-visibility-grader`) NO está embebido en ninguna superficie pública** — no hay `page.tsx` pública en greenhouse-eo; `/aeo-2/` es el form COMERCIAL `efeonce-aeo-diagnostic` (form-key `b120566a…`) → HubSpot, que **NO corre el grader** (el consumer `growth-grader-run-from-submission` filtra por `form_id === fdef-ai-visibility-grader`); ni existe en efeonce-web. **→ 0 tráfico self-serve real hoy** (aunque el intake API está ON). El render/form en efeonce-web del ADR 2026-06-28 está **sin construir**; el grader ya es un form gobernado embebible con el mismo `<greenhouse-form>` que usa `/aeo-2/`. | Intake público del lead magnet (EPIC-020 B). Gateado por el kill switch `isGraderEnabled`. Default OFF → el POST público responde `disabled` (404). ON → captcha (Turnstile) + rate-limit (per-IP 10/email 3 por día) + presupuesto global diario (circuit breaker → 503) + modo `light` forzado; encola `public_diagnostic`. El email (PII) vive sólo en `grader_leads` con consent, NUNCA a providers. Revert (<5 min): flag a false → 404. |
| `GROWTH_GRADER_INTAKE_ON_FORMS_ENGINE_ENABLED` (Vercel) | TASK-1251 | **staging: ON (2026-06-25, redeploy `greenhouse-h26jgk4dc` + smoke E2E verde)** · prod: OFF | (1) Migración `task-1251-grader-forms-engine-convergence` + UNIQUE parcial aplicadas (seed grader-form gobernado `fdef-ai-visibility-grader` + binding `grader_leads.submission_id`). (2) `vercel env add ... staging` HECHO + redeploy. (3) cron `ops-reactive-growth` creado (deploy ops-worker on push develop). (4) **smoke E2E staging HECHO:** POST → `submissionId` (no inline) → outbox `published` → reactive consumer materializó lead `glead-2d1e97f9` + run `EO-GRUN-00012` linkeados; email en PG con consent, nunca al provider. (5) prod = release control plane junto a EPIC-020 + sign-off legal. Revert (<5 min): flag a false → vuelve al intake a-medida (TASK-1240). | Convergencia del intake del AI Visibility Grader sobre el motor Growth Forms (TASK-1251). Default OFF → `POST /run` usa el path a-medida actual (`createPublicGraderRun` inline). ON → fachada que persiste `form_submission` + consent_snapshot + outbox `growth.forms.submission_accepted`; un reactive consumer (`growth_grader_run_from_submission`, domain `growth`, lane `ops-reactive-growth`) encola el run + materializa el lead (no inline). Un solo stack de public-submission. |
| `GROWTH_AEO_FORM_GRADER_INTAKE_ENABLED` (**ops-worker**) | TASK-1321 | **develop pushed (`daf55cb7c`, 2026-07-02) · staging: LIVE + validado E2E** (run EO-GRUN-00034: auto-score 48.9 → informe → correo SENT vía Resend). **Prod: pendiente SOLO de la promoción `develop→main`** (el push directo a `main` lo bloquea el harness auto-mode + la batch policy pide break-glass). El bloque `production` de `deploy.sh` YA espeja staging (grader stack completo ON, Gemini ON post-billing-unblock) → el prod release lo activa por construcción, sin flip manual. Root-cause del path público que no generaba informe RESUELTO (auto-score en `executeClaimedGraderRun`, `run-engine.ts`). ⚠️ **KILL-SWITCH default-ON** (desvía del patrón default-OFF por directiva explícita del operador 2026-07-02): la capacidad nace activa; el flag APAGA (`=false`). | El flag corre donde se drena la projection `growth_aeo_diagnostic_grader_run_from_submission` (lane `ops-reactive-growth`, ops-worker). **Palanca de activación REAL ≠ el flag:** con el flag ON pero el form `/aeo-2/` SIN el campo `brandName`, cada submit cae en `mapAeoDiagnosticToGraderIntake → missing_brand_name → skip` (degrada al lead comercial, sin run). El grader `/aeo-2/` **no corre de verdad** hasta publicar la versión nueva del form con `brandName` (activation script, Slice 1-form, es mutación live pendiente). Flags aguas abajo ya ON en prod (06-30 flip masivo): `GRADER`/`BRAND_INTELLIGENCE` (categoría)/`REPORT_EMAIL`/`LEAD_HANDOFF`. Consent: `/aeo-2/` usa `efeonce-aeo-diagnostic-consent-v1` (operador aceptó correr el grader + enviar informe bajo ese consent). Rollout: (1) ~~republicar form con `brandName`~~ **HECHO 2026-07-02** (v10 `fver-fc552f37-15a6-4d70-abfb-59929a409a62` publicada, v8 `fver-38d38bbc-…` deprecada; live verificado + screenshot, `overflowX=0`; `brandName` req full-width + `brandWebsite`→"Sitio web de tu marca" + `country` opcional). El form ya captura `brandName` en prod, pero el grader **no corre** hasta el step 2 (código sin deploy) → hoy `/aeo-2/` sigue siendo captura de lead comercial + campo nuevo. → (2) deploy código a ops-worker (staging→prod vía release control plane) → (3) smoke E2E: submit real `/aeo-2/` → run → correo con PDF → contacto HubSpot único. Revert (<5 min): `GROWTH_AEO_FORM_GRADER_INTAKE_ENABLED=false` en ops-worker → `/aeo-2/` vuelve a solo-comercial. | `/aeo-2/` (`fdef-efeonce-aeo-diagnostic`) submit → grader run auto-disparado + informe por correo (event-driven, apenas `ready`/`partial`) + lead HubSpot dedup (enriquece, no duplica). Guardrails que hacen segura la posición ON: cost-cap global (budget 24h + per-email 3/IP 10) aplicado en la projection + `grader_intake_events`, gate de categoría (unknown→skip, sin runs basura), corporate-email + Turnstile del form, dedup HubSpot nativo por email/dominio. PII (email/nombre) solo en `grader_leads` + el correo, NUNCA al run engine. |
| `GROWTH_AI_VISIBILITY_REPORT_EMAIL_ENABLED` (**ops-worker** + Vercel) | TASK-1250 | **staging: ON (2026-06-27, ops-worker rev `00409-fxd` + Vercel staging, smoke E2E real verde)** · prod: OFF | **⚠️ DUAL-LOCATION (espeja el handoff):** el WRITE (`dispatchAiVisibilityReportEmail`) corre en el **ops-worker Cloud Run** (reactive consumer `growth_ai_visibility_report_email`, lane `ops-reactive-growth`) → el flag se necesita ahí; el enqueue corre en Vercel pero NO mira el flag (gate vive en el write); el signal reader (Vercel) sí lo lee para la severidad. (1) ~~redeploy ops-worker~~ **HECHO** (push develop → GitHub Actions Ops Worker Deploy, rev `00409-fxd`, bundle con el consumer + report PDF renderer; fonts + `public/branding` en la imagen). (2) `RESEND_API_KEY` ya configurado. (3) ~~flag staging ON~~ **HECHO** (deploy.sh declarativo staging ON/prod OFF + Vercel `env add ... staging`). (4) ~~smoke staging~~ **HECHO 2026-06-27**: lead consentido (`grun-689323b1`) → enqueue `report_email_requested` → outbox published → reactive-growth drenó → `grader_report_email_dispatches.status='sent'` (1 fila, sin doble-envío) + `email_deliveries.status='sent'`, `has_attachments=true`, `recipient=jreysgo@gmail.com`, `resend_id=501dcd8e-3c4a-4943-a7d7-f3fb95183b9c`; signal `growth.ai_visibility.report_email_failed` sin failed. **Prod (5):** release control plane develop→main junto a EPIC-020 + sign-off (legal/from-address del lead magnet, TASK-1246). | Entrega del informe del grader por email (EPIC-020). Default OFF → el consumer resuelve `skipped:disabled` (NUNCA envía, NUNCA crash); el enqueue del evento igual ocurre (gate en el write para no perder eventos al prender). ON → arma el adjunto PDF público-safe (TASK-1273) + envía vía `sendEmail` (domain `growth`, marca **Efeonce** agencia). Consent-gate + report-state gate (`ready`/`partial`) + idempotencia DB-level `UNIQUE(report_id, email_type)`. Revert (<5 min): flag a false → cero envíos. |
| `GROWTH_AI_VISIBILITY_OPERATOR_SEND_ENABLED` (Vercel **+ ops-worker**) | TASK-1279 | ⚠️ **CORREGIDO 2026-06-29 (TASK-1291 rollout): `vercel env ls` (SoT) = ABSENT en TODOS los envs Vercel** → la cross-sell SEND está OFF (command rechaza `aeo_send_disabled`). El "staging ON + smoke E2E verde" de abajo fue cierto en el momento del smoke de TASK-1279 pero **el flag NO quedó persistido en Vercel** (o se removió post-ISSUE-110) → estado real OFF. _Histórico: staging smoke (2026-06-29): Sky Airlines → email `sent` + Lead HubSpot `566830815132` + `aeo_check_result='No aparece'`. ops-worker rev `00422-cz7`._ · prod: OFF | **⚠️ DUAL-LOCATION (espeja report-email/handoff):** el WRITE (`executeOperatorReportSend`) corre en el **ops-worker Cloud Run** (reactive consumer `growth_ai_visibility_operator_send`, lane `ops-reactive-growth`) → el flag se necesita ahí; el command (Vercel) lo lee al frente para gatear el 409 `aeo_send_disabled`. (1) **provisionar la property HubSpot `aeo_check_result`** (string en Company, grupo `aeo`) vía `scripts/growth/provision-ai-visibility-hubspot-properties.ts` — HOY NO EXISTE en el portal (verificado live 2026-06-29); sin ella el upsert de Company 400ea. (2) **confirmar el objeto `leads` habilitado** en el portal (verificado vía REST: `crm/v3/objects/leads` responde 154 props; el MCP no lo soporta pero la API sí) + revisar la asociación default Lead↔Company. (3) `vercel env add ... staging` + flag en el ops-worker (`deploy.sh` staging ON / prod OFF). (4) smoke staging real: correr+publicar un run operador → `POST runs/[runId]/send-lead` → outbox `report_send_requested` → reactive-growth drena → email `sent` (`email_deliveries`) + Lead creado en HubSpot + `aeo_check_result` en la Company + consent gate (prospecto sin consent → 422). (5) sign-off comercial/legal del copy a prospectos (interés legítimo) + prod vía release control plane junto a EPIC-020. | Cross-sell operador: enviar informe AEO + crear **Lead** HubSpot (objeto `leads`, NUNCA Deal). Default OFF → el command rechaza `aeo_send_disabled` (NUNCA envía/escribe) y el consumer resuelve `skipped:disabled`. ON → email público-safe (mismo path leak-safe del lead magnet, marca Efeonce) + upsert Contact/Company + Lead + `aeo_check_result`; consent gate server-side (prospecto exige consentimiento), idempotencia run+recipient, signal `growth.ai_visibility.operator_send_failed` steady=0. Revert (<5 min): flag a false. |
| `GROWTH_AI_VISIBILITY_LEAD_HANDOFF_ENABLED` (Vercel **+ ops-worker**) | TASK-1242 | **staging: ON (2026-06-25, smoke E2E real verde)** · prod: OFF | **⚠️ DUAL-LOCATION (hallazgo del smoke):** el flag se necesita en DOS lugares — **Vercel** (auto-trigger del publish + admin endpoints + status reader) Y el **ops-worker Cloud Run** (donde corre `executeLeadHandoff`, el WRITE). Prender solo Vercel → el consumer salta `reason=disabled`. (1) ~~HubSpot custom properties `ai_visibility_*` + grupo "AEO"~~ **HECHO** (portal 48713323, 8 props + grupo `aeo`, vía `scripts/growth/provision-ai-visibility-hubspot-properties.ts`). (2) ~~`vercel env add ... true staging`~~ **HECHO** + redeploy. (2b) ~~flag en el ops-worker~~ **HECHO** (`gcloud run services update ops-worker --update-env-vars` + declarativo en `services/ops-worker/deploy.sh` staging ON/prod OFF para que `--set-env-vars` no lo borre). (3) ~~smoke staging~~ **HECHO 2026-06-25**: run `EO-GRUN-00012` score `completed` (25.8) → publish snapshot → auto-trigger → reactive consumer upsert contact `smoke-task1251@example.com` + company `example.com` con props `ai_visibility_*` + `hubspot_synced_at` set; registros de prueba borrados post-verificación (CRM limpio). **Prod (4):** release control plane develop→main junto a EPIC-020 + sign-off (flag ON en Vercel prod + en el ops-worker prod vía deploy.sh prod-branch o gcloud). (3) smoke staging: publicar snapshot de un run real con lead → outbox `lead_handoff_requested` → reactive consumer upsert contact/company en HubSpot sandbox + `hubspot_synced_at` + signal `lead_handoff_uncovered` steady=0. (4) prod = release control plane junto a EPIC-020 + sign-off. | HubSpot lead handoff (EPIC-020 D). Default OFF → el reactive consumer resuelve `disabled` (NUNCA escribe a HubSpot, NUNCA crash); el enqueue del evento igual ocurre (gate vive en el write para no perder eventos al prender). ON → upsert contact/company vía cliente HubSpot **in-app directo** (NO Cloud Run bridge). Consent-gate + score `completed` gate (sin score falso). Dedup company por dominio corporativo. Revert (<5 min): flag a false → cero writes. |
| `GROWTH_AI_VISIBILITY_PORTAL_RUN_ENABLED` (Vercel) + `GROWTH_AI_VISIBILITY_TRIAL_ENABLED` (Vercel) | TASK-1277 | **staging: PORTAL_RUN + TRIAL ON (2026-06-29)** · prod: OFF | Run gobernado AEO de portal (chokepoint per-org). `PORTAL_RUN` (gateado además por `isGraderEnabled`) habilita las puertas cliente (contratado/trial/pilot) vía `requestGraderRunForOrganization`; `TRIAL` habilita el tier trial PLG. Config asociada (no `*_ENABLED`, env-override, defaults conservadores): `GROWTH_AI_VISIBILITY_TRIAL_RUNS_PER_MONTH=1`, `_CONTRACTED_RUNS_PER_MONTH=20`, `_PILOT_RUNS_PER_MONTH=3`, `_TRIAL_GLOBAL_MONTHLY_BUDGET_USD=25`. **Rollout staging aplicado 2026-06-29:** (1) migraciones ya en develop ✓ → (2) flip `PORTAL_RUN` staging ✓ + smoke real: Berel (contratado) → chokepoint **accepted** (run `portal_contracted` `light` encolado y drenado por el worker), org sin entitlement → `no_entitlement` ✓ → (3) flip `TRIAL` staging ✓ (inerte: sin orgs trial provisionadas + el trial exige `grader_profile` enlazado → costo ~0). **Binding del contratado:** se creó el `grader_profile` de Grupo Berel enlazado a su org (`EO-GAVP-0014`, web `https://berel.com` derivada de la org tras TASK-1285), vía `scripts/growth/provision-grader-profile-for-org.ts --apply`. **Pendiente:** (4) sign-off comercial qué otras orgs son `trial` vs `contracted` (hoy **solo Berel contratado**; trial PLG a otros clientes NO provisionado — decisión comercial outward-facing) → (5) `scripts/growth/provision-aeo-trials.ts --apply` cuando se decida → (6) prod vía release control plane + EPIC-020. La puerta operador (`requestGraderRunAsOperator`) NO depende de estos flags (capability-gated + kill switch global). | Entitlement & metering AEO (EPIC-020). Default OFF → el chokepoint de portal responde `aeo_run_disabled` (cero costo). ON → entitlement (módulo per-org) → ventana → allowance (runs/mes con reset) → costo. Revert (<5 min): `vercel env rm` de los 2 flags en staging. |
| `GROWTH_AI_VISIBILITY_REGRADE_ENABLED` (**ops-worker**) | TASK-1270 | **staging: ON en develop rollout (2026-06-29)** · prod: OFF. `ops-growth-grader-regrade` activo en staging y pausado por default en production. | Re-grade recurrente AEO para perfiles `grader_profiles.recurring_regrade_enabled=true` + módulo `ai_visibility_v1` contratado. Config asociada: `GROWTH_AI_VISIBILITY_REGRADE_BATCH_SIZE=5`, `GROWTH_AI_VISIBILITY_REGRADE_MONTHLY_BUDGET_USD=50`, `GROWTH_AI_VISIBILITY_REGRADE_SCHEDULER_PAUSED=false` en staging / `true` en production. **Rollout staging aplicado:** migración TASK-1270 OK; ops-worker `ops-worker-00417-m86` (`GIT_SHA=b5cf815d7…`) Ready con flag ON; Scheduler `ENABLED`; smoke manual `ops-growth-grader-regrade` → `claimed=0 enqueued=0 failed=0 skipped=no_due_profiles` porque `opt_in_profiles=0`, `due_profiles=0` (cero costo). **Prod pendiente:** budget sign-off + release control plane. **E2E pendiente:** opt-in gobernado de 1 perfil contratado para verificar run recurrente `pending→terminal`. | Default OFF en prod → `/growth/grader/regrade` responde `disabled`, cero claim de perfiles, cero costo. ON staging → claim `FOR UPDATE SKIP LOCKED` de perfiles due, enqueue `full` idempotente con key `growth-ai-visibility-regrade:*`, retry corto si falla el enqueue, y ejecución real queda a cargo de `/growth/grader/drain`. Revert staging (<5 min): `GROWTH_AI_VISIBILITY_REGRADE_ENABLED=false` + pausar `ops-growth-grader-regrade` o redeploy con `GROWTH_AI_VISIBILITY_REGRADE_SCHEDULER_PAUSED=true`. |
| `GROWTH_AI_VISIBILITY_PROBES_ENABLED` (Vercel **+ ops-worker**) + `GROWTH_AI_VISIBILITY_AGENTIC_READINESS_ENABLED` (Vercel **+ ops-worker**) | TASK-1266 | **staging: ON (2026-06-28, ops-worker rev deploy `83927197756` + Vercel staging, smoke E2E real verde)** · prod: OFF | Site Readiness Probe Layer (probes técnicos read-only del sitio analizado → ejes `structural` + `agentic` ortogonales al de percepción). `PROBES` (gateado además por `isGraderEnabled`) es el flag MAESTRO: habilita el eje structural (robots IA, JSON-LD, llms.txt, sitemap; CWV degrada a skipped sin Chromium). `AGENTIC_READINESS` (requiere `PROBES` ON) suma el eje agentic (.well-known/mcp, API discoverability, potentialAction, DOM semántico; WebMCP degrada a skipped). **⚠️ DUAL-LOCATION:** el probe gatherer corre dentro de `executeClaimedGraderRun` → en **ops-worker** (path async `full`) y en **Vercel** (path inline `light`); ambos necesitan el flag. **Rollout (pendiente):** (1) migración `task-1266-grader-probe-results` aplicada en staging (ya en develop) → (2) flip `PROBES` staging (Vercel + deploy.sh ops-worker) + run real sobre dominio de prueba → verificar `grader_probe_results` + `structural_readiness` + honest degradation de una señal ausente → (3) flip `AGENTIC_READINESS` staging + verificar `agentic_readiness` → (4) revisar costo (fetches read-only, sin Lighthouse aún) + signals `probe_failure_rate`/`probe_headless_coverage` steady → (5) prod vía release control plane + EPIC-020. **Headless (CWV/WebMCP) queda degradado a `skipped/no_headless`** hasta cablear Chromium en el worker (follow-up; out-of-band coordination de la task). | Site readiness (EPIC-020). Default OFF → el run-engine no invoca el gatherer (run de percepción idéntico; readiness omitido del report). ON → tras cada run, probes read-only del dominio → readiness lado a lado del de percepción (NUNCA blended). Revert (<5 min): flags a false → el report omite los ejes de readiness y sigue con percepción intacta. |
| `GROWTH_AI_VISIBILITY_ENTITY_PROBES_ENABLED` (Vercel **+ ops-worker**) | TASK-1267 | **staging: ON** (2026-06-28 — Vercel `env add` env `staging` + `deploy.sh` ENTITY ON; toma efecto en el redeploy del push develop) · prod: OFF | Entity Infrastructure Probes — tercer eje ortogonal `entity` del probe layer (Google Knowledge Graph + Wikidata/Wikipedia + Reddit-UGC). Requiere `GROWTH_AI_VISIBILITY_PROBES_ENABLED` ON (aditivo, igual que `AGENTIC_READINESS`). **⚠️ DUAL-LOCATION** (mismo patrón que TASK-1266): el gatherer corre en **ops-worker** (async `full`) y **Vercel** (inline `light`); ambos tienen el flag ON en staging. **KG api key publicada (2026-06-28):** secret `greenhouse-google-knowledge-graph-api-key` en Secret Manager (key GCP restringida a `kgsearch.googleapis.com`, probada live contra la API) + grant `secretAccessor` a `greenhouse-portal@` + `GOOGLE_KNOWLEDGE_GRAPH_API_KEY_SECRET_REF` wired en `deploy.sh` (staging) y Vercel `staging` → los **3 probes de entidad activos** en staging. **Rollout:** (1) migración `task-1267-grader-probe-axis-entity` aplicada dev (✓) → (2) ✅ flip `ENTITY_PROBES` staging (Vercel + `deploy.sh`) → (3) ✅ KG api key publicada + wired → (4) ✅ **verificado E2E** (run `EO-GRUN-00026`, vercel.com): `grader_probe_results` eje entity → `wikidata` 100 (P856 + 38 wiki), `knowledge_graph` 75 (KG key funcional, homónimo sin dominio), `reddit_ugc` failed/null por 403 (honest degradation correcta) → (5) prod vía release control plane + EPIC-020. **Reddit** vía búsqueda pública read-only (sin secret); si la quota la bloquea, degrada honesto. | Entity backbone (EPIC-020). Default OFF → el gatherer no registra los probes de entidad (run + readiness structural/agentic idénticos). ON → suma el eje `entity` lado a lado (NUNCA blended). Revert (<5 min): flag a false → el report omite el eje entity. El render del detalle de entidad lo gobierna TASK-1252 (out of scope acá). |
| `GROWTH_AI_VISIBILITY_FIX_IT_ENABLED` (Vercel) | TASK-1269 | **staging: ON (2026-06-28, Vercel env + deploy develop `greenhouse-fyoycag0t` Ready, CI/Playwright smoke verde)** · prod: OFF | Fix-It Artifacts del AI Visibility Grader: genera on-demand artefactos public-safe deterministas (`Organization`/`Service` JSON-LD starter, `llms.txt`, content brief AEO y brief de entidad cuando hay gaps KG/Wikidata/Reddit) desde el snapshot/report + probe findings existentes. Gateado por `GROWTH_AI_VISIBILITY_GRADER_ENABLED`; no llama LLM, no escribe en el sitio del prospecto y no persiste cache. **Rollout:** ~~(1) flip staging + redeploy develop~~ HECHO (2026-06-28); (2) smoke funcional por token público y run admin con reporte real; (3) revisión copy/legal de claims y disclaimers + validar JSON-LD con schema.org/Rich Results y revisar `llms.txt` formado; (4) prod vía TASK-1246/EPIC-020 release control plane. | Default OFF → endpoints `/fix-it` responden no disponible/404. ON → entrega archivos public-safe por token público o capability interna `growth.ai_visibility.fix_it.generate`. Revert (<5 min): flag a false. |
| `TURNSTILE_SECRET` + `NEXT_PUBLIC_TURNSTILE_SITE_KEY` (Vercel) | TASK-1240/1251/TASK-1367/TASK-354 | **staging + Production: SET (2026-07-09, verdad live `vercel env ls`; valores no versionados).** | Mantener ambos lados publicados antes de prender intakes públicos: el site key se hornea en el cliente (`NEXT_PUBLIC_*`, requiere build fresco) y el secret verifica server-side. Sin secret con el intake ON, el verifier hace **fail-closed** (rechaza) — comportamiento correcto. | Secret/site key de Turnstile. Lo consume el port compartido `turnstileCaptchaVerifier` del motor + grader y el apply público de Careers. `NODE_ENV=production` en todo deploy Vercel (staging incluido) → el bypass dev NO aplica en staging. |
| `GROWTH_AI_VISIBILITY_ASYNC_EXECUTION_ENABLED` (Vercel) + flags del worker `GROWTH_AI_VISIBILITY_*` (deploy.sh ENV-branch) | TASK-1234 | **Vercel: OFF en todos (verdad live `vercel env ls`)** · **Worker ops-worker (Cloud Run, deploy.sh): staging ON / prod OFF** (ENV-branch) · _en rollout 2026-06-24_ | ~~(1) worker deploy~~ vía CI `ops-worker-deploy.yml` on push develop (ENV=staging) → crea Cloud Scheduler `ops-growth-grader-drain` (*/5) + monta flags **staging ON** (GRADER+OpenAI+Anthropic+Gemini) / **prod OFF** + `OPENAI/ANTHROPIC_API_KEY_SECRET_REF` + TIMEOUT 3600s; el handler hace **no-op prod-safe** con `isGraderEnabled()` OFF (cero queries — el schema `greenhouse_growth` no está migrado en prod). (2) `vercel env add GROWTH_AI_VISIBILITY_ASYNC_EXECUTION_ENABLED=true` (staging/develop) + redeploy → endpoint admin pasa a enqueue+poll (202+runId). (3) smoke real `full` multi-provider (Gemini 3) sin timeout + observations incrementales + signals `run_execution_lag`/`run_stuck_running` en steady. (4) **prod fuera de scope** (migración `greenhouse_growth` + capabilities + env vía release control plane). | Cutover inline → async del AI Visibility Grader (TASK-1234). Vercel flag OFF → endpoint ejecuta inline (sólo `light`/OpenAI cabe en el timeout Vercel); ON → encola y el worker Cloud Run drena sin límite de duración (única vía para `full` multi-provider). Worker compartido staging+prod: flags ramificados por ENV + gate `isGraderEnabled()` en el drain → prod no-op seguro. Revert (<5 min): Vercel flag a false → endpoint vuelve a inline. |
| `GROWTH_SEARCH_CONSOLE_ENABLED` (Vercel) | TASK-1282 | **staging: ON (2026-06-29, wiring smoke verde)** · prod: OFF | Conexión multi-tenant a Google Search Console (**flujo property-picker** estilo Semrush: token de OPERADOR + desplegable de propiedades). **Rollout staging APLICADO (2026-06-29):** OAuth client web "Greenhouse Search Console" en `efeonce-group` + secret `greenhouse-search-console-oauth-client-secret` + Vercel env; consent screen pasado a **External + En producción** (Internal bloquea cuentas externas; Testing expira refresh tokens a 7 días) — sin verificación de Google (aviso "app no verificada"); grant IAM secret-write `search-console-token-*` (condición `client-integration-tokens`); flag staging ON; wiring smoke verde. **Pendiente:** smoke humano del flujo nuevo (consent → desplegable → elegir) + prod vía release control plane. **Rollout original (out-of-band, pesado):** (1) crear OAuth 2.0 Client ID (web) en `efeonce-group` + branding consent screen + redirect URIs staging/prod → secrets `GOOGLE_SEARCH_CONSOLE_OAUTH_CLIENT_ID(_SECRET_REF)` / `_CLIENT_SECRET(_SECRET_REF)`; (2) **verificación de Google del scope sensible `webmasters.readonly`** (external user type, toma días) — mientras tanto, test users; (3) **ampliar el grant IAM de secret-write** del SA `greenhouse-portal@` al prefijo `search-console-token-*` (hoy sólo cubre `notion-integration-token-greenhouse-*`; sin esto `createOrAddSecretVersion` degrada `permission_denied`); (4) flip flag staging + OAuth round-trip real con propiedad de prueba → token en Secret Manager + fila `active` + reader devuelve filas; (5) prod vía release control plane (migración `greenhouse_growth` + capability seed + env) tras sign-off. | Default OFF → entrypoint OAuth no expuesto, reader retorna `disabled`. ON → operador conecta la propiedad GSC de un cliente (capability `growth.search_console.connect`). Revert (<5 min): flag a false. |

| `GROWTH_AI_VISIBILITY_SHORT_LINKS_ENABLED` (Vercel) | TASK-1330 | **Production: ON (2026-07-04)** · staging/dev: OFF · code+rollout complete | ✅ RESUELTO 2026-07-04: código promovido a `main` (release `9818f47d3`, manifest `released`) + `vercel env add …=true Production` + redeploy prod `greenhouse-k679lwhtg` (alias `greenhouse.efeoncepro.com`, health 200). Think `/s/[code].astro` verificado deployado (commit `681f1e4`, ancestro del HEAD live de `efeonce-think`). Consumers (token route, correo, HubSpot) prefieren el short URL `think.efeoncepro.com/s/<code>` con fallback al largo. Revert <5 min: `vercel env rm` + redeploy. Pendiente menor: prender en staging/dev para paridad de QA. | Short links gobernados del informe público (EPIC-020, follow-up de TASK-1329/1331). Default OFF → `resolvePreferredReportUrl`/`resolveReportShareUrlForRun` devuelven el URL LARGO sin tocar la DB (short-circuit); el resolve endpoint + auto-create en publish operan igual (deploy con tráfico cero). ON → los consumers prefieren el corto con fallback al largo. Revert (<5 min): flag a false → todos vuelven al largo. |

_(Agregá acá cualquier flag que dejes code-complete sin prender. Si está vacío, ¡no hay deuda pendiente!)_

---

| `PUBLIC_SITE_COMPARISON_TABLE_WRITES_ENABLED` | TASK-1225 | **staging: ON (2026-06-23)** — write path activado y probado end-to-end (WordPress `writes_enabled=true` + secreto canónico + bridge v0.5.0 desplegado + grant `secretAccessor` a greenhouse-portal@ + smoke `execute`→draft real) · prod: OFF ~~(1) secret + writes-enabled WordPress~~ HECHO · ~~(2) flag staging + smoke execute~~ HECHO (draft real verificado). **(3) prod pendiente:** `vercel env add PUBLIC_SITE_COMPARISON_TABLE_WRITES_ENABLED + los 2 refs Production` + redeploy, tras sign-off del operador (la escritura es draft-only; publish siempre humano). | Write gobernado del widget `greenhouse_comparison_table` (TASK-1225). Default OFF → el command corre solo `dry_run` (propose, sin red); `execute` lanza `comparison_table_writes_disabled` (409). El LLM nunca muta directo. |
| `GROWTH_FORMS_PUBLIC_API_ENABLED` (Vercel) | TASK-1229 | **staging: ON (2026-06-25, redeploy d6szlyp11 + GET público verificado live → render contract)** · prod: OFF | (1) la fundación (schema/contracts/commands/compiler/fake adapter/APIs) está code-complete + smoke e2e verde en dev; el público nace **disabled by design** (sin forms `published`). (2) Prender requiere: un form real publicado (TASK-1232 first migration) + el adapter HubSpot real (TASK-1230) + el dispatcher productivo (ops-worker drain `POST /growth/forms/dispatch` + Cloud Scheduler — **ROLLOUT pendiente**, hoy el dispatch se ejercita por el endpoint admin `POST /api/admin/growth/forms/dispatch`). (3) `vercel env add GROWTH_FORMS_PUBLIC_API_ENABLED=true` por environment + redeploy → render/submit público abiertos (igual gated por ausencia de forms publicados). | Motor Growth Forms (TASK-1229). Default OFF → `GET/POST /api/public/growth/forms/*` responden 404 `disabled`. Transversal (no grader-céntrico): 11 `form_kind`, destinos genéricos, port compartido abuse-guard/captcha. Revert (<5 min): flag a false → 404. |
| `GROWTH_FORMS_CATALOG_API_ENABLED` (Vercel) | TASK-1258 | **staging: ON (2026-06-26, `vercel env add` + push develop `97957029b` + smoke live verde)** · dev: OFF · prod: OFF | **Smoke live staging 2026-06-26 (deploy `greenhouse-iswpmjnmy`):** embed key minteada para surface `fhsf-efeonce-lead-gen-web` (`ehk_ee65ad44c942`); `GET /catalog` con key+origin `efeoncepro.com` → **200** + 1 form (`efeonce-lead-gen-web` v1 published, `destinationReadiness:"destination_disabled"` honesto — destino TASK-1261 sigue `disabled`); origin no permitido → **401**; key inválida → **401**; sin key → **400**. Prod = release control plane develop→main + provisionar embed key prod cuando el plugin WordPress (TASK-1259) la consuma. | (1) **code-complete**: reader `listInsertableFormCatalog` + endpoint `GET /api/public/growth/forms/catalog` + auth per-site (embed key + origin allowlist, Opción A) + 12 tests verde + full test/build verde. Es la precondición backend de TASK-1259. (2) Prender requiere: **provisionar la embed key** de la(s) surface(s) que consumirán el catálogo (`pnpm growth:forms:embed-key --surface-id <id>` → guardar el secreto server-side en el plugin WordPress) + `vercel env add GROWTH_FORMS_CATALOG_API_ENABLED=true` por environment + redeploy. Flag **separado** de `GROWTH_FORMS_PUBLIC_API_ENABLED` a propósito: el editor lista forms insertables ANTES de abrir render/submit público. (3) Sin embed key provisionada, el endpoint responde 401 aunque el flag esté ON (fail-closed). | Catálogo externo de forms insertables (TASK-1258, precondición TASK-1259). Default OFF → `GET /api/public/growth/forms/catalog` responde 404 `disabled`. ON → devuelve metadata editor-safe (displayName/slug/version/surfaceIds/destinationReadiness) autorizando por credencial per-site server-side; NUNCA GUIDs/mapping/secretos. Revert (<5 min): flag a false → 404. |
| `GROWTH_FORMS_DISPATCH_ENABLED` (Cloud Run ops-worker, deploy.sh ENV-branch) | TASK-1229 | **staging: ON (2026-06-25, deploy.sh ENV-branch)** · prod: OFF | (1) el dispatcher productivo (ops-worker drain `POST /growth/forms/dispatch` + Cloud Scheduler `ops-growth-forms-dispatch` */2) está **code-complete**: el handler + el job se crean en el próximo deploy del ops-worker (CI `ops-worker-deploy.yml` on push develop). Con el flag OFF el handler hace **no-op prod-safe** (cero queries — el schema `greenhouse_growth` puede no existir en prod). (2) Prender (`gcloud run services update ops-worker --update-env-vars GROWTH_FORMS_DISPATCH_ENABLED=true` o ENV-branch en deploy.sh) cuando haya submissions reales que entregar (junto al adapter HubSpot real TASK-1230 + primer form TASK-1232). | Dispatcher async del motor Growth Forms (TASK-1229). Entrega submissions aceptadas (fake/echo en 1229; HubSpot real TASK-1230) fuera del request Vercel, vía Cloud Scheduler (mismo motivo que el outbox publisher TASK-773: staging no corre crons Vercel). Default OFF → el job dispara pero el handler no-opea. Hoy el dispatch también es operable manual por el endpoint admin `POST /api/admin/growth/forms/dispatch`. |
| `GROWTH_FORMS_HUBSPOT_SECURE_SUBMIT_ENABLED` (Cloud Run ops-worker, deploy.sh ENV-branch) | TASK-1230 | **staging: ON (2026-06-25, deploy.sh ENV-branch + live smoke verde)** · prod: OFF | (1) el adapter HubSpot Forms secure-submit está **code-complete + unit-tested** (9 tests fetch-mockeado) + state-machine de retry/dead-letter verificada (smoke e2e). (2) **Rollout pendiente del LIVE smoke** (out-of-band): un **HubSpot test form GUID** + verificar que el private app token (`hubspot-access-token`) tiene scope `forms` (secure-submit lo exige). (3) Prender (`gcloud run services update ops-worker --update-env-vars GROWTH_FORMS_HUBSPOT_SECURE_SUBMIT_ENABLED=true`) tras el live smoke + junto al primer form real (TASK-1232). | Adapter HubSpot Forms secure-submit del motor (TASK-1230). Default OFF → el adapter resuelve `skipped` (cero writes a HubSpot, la submission queda `accepted` esperando). ON → POST a `api.hsforms.com/.../secure/submit/{portalId}/{formGuid}` desde el dispatcher async (NUNCA inline). At-most-once vía state machine de attempts (solo reintenta fallas retryables; nunca re-entrega `delivered`). Revert (<5 min): flag a false → skipped. |
| `GROWTH_FORMS_EMAIL_VERIFICATION_ENABLED` (Vercel) | TASK-1254 | **staging: ON (2026-06-26 — `vercel env add` al env `staging` + push develop + redeploy + smoke live verde:** `ana@acme.com`→corporate pasa, `gmail`→`email_not_corporate`/suspect, `mailinator`→`email_disposable`/suspect, typo-suggest `gmial→gmail`**)** · **prod: OFF — diferido al cutover de forms (TASK-1258):** el master `GROWTH_FORMS_PUBLIC_API_ENABLED` está OFF en prod y el endpoint `verify-email` ahora exige AMBOS flags → flipear el email flag solo en prod es 404/no-op hasta el primer submit prod (mismo patrón que `GROWTH_FORMS_SERVER_VALIDATION_ENABLED`) | (1) **code-complete (scaffold sin provider real)**: Tier 1 local (gratis) + validador `corporate_email` + puerto `EmailVerificationProvider` con adapter **noop** (`deliverable:'unknown'`) + orquestador Tier1→Tier2 + cache (`email_verification_cache`) + circuit breaker + endpoint público `verify-email` + gate de política en `submitForm` (`block_field`/`warn`/`tag_only`) + 2 reliability signals. (2) Prender requiere: **dar de alta el provider económico** (Abstract API u otro) + publicar el secreto en GCP Secret Manager + grant `secretAccessor` a `greenhouse-portal@` + agregar su adapter en `resolveVerificationProvider()`; **definir la política por form** (`validation_schema_json.emailPolicy`); shadow en staging (observar `growth.forms.email_rejection_rate`). (3) `vercel env add GROWTH_FORMS_EMAIL_VERIFICATION_ENABLED=true` por environment + redeploy. **Cutover gradual:** arrancar en `warn` (etiqueta `suspect`, no rechaza), observar rejection rate, subir a `block_field` donde el negocio lo pida. **TASK-1263 (2026-06-28) — gate cableado en el grader:** el gate de TASK-1254 vivía solo en `submitForm`, pero el intake del grader NUNCA pasa por ahí → se extrajo el helper canónico `evaluateFormEmailGate` (un primitive, 3 consumers) y se cableó en las DOS fachadas del grader (`createPublicGraderRun` + `createPublicGraderRunViaFormsEngine`). Activación gobernada del grader-form = `npx tsx --require ./scripts/lib/server-only-shim.cjs scripts/growth/activate-grader-email-gate.ts --apply` (dry-run default; publica v3 con `emailPolicy.block_field` + `destination_policy`/`retention_policy`, deprecando v2). **Rollout staging APLICADO + verificado (2026-06-28):** deploy staging READY → `--apply` publicó v3 (`fver-9475a038…`) con `emailPolicy.block_field`, deprecó v2; smoke funcional contra config real (gmail/temporal rechazados, corporativo pasa); captcha fail-closed confirmado. **Prod pendiente:** cutover TASK-1246 (flag ya OFF en prod). | Gate de correo corporativo del motor Growth Forms (TASK-1254). Default OFF → `submitForm` NO aplica política de email (legacy) y `POST /api/public/growth/forms/{slug}/verify-email` responde 404 `disabled`. ON → Tier1-first (gratis) + Tier2 (provider, hoy noop=unknown) + cache + política por form. Autoridad server-side (el botón es UX). Revert (<5 min): flag a false → legacy. Follow-up: enchufar provider real + signals `email_provider_error_rate`/`email_verification_cache_hit_rate` (requieren contadores runtime). |
| `GROWTH_FORMS_SERVER_VALIDATION_ENABLED` (Vercel) | TASK-1253 | **staging: ON (2026-06-26, shadow TASK-1261 → 0 falsos rechazos + flip; redeploy al push)** · prod: OFF (flip prod **diferido a propósito** al cutover de TASK-1258, bundle con `GROWTH_FORMS_PUBLIC_API_ENABLED` — decisión operador 2026-06-26: en prod ambos están OFF, sin tráfico → flipear validación sola sería no-op y no smoke-verificable; debe nacer ON junto con el primer submit prod) | (1) la autoridad de validación server-side está **code-complete** (validator registry canónico compartido renderer+`submitForm`, `national_id` multi-país, normalización, test de paridad). Nace **OFF by design** (patrón flag default-OFF + shadow + flip): una re-validación que RECHAZA podría bloquear submissions legítimas si un validador queda muy estricto. (2) Prender requiere shadow en staging (observar tasa de rechazo con un form real publicado) + sign-off. (3) `vercel env add GROWTH_FORMS_SERVER_VALIDATION_ENABLED=true` por environment + redeploy → `submitForm` re-valida por tipo + normaliza + rechaza basura; sin el flag, comportamiento legacy (cliente valida por UX, server no re-valida). | Autoridad de validación del motor (TASK-1253). Default OFF → `submitForm` persiste `{...input.fields}` sin re-validar por tipo (legacy). ON → re-valida cada campo presente con el registry canónico (mismo core que el renderer → paridad), normaliza (email lowercased / E.164 / RUT / número), hashea el email YA normalizado para dedupe, y rechaza con `invalid` el primer campo con formato inválido. Degradación honesta: `field_schema_json` no parseable → señal Sentry + sigue con raw (no rompe el form). Revert (<5 min): flag a false → legacy. Follow-up: reliability signals `server_validation_rejected` / `validation_fallback_used` + flip post-shadow. |
| `GROWTH_FORMS_PII_ENCRYPTION_ENABLED` (Vercel) | TASK-1255 | **staging: ON (2026-06-26 — `vercel env add` flag=true al env `staging` + `GROWTH_FORMS_PII_ENCRYPTION_KEY_SECRET_REF=greenhouse-growth-forms-pii-encryption-key` a staging+production + push develop + redeploy)** · **prod: OFF (SECRET_REF ya seteado en production; el flip del flag va por el release control plane develop→main, no unilateral — el cifrado en prod es seguro/recomendado, no requiere sign-off legal: lo legal es la ventana de retención de Slice 4)** · dev local: SECRET_REF en `.env.local`, flag OFF | (1) **code-complete (Slices 1-3)**: cifrado at-rest de `national_id` (AES-256-GCM, key `greenhouse-growth-forms-pii-encryption-key` en GCP Secret Manager + grant `secretAccessor` a `greenhouse-portal@` — ya creada) + masked-reader por default + reveal gobernado (capability `growth.forms.lead_pii.reveal` + reason ≥10 + audit append-only + outbox + signal). Nace **OFF by design** (patrón flag default-OFF + shadow + flip): el cifrado mueve `national_id` fuera de `normalized_fields_json` → con flag ON la cédula deja de viajar al dispatcher (el boundary de redacción es incondicional, igual aplica con flag OFF). (2) Prender requiere: la key publicada por environment vía `GROWTH_FORMS_PII_ENCRYPTION_KEY_SECRET_REF` (Vercel env → secret name) + redeploy; verificar con un lead de prueba que el RUT queda cifrado en DB y el log redactado. **Fail-closed**: con el flag ON sin key resuelta, `submitForm` devuelve `error` (503) y NO persiste la cédula en claro. (3) `vercel env add GROWTH_FORMS_PII_ENCRYPTION_ENABLED=true` + `GROWTH_FORMS_PII_ENCRYPTION_KEY_SECRET_REF=greenhouse-growth-forms-pii-encryption-key` por environment + redeploy. Prerrequisito del cutover público que capture RUT (TASK-1246). | Cifrado at-rest de national_id del motor Growth Forms (TASK-1255, Ley 21.719). Default OFF → la cédula queda en claro en `normalized_fields_json` (legacy), pero el masked-reader igual la enmascara en lectura y el dispatcher igual la redacta (boundary incondicional). ON → `submitForm` separa national_id del blob, lo cifra → `encrypted_fields_json`. Reveal gateado por capability (no por flag). Revert (<10 min): flag a false → escribe en claro; la key se conserva → descifrar siempre posible. **Pendiente Slice 4 (diferido):** `GROWTH_FORMS_RETENTION_PURGE_ENABLED` (purga de retención, ventana default 24m) — aún NO declarado en código, espera sign-off legal sobre la ventana. |

## § Snapshot de estado por environment

> Snapshot **2026-06-18** vía `vercel env ls`. Un flag **ausente** de una columna = NO seteado = OFF/default en ese environment. **Verdad live: `vercel env ls`.**

| Flag | Production | staging | Preview/dev | Owner |
|---|---|---|---|---|
| `NEXA_KNOWLEDGE_RETRIEVAL_ENABLED` (+`NEXT_PUBLIC`) | ✅ | ✅ | — | TASK-1085 |
| `NEXA_SYSTEM_PROMPT_V2_ENABLED` | ✅ | ✅ | — | TASK-1124 |
| `NEXA_ACTION_RUNTIME_ENABLED` | ✅ | ✅ | — | TASK-1137 |
| `NEXA_AUTO_ROUTER_ENABLED` | ✅ | ✅ | — | TASK-1091 |
| `NEXA_SUGGESTED_PROMPTS_DATA_AWARE_ENABLED` (`NEXT_PUBLIC`) | ✅ | ✅ | — | TASK-1087 |
| `NEXA_FORCE_KNOWLEDGE_RETRIEVAL_ENABLED` | — | ✅ | — | TASK-1156 |
| `NEXA_KNOWLEDGE_SYNTHESIS_BRIEF_ENABLED` | — | ✅ | — | TASK-1124 |
| `NEXA_ANSWERS_CANVAS_LENS_ENABLED` | — | ✅ | — | TASK-1101 |
| `NEXA_INTERACTION_LANE_ENABLED` (+`NEXT_PUBLIC`) | — | ✅ (redeploy pend.) | — | TASK-1079 |
| `NEXA_QUOTE_AUTHOR_ACTION_ENABLED` | — | — | — | TASK-1212 |
| `COMMERCIAL_Q2C_CANONICAL_CLOSE_ENABLED` | ✅ (2026-06-29, release `056c2dde8` + redeploy `9mxqphay0`) | ✅ (2026-06-22, smoke HTTP PASS) | — | TASK-1206 |
| `COMMERCIAL_Q2C_CONTRACT_ONLY_ENABLED` | — | — | — | TASK-1206 |
| `PUBLIC_SITE_COMPARISON_TABLE_WRITES_ENABLED` | — | ✅ (2026-06-23, write path live + proven) | — | TASK-1225 |
| `NEXT_PUBLIC_NEXA_FLOATING_EXPANDABLE_ENABLED` | ✅ | ✅ | ✅ | TASK-1078 |
| `KNOWLEDGE_SEARCH_HYBRID_ENABLED` | — | ✅ | — | TASK-1151 |
| `KNOWLEDGE_SEARCH_RERANK_ENABLED` | — | ✅ | — | TASK-1140 |
| `NOTION_KNOWLEDGE_WEBHOOK_ENABLED` | ✅ | — | — | TASK-1094 |
| `NOTION_STATUS_TRANSITIONS_WEBHOOK_ENABLED` | ✅ | — | — | TASK-913 |
| `HOME_V2_ENABLED` | — | ✅ | — | TASK-696/780 |
| `CLIENT_LIFECYCLE_ONBOARDING_ENABLED` (+`NEXT_PUBLIC`) | ✅ | ✅ | Preview | TASK-1001/1009 |
| `CLIENT_LIFECYCLE_HUBSPOT_DEAL_TRIGGER_ENABLED` | ✅ | — | — | TASK-1001 |
| `ONBOARDING_ITEM_EVIDENCE_AUTOCOMPLETE_ENABLED` | ✅ | ✅ | Preview | TASK-1017 |
| `PAYROLL_WORKFORCE_INTAKE_GATE_ENABLED` | ✅ | ✅ | Preview | TASK-872 |
| `PAYROLL_PARTICIPATION_WINDOW_ENABLED` | ✅ | ✅ | — | TASK-890 |
| `PAYROLL_EXIT_ELIGIBILITY_WINDOW_ENABLED` | ✅ | ✅ | — | TASK-891 |
| `LEAVE_PARTICIPATION_AWARE_ENABLED` | ✅ | ✅ | — | TASK-892 |
| `SCIM_INTERNAL_COLLABORATOR_PRIMITIVE_ENABLED` | ✅ | ✅ | Preview | TASK-872 |
| `CONTRACTOR_PAYABLE_SETTLEMENT_ENABLED` | ✅ | ✅ | — | EPIC-013 |
| `RETENTION_POSITION_ENABLED` | ✅ (2026-06-29, release `056c2dde8` + redeploy `9mxqphay0`) | ✅ (2026-06-20, redeploy via push) | — | TASK-1188 |
| `PPM_POSITION_ENABLED` | — | — | — | TASK-1189 |
| `FINANCE_BIGQUERY_WRITE_ENABLED` | ✅ | — | — | Finance |
| `FINANCE_RECONCILIATION_AI_ENABLED` | ✅ | — | — | TASK-934 |
| `FINANCE_DISTRIBUTION_AI_ENABLED` | ✅ | — | — | Finance |
| `FINANCE_CORE_MXN_ENABLED` | — | ✅ | Preview | Finance MXN |
| `FINANCE_MULTI_CURRENCY_REPORTING_ENABLED` | — | ✅ | Preview | Finance MXN |
| `FINANCE_MXN_PAYMENT_ORDERS_ENABLED` | — | ✅ | Preview | Finance MXN |
| `NUBOX_EXPORT_FOREIGN_CURRENCY_ENABLED` | — | ✅ | Preview | Finance/Nubox |
| `HIRING_HANDOFF_BRIDGES_ENABLED` | TASK-356 | **OFF/default en todos los environments** (verdad live: `vercel env ls`) · code-complete + gated. El **materializer reactivo NO va gateado** (corre siempre — un no-op sería terminal en `outbox_reactive_log` y perdería eventos); el flag gatea SOLO los readers/bridges: `listInternalHireReadyForOnboarding` (cola para TASK-770) y `listStaffAugmentationHandoffIntents` (bridge Staff Aug), ambos retornan `enabled:false` explícito con flag OFF. | ✅ ops-worker redeployado 2026-07-10 (push a develop → workflow `Ops Worker Deploy` success, revisión `ops-worker-00476-l6z` sirviendo 100%; lane `ops-reactive-people` drenando en la revisión nueva — projection `hiring_handoff_materialize` registrado). Pendiente: (1) flip en staging cuando TASK-770 tenga su primer consumer → verificar cola + señales `hiring.*` steady; (2) prod tras sign-off. | **Runtime: Vercel** (readers los consume la UI/API de 770) **+ ops-worker si un bridge reactivo futuro lo lee** — hoy ningún código del worker lo consume, pero declarar en `deploy.sh` al prenderlo para que no lo borre un `--set-env-vars` destructivo. Config: `src/lib/hiring/handoff/config.ts`. |
| `HIRING_ACTIVATION_ENABLED` | TASK-770 | **OFF/default en todos los environments** (verdad live: `vercel env ls`) · code-complete + gated. Gatea los commands + readers del bridge hiring→HRIS (`/api/hr/hiring-activation/**`, cola merged, detail). Se apila sobre `HIRING_HANDOFF_BRIDGES_ENABLED` (356): sin la cola, el queue reader retorna `enabled:false` aunque este flag esté ON. | ✅ código desplegado 2026-07-10 (push `e10400603`, 7/7 workflows verdes, ops-worker `00477-nm6`, staging Ready). Pendiente: (1) flip conjunto con `HIRING_HANDOFF_BRIDGES_ENABLED` en staging cuando TASK-1368 entregue la UI → smoke con un internal_hire real (member pending_intake + onboarding + complete con evidencia); (2) sign-off People/HRIS; (3) prod con cooldown. | **Runtime: Vercel** (API/UI; ningún worker lo lee — verificado con grep). Config: `src/lib/workforce/hiring-activation/config.ts`. La ACTIVACIÓN del colaborador no depende del flag (pasa por completeWorkforceMemberIntake, siempre ON). |
| `GREENHOUSE_SISTER_PLATFORM_OAUTH_ENABLED` | — | ✅ | — | Kortex bridge |
| `KORTEX_COMMAND_*` / `KORTEX_GITHUB_*` (varios) | — | ✅ | — | Kortex bridge |
| `WORKFORCE_CONTRACTING_AI_ENABLED` | — | — | Dev/Preview | TASK-1019 |
| `HIRING_ASSESSMENT_AI_ENABLED` | — | — | — | TASK-1361 |
| `HIRING_PUBLIC_APPLICATIONS_ENABLED` | ✅ (2026-07-09, env SET; route live; opening real `EO-OPN-0009` publicado; submit smoke pendiente) | ✅ (2026-07-09, env SET; release `433cfa2b0fd3` + follow-up `915be02a86ab`, route live; submit smoke pendiente) | — | TASK-1367/TASK-354 |
| `HIRING_HANDOFF_BRIDGES_ENABLED` | — | — | — | TASK-356 (materializer siempre-ON sin flag; el flag gatea solo readers/bridges) |
| `HIRING_ACTIVATION_ENABLED` | — | — | — | TASK-770 (bridge hiring→HRIS; se apila sobre el flag de 356) |
| `GROWTH_AI_VISIBILITY_GRADER_ENABLED` | — | ✅ (2026-06-24) | — | TASK-1226 |
| `GROWTH_AI_VISIBILITY_OPENAI_ENABLED` | — | ✅ (2026-06-24, smoke real local OK) | — | TASK-1226 |
| `GROWTH_AI_VISIBILITY_ANTHROPIC_ENABLED` | — | ✅ (2026-06-24, smoke real local OK) | — | TASK-1226 |
| `GROWTH_AI_VISIBILITY_PERPLEXITY_ENABLED` | — | ✅ (2026-06-29, ops-worker `00418-2m6`; Vercel staging ya estaba registrado) | — | TASK-1226/TASK-1249 (prod OFF; smoke async post-flip recomendado) |
| `GROWTH_AI_VISIBILITY_GEMINI_ENABLED` | — | ✅ (2026-06-24, TASK-1233, smoke real local OK) | — | TASK-1226/1233 |
| `GROWTH_AI_VISIBILITY_GOOGLE_AIO_ENABLED` | — | ✅ (2026-06-28, ops-worker + Vercel staging, smoke real DataForSEO OK) | — | TASK-1265 (prod OFF; rotar credencial antes de prod) |
| `GROWTH_AI_VISIBILITY_PUBLIC_INTAKE_ENABLED` | — | ✅ (2026-06-25, smoke staging OK) | — | TASK-1240 (prod OFF; sign-off legal + captcha prod) |
| `GROWTH_AI_VISIBILITY_REPORT_EMAIL_ENABLED` (ops-worker + Vercel) | — | ✅ (2026-06-27, smoke E2E sent) | — | TASK-1250 (prod OFF; gated TASK-1246) |
| `GROWTH_AI_VISIBILITY_OPERATOR_SEND_ENABLED` (ops-worker + Vercel) | — | ⚠️ **CORREGIDO 2026-06-29 (TASK-1291): ABSENT en TODOS los envs Vercel** (`vercel env ls` = SoT; el "staging ON" anterior quedó stale desde el smoke de TASK-1279). La cross-sell SEND está gateada OFF (command rechaza `aeo_send_disabled`). | — | TASK-1279/1291 (re-enable gateado por eval TASK-1292 + sign-off) |
| `GROWTH_AI_VISIBILITY_LLM_EXTRACTION_ENABLED` | — | — | — | TASK-1227 (gatea el extractor de prosa; el proveedor lo elige `_PROSE_EXTRACTION_PROVIDER`, TASK-1271, default `anthropic`) |
| `GROWTH_EBOOK_EMAIL_DELIVERY_ENABLED` (**ops-worker / Cloud Run**, NO Vercel) | ✅ (2026-07-10, rev `ops-worker-00474-jrf` + declarado en `deploy.sh`) | — | — | TASK-1375 (flujo crítico: la success card promete el email). El flip vía sólo `--update-env-vars` fue borrado por el deploy siguiente |
| `GROWTH_AI_VISIBILITY_PROSE_EXTRACTION_SHADOW_ENABLED` | — | — | — | TASK-1271 (eval-only; default OFF; NO afecta el path de runs — lo consume sólo el harness/CLI de eval) |
| `GROWTH_AI_VISIBILITY_ASYNC_EXECUTION_ENABLED` | — | ✅ (2026-06-24, smoke real `full` EO-GRUN-00011 OK) | — | TASK-1234 |
| `GROWTH_AI_VISIBILITY_PORTAL_RUN_ENABLED` | — | ✅ (2026-06-29, smoke real: Berel contratado accepted + run `portal_contracted` drenado; org sin entitlement denied) | — | TASK-1277 (prod OFF; gated EPIC-020 + sign-off comercial) |
| `GROWTH_AI_VISIBILITY_TRIAL_ENABLED` | — | ✅ (2026-06-29, inerte sin orgs trial provisionadas; provisión PLG = decisión comercial) | — | TASK-1277 (prod OFF; tier trial PLG) |
| `GROWTH_AI_VISIBILITY_PROBES_ENABLED` | — | ✅ (2026-06-28, smoke E2E real EO-GRUN-00025 — structural 78.8, 8/8 probes sin fallo) | — | TASK-1266 (prod OFF, gated EPIC-020) |
| `GROWTH_AI_VISIBILITY_AGENTIC_READINESS_ENABLED` | — | ✅ (2026-06-28, agentic 43.8 en el mismo run) | — | TASK-1266 (prod OFF; requiere `PROBES` ON) |
| `GROWTH_AI_VISIBILITY_ENTITY_PROBES_ENABLED` | — | ✅ (2026-06-28, flip staging Vercel+ops-worker; KG api key publicada+wired → 3 probes activos) | — | TASK-1267 (prod OFF; eje `entity` KG/Wikidata/Reddit; requiere `PROBES` ON; secret `greenhouse-google-knowledge-graph-api-key`) |
| `GROWTH_AI_VISIBILITY_FIX_IT_ENABLED` | — | ✅ (2026-06-28, Vercel staging + deploy `greenhouse-fyoycag0t` Ready) | — | TASK-1269 (artifacts JSON-LD/llms/briefs, gated copy/legal; prod OFF) |
| `GROWTH_AI_VISIBILITY_REGRADE_ENABLED` (ops-worker) | — | ✅ (2026-06-29, scheduler enabled; no-op smoke `no_due_profiles`) | — | TASK-1270 (prod OFF; E2E opt-in pendiente) |
| `GROWTH_SEARCH_CONSOLE_ENABLED` | — | ✅ (2026-06-29, wiring smoke verde) | — | TASK-1282 (staging ON; flujo property-picker; OAuth External+Producción; prod pendiente release control plane) |

---

## § Cómo prender un env-var flag

> ⚠️ **Un flag no es "el flag de Vercel".** Es una env var leída por **cada runtime que ejecuta el código gateado**. Greenhouse tiene **5 runtimes con env vars totalmente independientes**. Prenderlo en el runtime equivocado = no pasa nada (y peor: parece prendido en el ledger). **El paso 0 no es opcional.**

### Los 5 runtimes y sus env vars

| Runtime | Qué ejecuta | Cómo se prende | Cómo se verifica |
|---|---|---|---|
| **Vercel** (app Next.js) | API routes, server components, UI, crons de `vercel.json` | `vercel env add` + redeploy | `vercel env ls` |
| **`ops-worker`** (Cloud Run, `us-east4`) | **projections reactivas, consumers del outbox, crons de Cloud Scheduler**, emails async | `gcloud run services update ops-worker` | `gcloud run revisions describe` |
| **`commercial-cost-worker`** (Cloud Run, `us-east4`) | jobs de costo comercial | `gcloud run services update commercial-cost-worker` | idem |
| **`ico-batch-worker`** (Cloud Run, `us-east4`) | batches ICO / delivery metrics | `gcloud run services update ico-batch-worker` | idem |
| **`hubspot-greenhouse-integration`** (Cloud Run, `us-central1`) | bridge + webhooks HubSpot | `gcloud run services update hubspot-greenhouse-integration --region us-central1` | idem |

### Paso 0 — Mapear en QUÉ runtime(s) vive el flag (ANTES de prender)

```bash
# Encontrar dónde se LEE el flag (no dónde se declara)
grep -rn "<FLAG_NAME>\|is<Feature>Enabled" src/ services/ | grep -v __tests__
```

Traducí cada path a su runtime:

| Path donde se lee | Runtime real |
|---|---|
| `src/app/**`, `src/views/**`, `src/components/**` | **Vercel** |
| `src/lib/sync/projections/**` (projection reactiva) | **ops-worker** (la drena `ops-reactive-*`) |
| `services/ops-worker/**` | **ops-worker** |
| `services/<otro-worker>/**` | ese worker |
| `src/lib/**` compartido | **depende de los callers** → seguir la cadena hasta un `src/app/**` (Vercel) y/o una projection/worker (Cloud Run). Puede ser **los dos**. |

**Heurística de bolsillo:** ¿el flag gatea algo **async** (email de respaldo, projection, consumer del outbox, cron de Scheduler, materializer)? → **ops-worker, NO Vercel.** ¿Gatea una **ruta/superficie visible**? → **Vercel.** ¿Ambas cosas? → **ambos** (ej. `GROWTH_AI_VISIBILITY_REPORT_EMAIL_ENABLED`, marcado `(ops-worker + Vercel)` en el snapshot).

### Paso 1 — Prender en CADA runtime del mapeo

> ⚠️ **En Cloud Run, `--update-env-vars` NO es el flip: es un parche efímero.** Los `deploy.sh` de los workers usan **`--set-env-vars` (destructivo)**, que reescribe el set completo de env vars. Una var agregada out-of-band con `--update-env-vars` **sobrevive hasta el siguiente deploy del worker y después desaparece en silencio**. El **SoT es `services/<worker>/deploy.sh`**: ahí se declara el flag. Por eso ese archivo está lleno de comentarios "Declarativo acá para que `--set-env-vars` (destructivo) NO lo borre".

```bash
# --- Vercel (si aplica) --------------------------------------------------
printf %s "true" | vercel env add <FLAG_NAME> <environment> --scope efeonce-7670142f
#    environments: Production | staging | preview | development  (staging = custom env)
#    Si el flag tiene mirror NEXT_PUBLIC_*, agregar AMBAS.
```

**Cloud Run (si aplica) — SIEMPRE los dos pasos, en este orden:**

**(a) Declararlo en `services/<worker>/deploy.sh`** (SoT, sobrevive deploys) siguiendo la convención del archivo:

```bash
# <TASK-###> — <qué gatea>. Declarativo acá para que `--set-env-vars` (destructivo) NO lo
# borre en cada redeploy. Rollback (<5min): gcloud run services update <svc> --update-env-vars <FLAG>=false
<FLAG_NAME>="${<FLAG_NAME>:-true}"
ENV_VARS="${ENV_VARS},<FLAG_NAME>=${<FLAG_NAME>}"
```

**(b) Aplicarlo en vivo** para que tome efecto ya, sin esperar un deploy del worker:

```bash
gcloud run services update <servicio> \
  --region <us-east4|us-central1> --project efeonce-group \
  --update-env-vars <FLAG_NAME>=true
#    Crea la revisión y enruta tráfico. Pero SIN el paso (a), el próximo deploy lo borra.
```

Hacer sólo (b) es el bug class del 2026-07-10: `GROWTH_EBOOK_EMAIL_DELIVERY_ENABLED` se prendió en la revisión `00470`, tres deploys después (`00473`) ya no estaba, y el consumer reactivo registró `skip: flag OFF` mientras la success card del form le prometía el email al usuario. **Fallo silencioso: el ledger decía ON, la realidad era OFF.**

### Paso 2 — Redeploy donde haga falta

- **Vercel server var** (`*_ENABLED`): aplica en el próximo deploy (las env vars NO se toman en caliente).
- **Vercel `NEXT_PUBLIC_*`**: se hornea en build → requiere **BUILD FRESCO** (push a la rama del env, o Redeploy con "Use existing Build Cache" DESMARCADO).
- **Cloud Run**: el `services update` ya crea la revisión. Nada más.

### Paso 3 — Verificar en CADA runtime (que la var exista Y que el consumer se comporte)

```bash
# Vercel
vercel env ls | grep <FLAG_NAME>

# Cloud Run — leer la env de la REVISIÓN ACTIVA (no del servicio "en general")
REV=$(gcloud run services describe <servicio> --region <region> --project efeonce-group \
        --format='value(status.latestReadyRevisionName)')
gcloud run revisions describe "$REV" --region <region> --project efeonce-group \
  --format="json(spec.containers[0].env)" | grep -A1 <FLAG_NAME>
```

Y además: **ejercitar el flujo real**. Que la var exista no prueba que el consumer haga lo suyo. Si el flag gatea un email async, el smoke es "llega el email", no "la var está".

### Paso 4 — Actualizar este ledger

Registrar en la fila **el/los runtime(s)** donde quedó prendido + fecha + revisión (Cloud Run) — igual que
`GROWTH_EBOOK_EMAIL_DELIVERY_ENABLED` (**ops-worker**, rev `ops-worker-00470-898`). Sin el runtime explícito, el próximo agente asume Vercel y se equivoca.

### Apagar (rollback)

```bash
vercel env rm <FLAG_NAME> <environment> --scope efeonce-7670142f   # + redeploy
gcloud run services update <servicio> --region <region> --project efeonce-group \
  --remove-env-vars <FLAG_NAME>
```

(Quitar la var = OFF/default.) **Apagar también es multi-runtime**: quitarla de Vercel no la apaga en el worker.

Para los **PG rollout flags** (`home_rollout_flags`): se prenden vía admin endpoint sin redeploy — ver el platform doc.

---

## § Inventario completo (referencia por dominio)

> Todos los `*_ENABLED` referenciados en código. **Default = OFF** salvo nota. Owner = task ancla. El estado live por env está arriba (§ Snapshot) o en `vercel env ls`.

**Nexa / Knowledge** (`src/lib/nexa/flags.ts`, `src/lib/knowledge/search/flags.ts`):
`NEXA_FLOATING_EXPANDABLE_ENABLED` (panel B, TASK-1078) · `NEXA_INTERACTION_LANE_ENABLED` (lane C, TASK-1079) · `NEXA_KNOWLEDGE_RETRIEVAL_ENABLED` (TASK-1085) · `NEXA_FORCE_KNOWLEDGE_RETRIEVAL_ENABLED` (TASK-1156) · `NEXA_AUTO_ROUTER_ENABLED` (TASK-1091) · `NEXA_SYSTEM_PROMPT_V2_ENABLED` (TASK-1124) · `NEXA_KNOWLEDGE_SYNTHESIS_BRIEF_ENABLED` (TASK-1124) · `NEXA_ACTION_RUNTIME_ENABLED` (TASK-1137) · `NEXA_QUOTE_AUTHOR_ACTION_ENABLED` (TASK-1212) · `NEXA_ANSWERS_CANVAS_LENS_ENABLED` (TASK-1101) · `NEXA_SUGGESTED_PROMPTS_DATA_AWARE_ENABLED` (TASK-1087) · `KNOWLEDGE_SEARCH_HYBRID_ENABLED` (TASK-1151) · `KNOWLEDGE_SEARCH_RERANK_ENABLED` (TASK-1140) · `KNOWLEDGE_REACTIVE_EMBEDDING_ENABLED` (TASK-1155) · `NOTION_KNOWLEDGE_WEBHOOK_ENABLED` (TASK-1094). Provider pin (no `*_ENABLED`): `NEXA_PROVIDER`.

**Payroll / Workforce:** `PAYROLL_PARTICIPATION_WINDOW_ENABLED` (TASK-890) · `PAYROLL_EXIT_ELIGIBILITY_WINDOW_ENABLED` (TASK-891) · `LEAVE_PARTICIPATION_AWARE_ENABLED` (TASK-892) · `PAYROLL_CONTRACTOR_ENGAGEMENT_EXCLUSION_ENABLED` · `PAYROLL_WORKFORCE_INTAKE_GATE_ENABLED` (TASK-872) · `WORKFORCE_ACTIVATION_READINESS_GUARD_ENABLED` · `SCIM_INTERNAL_COLLABORATOR_PRIMITIVE_ENABLED` (TASK-872) · `WORKFORCE_CONTRACTING_AI_ENABLED` (TASK-1019).

**Hiring / ATS (EPIC-011):** `HIRING_ASSESSMENT_AI_ENABLED` (TASK-1361 — gatea los propose paths del AI assist del motor de assessment; `src/lib/hiring/assessment/ai/config.ts`; code-complete + gated, ver § Pendientes) · `HIRING_PUBLIC_APPLICATIONS_ENABLED` (TASK-1367/TASK-354 — gatea el endpoint público de postulación `POST /api/public/hiring/applications`; `src/lib/hiring/public-careers/config.ts`; staging+Production env SET 2026-07-09, production release `433cfa2b0fd3` live, opening real `EO-OPN-0009` publicado, submit smoke pendiente; default OFF = 404 invisible) · `HIRING_HANDOFF_BRIDGES_ENABLED` (TASK-356 — gatea SOLO los readers/bridges downstream del HiringHandoff: cola internal_hire para TASK-770 + intents Staff Aug; el materializer reactivo corre siempre sin flag; `src/lib/hiring/handoff/config.ts`; default OFF = `enabled:false` explícito) · `HIRING_ACTIVATION_ENABLED` (TASK-770 — gatea commands+readers del bridge de activación hiring→HRIS; `src/lib/workforce/hiring-activation/config.ts`; default OFF).

**Storage / Assets (TASK-1362):** `ASSET_MALWARE_SCAN_ENABLED` (TASK-1362 — suma el adapter `clamav-http` encima del scanner estructural en `scanAssetBytes`; `src/lib/storage/asset-scan/config.ts`; **runtime: Vercel** — el upload público de CV corre en el route handler de Next, ningún Cloud Run sube assets de usuario hoy; code-complete + NO verificado contra servicio real, ver § Pendientes). Acompañantes (no `*_ENABLED`): `ASSET_MALWARE_SCAN_ENDPOINT`, `ASSET_MALWARE_SCAN_TIMEOUT_MS`.

**Finance:** `FINANCE_BIGQUERY_WRITE_ENABLED` · `FINANCE_RECONCILIATION_AI_ENABLED` (TASK-934) · `FINANCE_DISTRIBUTION_AI_ENABLED` · `FINANCE_CORE_MXN_ENABLED` · `FINANCE_MULTI_CURRENCY_REPORTING_ENABLED` · `FINANCE_MXN_PAYMENT_ORDERS_ENABLED` · `FINANCE_MXN_BEREL_BACKFILL_APPLY_ENABLED` · `NUBOX_EXPORT_FOREIGN_CURRENCY_ENABLED` · `RETENTION_POSITION_ENABLED` (TASK-1188, línea retenciones del F29 — code-complete + shadow, ver § Pendientes; `src/lib/finance/retention/flags.ts`) · `PPM_POSITION_ENABLED` (TASK-1189, línea PPM del F29 — code-complete + shadow; `src/lib/finance/ppm/flags.ts`). **Contractor:** `CONTRACTOR_PAYABLE_SETTLEMENT_ENABLED` · `CONTRACTOR_AGREED_AMOUNT_GUARDRAIL_ENABLED` (EPIC-013).

**ICO / Delivery metrics:** `ICO_MATERIALIZER_FRESHNESS_GATE_ENABLED` · `ICO_MATERIALIZER_INCREMENTAL_DELTA_ENABLED` · `ICO_MATERIALIZER_MERGE_PATTERN_ENABLED` · `ATTRIBUTABLE_LATENESS_OTD_ENABLED` · `OTD_CLASSIFIER_GH_SHADOW_ENABLED` · `CT_DAYS_CANONICAL_FORMULA_ENABLED` · `CT_SLO_PCT_METRIC_ENABLED` · `NOTION_RPA_WRITEBACK_ENABLED` · `NOTION_FTR_WRITEBACK_ENABLED` · `NOTION_OTD_WRITEBACK_ENABLED` (+ `_EFEONCE`/`_SKY`, TASK-927) · `NOTION_DUE_DATE_CAPTURE_ENABLED` · `NOTION_STATUS_TRANSITIONS_WEBHOOK_ENABLED` (TASK-900…943).

**Client lifecycle / Onboarding:** `CLIENT_LIFECYCLE_ONBOARDING_ENABLED` (+`NEXT_PUBLIC`) · `CLIENT_LIFECYCLE_HUBSPOT_DEAL_TRIGGER_ENABLED` · `CLIENT_BIRTH_CANONICAL_WRITE_ENABLED` · `ONBOARDING_ITEM_EVIDENCE_AUTOCOMPLETE_ENABLED` · `GREENHOUSE_NOTION_PG_PROJECTION_ENABLED` (TASK-991…1017).

**UI / Design tokens:** `HOME_V2_ENABLED` (TASK-696/780) · `NEXT_PUBLIC_AXIS_NEUTRALS_ENABLED` · `NEXT_PUBLIC_AXIS_SECONDARY_LIME_ENABLED` (TASK-1034/1053).

**Kortex bridge / sister platform:** `GREENHOUSE_SISTER_PLATFORM_OAUTH_ENABLED` · `KORTEX_COMMAND_ADAPTER_ENABLED` · `KORTEX_COMMAND_ADMIN_ENABLED` · `KORTEX_COMMAND_LIVE_EXECUTE_ENABLED` · `KORTEX_GITHUB_COMMANDS_ENABLED` · `KORTEX_GITHUB_WORKFLOW_DISPATCH_ENABLED`.

**Growth / AI Visibility Grader** (`src/lib/growth/ai-visibility/flags.ts`): `GROWTH_AI_VISIBILITY_GRADER_ENABLED` (kill switch global) · `GROWTH_AI_VISIBILITY_OPENAI_ENABLED` · `GROWTH_AI_VISIBILITY_ANTHROPIC_ENABLED` · `GROWTH_AI_VISIBILITY_PERPLEXITY_ENABLED` · `GROWTH_AI_VISIBILITY_GEMINI_ENABLED` · `GROWTH_AI_VISIBILITY_GOOGLE_AIO_ENABLED` (TASK-1226/TASK-1265 — todos default OFF; sin flag/secret el adapter resuelve skip limpio) · `GROWTH_AI_VISIBILITY_LLM_EXTRACTION_ENABLED` (TASK-1227 — fallback LLM de extracción de prosa para el normalizer; default OFF → determinista-first preserva `unknown`) · **Prose extraction router (TASK-1271):** `GROWTH_AI_VISIBILITY_PROSE_EXTRACTION_PROVIDER` (no `*_ENABLED`; `anthropic`|`gemini`|`openai`, default `anthropic` behavior-preserving — elige el proveedor del extractor cuando `LLM_EXTRACTION` está ON) · `GROWTH_AI_VISIBILITY_PROSE_EXTRACTION_SHADOW_ENABLED` (eval-only, default OFF) · config no-`*_ENABLED`: `_PROSE_EXTRACTION_MAX_TOKENS` (default 1024), `_PROSE_EXTRACTION_MAX_COST_USD` (default 0.02), `_PROSE_EXTRACTION_MODEL_{ANTHROPIC,GEMINI,OPENAI}` (override de modelo por proveedor, sin deploy) · `GROWTH_AI_VISIBILITY_ASYNC_EXECUTION_ENABLED` (TASK-1234 — cutover inline→async; default OFF → endpoint ejecuta inline; con ON encola y el worker Cloud Run `ops-growth-grader-drain` ejecuta sin límite de duración). Pin de modelo (no `*_ENABLED`): `GREENHOUSE_GEMINI_GROUNDED_MODEL` (TASK-1233 — override del modelo Gemini de grounding; default `gemini-3-flash-preview`, la última generación disponible en Vertex; bumpear a 3.1/3-pro apenas lleguen, sin deploy). **Site Readiness (TASK-1266):** `GROWTH_AI_VISIBILITY_PROBES_ENABLED` (flag maestro del probe gatherer + eje structural; gateado por `isGraderEnabled`) · `GROWTH_AI_VISIBILITY_AGENTIC_READINESS_ENABLED` (eje agentic; requiere `PROBES` ON) · `GROWTH_AI_VISIBILITY_ENTITY_PROBES_ENABLED` (TASK-1267 — eje `entity`: Google Knowledge Graph + Wikidata/Wikipedia + Reddit-UGC; requiere `PROBES` ON; KG api key vía `GOOGLE_KNOWLEDGE_GRAPH_API_KEY[_SECRET_REF]`, sino degrada honesto) — todos default OFF; readiness es ortogonal al de percepción (NUNCA blended). **Fix-It Artifacts (TASK-1269):** `GROWTH_AI_VISIBILITY_FIX_IT_ENABLED` (default OFF; genera artifacts deterministas public-safe desde report/probes, sin LLM ni writes al sitio). **Run gobernado de portal (TASK-1277):** `GROWTH_AI_VISIBILITY_PORTAL_RUN_ENABLED` · `GROWTH_AI_VISIBILITY_TRIAL_ENABLED` (default OFF; ver § Pendientes). **Recurring re-grade (TASK-1270):** `GROWTH_AI_VISIBILITY_REGRADE_ENABLED` (ops-worker; staging ON, prod OFF; Scheduler active staging/paused prod; opt-in por perfil contratado). **Search Console connection (TASK-1282)** (`src/lib/growth/search-console/flags.ts`): `GROWTH_SEARCH_CONSOLE_ENABLED` (default OFF → `oauth/start` y el reader resuelven disabled; ON tras verificación del consent screen de Google + grant IAM secret-write al prefijo `search-console-token-*`). **AEO brand-aware (EPIC-021):** `GROWTH_AI_VISIBILITY_CATEGORY_GUARD_ENABLED` (TASK-1288 — guard de categoría no resuelta) · `GROWTH_AI_VISIBILITY_BRAND_INTELLIGENCE_ENABLED` (TASK-1288 — lectura grounded) · `GROWTH_AI_VISIBILITY_ARCHETYPE_PROMPTS_ENABLED` (TASK-1290 — prompts por arquetipo/`business_model`; default OFF → pack agencia v1 bit-for-bit; ON → baseline del arquetipo, `unknown`→genérico) · `GROWTH_AI_VISIBILITY_PROMPT_AUTHORING_ENABLED` (TASK-1290 Slice 3 — autoría LLM del set por marca, gateado además por `isGraderEnabled`; default OFF → baseline determinista; ON → el LLM propone el Query Fan-Out brand-specific, 1×/marca, se congela vía approve; smoke live gemini-2.5-flash-lite SKY ok) — todos default OFF, ver § Pendientes. **Host del hub público (TASK-1324, no `*_ENABLED`):** `PUBLIC_GRADER_HUB_URL` — base del hub headless `efeonce-think` que renderiza el informe público (`buildPublicReportUrl` en `hubspot/report-link.ts`, fuente única del link del correo + HubSpot `report_url`; también el redirect puente `next.config.ts`). **Default seguro = `https://think.efeoncepro.com` (hub prod)** → funciona sin setearla; override solo para apuntar a staging del hub. NO reusar `NEXT_PUBLIC_APP_URL` (portal).

**Mirrors `NEXT_PUBLIC_*` (client-readable)** — pares de un flag server que la UI necesita leer client-side: `NEXT_PUBLIC_NEXA_FLOATING_EXPANDABLE_ENABLED` · `NEXT_PUBLIC_NEXA_INTERACTION_LANE_ENABLED` · `NEXT_PUBLIC_NEXA_KNOWLEDGE_RETRIEVAL_ENABLED` · `NEXT_PUBLIC_NEXA_SUGGESTED_PROMPTS_DATA_AWARE_ENABLED` · `NEXT_PUBLIC_CLIENT_LIFECYCLE_ONBOARDING_ENABLED` · `NEXT_PUBLIC_AXIS_NEUTRALS_ENABLED` · `NEXT_PUBLIC_AXIS_SECONDARY_LIME_ENABLED`. Recordá: se hornean en build → prenderlos requiere build fresco.

> Para regenerar/auditar el inventario desde código + cruzarlo contra Vercel y este ledger:
> **`pnpm flags:audit`** (resalta: en código sin registrar · ON en staging pero no prod · OFF everywhere · en Vercel sin código). `--strict` falla si hay flags en código sin registrar acá. Script: `scripts/ci/feature-flags-audit.mjs`.

---

## § Mantenimiento

Este ledger es **doc viva**. Al cerrar una task con flag:

1. Agregá el flag al **§ Inventario** (dominio correcto) en el PR de la feature.
2. Si queda code-complete sin prender → fila en **§ Pendientes de acción**.
3. Al prender/apagar en cualquier env → actualizá **§ Snapshot** con fecha y, si cerraste un pendiente, remové su fila.
4. Refrescá el snapshot completo periódicamente con `vercel env ls` (la verdad live).

**Idea de follow-up (no implementada):** un `pnpm flags:audit` que cruce los flags de código vs `vercel env ls` y resalte "en staging pero no en prod" / "en código pero sin registrar acá" — automatizaría la detección de deuda. Si se materializa, este doc se vuelve el output humano de ese script.
