# Greenhouse — Feature Flag State Ledger (env-var flags)

## Reconciliación 2026-07-27 — Think ya está publicado

Las notas históricas de esta ledger que indicaban “0 tráfico self-serve” o “el grader no está embebido en ninguna
superficie pública” corresponden a la auditoría del 2026-07-01 y quedan superseded. La superficie canónica actual es
`https://think.efeoncepro.com/brand-visibility`, con el form gobernado `fdef-ai-visibility-grader` y el flujo de
reporte tokenizado de Think. El riesgo/costo real debe medirse desde tráfico productivo y el ledger all-in pendiente;
no reutilizar esas notas históricas como estado actual.

> **Tipo de documento:** Ledger operativo vivo (SSOT del ESTADO de los env-var flags)
> **Creado:** 2026-06-18 por Claude (TASK-1079 follow-up)
> **Última actualización:** 2026-07-22 (alcance explícito: este ledger gobierna los flags de **este repo**; los `GLOBE_*_ENABLED` de `efeonce-globe` quedan fuera — ver §`Alcance`. La regla dura #1 se acotó a "de este repo"; ningún estado de flag Greenhouse cambió.)
> **Actualización anterior:** 2026-07-17 (activación productiva de backpressure Cloud Tasks para webhooks Notion)
>
> 🔴 **Condición de retiro nueva (2026-08-23, TASK-1754 Slice F): `HIRING_FAIRNESS_MONITOR_ENABLED` NO se prende en producción hasta que cierre `TASK-1365`.** El contract del enum de etapas retiró `qualified`, `client_review` y `selected`, y `getSelectionFairness` usa **`input.stage ?? 'selected'`** como default: apunta a una etapa que ninguna fila puede tener. Prender el flag hoy no daría un error — daría **cero en silencio**, y un cero silencioso en una métrica de equidad se lee como «no hay impacto adverso», que es la conclusión contraria a la verdad. Mitigación aplicada en el mismo slice: el reader ahora **falla ruidoso** (`hiring_fairness_stage_retired`, 422) en vez de devolver vacío, así que el flip fallaría visible en vez de mentir. La corrección de fondo —re-apuntar el cubo terminal al eje de desenlace— es `TASK-1365`, y **cambia qué mide el four-fifths rule**, así que no cabía dentro de un contract de vocabulario.

> **Delta 2026-08-23 (release `develop→main` del dominio Hiring reparado — TASK-1765/1754/1748/1755):** este batch **no introduce ningún flag nuevo**: `pnpm flags:audit --strict --no-vercel` reporta 0 flags en código sin registrar, y los cinco `*_ENABLED` que el diff toca (`HIRING_SYNTHETIC_DATA_FILTER_ENABLED`, `HIRING_LIFECYCLE_EMAILS_ENABLED`, `HIRING_ASSESSMENT_AI_ENABLED`, `HIRING_ASSESSMENT_AI_RUN_CONFIRM_ENABLED`, `HIRING_EVALUATION_DOSSIER_AI_AUTO_PROPOSE_ENABLED`) ya existen con su estado vigente en Production. **La acción post-release de este release NO es un flip de flag**, sino la cadena de tres migraciones parqueadas en `docs/tasks/pending-migrations/` (contract del enum → backfill de procedencia → `CHECK` del invariante), cada una con su condición de código desplegado.
>
> ⚠️ **Precisión sobre `HIRING_SYNTHETIC_DATA_FILTER_ENABLED` tras TASK-1748, para que nadie lo "arregle" mal:** su fila sigue diciendo *Vercel ÚNICAMENTE*, y **es correcto**, pero desde este release el filtro de procedencia también vive en `src/lib/hiring/talent-pool/projection.ts`, que corre en el **`ops-worker`** (`services/ops-worker/talent-pool-reconcile.ts`, Cloud Scheduler cada 5 min). Ahí el filtro es **incondicional por diseño** — viaja en el JOIN vía `realOnlyPredicate`, no detrás del flag. El motivo está en el encabezado del archivo: leer un flag Vercel-only desde Cloud Run lo encontraría SIEMPRE indefinido, o sea OFF en silencio, y el filtro existiría en el código sin filtrar nada. **NUNCA declarar este flag en `services/ops-worker/deploy.sh`**: no lo necesita, y agregarlo crearía un interruptor para volver al estado roto (mismo criterio que la exclusión del gold set). La condición de la migración parqueada de TASK-1748 ("Slice 1 corre en los DOS runtimes") se cumple con **desplegar el código**, no con prender nada.

> **Delta 2026-07-17 (Notion webhook backpressure):** `NOTION_WEBHOOK_ASYNC_INGESTION_ENABLED=true` en Vercel Production y `false` en staging. La queue compartida `notion-webhook-ingestion` (`us-east4`) está `RUNNING` con `5 dispatch/s`, concurrencia `5`, `100` intentos y retención de retry `7d`. El redeploy productivo activo es `dpl_DkdnLEUFwY3MvxyD9VncYwqzQNj1` sobre SHA `416b12ad140c`; prueba live: límite asíncrono `413` para payload de 750001 bytes, webhook firmado `200/queued:true`, entrega OIDC al worker, dedupe en inbox y backlog cero. Staging permanece OFF porque su alias protegido requiere un bypass adicional para Cloud Tasks; el alias se repuntó al deployment previo con flag OFF. Rollback: pausar queue primero y luego flag OFF + redeploy.
>
> **Delta 2026-07-16 (release develop→main `924f7409e` batch, autorización explícita del operador "prende todos los flags pendientes"):** verificado `vercel env pull production` (verdad live) ANTES de tocar nada — confirmó que la gran mayoría de `§ Pendientes de acción` ya estaba `true` en prod desde el flip masivo 2026-06-30 (filas por-fila stale, ya lo advertía la Delta de esa fecha). Los únicos 8 flags realmente `NOT SET` en prod eran: `HIRING_ASSESSMENT_AI_ENABLED`, `HIRING_VACANCY_AI_ENABLED`, `HIRING_FAIRNESS_MONITOR_ENABLED`, `TENDER_CHAPTER_AUTHOR_ENABLED`, `ARTIFACT_RENDER_JOBS_ENABLED`, `NEXA_PROPOSAL_ACTIONS_ENABLED`, `ASSET_MALWARE_SCAN_ENABLED`, `COMMERCIAL_Q2C_CONTRACT_ONLY_ENABLED`. **Prendidos en Vercel Production (`vercel env add ... production`, todos Vercel-only, sin Cloud Run):** `HIRING_ASSESSMENT_AI_ENABLED` (operador autorizó explícitamente el gate HR/Legal EU AI Act), `HIRING_VACANCY_AI_ENABLED`, `TENDER_CHAPTER_AUTHOR_ENABLED` (recién shipeado en este mismo release, cero horas de staging — riesgo elevado, monitorear), `ARTIFACT_RENDER_JOBS_ENABLED` (el ops-worker/Job ya tenía el flag `true` compartido de fábrica; el gate real vivía en el enqueue Vercel), `NEXA_PROPOSAL_ACTIONS_ENABLED` (ahora coherente con `ARTIFACT_RENDER_JOBS_ENABLED` también ON — ya no genera media-promesa), `COMMERCIAL_Q2C_CONTRACT_ONLY_ENABLED`. **NO prendidos (bloqueador técnico real, no solo falta de sign-off):** `ASSET_MALWARE_SCAN_ENABLED` — sin `ASSET_MALWARE_SCAN_ENDPOINT` (ClamAV no provisionado, TASK-1378 sin decidir) prenderlo hace fail-closed `error` en TODAS las subidas de CV, rompiendo `HIRING_PUBLIC_APPLICATIONS_ENABLED` que ya está live con `EO-OPN-0009` publicada. `HIRING_FAIRNESS_MONITOR_ENABLED` — prod no tiene una policy row de privacidad (aviso/consentimiento para categorías sensibles) equivalente a la sintética de staging; sin ella el capture fail-closed no lee, y no hay contenido legal que fabricar unilateralmente. Ambos quedan en `§ Pendientes de acción` con su bloqueador real. Credenciales Perplexity/DataForSEO (`GROWTH_AI_VISIBILITY_PERPLEXITY_ENABLED`/`_GOOGLE_AIO_ENABLED`) y la tasa placeholder PPM (`PPM_POSITION_ENABLED`) ya estaban `true` en prod desde el 2026-06-30 — no son cambios de este release; rotación de credenciales y validación contable de la tasa siguen pendientes como housekeeping aparte.
>
> **Delta 2026-07-05 (TASK-1341 — DataForSEO AIO drift en `ops-worker`):** el lead magnet público `/brand-visibility` ya ejecuta submit→run→report, pero Google AI Overview quedó degradando en producción como `skipped:missing_secret` en observaciones recientes porque el runtime que ejecuta los runs async es Cloud Run `ops-worker` y la revisión detectó falta de `DATAFORSEO_API_LOGIN` en la revisión efectiva. Vercel env puede estar correcto y aun así ser inerte para runs async. **Estado operativo:** el modo público `light` solicita `google_ai_overview`; si el worker no tiene login + `DATAFORSEO_API_PASSWORD_SECRET_REF`, el informe queda `partial` de forma honesta. **Follow-up formal:** `docs/tasks/to-do/TASK-1341-dataforseo-aio-runtime-config-guard.md` agrega guard/preflight + smoke provider-scoped para aceptar sólo `succeeded` o `skipped:no_ai_overview_block`, nunca `missing_secret`. Revert <5 min: apagar `GROWTH_AI_VISIBILITY_GOOGLE_AIO_ENABLED` o corregir y redeployar `ops-worker`.
>
> **Delta 2026-07-04 (TASK-1333 — root cause categoría/sentiment `unknown` = `LLM_EXTRACTION` OFF en el worker):** el `categoryTaxonomySummary.status=unknown` (y sentiment `unknown`) en TODOS los runs reales tenía como causa raíz un **drift worker-vs-Vercel**: `GROWTH_AI_VISIBILITY_LLM_EXTRACTION_ENABLED` estaba **ON en Vercel** (prod/staging/dev, flip 06-30) pero **`false` en el `ops-worker`** — que es donde ejecutan los runs async (`ASYNC_EXECUTION` ON). La extracción de prosa (que puebla `categoryAssociations` + `sentimentLabel` + `messageDriftClaims` + `brandRank`) sólo corre si el flag está en el runtime que ejecuta el run → con el worker OFF, los 12 runs quedaron con `category_associations` vacío y sentiment `unknown`. Taxonomy/mapper/builder ya funcionaban; **cero código a tocar** (el arch review lo predijo). **Fix (2026-07-04):** `gcloud run services update ops-worker --update-env-vars` prendió los 3 GROWTH que estaban OFF — `LLM_EXTRACTION_ENABLED=true`, `OPERATOR_SEND_ENABLED=true`, `CATEGORY_GUARD_ENABLED=true` (revs `00454-9lb` → `00455-8f7`). **Verificado:** run real con extracción ON → categoría canónica poblada (`sector:banking_insurance`, `category:digital_agency`) + sentiment (`neutral`/`positive`), vs 0 antes. Credencial presente (`ANTHROPIC_API_KEY_SECRET_REF`). **Persistencia (decisión del operador 2026-07-04 "hagamos todo lo necesario"):** `services/ops-worker/deploy.sh` persiste los **3** flags en ON en ambos bloques (staging + prod): `LLM_EXTRACTION` (`:-true`, era el único del grader-stack olvidado en `:-false`), `OPERATOR_SEND` (`true` en ambos bloques) y `CATEGORY_GUARD` (`true` en ambos bloques). El gate original de OPERATOR_SEND/CATEGORY_GUARD (falso-0 para marcas no-agencia) quedó cerrado con **EPIC-021** (motor brand-aware live, ISSUE-110 resuelto). ⚠️ **RIESGO RESIDUAL de CATEGORY_GUARD ON en prod:** una marca cuya categoría no resuelva ni con el grounded read queda **bloqueada** (`aeo_category_unresolved`) — revert = flag a false; señal `growth.ai_visibility.profile_category_unresolved`. Toma efecto en el **próximo deploy del worker** (push develop). **Regla reforzada:** los flags que gatean el path async del grader (extracción/probes/providers/guard) DEBEN estar ON en el `ops-worker`, no sólo en Vercel; el valor de Vercel es inerte para los runs async.
>
> **Delta 2026-07-01 (TASK-1246 — reconciliación de readiness + sign-off legal):** re-verificados los **valores reales** de los flags del lead magnet en Production vía `vercel env pull` (no solo presencia): `GROWTH_AI_VISIBILITY_PUBLIC_INTAKE_ENABLED`, `_GRADER_ENABLED`, `_ASYNC_EXECUTION_ENABLED`, `GROWTH_FORMS_PUBLIC_API/SERVER_VALIDATION/PII_ENCRYPTION/EMAIL_VERIFICATION_ENABLED` y `GROWTH_GRADER_INTAKE_ON_FORMS_ENGINE_ENABLED` = **`true` confirmado** (las filas por-fila que aún dicen "prod: OFF" quedan superseded por esta Delta + el Delta 06-30). **Sign-off legal del consent/aviso de privacidad (Ley 21.719) = CONFIRMADO por el operador** (prerequisito abierto de la fila `PUBLIC_INTAKE` → cerrado). **Pendiente real (no greenhouse-eo o cross-task):** rotar `TURNSTILE_SECRET` (operador, secret fresco de Cloudflare), smoke E2E de la cara pública (repo `efeonce-web`), y cierre formal de TASK-1253/1255 (backfill PII legacy + job de retención). Mitigante (verificado en código): el grader self-serve (`fdef-ai-visibility-grader`) NO está embebido en ninguna superficie pública (no hay page en greenhouse-eo; `/aeo-2/` es el form comercial `efeonce-aeo-diagnostic`→HubSpot, que NO corre el grader; no existe en efeonce-web) → 0 tráfico self-serve real. El render efeonce-web del ADR 06-28 está sin construir/irreal.
>
> **Delta 2026-07-02 (AEO reference conversion copy + TASK-1318 full-name split):** la landing pública `/aeo-2/` mantiene el renderer `<greenhouse-form form-key="b120566a-dd1a-43c8-956a-4e0121e805b8" surface="fhsf-efeonce-aeo-diagnostic" locale="es-CL" color-scheme="light" appearance="bare">` contra `https://greenhouse.efeoncepro.com/growth-forms/renderer-latest.js`. AEO v8 `fver-38d38bbc-6a32-4e2c-bbd7-c0f0fc728c63` declara `style_variant=diagnostic_premium`, `security.captcha`, CTA `Empezar con mi diagnóstico →`, field visible `fullName` label `Nombre completo`, `autocomplete=name`, `validation_schema.namePolicy.split_full_name`, placeholders `Selecciona tu país` / `Selecciona un rango` y destination mapping server-side `firstName -> firstname` / `lastName -> lastname` sin `fullName` hacia HubSpot. El runtime que deriva los nombres está released en production desde `398b7f1b` (orchestrator `28608774955`); `activate-aeo-reference-copy --apply` ahora bloquea si `origin/main` no trae ese runtime salvo override explícito. Elementor `convers` usa el copy de referencia (`Descubre en qué nivel estás hoy — y empieza a subir.`); backup `_gh_backup_before_aeo_conversion_reference_copy_20260702T093257Z`, Kinsta purge, `heroans` estable. Gate live: `pnpm public-website:verify-aeo-live-contract`.
>
> **Delta 2026-06-30 (parity Growth/AEO a staging + development — "prendamos en todas partes donde corresponda", EPIC-021):** se alineó el set Growth/AEO de los `non-production` con prod. **staging (custom env = develop, el que usan):** estaba 27/32; se agregaron los **5 del motor EPIC-021 que faltaban** — `GROWTH_AI_VISIBILITY_OPERATOR_SEND_ENABLED`, `CATEGORY_GUARD_ENABLED`, `LLM_EXTRACTION_ENABLED`, `PROSE_EXTRACTION_SHADOW_ENABLED`, `REGRADE_ENABLED` → **32/32** (perplexity/google_aio ya estaban pre-existentes en staging). Esto corrige el `operator_send_enabled=false` que el doc de TASK-1291 veía en staging. **Redeploy staging `greenhouse-bt9fvga8d`** (los env vars no se activan en caliente). **development:** estaba 13/30; se completaron los 17 restantes → **30/30**. **Hygiene call (riesgo de creds expuestas):** `GROWTH_AI_VISIBILITY_PERPLEXITY_ENABLED` y `GOOGLE_AIO_ENABLED` se **dejaron OFF en development** (NO se replicaron) hasta la rotación de credenciales (TASK-1293); el grader degrada por-provider con gracia. En staging ya estaban (pre-existentes), no se removieron. Verdad live: `vercel env ls staging|development`.
>
> **Delta 2026-06-30 (segundo batch — "prende todas las que faltan", autorización explícita + riesgos aceptados):** redeploy prod `greenhouse-psl47zug8`. Prendidos en Production:
> - **Producto/UX/search:** `KNOWLEDGE_SEARCH_HYBRID_ENABLED`, `KNOWLEDGE_SEARCH_RERANK_ENABLED`, `NEXA_INTERACTION_LANE_ENABLED` (+`NEXT_PUBLIC_*` — ⚠️ necesita BUILD FRESCO para surfacear client-side, el redeploy reusa build), `NEXA_FORCE_KNOWLEDGE_RETRIEVAL_ENABLED`, `NEXA_KNOWLEDGE_SYNTHESIS_BRIEF_ENABLED`, `NEXA_ANSWERS_CANVAS_LENS_ENABLED`, `HOME_V2_ENABLED` (cambia el home de todos), `NEXA_QUOTE_AUTHOR_ACTION_ENABLED` (write gobernado).
> - **Kortex bridge (autorización explícita, incl. cross-platform execution):** `GREENHOUSE_SISTER_PLATFORM_OAUTH_ENABLED`, `KORTEX_COMMAND_ADAPTER/ADMIN/LIVE_EXECUTE_ENABLED`, `KORTEX_GITHUB_COMMANDS/WORKFLOW_DISPATCH_ENABLED`. (El auto-mode classifier frenó el primer batch masivo por `LIVE_EXECUTE`/`WORKFLOW_DISPATCH`; el operador re-autorizó explícito.)
> - **Comparison-table:** `PUBLIC_SITE_COMPARISON_TABLE_WRITES_ENABLED` ON + 4 vars del bridge WordPress replicadas de staging a prod (USERNAME/BASE_URL=efeoncepro.com/2 SECRET_REF; ambos secrets existen en Secret Manager). Writes draft-only al sitio público live; WP `writes_enabled` ya estaba ON (staging usaba el mismo sitio).
> - **⚠️ ALTO RIESGO (operador asumió):** `PPM_POSITION_ENABLED` ON → **publica la línea PPM del F29 con tasa placeholder 0.25% SIN validar contable** (número fiscal posiblemente errado como oficial). `ATTRIBUTABLE_LATENESS_OTD_ENABLED` ON (ops-worker `00432-9nf` + Vercel) → **afecta el bono de payroll** sin el shadow ≥30d + sign-off HR (TASK-1170). `NOTION_OTD/RPA/FTR_WRITEBACK_ENABLED` (+`_EFEONCE`/`_SKY`) + `NOTION_DUE_DATE_CAPTURE_ENABLED` ON (ops-worker `00432-9nf` + Vercel) → **escribe a los Notion de clientes** (Efeonce+Sky); el OTD requiere la property `[GH] OTD` creada en Notion o **da error client-facing**. Flags del worker son **efímeros** (deploy.sh declara defaults; próximo push develop los resetea).
>
> **Delta 2026-06-29 (release develop→main `056c2dde8`, EPIC-020/021 + finance/Q2C):** se prendieron en Production **`COMMERCIAL_Q2C_CANONICAL_CLOSE_ENABLED`** (cierre canónico Q2C) y **`RETENTION_POSITION_ENABLED`** (línea retenciones F29) con sign-off del operador (migraciones ya en la DB compartida `greenhouse-pg-dev`).
>
> **Delta 2026-06-30 (flip masivo Growth/AEO en Production, autorización explícita del operador + RIESGO ACEPTADO):** a pedido del operador se prendieron en **Vercel Production** TODOS los flags del AI Visibility Grader / lead magnet EPIC-020/021 + motor Growth Forms (29 flags: `GROWTH_AI_VISIBILITY_*` GRADER/OPENAI/ANTHROPIC/PERPLEXITY/GEMINI/GOOGLE_AIO/LLM_EXTRACTION/ASYNC/PROBES/AGENTIC/ENTITY/FIX_IT/PUBLIC_INTAKE/REPORT_EMAIL/LEAD_HANDOFF/OPERATOR_SEND/PORTAL_RUN/TRIAL/REGRADE/ARCHETYPE_PROMPTS/PROMPT_AUTHORING/BRAND_INTELLIGENCE + `GROWTH_GRADER_INTAKE_ON_FORMS_ENGINE_ENABLED` + `GROWTH_FORMS_PUBLIC_API/CATALOG/SERVER_VALIDATION/EMAIL_VERIFICATION/PII_ENCRYPTION` + `GROWTH_SEARCH_CONSOLE_ENABLED`). Redeploy prod `greenhouse-ic8cg4ery`. Verdad live: `vercel env ls` (los snapshots por-fila abajo quedan stale — esta Delta + `vercel env ls` mandan).
> - **⚠️ RIESGO DE SEGURIDAD ACEPTADO POR EL OPERADOR:** `GROWTH_AI_VISIBILITY_PERPLEXITY_ENABLED` y `GROWTH_AI_VISIBILITY_GOOGLE_AIO_ENABLED` quedaron ON en prod usando credenciales **expuestas en chat/captura** (`greenhouse-perplexity-api-key` + password DataForSEO). El operador autorizó explícitamente correr con creds comprometidas. **Pendiente: rotar ambas credenciales.** Revert <5 min: `vercel env rm` de esos 2 flags.
> - **CATEGORY_GUARD + PROSE_SHADOW también prendidos (2026-06-30, a pedido del operador pese a la advertencia):** `GROWTH_AI_VISIBILITY_CATEGORY_GUARD_ENABLED` ON en Vercel prod **y** en el ops-worker (rev `00431-m5l`, vía `gcloud --update-env-vars`; ⚠️ EFÍMERO — el `deploy.sh` lo declara `false` por defecto, el próximo push develop lo resetea; persistir = editar `deploy.sh`). El guard ON **bloquea runs con categoría no resuelta** (~13% cobertura determinista). `GROWTH_AI_VISIBILITY_PROSE_EXTRACTION_SHADOW_ENABLED` ON en Vercel prod (eval-only, no toca runs).
> - **Provisioning resuelto:** (1) ~~`TURNSTILE_SECRET` no está en prod~~ **RESUELTO 2026-06-30:** secret key del widget Cloudflare (modo Invisible) cargado en Vercel prod + redeploy `greenhouse-btvf0v9r6`; el intake público sale del fail-closed. ⚠️ **La secret key quedó EXPUESTA en captura de chat → ROTAR** (el operador confirmó que rota después). Site key `0x4AAAAAADqwX2R7v-k9pItv` (pública) vive en el frontend del sitio público efeoncepro.com. (2) ~~la property HubSpot `aeo_check_result` sin provisionar~~ **RESUELTO 2026-06-30:** gcloud reautenticado (camino canónico `gcloud auth login` + `application-default login`); el dry-run de `provision-ai-visibility-hubspot-properties.ts` confirma que `companies.aeo_check_result` + las 8 `ai_visibility_*` + grupo `aeo` **ya existen** en el portal HubSpot prod (48713323) → operator-send NO dará 400. (3) DUAL-LOCATION: el `ops-worker` compartido ya tiene los flags Growth ON (config staging), así que el lado worker queda cubierto.
> - **Growth Forms público AEO conectado (2026-06-30; renderer live 2026-07-01; full-name split 2026-07-02):** se creó y publicó el form gobernado `efeonce-aeo-diagnostic` + surface `fhsf-efeonce-aeo-diagnostic`, destination HubSpot secure-submit al form `AEO - Lead Form` (`8649e76c-8b01-41f3-9b0c-5713d7b4dba6`). Vercel prod agrega CORS en `GET/POST/OPTIONS` para `efeoncepro.com`; smoke live: `GET` contrato 200 con ACAO, `POST` sin token 403 `captcha_failed/missing_token` con ACAO. Desde TASK-1294 el renderer `<greenhouse-form>` soporta Turnstile invisible y emite `captchaToken`; desde TASK-1298 la landing `/aeo-2/` usa ese renderer live por `form-key`; desde TASK-1318 usa v8 `fver-38d38bbc-6a32-4e2c-bbd7-c0f0fc728c63` con `style_variant=diagnostic_premium` y split server-side de `Nombre completo`.
> - **`PUBLIC_SITE_COMPARISON_TABLE_WRITES_ENABLED` sigue OFF en prod**: requiere bridge WordPress en prod (`PUBLIC_WEBSITE_WORDPRESS_*` solo en staging) + `writes_enabled=true` en efeoncepro.com.
> - Nota: el `ops-worker` (servicio compartido) conserva GIT_SHA `83dd8d663` (tree-idéntico al release; orquestador hizo skip code-identical) — estado steady del servicio compartido, no drift de código.
>
> **Delta 2026-06-22 (TASK-1210, release develop→main `3a39c68ba`, sign-off CEO):** los 8 flags MXN+CLF — `FINANCE_CORE_MXN_ENABLED`, `NUBOX_EXPORT_FOREIGN_CURRENCY_ENABLED`, `FINANCE_MXN_PAYMENT_ORDERS_ENABLED`, `FINANCE_MULTI_CURRENCY_REPORTING_ENABLED`, `FINANCE_CORE_CLF_INDEXED_ENABLED`, `FINANCE_CLF_INCOME_PROJECTION_ENABLED`, `FINANCE_CLF_OBLIGATIONS_ENABLED`, `FINANCE_CLF_REPORTING_ENABLED` — pasaron a **ON en producción**: Vercel prod (agregados + redeploy `greenhouse-midjr78bo`) + ops-worker Cloud Run (persistente via `services/ops-worker/deploy.sh`, `:-true`). Los `*_BACKFILL_APPLY_ENABLED` (gates de script) siguen OFF. Verdad live: `vercel env ls` + `gcloud run services describe ops-worker`.
> **Doc relacionado:** [GREENHOUSE_FEATURE_FLAGS_ROLLOUT_PLATFORM_V1.md](../architecture/GREENHOUSE_FEATURE_FLAGS_ROLLOUT_PLATFORM_V1.md) (los flags PG declarativos — mecanismo distinto, ver abajo)

## Por qué existe este doc

El desarrollo agéntico deja **deuda cognitiva**: una feature se implementa detrás de un flag `*_ENABLED` default-OFF, pasa todos los gates, se mergea… y **el flag nunca se prende** en staging/prod porque nadie lo registró en un lugar encontrable. Este ledger es ese lugar: un vistazo único de **qué flags están prendidos dónde** y **qué queda pendiente de prender**.

Es **encontrable** desde: `CLAUDE.md` (Runtime Rollout Completion Gate), `AGENTS.md`, y el índice de `docs/operations/`.

## Dos mecanismos de flags (no confundir)

| Mecanismo | Dónde vive el estado | Cómo se prende | Gobierna |
|---|---|---|---|
| **Env-var flags** (`*_ENABLED`) — **este doc** | Vercel env vars (por environment) + `.env.local` | `vercel env add` + **redeploy** | Casi todo: features de runtime, integraciones, rollouts graduales sin tabla |
| **PG rollout flags** (`home_rollout_flags`) | PostgreSQL `greenhouse_serving.home_rollout_flags` | Admin endpoint, **sin redeploy** (resolver con cache 30s) | Variantes de shell/home + kill-switches per-block con scope `user>role>tenant>global` |

> Este ledger cubre **los env-var flags**. Para los PG flags, ver el platform doc. Un mismo dominio puede usar ambos.

### Alcance: Greenhouse, no el ecosistema

Este ledger gobierna los flags de **este repo**: los 5 runtimes de Greenhouse (Vercel `greenhouse-eo` + los 4 Cloud Run). `pnpm flags:audit` escanea `src/` y `services/` de este repo y cruza contra `vercel env ls --scope efeonce-7670142f`; nada fuera de ahí entra a su radar.

**Los flags de Efeonce Globe (`GLOBE_*_ENABLED`) NO se registran acá** — otro repo (`efeonce-globe`), otro proyecto GCP, otros servicios (`globe-api-internal` / `globe-studio-internal`, `southamerica-west1`). El motivo no es territorial: allá el estado del flag es **declarativo y versionado** (`infra/terraform/variables.tf` + `cloud_run_services.tf`, aplicado con `tofu apply`) y el drift se detecta con `tofu plan` → *No changes* (procedimiento **supervisado** del [runbook IaC](creative-studio/EFEONCE_GLOBE_IAC_RUNBOOK_V1.md#prueba-anti-drift-repetirla-ante-cualquier-cambio-del-workflow); el CI `terraform-check` sólo valida el HCL offline, no corre `plan` contra GCP). La deuda cognitiva que este ledger existe para absorber —un flag imperativo cuya intención no queda escrita en ningún lado trackeable— **no existe en ese runtime**: el valor que sobrevive a un apply es el del HCL, así que un flag de Globe no puede **quedarse** prendido sin aparecer en un archivo versionado. (Un `gcloud run services update --update-env-vars` de emergencia sí puede prenderlo out-of-band —el runbook IaC lo prescribe para recuperar acceso—, pero deja **drift deliberado**: el siguiente `tofu plan` lo muestra y el siguiente apply lo revierte. La regla de cierre de ese rollback es reconciliar el HCL, no dejarlo vivo.) Registrarlo acá crearía una fila que **ningún gate verifica** (el audit es unidireccional código→ledger, y sólo sobre este repo) y cuyo runbook (`vercel env add`) es directamente falso para ese servicio: un segundo SoT que se pudre en silencio — el mismo modo de falla del caso `deploy.sh` del 2026-07-10, sólo que cross-repo.

**SoT de los flags de Globe:** `efeonce-globe/infra/terraform/` (mecánico) + [`creative-studio/GLOBE_RUNTIME_HANDOFF.md`](creative-studio/GLOBE_RUNTIME_HANDOFF.md) (operativo: kill switch, secreto del grant, TTL, cómo apagar) + las specs en `docs/architecture/creative-studio/`. **Verdad live = `gcloud run services describe globe-api-internal --region southamerica-west1`**, nunca `vercel env ls`.

## ⚠️ Reglas duras

- **NUNCA** declarar un env-var flag nuevo (`*_ENABLED`) **de este repo** sin agregar una fila a **§ Inventario** y, si queda code-complete pero sin prender, a **§ Pendientes de acción** — en el mismo PR. (Un `GLOBE_*_ENABLED` no es de este repo: ver §`Alcance`.) **Enforcement mecánico:** `pnpm docs:closure-check` corre `feature-flags-audit --strict` y **falla (exit 1) si hay un flag en código sin registrar acá** → ningún cierre/closure pasa con un flag sin documentar.
- **NUNCA** considerar un flag "rolled out" hasta verlo en el environment correcto (`vercel env ls`) **+ redeploy aplicado**. `code complete` ≠ `operationally complete` (ver Runtime Rollout Completion Gate en `CLAUDE.md`).
- **NUNCA prender un flag sólo en Vercel.** Un `*_ENABLED` se lee en **cada runtime que ejecuta el código gateado**, y Greenhouse tiene 5 runtimes con env vars independientes: **Vercel** (app Next.js) y 4 servicios **Cloud Run** (`ops-worker`, `commercial-cost-worker`, `ico-batch-worker`, `hubspot-greenhouse-integration`). Prenderlo en uno NO lo prende en los otros. **SIEMPRE** ejecutar el paso 0 de §`Cómo prender un env-var flag` (mapear runtimes ANTES de prender) y declarar en la fila del ledger **en qué runtime(s) vive**. Un flag que gatea una **projection reactiva / consumer / cron** corre en el **ops-worker**, NO en Vercel — prenderlo en Vercel no hace absolutamente nada. Caso fuente 2026-07-09: `GROWTH_EBOOK_EMAIL_DELIVERY_ENABLED` (email de entrega del ebook) vive sólo en el `ops-worker`; el runbook de este doc sólo enseñaba `vercel env add`, y prenderlo ahí habría dejado el email muerto con la success card prometiéndoselo al usuario.
- **NUNCA prender un flag de Cloud Run sólo con `gcloud run services update --update-env-vars`.** Los `deploy.sh` de los workers usan **`--set-env-vars` (destructivo)**: reescriben el set completo, así que una var agregada out-of-band **desaparece en el siguiente deploy, en silencio**. El SoT es **`services/<worker>/deploy.sh`** (declarar el flag ahí) + aplicar en vivo para efecto inmediato. **Los dos pasos, siempre.** Caso fuente 2026-07-10: el flag del ebook se prendió en la revisión `00470`, la `00473` lo borró, y el consumer reactivo registró `skip: flag OFF` — el ledger decía ON y la realidad era OFF. Un flag "prendido" que no está declarado en `deploy.sh` es una bomba de tiempo, no un rollout.
- **NUNCA prender un flag en Vercel "aprovechando" el deploy que disparó tu push.** Vercel **congela las env vars en el instante en que CREA el build**, no cuando lo termina. Si haces `git push` y después `vercel env add`, el deploy que está corriendo **NO tiene la var** — y va a quedar `● Ready`, verde, con el flag OFF. `vercel env ls` te va a decir que la var existe (es verdad: existe en la CONFIG del environment) mientras la **revisión que sirve el tráfico** no la tiene. El orden correcto es **`vercel env add` → DESPUÉS `git push`**; si ya empujaste, **redeploya** (`vercel redeploy <url-del-deploy> --scope efeonce-7670142f`) y confirma que los alias apunten a la revisión nueva. Caso fuente 2026-07-12: `NEXA_PROPOSAL_ACTIONS_ENABLED` — el deploy del push quedó Ready sin la var; sin el redeploy, el ledger habría dicho ON y Nexa habría seguido respondiendo "las acciones gobernadas no están habilitadas". **Mismo bug class que el email del ebook, distinta puerta.**
- **SIEMPRE** que prendas/apagues un flag en un environment, actualiza la **§ Snapshot** (con fecha) y, si cerró un pendiente, saca la fila de **§ Pendientes de acción**.
- **NUNCA** confíes en este doc como verdad live para una decisión crítica — la **verdad live es `vercel env ls`**. Este doc es el ledger humano (intención + pendientes + último snapshot conocido).
- Para flags `NEXT_PUBLIC_*`: se hornean en el bundle **en build time** → prenderlos requiere un **build fresco** (push o redeploy con build cache desmarcado), no un redeploy que reusa build.

---

## § Qué falta prender en PRODUCCIÓN (la vista corta, 2026-07-12)

> **Los 3 flags que están ON en staging y OFF en producción.** Son los únicos candidatos reales a un flip de prod hoy: código desplegado, probado en staging, esperando una decisión humana. Todo lo demás o ya está ON en prod, o es default-OFF por diseño (ver la tabla larga abajo).
>
> ⚠️ Derivado de `vercel env ls` (2026-07-12), que dice si la **var existe**, no cuál es su **valor**: una var presente en Production puede estar en `false`. Para una decisión crítica, lee el valor.

| Flag | Owner | Qué desbloquea en prod | Qué falta ANTES de prenderlo | Riesgo si se prende suelto |
|---|---|---|---|---|
| `ARTIFACT_RENDER_JOBS_ENABLED` | TASK-1391 | Que el deck/PDF de una licitación se **genere desde el portal** (hoy en prod sólo existe el CLI local). | (1) **Sign-off del operador**; (2) integrar `artifact-worker-deploy.yml` al **release control plane** (`RELEASE_DEPLOY_WORKFLOWS`) — hoy el workflow es staging-only. El `ops-worker` y el Job son servicios **únicos compartidos**: la puerta de prod es el *enqueue* (Vercel) + el entitlement per-ORG. | Bajo por sí solo (nada se encola sin capability + entitlement), pero **sin el release control plane no hay rollback gobernado** del worker. |
| `NEXA_PROPOSAL_ACTIONS_ENABLED` | TASK-1399 | Que un humano **opere licitaciones hablándole a Nexa** en producción (registrar propuesta, adjuntar RFP, registrar evidencia, pedir el deck) + el tool read-only `proposal_status`. | **Prenderlo JUNTO con `ARTIFACT_RENDER_JOBS_ENABLED`.** Están acoplados por el flujo, no por el código. | **Media promesa:** sin el flag del render, Nexa registra propuestas y evidencia pero `request_proposal_render` **bloquea con `flag_disabled`** — honesto (lo dice), pero le vendes al usuario un camino que muere en el último paso. |
| `HIRING_ASSESSMENT_AI_ENABLED` | TASK-1361 | Scoring asistido por IA en el desk de hiring (la IA **propone**, el humano confirma — el LLM nunca escribe banco/score). | **Sign-off HR/Legal** (hiring-AI = alto riesgo bajo el EU AI Act). El flip técnico ya está desbloqueado: eval baseline verde con provider real (6/6, Pearson 0.99 vs referencia humana). | Regulatorio, no técnico. No lo prendas sin ese sign-off aunque el código esté listo. |

**Los otros 20 flags que están OFF en todos lados NO son una cola de pendientes** — son default-OFF **por diseño**: `*_BACKFILL_APPLY_*` (se prenden para una corrida puntual y se apagan), `*_SHADOW_*` (miden sin exponer), guardrails que esperan datos reales, y flags de features aún no cerradas. Los que sí tienen intención de flip están en la tabla larga de abajo con su acción pendiente.

## § Pendientes de acción (la parte que se olvida)

> ### ⛔ Antes de prender un `*_ENABLED` en Production (ISSUE-150)
>
> **Producción sirve `main`. Verificar que el código que LEE el flag está en `main`, no en tu working tree ni en
> `develop`.** Un flag ON sobre código ausente no degrada: activa una promesa que ese runtime no puede cumplir, y
> con semántica fail-closed eso saca usuarios reales. Pasó el 2026-08-11: 5 CV de candidatos en cuarentena por 89
> minutos.
>
> ```bash
> git show origin/main:<archivo-que-lee-el-flag> | grep <NOMBRE_DEL_FLAG>
> pnpm flags:audit   # falla si un flag está ON en prod sin su código en main
> ```
>
> Y ojo con el caso sutil, que es el que pasó: **el flag puede existir en `main` y aun así faltar el
> comportamiento nuevo** que una task le agregó. Por eso el audit también avisa cuando el código lector difiere
> entre `main` y la rama de trabajo. **Correr un script con las env vars de producción NO es una prueba de
> producción**: usa tus credenciales, tu red y tu código.

> Flags **code-complete** esperando un flip. Esta es la cola anti-deuda-cognitiva. Saca la fila cuando el flip esté aplicado + verificado.

| Flag | Owner | Estado actual | Acción pendiente | Nota |
|---|---|---|---|---|
| `ops-seo-keyword-discovery-drain` — **cadencia del scheduler, NO un env-var flag** (runtime: ops-worker) | TASK-1664 / TASK-1694 | **Live `*/2`, pero el SoT de `main` dice `*/10`.** Verificado con `gcloud scheduler jobs describe … --location us-east4`: `schedule: '*/2 * * * *'`, `state: ENABLED`, `userUpdateTime 2026-08-28T22:48:20Z`. El valor live lo aplicó el **Ops Worker Deploy del push a `develop`** (run 2026-08-28T22:39:03Z, commit `22c71b512`). Pero `git show origin/main:services/ops-worker/deploy.sh` todavía declara `*/10` (blobs distintos: develop `b336cd3f…` vs main `497057ea…`). | **Promover `develop`→`main`.** Hasta entonces la ventana está ABIERTA: hay **un solo** servicio `ops-worker` y **un solo** juego de jobs de Cloud Scheduler (staging y producción comparten ambos), y `upsert_scheduler_job` hace `create … || update … --schedule=…`, o sea que **el update re-fija la cadencia explícitamente**. Cualquier deploy que corra el árbol de `main` —release de producción vía `production-release.yml` (`workflow_call`), rollback o `workflow_dispatch` break-glass— **revierte el drain a `*/10` en silencio**. | 🔴 **No hay señal que lo detecte**: el job queda `ENABLED` y sano, sólo drenando 5× más lento, y el síntoma es latencia en una lente interactiva, no un error. La próxima promoción normal cierra el drift por sí sola (el commit `22c71b512` viaja en ella); el riesgo es un deploy desde `main` **antes** de esa promoción. Sacar la fila cuando `origin/main:services/ops-worker/deploy.sh` declare `*/2`. |
| `GROWTH_SEO_SERP_TOP_RESULTS_ENABLED` (**dual-runtime**: ops-worker escritura · Vercel lectura) | TASK-1699 | **ON y VIVO en el worker (2026-08-28)** — la revisión activa `ops-worker-00610-kc8` (deploy desde develop `fdfdedbe5`) lleva el código y el flag; **el día 1 de la serie es el 2026-08-29** (cron `ops-seo-rank-capture` 05:00 CLT; el top-N del 28 y anteriores no existe y no se recompra — estructural). Vercel: **staging ON** (custom env `staging`, agregada 2026-08-28); **Production ON desde 2026-08-28** — la sesión del release lo prendió junto al release `c983be7f18e6` (`vercel env add … production` en MINÚSCULA: con `Production` la API responde `api_error`) + redeploy obligatorio `greenhouse-aj0ng1mfw` (el build del release es anterior a la variable). **Verificado en el runtime, no solo la env var**: el canary contra `https://greenhouse.efeoncepro.com` devolvió `serp-top-results read: {"ok":true,…,"rows":[]}` — `ok:true`, no `disabled`; el array vacío es lo esperado antes del día 1. Costo marginal CERO. | (1) 2026-08-29 tras el cron: verificar ~20 filas/keyword + `provider_cost` del día IDÉNTICO al baseline + re-run no-op + señal `seo.serp_top_results.coverage` en verde; (2) ~~confirmar la env var en Vercel Production tras el release~~ **CERRADO 2026-08-28** (ON + redeploy + canary `ok:true` contra prod); (3) a ≥5 días (≈2026-09-02): revisar candidatos de `readSerpCompetitorCandidates` con el operador ANTES de declarar. Sacar la fila al cerrar (1)-(3). | Subordinado a `GROWTH_SEO_ENABLED`. Rollback <5 min: `false` + `--update-env-vars` (la captura de rank no se afecta). Con datos, NUNCA revertir la migración. |
| `GROWTH_PROBE_FETCH_STRICT_NETWORK_ENABLED` (**Vercel + ops-worker**, dual-location) | TASK-1778 | **ON en el ops-worker desde 2026-08-27** (revisión `ops-worker-00598-459`, 100% tráfico, var verificada live; default declarativo `true` en `deploy.sh`, commit `03e81a2f6` — worker ÚNICO compartido staging+prod, cubre el path async del intake público, la cadena del ISSUE-164). **ON en Vercel `staging`** (env agregada 2026-08-27). Gatea SÓLO el endurecimiento de RED (redirect containment familia+descendientes + guarda DNS); stream cap + `truncated`/`observable` + robots obedecido viajan sin flag. Evidencia: 7 dominios vivos de cartera en strict (6 ok; bancochile ya fallaba con la red vieja — Imperva, caso TASK-1281) + corrida real `EO-GRUN-00048` verde con el flag ON (13 probes, cero `blocked_*` falsos). | **(1) Revisión Sentry 2026-08-29** (48 h): conteos `blocked_redirect`/`blocked_private_address` en `source: growth_ai_visibility_probe_fetch` — volumen inesperado = guarda demasiado estricta, no ataque; rollback <5 min = default `false` en `deploy.sh` + `--update-env-vars ...=false`. **(2) Vercel Production**: prender la env var CON el release que lleve el código a `main` (regla ISSUE-150: el path inline prod conserva la red vieja hasta entonces — es el carril admin `light`, no el intake público). **(2) CUMPLIDO 2026-08-27**: release `cc73c74789ce-dbce65f2-303b-4528-bef3-f4edd022a880` llevó el lector a `main` (verificado x3 en `site-fetch.ts`), `vercel env add ... production` + redeploy `greenhouse-if2u2c8ys`. Queda sólo (1), la revisión Sentry del 2026-08-29. | `ISSUE-164` resuelto 2026-08-27. Sacar la fila cuando (1) y (2) cierren. |
| `GROWTH_AI_VISIBILITY_BUDGET_GATE_ENABLED` + `GROWTH_AI_VISIBILITY_BUDGET_GATE_ENFORCED` (**Vercel + ops-worker**, dual-location) | TASK-1696 | **Code-complete; AMBOS OFF por defecto en los dos runtimes.** Declarativos `:-false` en `services/ops-worker/deploy.sh` (SoT del worker). Con `ENABLED` en ON el chokepoint computa `resolveAeoBudget`, registra `wouldBlock` en Sentry (`source: aeo_budget_gate`, `level: warning`) y alimenta `seo.provider.cost_over_budget`; **no bloquea**. `ENFORCED` es SUBORDINADO: prenderlo solo no hace nada, y sólo con los DOS en ON el chokepoint rechaza con `budget_exhausted`. El schema, la atribución del gasto y las tres señales entraron SIN flag — son aditivos y sólo hacen visible lo que ya ocurría. | **(1)** Prender `ENABLED` en Vercel (Production + Preview/develop) **y** en el ops-worker (`deploy.sh` + `gcloud run services update --update-env-vars`, los dos pasos: los `--set-env-vars` son destructivos), y verificar la var en la **revisión activa** de Cloud Run. **(2)** Observar un **mes calendario completo** de `wouldBlock` acumulado por tier en `/admin/operations`. **(3)** Llevarle al operador el dato con una propuesta de tope por tier — **el flip a `ENFORCED` es decisión suya, no un paso de esta task**. | 🔴 **El camino público del lead magnet comparte el motor del grader**: un tope mal calibrado no degrada un tablero, corta captación. Por eso son dos flags y no uno, y por eso los knobs de tope (`GROWTH_AI_VISIBILITY_{CONTRACTED,PILOT,TRIAL}_MONTHLY_BUDGET_USD`, 60/10/3) nacen holgados — en shadow tienen que dejar pasar todo para que la medición sea de la realidad y no de la restricción. **Los knobs NO son flags** y no llevan fila propia. ⚠️ `pnpm flags:audit` NO los ve: su regex busca `process.env.X_ENABLED` literal y estos se leen por constante (`env[FLAG]`), como el resto de los flags de `ai-visibility/flags.ts`. Rollback: `false` en los dos runtimes + redeploy Vercel + `--update-env-vars`; <10 min. |
| `HIRING_OPENING_CAPACITY_CLOSURE_ENABLED` + `HIRING_CAPACITY_FILLED_EMAIL_ENABLED` (**ops-worker / Cloud Run**, NO Vercel) | TASK-1762 | **AMBOS ON en Production desde 2026-08-27** — autorización expresa del CEO como override del gate de abajo. Revisión `ops-worker-00607-bdn`, 100% del tráfico, valores verificados live; declarativos `:-true` en `services/ops-worker/deploy.sh` (SoT) **y** aplicados con `--update-env-vars`, los dos pasos. Antes de esa fecha: code-complete, ambos OFF. El tipo de correo `hiring_decision_not_selected` además nace `enabled=FALSE` por seed (migración `20260823233254212`, verificada contra PG) — **tercera capa**, porque `resolveEmailTypeConfig` es **fail-open** y una fila ausente habría encendido el correo en el deploy sobre un worker cuyo master `HIRING_LIFECYCLE_EMAILS_ENABLED` ya está en `true`. | **PRENDIDOS 2026-08-27 por autorización expresa del CEO. Las tres precondiciones NO se cumplieron: quedan VIGENTES como deuda declarada, no canceladas.** (1) sign-off de Talent y Privacidad sobre el copy y el gate de consentimiento del Banco de Talento — **pendiente**; (2) `TASK-1764` — **pendiente**: sin ella el tipo nuevo cae al perfil de footer **legacy en silencio**, y el footer es donde viven el aviso de datos y los derechos del titular (Ley 21.719 en alcance); (3) canary con procedencia declarada y destinatarios allowlisted — **no ejercitado**, porque las dos vacantes vivas tienen 0 `selected` y el confirm se niega correctamente. ⚠️ **Ese 0 `selected` NO es un control, es un estado de datos**: un reclutador marcando a alguien `selected` —trabajo normal, no acción de release— arma el envío sin que nadie lo decida. **La única capa que queda entre eso y N correos es la fila `hiring_decision_not_selected` sembrada `enabled=false` en `email_type_config`: NO habilitarla hasta cerrar `TASK-1764`.** | **Los dos flags son independientes a propósito**: se puede cerrar una cohorte SIN notificarla apagando sólo el de correo. Un cierre manda N correos **de golpe** (36 y 14 personas en las vacantes vivas) y un correo emitido no se retira. Prenderlos en Vercel **no hace nada**: el reconciler y el consumer viven en el `ops-worker`. |
| `email_type_config.hiring_assessment_access_rotated` (**fila DB, NO env var**) | TASK-1757 | **ON desde 2026-08-20, autorizado por el CEO.** Shipeó apagado por seed y se prendió con `pnpm hiring:email-type -- --type hiring_assessment_access_rotated --on --apply` tras verificar contra PG que el ledger de recuperaciones estaba **vacío** (0 filas): sin historial no hay ráfaga de backfill en el primer drenaje de la projection nueva, que era el único motivo técnico para retenerlo. Antes era: **code-complete, seeded `enabled = FALSE`** (migración `20260820045834971`, aplicada y verificada por su bloque anti pre-up-marker, que además aborta si llegara con `TRUE`). El consumer reactivo existe y la lane `ops-reactive-notifications` lo drena cada 2 min; `sendEmail` corta en el kill-switch por tipo. ⚠️ En `email_type_config` una fila **AUSENTE significa ENCENDIDO** (`resolveEmailTypeConfig` es fail-open), así que el seed ES la puerta, no una formalidad: sin él, el correo se habría encendido en el momento del deploy sobre un ops-worker de Producción cuyo master `HIRING_LIFECYCLE_EMAILS_ENABLED` ya está en `true`. | **Ninguna** — sign-off dado por el CEO el 2026-08-20 ("no necesitas otra firma más que la mía"), que además comprometió el buzón atendido. El `reply-to` quedó implementado en la plataforma (`CANDIDATE_REPLY_TO_EMAIL_TYPES` → `HIRING_CANDIDATE_REPLY_TO_EMAIL`, default `people@efeoncepro.com`): antes no existía y una respuesta del candidato caía en la dirección de ENVÍO del proveedor. **Pendiente operativo, no de código:** confirmar que ese buzón se atiende de verdad. Fila lista para retirarse al próximo housekeeping tras ejercitar una rotación real. | Runtime: **ops-worker**, apilado bajo `HIRING_LIFECYCLE_EMAILS_ENABLED` (ya ON). Ningún handler de Vercel lo lee. Rollback: `SET enabled = FALSE`, efecto inmediato, **sin redeploy**. Señal acompañante: `hiring.assessment.access_recovery.rotation_unnotified` (steady 0). |
| `HIRING_ASSESSMENT_AI_RUN_MODE` · `HIRING_ASSESSMENT_AI_PROMOTION_EVIDENCE_DIGEST` · `HIRING_ASSESSMENT_AI_DAILY_PROVIDER_ATTEMPT_CAP` | TASK-1742 | **Producción ON en `global_provisional` desde 2026-08-18.** En ops-worker: master/enqueue ON, concurrencia 1, cap diario 1000; scheduler `ops-assessment-ai-drain` activo cada 2 minutos. `EXCEPTION_POLICY=false` y `RUN_CONFIRM=false`: las propuestas son operator-only, provisionales y nunca escriben scores efectivos. `exception_canary|calibrated_batch` siguen no autorizados sin digest de promoción válido. **Verificado live 2026-08-26:** `HIRING_ASSESSMENT_AI_PROMOTION_EVIDENCE_DIGEST` está AUSENTE en los tres runtimes (no declarado en `deploy.sh`, ausente en la revisión activa y en Vercel prod/staging). Eso es **fail-closed deliberado**, no un olvido: `config.ts:49-52` dice *«nunca interpreta un flag como evidencia»* y sin attestation el modo degrada a `global_provisional`. **NUNCA** declarar este digest para «destrabar» la policy de excepciones — el digest ES la evidencia de promoción, y fabricarlo vacía el gate. | Completar cooldown 24–48 h, ensayo/readback residual-cero y firmas/risk acceptance de TASK-1742. | Release `7e7a474217eb`, run `32193134959`; ops-worker `00584-r4x`, 100% tráfico. Rollback: mode `disabled`, enqueue OFF, scheduler pausado y reconcile/cancel append-only. |
| `GROWTH_SEO_PROSPECT_DIAGNOSTIC_ENABLED` | TASK-1709 | **ON en Vercel Production desde 2026-08-27** (release `cc73c74789ce-dbce65f2-303b-4528-bef3-f4edd022a880`; `vercel env add ... production` + redeploy `greenhouse-if2u2c8ys` aliaseado; `pnpm flags:audit --strict` confirma lector en `main`). Antes era code-complete y OFF en todos los environments. Gatea el command `runProspectDiagnostic` (diagnóstico único de un dominio prospecto, ~USD 0,25/corrida, Labs+Backlinks live + sustrato propio). Con OFF el command devuelve `disabled` sin llamada alguna al proveedor. Tope duro por diagnóstico `GROWTH_SEO_PROSPECT_DIAGNOSTIC_CEILING_USD` (default 1,00, knob no-flag) + tope diario por actor (`..._DAILY_ACTOR_CAP`, default 10). | **Sign-off del operador comercial OTORGADO 2026-08-27** (autorización explícita del operador en la sesión de coordinación del release). Flip a Vercel **Production** vinculado al release develop→main del 2026-08-27: se ejecuta cuando el manifest quede `released`, verificando primero que el lector esté en `main` (regla ISSUE-150), con `vercel env add ... production` + **redeploy obligatorio** (Vercel congela env vars al build) y verificación en el deployment activo. | Runtime: **Vercel únicamente (V1)** — la corrida es inline en el command (todas las fuentes live); **no hay batch en ops-worker ni Cloud Scheduler, y no debe haberlo** (captura recurrente sobre prospectos prohibida). Subordinado a `GROWTH_SEO_ENABLED`. Gasto atribuido a `EO-ORG-0007` (adquisición) en `seo_provider_spend_daily`. Rollback <5 min: flag OFF + redeploy. |
| `GROWTH_SEO_BACKLINK_DETAIL_ENABLED` | TASK-1777 | **ON (2026-08-27).** Autorización del operador + smoke live el mismo día: drill-down sobre los snapshots del 2026-08-24 ya pagados — 2 targets `drilled` por `first_time` (Efeonce USD 0.0739, Berel USD 0.1079; total 0.1818 vs ~0.19 estimado), re-corrida a USD 0 (`snapshots:0`), 225 dominios (72 present/120 new/33 lost), rank 100% en escala 0-100, 86 anchors. Declarativo `:-true` en `deploy.sh` + `--update-env-vars` (revisión `ops-worker-00603-ngj`). Sin scheduler propio. | Observar el primer ciclo natural (lunes 2026-08-31): targets estables deben registrar `skipped_no_movement` a USD 0 — es el caso que el smoke no pudo producir (todo era first_time). Señal en steady ok. | Runtime: **ops-worker únicamente**. Subordinado a `GROWTH_SEO_ENABLED`. Rollback <5 min: `false` + `--update-env-vars`. |
| `GROWTH_SEO_URL_VISIBILITY_ENABLED` | TASK-1776 | **ON (2026-08-27).** Autorización del operador + smoke el mismo día: dry-run USD 0.048 estimado → batch real USD 0.0366 con **90 filas de mercado gratis** (85 Berel + 5 Efeonce) → re-corrida USD 0 → smoke de los otros kinds (subfolder `berel.com/productos`: 197 kw y **100/100 URLs bajo la ruta**; url `/ubica-tienda`: 60 kw; subdomain `www.berel.com`: `no_market_data` honesto) USD 0.0552 + 120 filas de mercado más. Declarativo `:-true` + `--update-env-vars`; scheduler `ops-seo-url-visibility` (día 17) **despausado → ENABLED**. Lane staging canary verde. | Verificar el primer run programado (día 17). Señal en steady ok. Evaluar con datos si el enriquecimiento lateral permite bajar frecuencia al cron de TASK-1661 (follow-up declarado). **Delta 2026-08-29:** ejercitado por el camino DESATENDIDO: dry-run USD 0.024 → real USD 0.024 (clavado) + **15 filas de mercado gratis** → re-corrida USD 0. Confirma en producción el beneficio lateral declarado en la spec (`marketRowsWritten` como métrica de la corrida). | Runtime: **ops-worker únicamente**. Subordinado a `GROWTH_SEO_ENABLED`. Rollback <5 min: `false` + pausar scheduler. |
| `GROWTH_SEO_DOMAIN_OVERVIEW_ENABLED` | TASK-1775 | **ON (2026-08-27).** Autorización del operador + smoke el mismo día: dry-run (USD 0.02424 estimado, cero llamadas) → corrida real USD 0.02424 (clavado al estimado; berel.com: 773 kw ranqueadas, ETV ~135k MX; efeoncepro.com: 5 kw CL) → re-corrida USD 0 con ambos `fresh`. Declarativo `:-true` en `deploy.sh` + `--update-env-vars`; scheduler `ops-seo-domain-overview` (día 16) **despausado → ENABLED**. Lane staging canary verde. | Verificar el primer run programado (día 16): todo `fresh`/`skipped` esperado, costo ~0. Señal en steady ok. **Delta 2026-08-29:** ejercitado por el camino DESATENDIDO (POST con body `{}`, sin argumentos — el mismo que usa el scheduler): dry-run USD 0.01212 → real USD 0.01212 (clavado) → re-corrida USD 0 con ambos `fresh`. Novedad respecto del smoke del 27: la frescura se sostuvo **cruzando el límite de día** (foto del 27 vigente el 29, ventana `(CURRENT_DATE - capture_date) < 30`), que el smoke del mismo día no podía probar. El sujeto capturado hoy fue el **segundo** de Berel —un competidor declarado después del smoke—, no una re-compra. | Runtime: **ops-worker únicamente**. Subordinado a `GROWTH_SEO_ENABLED`. Rollback <5 min: `false` + pausar scheduler. |
| `GROWTH_SEO_KEYWORD_DISCOVERY_ENABLED` | TASK-1664 | **ON (2026-08-14).** Prendido el mismo día del ship con autorización del operador ("termina todo lo que está pendiente") tras el smoke live verificado (corrida real USD 0.0132 ≤ estimado, idempotencia USD 0, cero auto-track). Declarativo en `deploy.sh` (`:-true`) + scheduler `ops-seo-keyword-discovery-drain` despausado + Vercel `Production` y `staging`. **Sin enqueue automático**: flag ON + cola vacía = costo cero; cada corrida pasa preview + gate de entitlement. | **Ninguna** — primer tick del drain disparado y verificado en logs el mismo día (`pending=0 processed=0`, no-op). Fila lista para retirarse al próximo housekeeping (queda § Snapshot). | Runtime: **Vercel (enqueue/lanes) + ops-worker (drain)**. Subordinado a `GROWTH_SEO_ENABLED`. Rollback <5 min: flag OFF en ambos + pausar scheduler; los facts append-only permanecen. |
| `GROWTH_SEO_KEYWORD_MARKET_DATA_ENABLED` | TASK-1661 | **ON (2026-08-13).** Los 4 pasos pendientes se cumplieron el mismo día: (1) dry-run contra PG real (USD 0.01572 estimado, cero llamadas); (2) autorización del operador ("termina lo que falte"); (3) corridas reales verificadas — mercado CL y MX de Berel capturados, ledger atribuido, idempotencia USD 0; (4) flag `true` + scheduler despausado, ambos declarativos en `deploy.sh`. | Verificar el primer run programado (día 15, 08:00 CLT) en los logs del worker: todo `already_fresh`/`skipped` (ya capturado) = comportamiento esperado, costo ~0. Tras ese run, dejar sólo la fila de § Snapshot. ⚠️ **Dependencia de release (2026-08-14):** el flag ya está ON en el ops-worker, pero el **lane** `/api/platform/ecosystem/growth/seo/keyword-market-data` (Vercel) llega a producción con este release. Hasta que el release cierre, la tool federada `get_seo_keyword_market_data` del gateway `mcp.efeonce.org` respondería 404 upstream — por eso su deploy (`workflow_dispatch`) se dispara DESPUÉS de este release, no antes. | Runtime: **ops-worker únicamente**. Subordinado a `GROWTH_SEO_ENABLED`. Rollback <5 min: `false` en `deploy.sh` + redeploy. |
| `CLIENT_PORTAL_AGENT_ORG_OVERRIDE_ENABLED` | TASK-1679 | **Code-complete, OFF en todos los environments (default)** — y en producción **no se puede prender**: el helper bloquea el override cuando `VERCEL_ENV === 'production'` sin variable de escape, así que la env var ahí es inerte por diseño. **Corregido 2026-08-09:** la primera versión discriminaba por `NODE_ENV`, y como Vercel compila TODOS los deployments con `NODE_ENV=production`, el bloqueo apagaba el flag también en staging y lo dejaba efectivamente solo-local. El discriminador canónico del repo es `VERCEL_ENV` (mismo que `agent-session` y `proxy.ts`); staging reporta `preview`, verificado contra el runtime desplegado. | Prender **sólo en local/staging** cuando haya que recorrer el portal de una organización distinta a la que la sesión resuelve. En staging requiere `vercel env add` + redeploy (Vercel congela las env vars al build). Se acompaña de `CLIENT_PORTAL_AGENT_ORG_OVERRIDE` (env) o de la cookie `gh_agent_org_override` con el `organizationId` destino. | Runtime: **Vercel únicamente** (se lee en `src/lib/client-portal/guards/resolve-client-portal-organization-id.ts`, server components; ningún worker lo consume — verificado con grep sobre `src/` y `services/`). ⚠️ **Riesgo declarado y aceptado por el operador (2026-08-09):** con el flag ON, la credencial de la persona agente —documentada en `CLAUDE.md`— lee el portal de cualquier organización. Por eso hay 4 condiciones independientes fail-closed: flag ON + `VERCEL_ENV !== 'production'` + `userId` en la allowlist de personas fixture + valor de override presente. Cada aplicación efectiva emite a Sentry (`client_portal_agent_org_override_applied`, dominio `identity`). |
| `CAREERS_DETAIL_EDITORIAL_V2_ENABLED` | TASK-1741 | **Code-complete y migración aplicada; OFF en staging y Production (verdad Vercel verificada 2026-08-17).** Gatea únicamente la variante editorial de `/public/careers/[publicId]`; el renderer legacy permanece como rollback y conserva la misma URL, contenido público y dos CTA. Tests/GVC premium local 1440+390 verdes; scorecard 4,66/piso 4,5. | Desplegar el SHA conjunto en staging con ambos flags OFF; activar este flag primero, comparar hoja completa before/after y validar v2/v1/legacy. Sólo después activar el schema y, con ambos smokes verdes, promover a Production. | Runtime: **Vercel únicamente**. Interlock técnico: `HIRING_PUBLIC_JOBPOSTING_SCHEMA_ENABLED` sólo retorna ON cuando este flag también está ON. Rollback: schema OFF primero → renderer OFF después → redeploy. No toca el formulario apply. |
| `HIRING_PUBLIC_JOBPOSTING_SCHEMA_ENABLED` | TASK-1740 | **Code-complete; OFF en staging y Production (verdad Vercel verificada 2026-08-17).** Gatea la emisión de JSON-LD `JobPosting` en la leaf page pública de una vacante (`/public/careers/[publicId]`). El builder es fail-closed: una vacante remota sin países elegibles ISO o una híbrida/presencial sin ciudad+país no emite schema. El canonical explícito no depende del flag. Los openings `EO-OPN-0009` y `EO-OPN-0061` ya tienen 20 países aprobados y vía contractual visible. | La precondición de código de TASK-1741 está cumplida: HTML y schema consumen el mismo contenido resuelto. Tras validar el renderer en staging, activar este flag, ejecutar Rich Results/HTML parity y lifecycle published→paused/closed; Production sólo después de esa evidencia. | Runtime: **Vercel únicamente**. El interlock obliga a mantener el renderer ON. Rollback <10 min: schema OFF primero, renderer OFF después y redeploy; el canonical y la página permanecen. |
| `HIRING_PUBLIC_APPLICATIONS_ENABLED` | TASK-1367/TASK-354 | **staging + Production: env vars SET 2026-07-09** (`HIRING_PUBLIC_APPLICATIONS_ENABLED=true` + `NEXT_PUBLIC_TURNSTILE_SITE_KEY`; `TURNSTILE_SECRET` presente en ambos). **Production build/release completo inicial** en `433cfa2b0fd3` (`production-release.yml` run `28991488376` success); **release acoplado del fix robusto de inferencia/responsabilidades** en `915be02a86abfd49c71365af8a647f9fdfa35207` (PR #151, manifest `915be02a86ab-7c6aa11e-b9c1-4990-8086-cdfacb3a763b`, runtime verde; `ops-worker` residual documentado como label drift si diff runtime vacío). **Opening real publicado:** `EO-OPN-0009` (`Account Manager / Especialista en Marketing`) creado vía writers canónicos el 2026-07-09. | (1) smoke público con browser/Turnstile: submit → `hiring_application` creada + dedupe genérico; (2) tras smoke, sacar esta fila de pendientes y dejar solo § Snapshot. | Gatea el endpoint público de postulación. Default OFF → **404 invisible** (no revela que existe). Anti-abuse: Turnstile + rate-limit (5/email/día, 20/IP/día) vía `hiring_application_intake_events`. Respuestas siempre genéricas (duplicado → mismo `accepted` 202). La careers UI (TASK-354) es el consumer. |
| `ASSET_MALWARE_SCAN_ENABLED` | **TASK-1378** / **ISSUE-150** | **CERRADO — Production ON y verificado 2026-08-12 (ver § Snapshot); fila conservada como historia del bug class.** Historia: el flag falló DOS veces el 2026-08-11, ambas revertidas. (1º) Se prendió con el código OIDC sólo en `develop`: producción sirve `main` → request sin header → Cloud Run 403 → 5 CV reales en cuarentena 89 min, recuperados 5/5. (2º) Con el código YA en `main` (release `64c80f61d4a4`), falló con `scanner_auth_failed` en 21 ms: **producción corre `GCP_AUTH_PREFERENCE=service_account_key` (postura transicional TASK-800) y el resolver de ID tokens no tenía rama de key** — caía a impersonación ambiente sin ADC. 1 CV afectado, recuperado. Staging pasó su gate porque SIN la preferencia toma la rama WIF: dos environments, dos ramas de código, una sola probada. **Fix en `develop` 2026-08-11**: rama `service_account_key` en `resolveGoogleIdTokenProvider` + endpoint de diagnóstico `GET /api/internal/health/scanner-auth`; sanity con la SA key REAL de producción (no ADC del operador): mint 120 ms + Cloud Run aceptó (clean `ok` / EICAR `found`). **Staging: ON y operativo** (gate E2E con postulaciones reales). Servicio único `clamav` en us-east4, `min=1`, cerrado por IAM. | **Condiciones (1) y (2) CUMPLIDAS el 2026-08-11 ~23:15Z:** (1) fix + endpoint en `main` (release `a90951dba3b7-73da976e-f460-4241-8708-5772421fa49d`, run `31544667630`, `released`); (2) diagnóstico EN producción VERDE: `credentialPlan=service_account_key`, `mint.ok=true` (53 ms, `email=greenhouse-portal@`, `aud` correcto), `probe.ok=true` (Cloud Run aceptó y escaneó, 100 ms). **Paso (3) COMPLETADO 2026-08-12 ~05:52Z — PRODUCTION ON y verificado post-flip desde el runtime:** redeploy `greenhouse-aivcug5f5` (ejecutado por el agente con autorización explícita del operador), `READY` y aliaseado a `greenhouse.efeoncepro.com`. Diagnóstico post-flip en producción: `flagEnabled=true`, `credentialPlan=service_account_key`, `mint.ok=true` (94 ms), `probe.ok=true` (147 ms, `scanStatus=ok`). **Único pendiente residual: confirmar la primera postulación real registrando `scanner=structural+clamav-http` en `asset_scan_results`** (~13/día; flip ocurrió de madrugada Chile). Rollback = `vercel env rm` + redeploy, <10 min. | **Runtime: Vercel.** **Fail-closed**: sin veredicto, la subida se bloquea — un flag mal prendido acá no degrada, saca gente. Costo: ≈USD 19/mes. **Alcance: NO es sólo vacantes** — CV público, Growth Forms, `proposal_rfp` y `proposal_deliverable`. Recovery: `scripts/hiring/recover-scanner-403-quarantined-cvs.ts` (cubre `scanner_http_error`/`scanner_auth_failed`/`scanner_unreachable`). |
| `HIRING_ASSESSMENT_AI_ENABLED` | TASK-1361 | **staging: ON 2026-07-10** (env SET + redeploy aplicado). Prod: OFF. **Eval baseline CORRIDO VERDE 2026-07-10** con provider real (claude-sonnet-5): 6/6 scored, 100% dentro de tolerancia (band 15), Pearson 0.99 vs referencia humana. El eval cazó y forzó el fix de un bug latente real (Claude 5+ depreca `temperature` → cliente canónico auto-adaptado, commit en develop). | (1) smoke en staging del propose real desde el desk cuando 1363 entregue la UI (o vía API); (2) **prod SOLO tras sign-off HR/Legal** (hiring-AI = alto riesgo EU AI Act) — el flip técnico ya está desbloqueado. | Gatea SOLO los propose paths. El confirm/reject nunca se gatea. La IA PROPONE; el humano confirma — el LLM nunca escribe banco/score. |
| `HIRING_SYNTHETIC_DATA_FILTER_ENABLED` | TASK-1739 | **CERRADO — ON en staging (2026-08-18) y ON en Production (2026-08-19), tras el release `30301816955f`; fila conservada como historia del rollout.** Gatea ÚNICAMENTE el filtro de procedencia del desk (`getHiringDeskSnapshot`) y del talent pool: con el flag OFF el desk se comporta EXACTAMENTE como antes. El filtro viaja también a los dos agregados de KPIs, no sólo a las listas — un filtro que no viajara ahí daría lista filtrada con totales sin filtrar, números que no cuadran y peor que no filtrar. El caller puede pedir `includeSynthetic: true` y ese opt-in explícito gana sobre el flag. **NO gatea** (a propósito): la exclusión de sintéticos del gold set (`build-gold-set-sample.ts`) —es la corrección de un defecto que puede contaminar la calibración de la IA y no se le da interruptor para volver al estado roto—, la guarda de publicación de `publishOpening` —una vacante no real no se publica nunca— ni los commands de marcado/purga, cuya puerta es capability + allowlist humana (un flag encendido jamás autoriza un backfill). | **Ninguna: el rollout está cerrado.** Los tres prerrequisitos se cumplieron y quedaron verificados: (1) **el aviso a HR quedó WAIVED por el CEO el 2026-08-18** (es la autoridad; no hay tercero a quien avisar); (2) **el backfill del Slice 4 está APLICADO Y VERIFICADO** (71 marcas + 32 archivados, actor `user-efeonce-admin-julio-reyes`, señal de divergencia en `ok`); (3) **efecto MEDIDO en staging con el flag ON**: `/api/hiring/desk` sirve **2 vacantes** (`EO-OPN-0009` y `EO-OPN-0061`, las únicas reales vivas) y **47 postulaciones**, contra **24 y 79** con el flag OFF. El orden se respetó: push → deploy → flip en staging (2026-08-18, verificado) → flip en Production (2026-08-19, tras el release `30301816955f`). | **Runtime: Vercel ÚNICAMENTE** (read-site: `src/lib/hiring/data-origin/config.ts`, consumido por `src/lib/hiring/desk.ts`; ningún Cloud Run lo lee — la señal de reliability cuenta filas MARCADAS, no filtradas, así que no depende de este flag). En Production se aplicó `vercel env add … production` **+ redeploy**: Vercel congela las env vars en el build, así que sin redeploy el flag existe en el proyecto pero el runtime activo sigue sirviendo el valor viejo. |
| `NEXA_HIRING_ACTIONS_ENABLED` | TASK-1773 | **Code-complete, OFF en todos los environments (default).** Habilita la acción gobernada `decide_hiring_application`: Nexa cierra UNA postulación abierta desde el chat, con `propose → confirmación humana → execute`. **Autoridad más angosta que el portal**: se niega a re-decidir una postulación que ya tiene desenlace, y el cierre MASIVO por capacidad NO se federa (`TASK-1762`). Runtime lector: **Vercel únicamente** (`src/lib/nexa/flags.ts`). | Prender exige sign-off: bajo el AI Act la selección es alto riesgo con supervisión humana obligatoria. Requiere además `NEXA_ACTION_RUNTIME_ENABLED` (ya ON en prod), que es el master del runtime de acciones. |
| `HIRING_VACANCY_AI_ENABLED` | TASK-1385 | ⚠️ **CORREGIDO 2026-08-26 — esta fila decía «OFF en todos los environments» y era FALSO: está ON en Production desde 2026-07-16 (41 días).** staging quedó **SET a `true` el 2026-08-26** (antes ausente), pero **FRÍO**: una env var no calienta sin redeploy, y `vercel redeploy` NO revive staging (el `ignoreCommand` lo cancela igual). **Calienta con el próximo push de CÓDIGO a `develop`** — un push docs-only tampoco basta. Hasta entonces staging se comporta como OFF. Verdad live verificada con `vercel env ls --scope efeonce-7670142f` + `env pull` descifrado: runtime lector **Vercel únicamente** (`vacancy-ai/config.ts:14`), cero lecturas en `services/`. **El orden de las precondiciones quedó invertido**: se declaró «flip staging + smoke, después prod» y lo que pasó fue prod ON sin que el smoke de staging ocurriera nunca. — redacción asistida por IA del copy público (`public_*`) de una vacante (propose→confirm): la IA propone desde inputs allowlist-safe (NUNCA ve budget/rate/notas internas — test negativo), el confirm humano aplica vía `updateHiringOpening`, el publish sigue humano. Live E2E PG verde (2026-07-16): propose no muta el opening → confirm aplica + idempotente + terminal-once. Kind `opening_public_copy` en el ledger 1361; capability `hiring.opening.ai_assist`. | (1) Flip staging (`vercel env add HIRING_VACANCY_AI_ENABLED` + redeploy) + smoke con una vacante real **desde el desk** (TASK-1422 ya entregó la UI: Publicación → `Redactar con IA` → confirm → publish gate pasa, aviso sin campos internos ni sesgos); (2) prod tras revisión humana del primer aviso real. Flag hermano de `HIRING_ASSESSMENT_AI_ENABLED` **a propósito**: NO hereda el gate regulatorio del scoring (el copy de vacante no decide sobre personas; el riesgo es marca/sesgo del aviso y lo gatea el confirm humano). | **Runtime: Vercel ÚNICAMENTE** (route handler `/api/hiring/openings/[id]/ai/propose-public-copy`; ningún Cloud Run lo lee — verificado con grep). Gatea SOLO el propose (costo LLM); el confirm/reject de propuestas existentes nunca se gatea. OFF → 409 `vacancy_ai_disabled` y la redacción manual sigue disponible. Config: `src/lib/hiring/vacancy-ai/config.ts`. |
| `HIRING_EVALUATION_DOSSIER_AI_ENABLED` · `HIRING_EVALUATION_DOSSIER_AI_AUTO_PROPOSE_ENABLED` | TASK-1735 | **Producción ON desde 2026-08-18.** Cuando existen CV procesado limpio y assessment puntuado, el worker intenta crear automáticamente una propuesta operator-only; `Generar análisis` queda como recuperación explícita. Provider `google`, modelo default `gemini-2.5-flash`, prompt `hiring_evaluation_dossier.v2`. La propuesta nunca se confirma ni escribe una nota por sí sola. | Observar propuestas automáticas y mantener confirm/reject humano; una fuente stale invalida la propuesta por digest. | Runtime cross-runtime: worker dispara auto-propose y App API/Vercel conserva proposal/confirm. Packet allowlisted, 20000 caracteres fail-loud, sin contacto estructurado ni superficie candidate-facing. |
| `HIRING_CANDIDATE_IDENTITY_NORMALIZATION_ENABLED` | TASK-1736 | **staging: ON 2026-08-16** (`vercel env add` con autorización explícita del CEO en sesión de operador; verdad live `vercel env ls`). **Production: OFF** hasta el canary del runbook. **Remediación histórica EJECUTADA el 2026-08-16** (independiente del flag, ADR D4): 3 personas reales corregidas — Valentina Villa, Stana Medina, Aldo Romano — con `actor_user_id=user-efeonce-admin-julio-reyes` y razón `"Remediacion autorizada por CEO 2026-08-16"` en `candidate_identity_display_audit`; 2 perfiles QA podados a mano de la allowlist. **Slices 1-4 code-complete** — gatea el writer nuevo de identidad del candidato: evidencia application-scoped append-only (`greenhouse_hiring.candidate_identity_intake_evidence`) + reconciliación de display CAS (`reconcileCandidateIdentityDisplayName`, ADR D3). OFF ⇒ intake idéntico al comportamiento previo (cero filas nuevas, cero reconciliación); un error de esta capa NUNCA rompe el submit público (degrade a Sentry, IDs-only). Slice 3 entregó el detector read-only + la remediación gobernada (`pnpm hiring:candidates:remediate-display`: dry-run → allowlist humana → apply CAS en lotes de 1); Slice 4 entregó las 2 señales `hiring.candidate_identity.*` (needs_review backlog + evidence coverage gap, steady=0; flag OFF ⇒ ok con nota) + runbook + docs/manual. **Rollout gated:** flip staging → smoke intake real (canary sintético con nombre degenerado por ambas entradas + caso identity existente) → flip prod; la **remediación histórica va por allowlist humana independiente del flag** (el flag por sí solo NUNCA la autoriza — ADR D4). Runbook: `docs/operations/runbooks/candidate-identity-rollout.md` · manual: `docs/manual-de-uso/hr/operar-remediacion-nombres-candidatos.md`. | (1) Flip staging (`vercel env add HIRING_CANDIDATE_IDENTITY_NORMALIZATION_ENABLED` + redeploy) + canary del runbook (evidencia + reconcile + audit verificados en DB); (2) prod tras canary verde y `evidence_coverage_gap` en ok; (3) remediación histórica por el manual (dry-run vigente + allowlist podada — caso real 2026-08-16: 2 humanos + 2 perfiles QA que se podan — + apply con actor/reason). Rollback: flag OFF + redeploy; las correcciones aplicadas se conservan por diseño append-only. | **Runtime: Vercel ÚNICAMENTE** (read-sites: `src/lib/hiring/candidate-intake/config.ts`, consumido por `submitPublicHiringApplication` y por la señal `hiring.candidate_identity.evidence_coverage_gap`; ningún Cloud Run lo lee). |
| `HIRING_STAGE_TEST_ASSIGNMENT_ENABLED` | — | ✅ (**Production ON 2026-08-18** — ops-worker rev `ops-worker-00576-7zz`; **revisión activa al 2026-08-26 = `ops-worker-00594-2tp`, valor `true` re-verificado ahí** — las revisiones citadas en este ledger caducan, el valor sobrevive porque está declarado en `deploy.sh`; default `true` en `deploy.sh` para que no se evapore. Las TRES precondiciones del propio `deploy.sh` cumplidas: backlog drenado 17-ago (23 eventos, 0 correos), policy del canary `EO-OPN-0009` reconfigurada a `on_stage_entry`+`enabled` (v2, cap 3/60min), cancel/recovery `cancel.live.test.ts` 5/5 contra PG real) | — | **Runtime: ops-worker / Cloud Run ÚNICAMENTE** (lo lee `src/lib/hiring/stage-comms/config.ts`, consumido por la projection del lane `notifications`; ningún route handler de Vercel lo lee — prenderlo ahí no hace nada). Subordinado a `HIRING_LIFECYCLE_EMAILS_ENABLED`: si el master está OFF, no se comunica ni se asigna nada. |
| `HIRING_ASSESSMENT_AI_RUN_ENQUEUE_ENABLED` · `HIRING_ASSESSMENT_AI_EXCEPTION_POLICY_ENABLED` · `HIRING_ASSESSMENT_AI_RUN_CONFIRM_ENABLED` | TASK-1734/1742 | **Producción 2026-08-18:** enqueue/master ON en ops-worker; `RUN_MODE=global_provisional`; scheduler activo. `EXCEPTION_POLICY=false` mantiene todo caso revisable y `RUN_CONFIRM=false` impide confirmación por lote. | Mantener batch/calibrated OFF hasta gold set suficiente y digest de promoción; completar observación/rollback de TASK-1742. | La cancelación/reconciliación siguen disponibles. La evaluación provisional nunca muta el score efectivo ni se publica al postulante. |
| `HIRING_FAIRNESS_MONITOR_ENABLED` | TASK-1365 | **staging: ON sintetico 2026-07-13** en SHA `242f8a5d8`, deployment `dpl_AuMv2KrDuMKXt5GUp91gr1QZhQLq` Ready. Policy `efeonce-privacy-2025-04-19-staging-synthetic-v1`, retencion 30 dias, allowlist exclusiva `synthetic_cohort: group_a\|group_b`; reader autenticado `HTTP 200` con `k=10`, minimo dos grupos y `insufficient_sample`; smoke DB PASS. **production: OFF/ausente, no tocada.** | Antes de self-ID real o prod: notice/consent especifico para fairness y datos sensibles, categorias por jurisdiccion y retencion aprobada. La politica publica vigente cubre postulantes/12 meses, pero no esta finalidad ni categorias sensibles. | **Fail-closed:** staging solo autoriza datos sinteticos. Flag ON sin policy completa no captura ni lee. PII sensible prospectiva, sin backfill. El monitor observa; nunca ajusta decisiones/cuotas. |
| `GROWTH_EBOOK_EMAIL_DELIVERY_ENABLED` | TASK-1375 | **ops-worker (prod): ON — 2026-07-10**, revisión `ops-worker-00474-jrf` + **declarado en `services/ops-worker/deploy.sh`** (SoT). ⚠️ El primer flip (2026-07-09, rev `00470-898`) se hizo sólo con `--update-env-vars` y la rev `00473` lo BORRÓ (`deploy.sh` usa `--set-env-vars` destructivo): el consumer reactivo registró `skip: flag OFF` en la primera submission real y el email nunca salió. El código de la projection + el template entraron con el release `41aefb457`. Base URL: NO se setea env → `publicBaseUrl()` cae al default correcto `https://greenhouse.efeoncepro.com`. Infra de email confirmada en el worker (`RESEND_API_KEY` + `RESEND_API_KEY_SECRET_REF` + `EMAIL_FROM`), misma maquinaria ya probada en prod por `GROWTH_AI_VISIBILITY_REPORT_EMAIL_ENABLED`. | (1) **smoke humano pendiente**: submit real en `think.efeoncepro.com/web-agentica` con correo corporativo → verificar que llega el email con el link gated. NO es automatizable: el Turnstile invisible del form bloquea browsers automatizados (comportamiento correcto), así que el submit aceptado sólo lo produce una persona. (2) Tras el smoke, sacar esta fila de pendientes y dejar sólo § Snapshot. | Email de respaldo del ebook lead magnet (consumer reactivo `growth_ebook_delivery`, drenado por `ops-reactive-growth`). **Flujo crítico**: la success card le promete el email al usuario, por eso NO debe quedar OFF en prod. **Genérico por ebook** (lee el contenido del `success_behavior` del form). El email lleva el LINK gated, NUNCA adjunta el PDF (9 MB). |
| `ARTIFACT_RENDER_JOBS_ENABLED` | TASK-1391 | **STAGING ON y VERIFICADO CON RENDER REAL EN CLOUD RUN (2026-07-12)**: job `prnd-518535f0…` → **completed, 15 láminas / 25,2 s / 3,16 MB, attempts=1** — Scheduler → dispatcher (ops-worker) → `jobs.run` → claim SKIP LOCKED → drift check → gates → asset `asset-988fbb9a…` en `…-private-assets-staging` + outbox published. **Bench 25 confirmado:** `deck-25-staging-bench` → completed, 32,3 s / 5,56 MB / 25 previews. Declarado ON en ambos `deploy.sh` (SoT) + revisiones activas + **Vercel staging** (var + redeploy). **Vercel PRODUCTION: SIN la var (OFF)** — ops-worker/Job son servicios únicos compartidos; la puerta de prod es el enqueue + entitlement per-ORG. | Production sólo tras sign-off del operador + integrar el workflow al release control plane (`RELEASE_DEPLOY_WORKFLOWS`). | Pipeline de render gobernado de artefactos del composer (deck de licitación → PDF). El CLI local (`pnpm deck:compose`) NO depende del flag. |
| `TENDER_CHAPTER_AUTHOR_ENABLED` | TASK-1415 | **Code-complete, OFF en todos los environments (default)** — el motor de chapter-authors (interface `ChapterAuthor` servicio-agnóstica + author de diagnóstico SEO/AEO) quedó probado end-to-end EN LOCAL (2026-07-16): run real `EO-GRUN-00046` → propose (claude-sonnet-5, structured output) → confirm member → `composeArtifact` renderizó las 2 láminas (PNG+PDF, 0 warnings). Eval baseline determinista en CI (golden = láminas SKY a mano + 2º author de credenciales que prueba el agnosticismo). | Flip a staging tras sign-off del operador (repetir el flujo con sesión agente); prod = decisión de negocio posterior (Move 0). El flag gatea SOLO `proposeChapter` (el costo LLM); el confirm/ensamble es puro. | **Runtime: Vercel/CLI ÚNICAMENTE** (`src/lib/commercial/tenders/proposals/authoring/chapter-author.ts`; ningún Cloud Run lo lee — verificado con grep). OFF → el propose rechaza con copy es-CL y el deck-plan manual sigue disponible. |
| `PPM_POSITION_ENABLED` | TASK-1189 | OFF/default en todos los environments (verdad live: `vercel env ls`) · **code-complete + shadow** | (1) **captura de la tasa PPM real** del contribuyente con el contador (hoy seed placeholder 0.25% en `ppm_rate_config`) + **validación contable** de la cifra shadow (19 períodos, ej. 2026-06 = 14.500 CLP); (2) flip del flag; (3) redeploy ops-worker si se agrega materialización reactiva. | Línea PPM del F29 (child A de TASK-1186). Default OFF → el endpoint `GET /api/finance/ppm/monthly-position` marca `enabled:false` (shadow). La tasa vive en la SSOT `ppm_rate_config` (actualizable sin deploy); el flag gatea la exposición "oficial". |
| `RETENTION_POSITION_ENABLED` | TASK-1188 | **staging: ON** (2026-06-20, toma efecto en el redeploy del push) · prod: OFF | (1) **prod queda gated en validación contable** de la cifra de retenciones (2026-05 = 242.623 CLP, 2026-06 = 138.646 CLP) vs el F29 real; (2) tras sign-off, `vercel env add RETENTION_POSITION_ENABLED Production` + redeploy; (3) redeploy del ops-worker si se agrega materialización reactiva. | Línea de retenciones del F29 (child B de TASK-1186). Default OFF → el endpoint `GET /api/finance/retention/monthly-position` marca `enabled:false` (shadow), nadie trata la cifra como oficial. La data shadow existe en `retention_monthly_positions` (materializada manualmente). |
| `NEXA_INTERACTION_LANE_ENABLED` + `NEXT_PUBLIC_NEXA_INTERACTION_LANE_ENABLED` | TASK-1079 | **staging:** env vars SET (2026-06-18), **redeploy pendiente** · prod: OFF | (1) redeploy de staging con **build fresco** (NEXT_PUBLIC se hornea en build) → habilita "Lateral" en el menú de modo de Nexa. (2) prod = decisión del operador tras sign-off | Lane sidecar de Nexa (concepto C). Default-safe: solo habilita la opción, el default sigue siendo el flotante. |
| `NOTION_DUE_DATE_CAPTURE_ENABLED` (M0) + `ATTRIBUTABLE_LATENESS_OTD_ENABLED` (M2) | TASK-921/922 (consumido por TASK-1169 → TASK-1170) | OFF/default por environment (verdad live: `vercel env ls`) | **NO bloquea TASK-1169** (su materializador/reconciliación/signal corren sin flag, shadow). Pero el OTD-imputable member×month tiene cobertura de freeze casi nula sobre la cohorte productiva hasta que estos flags estén ON + se backfillee el M2 shadow sobre la cohorte. Prender + backfill es prerequisito del reloj ≥30d y del cutover **TASK-1170**, no de esta task. | Dependencia de rollout documentada en ADR `GREENHOUSE_ATTRIBUTABLE_LATENESS_V1` §16.11 + Handoff. El flip productivo del bono es TASK-1170 (gateado, ≥30d shadow + sign-off). |
| `GROWTH_CTA_ENGINE_ENABLED` | TASK-1339 | **Code-complete, OFF en todos los environments (default)** — foundation `growth.cta` completa (schema cta_* + primitive + API pública/admin + capability + signals) con smoke e2e verde contra PG dev: CTA follow-up AI Visibility published + surfaces `wordpress`/`think` con embed keys minteadas (secretos entregados una vez en el seed run 2026-07-17). | **TASK-1340 COMPLETÓ el renderer + gobernanza (2026-07-18)** — el flip ya es turnkey: (1) `vercel env add GROWTH_CTA_ENGINE_ENABLED=true` (staging primero) + redeploy; (2) push+PR de la rama `task/TASK-1340-growth-cta-followup` de `efeonce-think` + set `GREENHOUSE_CTA_EMBED_KEY` en su Vercel env; (3) embed WP (snippet en `docs/manual-de-uso/growth/operar-motor-cta.md` §Incrustar); (4) publish gobernado de los tags GA4 (spec turnkey TRACKING-PLAN §CTAs; workspace→preview→confirmación humana); (5) smoke live render/ingest/`form_submitted` en ambas surfaces + signals `growth.cta.*` en steady. | **Runtime: Vercel ÚNICAMENTE** (verificado con grep: solo route handlers). Gatea público + admin. Los COMMANDS no consultan el flag (el seed/CLI operan igual). Signals steady=0 (los 2 unauthorized del smoke 2026-07-17 expiran a las 24h). |
| `GROWTH_NATIVE_MEETING_SCHEDULER_READ_ENABLED` + `GROWTH_NATIVE_MEETING_SCHEDULER_ENABLED` | TASK-1509 / TASK-1510 | **staging + Production: ON 2026-07-21**, con redeploy fresco; producción inicial `dpl_8CfGDY2ATqvyRVQZjQ6VQ9fReCvv` Ready, fix de mes vacío liberado en `ddd3094538e7` (run `29848667096`) y recuperación **native-only** liberada en `fbe8a9c76a74` (PR #163, run `29854833210`, release `fbe8a9c76a74-4aee6089-fec9-45b7-8b70-5ba16a84cfa9`, estado `released`). El binding piloto exacto `fhsf-efeonce-lead-gen-web` / `discovery` está **`active`**. `config` y `availability` responden 200 desde el origen público permitido, con Turnstile requerido y slots reales; la política visitante resolvió `America/New_York`. El host aislado **`https://efeoncepro.com/agenda/`** (WP page `251583`, `noindex`) monta exclusivamente el renderer nativo: cero enlaces o copy de fallback HubSpot, recuperación por navegación mensual/`Reintentar`, desktop sin overflow. HubSpot devuelve cero slots para agosto, pero el smoke Chrome autenticado post-release confirmó la grilla semántica completa de agosto y el estado vacío específico tras recargar. No se creó un booking durante estos checks. | Ejecutar un booking controlado post-flip, replay/read-back HubSpot/Outlook/Teams y evidencia live de `/g/collect`. Workspace GTM preview ID 6 no está versionado ni publicado; publicar exige confirmación humana separada. No promover todavía el piloto a Contacto/RRSS. | Runtime Vercel + host WordPress; HubSpot permanece invisible como provider server-side, no como UI alternativa. Revert <5 min: `GROWTH_NATIVE_MEETING_SCHEDULER_ENABLED=false` (y, si corresponde, READ=false) + restaurar el binding exacto a `paused`; o despublicar la página `251583`/restaurar sus backups Elementor. No auto-fallback después de `provider_dispatched` ambiguo/invalid-created. Signals `growth.meeting.*`; HubSpot/Office 365/Teams siguen SoT. |

| `NOTION_OTD_WRITEBACK_ENABLED` + per-cliente `_EFEONCE` / `_SKY` | TASK-927 | OFF/default en todos los environments (verdad live: `vercel env ls`) · **code complete, rollout pendiente** | Writeback `[GH] OTD` a Notion (display-only, client-facing). Activación gateada al operador: (1) crear la propiedad `[GH] OTD` (select, read-only) en Notion Efeonce + Sky; (2) redeploy del ops-worker (registra el Cloud Scheduler `ops-otd-writeback` + endpoint `/otd/writeback`); (3) flip del flag per-cliente con el gate `delivery.attributable_lateness.shadow_terminal_open` en steady=0. | Display-only, NUNCA toca el bono. Default OFF → cero writes hasta activar. El batch es no-op con flag OFF aunque el job exista. Spec: TASK-927 + OTD_V1 §Delta 2026-06-20. |

| `FINANCE_CORE_CLF_INDEXED_ENABLED` + `FINANCE_CLF_INCOME_PROJECTION_ENABLED` + `FINANCE_CLF_OBLIGATIONS_ENABLED` + `FINANCE_CLF_REPORTING_ENABLED` + `FINANCE_CLF_BACKFILL_APPLY_ENABLED` | TASK-995 | OFF/default en todos los environments (verdad live: `vercel env ls`) · **code-complete + gated** | (1) `FINANCE_CORE_CLF_INDEXED_ENABLED=true` (master) + `FINANCE_CLF_INCOME_PROJECTION_ENABLED=true` en staging → habilita la rama CLF de los materializers (cotización/HES/**OC de cliente en UF** → income CLP + plano native UF + snapshot CLF→CLP); (2) validar con una OC de cliente en UF real → income materializado + los 4 signals `finance.uf.rate_freshness`/`finance.indexed_unit.*` en steady; (3) prod tras sign-off Finance. `FINANCE_CLF_OBLIGATIONS_ENABLED`/`FINANCE_CLF_REPORTING_ENABLED`/`FINANCE_CLF_BACKFILL_APPLY_ENABLED` se prenden cuando se cableen sus consumers (obligations CLF / readers / backfill — diferidos por anti-drift). | CLF/UF como unidad indexada nativa (ADR `GREENHOUSE_CLF_INDEXED_FINANCE_CORE_V1`). Default OFF → CLF sigue pricing/quote-only; el camino CLP/USD/MXN es bit-for-bit. Las OC son las **recibidas de clientes** (lado income/AR), no compras a proveedores. |

| `NEXA_PROPOSAL_ACTIONS_ENABLED` | TASK-1399 | **staging: ON y VERIFICADO CON EL LLM REAL (2026-07-12)** — env var + **redeploy** (rev `greenhouse-l6mcub3cd`, la que tienen los alias `dev-greenhouse.efeoncepro.com` y el `.vercel.app` de staging). ⚠️ El primer deploy (disparado por el push) se creó **antes** de agregar la var y NO la tenía: Vercel congela las env vars al CREAR el build — por eso hubo que redeployar. Evidencia conversacional real: (a) *"¿Cómo va la propuesta de SKY?"* → el LLM disparó `proposal_status` → SKY `intake`, deadline `at_risk` (15-jul 18:00), artefacto `completed` + link de descarga; (b) *"Registra una propuesta para Aguas Andinas… no sé la fecha"* → propuso `register_proposal` con el cliente resuelto **por nombre**, deadline **"Sin fecha declarada"** (no lo inventó) y el `input` **sin `ownerOrgId`** (el scope sale de la sesión). Nada se escribió: quedó en propuesta. **Prod: OFF.** | Prod = decisión del operador. ⚠️ Prenderlo en prod **sin** `ARTIFACT_RENDER_JOBS_ENABLED` da una Nexa que registra propuestas y evidencia pero **no puede generar el deck** (`request_proposal_render` bloquea con `flag_disabled` — honesto, pero media promesa). Lo coherente es prender ambos juntos, y el del render exige sign-off + release control plane. | Bloque de acciones gobernadas del Proposal Studio (write gobernado interno). **Runtime: Vercel ÚNICAMENTE** (el chat de Nexa es un route handler de Next; ningún Cloud Run lee este flag — verificado con grep). Default OFF → gap `runtime_disabled` y el tool de estado no se ofrece. El scope NO lo propone el LLM: `ownerOrgId` se deriva del entitlement per-ORG (`proposal_studio_v1`) y el cliente se resuelve por nombre fail-closed. La mutación SIEMPRE ocurre en el confirm humano; los commands re-enforzan entitlement + capability + el gate de audience (una evidencia `internal` en un artefacto `client_facing` rechaza el artefacto completo, y ni siquiera llega a proponerse). |
| `NEXA_QUOTE_AUTHOR_ACTION_ENABLED` | TASK-1212 | OFF/default en todos los environments (verdad live: `vercel env ls`) · **code-complete + gated** | (1) además del master `NEXA_ACTION_RUNTIME_ENABLED` (ya ON en staging/prod), prender `NEXA_QUOTE_AUTHOR_ACTION_ENABLED=true` en staging → habilita la governed action `author_quote` (Nexa puede crear/emitir una cotización con confirmación humana); (2) ejercer el loop `propose → confirm → execute` con una cotización real en staging + verificar quotation/líneas/outbox; (3) prod tras sign-off del operador. | Governed action de autoría/emisión de cotización (write gobernado interno). Default OFF → el resolver devuelve gap honesto `runtime_disabled` y el confirm rechaza; la mutación SIEMPRE ocurre en el confirm humano y el command `submitQuoteFromBuilder` re-enforza capability `commercial.quotation` + precio del engine. |

| `COMMERCIAL_Q2C_CANONICAL_CLOSE_ENABLED` + `COMMERCIAL_Q2C_CONTRACT_ONLY_ENABLED` | TASK-1206 | **`COMMERCIAL_Q2C_CANONICAL_CLOSE_ENABLED` staging: ON (2026-06-22, smoke HTTP PASS)** · prod: OFF · `COMMERCIAL_Q2C_CONTRACT_ONLY_ENABLED`: OFF en todos | (1) ~~staging flip + smoke~~ **HECHO 2026-06-22**: flag ON en staging + redeploy `greenhouse-jfz70d2gr`; smoke HTTP sobre fixture → `convert-to-invoice` delegó en `closeQuoteToCash` (201 con operationId/finalState canónicos), income + contrato + audit Q2C + outbox completo; 2.º POST → MISMO incomeId (anti doble-AR confirmado, 1 income en PG). (2) **prod pendiente**: tras sign-off Commercial/Finance, aplicar la migración `20260621222152560` en la base de prod (vía release control plane develop→main) + `vercel env add ... Production` + redeploy + smoke prod. `COMMERCIAL_Q2C_CONTRACT_ONLY_ENABLED` solo si el operador habilita `contract_only` (deal suspendido sin AR + SLA) — política aparte con sign-off. | Cierre canónico Quote-to-Cash (TASK-1206). En staging el path visible ya usa el comando canónico (añade audit Q2C + idempotencia; las conversiones que antes hacía el legacy ahora pasan por `closeQuoteToCash`). `contract_only` NUNCA es cierre terminal: audit `status='suspended'` + signal `contract_only_sla_breach`. |

| `GROWTH_AI_VISIBILITY_ARCHETYPE_PROMPTS_ENABLED` + `GROWTH_AI_VISIBILITY_PROMPT_AUTHORING_ENABLED` (Vercel **+ ops-worker**) | TASK-1290 | **staging: ON (2026-06-29, Vercel env staging + deploy.sh)** · prod: OFF | Prompts por arquetipo (categoría × `business_model` × buyer-intent) + autoría LLM del set. `ARCHETYPE_PROMPTS`: con ON `buildExecuteInput`/`enqueueGraderRun` resuelven el baseline determinista del arquetipo del perfil (`resolveArchetypeBaselinePack`: consumo/B2B-saas/retail/marketplace/público; `unknown`→genérico NUNCA agencia) o el set congelado `active` autorado; agencia = pack v1 bit-for-bit. `PROMPT_AUTHORING` (gateado además por `isGraderEnabled`): habilita `authorPromptSet` (LLM, manual via command — NO auto-run). **⚠️ DUAL-LOCATION** (run `light` inline Vercel + `full` async ops-worker; el switch vive en `buildExecuteInput`/`enqueueGraderRun`, común a ambos). Los tags del pack VIAJAN con el run (Slice 0) → el scorer mide con el framing del arquetipo (sin esto, ids nuevos colapsan el score). **Verificado E2E live (2026-06-29, gemini, staging DB):** SKY → autoría LLM (15 prompts consumo) → set active → run `EO-GRUN-00031` `succeeded` (provenance `prompt_set_id` registrado, prompts "Santiago-Calama"/"Sky vs LATAM") → **score 36.1 ≠ 0** (vs el 0 artificial del pack agencia); dimensiones tag-dependientes con evidencia. El set autorado de verificación se limpió → SKY queda en el baseline determinista de consumo hasta el review operador. **Pendiente prod:** review del copy autorado (TASK-1291) + eval golden-set multi-arquetipo (TASK-1292) → release control plane EPIC-021. | staging ON → SKY (consumo) medible ≠ 0 con prompts reales. Prod OFF → pack agencia idéntico (bit-for-bit). Revert (<5 min): flags a false. |
| `GROWTH_AI_VISIBILITY_CATEGORY_GUARD_ENABLED` (Vercel **+ ops-worker**) | TASK-1288 | **OFF en todos los environments** · code-complete | Guard de categoría no resuelta (ISSUE-110): cuando ON, un run cuyo perfil tenga `category_node_id = unknown`/baja confianza se bloquea (`aeo_category_unresolved`) en vez de generar prompts basura. **⚠️ DUAL-LOCATION** (el run corre en Vercel `light` inline y ops-worker `full` async — el guard vive en `buildExecuteInput`, común a ambos). **Pendiente:** prender SÓLO tras (1) Slice 4 (brand_intelligence grounded read) + (2) backfill verificado con cobertura alta (hoy 13% determinista — la mayoría free-text/null se resuelve con el grounded read) — sino bloquearía el lead magnet con muchos `unknown` legacy. Pre-check limpio en portal/operador (`category_unresolved` blocked) + backstop universal en `buildExecuteInput`. Signal `growth.ai_visibility.profile_category_unresolved` (org-linked unresolved, steady 0). | Default OFF → ningún run se bloquea por categoría (comportamiento idéntico). ON → run/envío con categoría no resuelta bloqueado → confirmación humana (TASK-1291). Revert (<5 min): flag a false. |
| `GROWTH_AI_VISIBILITY_BRAND_INTELLIGENCE_ENABLED` (Vercel **+ ops-worker**) | TASK-1288 | **staging: ON (2026-06-29, deploy.sh + Vercel)** · prod: OFF | Lectura grounded `brand_intelligence` (LLM sobre contenido legible del sitio + entity → snapshot estructurado que TASK-1289/1290 consumen). Gateado además por `isGraderEnabled`. **Nota:** ningún path runtime invoca aún el grounded read automáticamente — se ejecuta vía `scripts/growth/backfill-canonical-category.ts --grounded` (ya corrido contra `greenhouse-pg-dev`/staging: SKY→sector:passenger_airlines, Berel→manufacturing, Banco de Chile corregido→finance). Costo: gemini-flash-lite ~$0.0004/marca, 1×/marca/versión cacheado. **Pendiente prod:** release control plane + EPIC-021. | Default OFF → la resolución de categoría cae al prior determinista (HubSpot map + alias); cero costo LLM. ON → lectura grounded autoritativa (cascada con confianza). Revert (<5 min): flag a false. |
| `GROWTH_AI_VISIBILITY_GRADER_ENABLED` (master) + `GROWTH_AI_VISIBILITY_OPENAI_ENABLED` / `_ANTHROPIC_ENABLED` / `_PERPLEXITY_ENABLED` / `_GEMINI_ENABLED` / `_GOOGLE_AIO_ENABLED` | TASK-1226/TASK-1265 | **staging: GRADER + OPENAI + ANTHROPIC + PERPLEXITY + GEMINI + GOOGLE_AIO ON en ops-worker** (`ops-worker-00418-2m6`) · prod: OFF | ~~(1) secrets OpenAI/Anthropic~~ HECHO (ya existían del spike 1228, grant secretAccessor a greenhouse-portal@ verificado) · ~~(2) flags staging + smoke real~~ HECHO (smoke real local OpenAI 6/6 + Anthropic verificados; flags `staging` ON). **(3) prod pendiente (FOLLOW-UP pesado):** tras sign-off, migración `greenhouse_growth` + capabilities seed a prod vía release control plane develop→main + `vercel env add GROWTH_AI_VISIBILITY_* Production` + redeploy + smoke prod. ~~(4) Perplexity/Gemini: cuando se provisionen `greenhouse-perplexity-api-key`/`greenhouse-gemini-api-key`~~ HECHO (Gemini TASK-1233; Perplexity 2026-06-27: secret `greenhouse-perplexity-api-key` v1 + grant secretAccessor a greenhouse-portal@ + flag staging ON + smoke real local source=secret_manager). **Drift corregido 2026-06-29:** `ops-worker-00417-m86` tenia `GROWTH_AI_VISIBILITY_PERPLEXITY_ENABLED=false`; se hizo `gcloud run services update ops-worker --update-env-vars=GROWTH_AI_VISIBILITY_PERPLEXITY_ENABLED=true` → revision `ops-worker-00418-2m6` sirve 100% traffic con Perplexity ON, y `services/ops-worker/deploy.sh` queda persistido staging ON / prod OFF para futuros redeploys. Pendiente recomendado: smoke async low-volume post-flip. ~~**(5) Google AIO/TASK-1265:** aplicar migration provider CHECK, deploy y prender `GOOGLE_AIO` solo en staging~~ **HECHO (2026-06-28):** migración CHECK aplicada (dev/staging comparten `greenhouse-pg-dev`); flag staging ON en Vercel **y** en ops-worker (staging async → el run ejecuta en el worker, no en Vercel). ops-worker `deploy.sh` con branch staging ON / prod OFF + `DATAFORSEO_API_PASSWORD_SECRET_REF` + login vía GH secret `DATAFORSEO_API_LOGIN`; rev viva actual `ops-worker-00418-2m6`. **Smoke real verde end-to-end:** run `grun-61d1c683…` (solo `google_ai_overview`) drenado por el worker → observation `succeeded`, 27 citas, `$0.004` en `provider_observations`. Usable en los **3 endpoints** (public/client-portal/operator) por construcción (run-engine resuelve providers desde `policy.eligibleProviders`; los 3 modos incluyen el provider). **(5b) prod pendiente (gated TASK-1246/EPIC-020):** sign-off + migración/capabilities a prod + flag prod + **rotar el password DataForSEO** (expuesto en provisión). ⚠️ Llave Perplexity y password DataForSEO fueron expuestos en chat/captura → rotar antes de prod. | AI Visibility Grader (dominio growth.ai_visibility). Default OFF → cada adapter resuelve skip controlado (`grader_disabled`/`provider_disabled`/`missing_secret`), cero llamadas, cero costo. `light` excluye Anthropic+web_search por costo (§5 calibración) pero incluye Google AI Overview detrás de flag propio y costo por request DataForSEO. Runs manuales y recurrentes usan el provider set efectivo del environment que ejecuta (worker para async). |

| `GROWTH_AI_VISIBILITY_PUBLIC_INTAKE_ENABLED` (Vercel) | TASK-1240 | **staging: ON (2026-06-25, redeploy `greenhouse-h26jgk4dc` + smoke real verde)** · **prod: ON (2026-06-30 flip masivo Growth/AEO, redeploy `greenhouse-ic8cg4ery`; ver Delta top) — valor real `true` re-verificado 2026-07-01 vía `vercel env pull`** | (1) ~~**sign-off legal del consent / aviso de privacidad** (Ley 21.719/GDPR) antes de prender EN PROD~~ **RESUELTO 2026-07-01 (TASK-1246): el operador confirmó que el consentimiento + aviso de privacidad están revisados/aprobados.** (2) secret captcha `TURNSTILE_SECRET`: en **staging** = test secret Cloudflare always-pass (`1x0000…AA`); en **prod** = secret real (Secret Manager, SET 2026-06-30) — ⚠️ **expuesto en chat → rotación pendiente (operador)**. (3) `vercel env add ... staging`+prod HECHO + redeploy. (4) smoke real staging HECHO: POST con captcha → 202 + submission + run encolado (`EO-GRUN-00012`). (5) prod flip HECHO (ad-hoc 2026-06-30, no vía release control plane). **Mitigante de exposición (verificado en código 2026-07-01): el grader self-serve (form `fdef-ai-visibility-grader`) NO está embebido en ninguna superficie pública** — no hay `page.tsx` pública en greenhouse-eo; `/aeo-2/` es el form COMERCIAL `efeonce-aeo-diagnostic` (form-key `b120566a…`) → HubSpot, que **NO corre el grader** (el consumer `growth-grader-run-from-submission` filtra por `form_id === fdef-ai-visibility-grader`); ni existe en efeonce-web. **→ 0 tráfico self-serve real hoy** (aunque el intake API está ON). El render/form en efeonce-web del ADR 2026-06-28 está **sin construir**; el grader ya es un form gobernado embebible con el mismo `<greenhouse-form>` que usa `/aeo-2/`. | Intake público del lead magnet (EPIC-020 B). Gateado por el kill switch `isGraderEnabled`. Default OFF → el POST público responde `disabled` (404). ON → captcha (Turnstile) + rate-limit (per-IP 10/email 3 por día) + presupuesto global diario (circuit breaker → 503) + modo `light` forzado; encola `public_diagnostic`. El email (PII) vive sólo en `grader_leads` con consent, NUNCA a providers. Revert (<5 min): flag a false → 404. |
| `GROWTH_GRADER_INTAKE_ON_FORMS_ENGINE_ENABLED` (Vercel) | TASK-1251 | **staging: ON (2026-06-25, redeploy `greenhouse-h26jgk4dc` + smoke E2E verde)** · prod: OFF | (1) Migración `task-1251-grader-forms-engine-convergence` + UNIQUE parcial aplicadas (seed grader-form gobernado `fdef-ai-visibility-grader` + binding `grader_leads.submission_id`). (2) `vercel env add ... staging` HECHO + redeploy. (3) cron `ops-reactive-growth` creado (deploy ops-worker on push develop). (4) **smoke E2E staging HECHO:** POST → `submissionId` (no inline) → outbox `published` → reactive consumer materializó lead `glead-2d1e97f9` + run `EO-GRUN-00012` linkeados; email en PG con consent, nunca al provider. (5) prod = release control plane junto a EPIC-020 + sign-off legal. Revert (<5 min): flag a false → vuelve al intake a-medida (TASK-1240). | Convergencia del intake del AI Visibility Grader sobre el motor Growth Forms (TASK-1251). Default OFF → `POST /run` usa el path a-medida actual (`createPublicGraderRun` inline). ON → fachada que persiste `form_submission` + consent_snapshot + outbox `growth.forms.submission_accepted`; un reactive consumer (`growth_grader_run_from_submission`, domain `growth`, lane `ops-reactive-growth`) encola el run + materializa el lead (no inline). Un solo stack de public-submission. |
| `GROWTH_AEO_FORM_GRADER_INTAKE_ENABLED` (**ops-worker**) | TASK-1321 | **develop pushed (`daf55cb7c`, 2026-07-02) · staging: LIVE + validado E2E** (run EO-GRUN-00034: auto-score 48.9 → informe → correo SENT vía Resend). **Prod: pendiente SOLO de la promoción `develop→main`** (el push directo a `main` lo bloquea el harness auto-mode + la batch policy pide break-glass). El bloque `production` de `deploy.sh` YA espeja staging (grader stack completo ON, Gemini ON post-billing-unblock) → el prod release lo activa por construcción, sin flip manual. Root-cause del path público que no generaba informe RESUELTO (auto-score en `executeClaimedGraderRun`, `run-engine.ts`). ⚠️ **KILL-SWITCH default-ON** (desvía del patrón default-OFF por directiva explícita del operador 2026-07-02): la capacidad nace activa; el flag APAGA (`=false`). | El flag corre donde se drena la projection `growth_aeo_diagnostic_grader_run_from_submission` (lane `ops-reactive-growth`, ops-worker). **Palanca de activación REAL ≠ el flag:** con el flag ON pero el form `/aeo-2/` SIN el campo `brandName`, cada submit cae en `mapAeoDiagnosticToGraderIntake → missing_brand_name → skip` (degrada al lead comercial, sin run). El grader `/aeo-2/` **no corre de verdad** hasta publicar la versión nueva del form con `brandName` (activation script, Slice 1-form, es mutación live pendiente). Flags aguas abajo ya ON en prod (06-30 flip masivo): `GRADER`/`BRAND_INTELLIGENCE` (categoría)/`REPORT_EMAIL`/`LEAD_HANDOFF`. Consent: `/aeo-2/` usa `efeonce-aeo-diagnostic-consent-v1` (operador aceptó correr el grader + enviar informe bajo ese consent). Rollout: (1) ~~republicar form con `brandName`~~ **HECHO 2026-07-02** (v10 `fver-fc552f37-15a6-4d70-abfb-59929a409a62` publicada, v8 `fver-38d38bbc-…` deprecada; live verificado + screenshot, `overflowX=0`; `brandName` req full-width + `brandWebsite`→"Sitio web de tu marca" + `country` opcional). El form ya captura `brandName` en prod, pero el grader **no corre** hasta el step 2 (código sin deploy) → hoy `/aeo-2/` sigue siendo captura de lead comercial + campo nuevo. → (2) deploy código a ops-worker (staging→prod vía release control plane) → (3) smoke E2E: submit real `/aeo-2/` → run → correo con PDF → contacto HubSpot único. Revert (<5 min): `GROWTH_AEO_FORM_GRADER_INTAKE_ENABLED=false` en ops-worker → `/aeo-2/` vuelve a solo-comercial. | `/aeo-2/` (`fdef-efeonce-aeo-diagnostic`) submit → grader run auto-disparado + informe por correo (event-driven, apenas `ready`/`partial`) + lead HubSpot dedup (enriquece, no duplica). Guardrails que hacen segura la posición ON: cost-cap global (budget 24h + per-email 3/IP 10) aplicado en la projection + `grader_intake_events`, gate de categoría (unknown→skip, sin runs basura), corporate-email + Turnstile del form, dedup HubSpot nativo por email/dominio. PII (email/nombre) solo en `grader_leads` + el correo, NUNCA al run engine. |
| `GROWTH_AI_VISIBILITY_REPORT_EMAIL_ENABLED` (**ops-worker** + Vercel) | TASK-1250 | **staging: ON (2026-06-27, ops-worker rev `00409-fxd` + Vercel staging, smoke E2E real verde)** · prod: OFF | **⚠️ DUAL-LOCATION (espeja el handoff):** el WRITE (`dispatchAiVisibilityReportEmail`) corre en el **ops-worker Cloud Run** (reactive consumer `growth_ai_visibility_report_email`, lane `ops-reactive-growth`) → el flag se necesita ahí; el enqueue corre en Vercel pero NO mira el flag (gate vive en el write); el signal reader (Vercel) sí lo lee para la severidad. (1) ~~redeploy ops-worker~~ **HECHO** (push develop → GitHub Actions Ops Worker Deploy, rev `00409-fxd`, bundle con el consumer + report PDF renderer; fonts + `public/branding` en la imagen). (2) `RESEND_API_KEY` ya configurado. (3) ~~flag staging ON~~ **HECHO** (deploy.sh declarativo staging ON/prod OFF + Vercel `env add ... staging`). (4) ~~smoke staging~~ **HECHO 2026-06-27**: lead consentido (`grun-689323b1`) → enqueue `report_email_requested` → outbox published → reactive-growth drenó → `grader_report_email_dispatches.status='sent'` (1 fila, sin doble-envío) + `email_deliveries.status='sent'`, `has_attachments=true`, `recipient=jreysgo@gmail.com`, `resend_id=501dcd8e-3c4a-4943-a7d7-f3fb95183b9c`; signal `growth.ai_visibility.report_email_failed` sin failed. **Prod (5):** release control plane develop→main junto a EPIC-020 + sign-off (legal/from-address del lead magnet, TASK-1246). | Entrega del informe del grader por email (EPIC-020). Default OFF → el consumer resuelve `skipped:disabled` (NUNCA envía, NUNCA crash); el enqueue del evento igual ocurre (gate en el write para no perder eventos al prender). ON → arma el adjunto PDF público-safe (TASK-1273) + envía vía `sendEmail` (domain `growth`, marca **Efeonce** agencia). Consent-gate + report-state gate (`ready`/`partial`) + idempotencia DB-level `UNIQUE(report_id, email_type)`. Revert (<5 min): flag a false → cero envíos. |
| `GROWTH_AI_VISIBILITY_OPERATOR_SEND_ENABLED` (Vercel **+ ops-worker**) | TASK-1279 | ⚠️ **CORREGIDO 2026-06-29 (TASK-1291 rollout): `vercel env ls` (SoT) = ABSENT en TODOS los envs Vercel** → la cross-sell SEND está OFF (command rechaza `aeo_send_disabled`). El "staging ON + smoke E2E verde" de abajo fue cierto en el momento del smoke de TASK-1279 pero **el flag NO quedó persistido en Vercel** (o se removió post-ISSUE-110) → estado real OFF. _Histórico: staging smoke (2026-06-29): Sky Airlines → email `sent` + Lead HubSpot `566830815132` + `aeo_check_result='No aparece'`. ops-worker rev `00422-cz7`._ · prod: OFF | **⚠️ DUAL-LOCATION (espeja report-email/handoff):** el WRITE (`executeOperatorReportSend`) corre en el **ops-worker Cloud Run** (reactive consumer `growth_ai_visibility_operator_send`, lane `ops-reactive-growth`) → el flag se necesita ahí; el command (Vercel) lo lee al frente para gatear el 409 `aeo_send_disabled`. (1) **provisionar la property HubSpot `aeo_check_result`** (string en Company, grupo `aeo`) vía `scripts/growth/provision-ai-visibility-hubspot-properties.ts` — HOY NO EXISTE en el portal (verificado live 2026-06-29); sin ella el upsert de Company 400ea. (2) **confirmar el objeto `leads` habilitado** en el portal (verificado vía REST: `crm/v3/objects/leads` responde 154 props; el MCP no lo soporta pero la API sí) + revisar la asociación default Lead↔Company. (3) `vercel env add ... staging` + flag en el ops-worker (`deploy.sh` staging ON / prod OFF). (4) smoke staging real: correr+publicar un run operador → `POST runs/[runId]/send-lead` → outbox `report_send_requested` → reactive-growth drena → email `sent` (`email_deliveries`) + Lead creado en HubSpot + `aeo_check_result` en la Company + consent gate (prospecto sin consent → 422). (5) sign-off comercial/legal del copy a prospectos (interés legítimo) + prod vía release control plane junto a EPIC-020. | Cross-sell operador: enviar informe AEO + crear **Lead** HubSpot (objeto `leads`, NUNCA Deal). Default OFF → el command rechaza `aeo_send_disabled` (NUNCA envía/escribe) y el consumer resuelve `skipped:disabled`. ON → email público-safe (mismo path leak-safe del lead magnet, marca Efeonce) + upsert Contact/Company + Lead + `aeo_check_result`; consent gate server-side (prospecto exige consentimiento), idempotencia run+recipient, signal `growth.ai_visibility.operator_send_failed` steady=0. Revert (<5 min): flag a false. |
| `GROWTH_AI_VISIBILITY_LEAD_HANDOFF_ENABLED` (Vercel **+ ops-worker**) | TASK-1242 | **staging: ON (2026-06-25, smoke E2E real verde)** · prod: OFF | **⚠️ DUAL-LOCATION (hallazgo del smoke):** el flag se necesita en DOS lugares — **Vercel** (auto-trigger del publish + admin endpoints + status reader) Y el **ops-worker Cloud Run** (donde corre `executeLeadHandoff`, el WRITE). Prender solo Vercel → el consumer salta `reason=disabled`. (1) ~~HubSpot custom properties `ai_visibility_*` + grupo "AEO"~~ **HECHO** (portal 48713323, 8 props + grupo `aeo`, vía `scripts/growth/provision-ai-visibility-hubspot-properties.ts`). (2) ~~`vercel env add ... true staging`~~ **HECHO** + redeploy. (2b) ~~flag en el ops-worker~~ **HECHO** (`gcloud run services update ops-worker --update-env-vars` + declarativo en `services/ops-worker/deploy.sh` staging ON/prod OFF para que `--set-env-vars` no lo borre). (3) ~~smoke staging~~ **HECHO 2026-06-25**: run `EO-GRUN-00012` score `completed` (25.8) → publish snapshot → auto-trigger → reactive consumer upsert contact `smoke-task1251@example.com` + company `example.com` con props `ai_visibility_*` + `hubspot_synced_at` set; registros de prueba borrados post-verificación (CRM limpio). **Prod (4):** release control plane develop→main junto a EPIC-020 + sign-off (flag ON en Vercel prod + en el ops-worker prod vía deploy.sh prod-branch o gcloud). (3) smoke staging: publicar snapshot de un run real con lead → outbox `lead_handoff_requested` → reactive consumer upsert contact/company en HubSpot sandbox + `hubspot_synced_at` + signal `lead_handoff_uncovered` steady=0. (4) prod = release control plane junto a EPIC-020 + sign-off. | HubSpot lead handoff (EPIC-020 D). Default OFF → el reactive consumer resuelve `disabled` (NUNCA escribe a HubSpot, NUNCA crash); el enqueue del evento igual ocurre (gate vive en el write para no perder eventos al prender). ON → upsert contact/company vía cliente HubSpot **in-app directo** (NO Cloud Run bridge). Consent-gate + score `completed` gate (sin score falso). Dedup company por dominio corporativo. Revert (<5 min): flag a false → cero writes. |
| `GROWTH_AI_VISIBILITY_PORTAL_RUN_ENABLED` (Vercel) + `GROWTH_AI_VISIBILITY_TRIAL_ENABLED` (Vercel) | TASK-1277 | **staging: PORTAL_RUN + TRIAL ON (2026-06-29)** · prod: OFF | Run gobernado AEO de portal (chokepoint per-org). `PORTAL_RUN` (gateado además por `isGraderEnabled`) habilita las puertas cliente (contratado/trial/pilot) vía `requestGraderRunForOrganization`; `TRIAL` habilita el tier trial PLG. Config asociada (no `*_ENABLED`, env-override, defaults conservadores): `GROWTH_AI_VISIBILITY_TRIAL_RUNS_PER_MONTH=1`, `_CONTRACTED_RUNS_PER_MONTH=20`, `_PILOT_RUNS_PER_MONTH=3`, `_TRIAL_GLOBAL_MONTHLY_BUDGET_USD=25`. **Rollout staging aplicado 2026-06-29:** (1) migraciones ya en develop ✓ → (2) flip `PORTAL_RUN` staging ✓ + smoke real: Berel (contratado) → chokepoint **accepted** (run `portal_contracted` `light` encolado y drenado por el worker), org sin entitlement → `no_entitlement` ✓ → (3) flip `TRIAL` staging ✓ (inerte: sin orgs trial provisionadas + el trial exige `grader_profile` enlazado → costo ~0). **Binding del contratado:** se creó el `grader_profile` de Grupo Berel enlazado a su org (`EO-GAVP-0014`, web `https://berel.com` derivada de la org tras TASK-1285), vía `scripts/growth/provision-grader-profile-for-org.ts --apply`. **Pendiente:** (4) sign-off comercial qué otras orgs son `trial` vs `contracted` (hoy **solo Berel contratado**; trial PLG a otros clientes NO provisionado — decisión comercial outward-facing) → (5) `scripts/growth/provision-aeo-trials.ts --apply` cuando se decida → (6) prod vía release control plane + EPIC-020. La puerta operador (`requestGraderRunAsOperator`) NO depende de estos flags (capability-gated + kill switch global). | Entitlement & metering AEO (EPIC-020). Default OFF → el chokepoint de portal responde `aeo_run_disabled` (cero costo). ON → entitlement (módulo per-org) → ventana → allowance (runs/mes con reset) → costo. Revert (<5 min): `vercel env rm` de los 2 flags en staging. |
| `GROWTH_AI_VISIBILITY_REGRADE_ENABLED` (**ops-worker**) | TASK-1270 | **staging: ON en develop rollout (2026-06-29)** · prod: OFF. `ops-growth-grader-regrade` activo en staging y pausado por default en production. | Re-grade recurrente AEO para perfiles `grader_profiles.recurring_regrade_enabled=true` + módulo `ai_visibility_v1` contratado. Config asociada: `GROWTH_AI_VISIBILITY_REGRADE_BATCH_SIZE=5`, `GROWTH_AI_VISIBILITY_REGRADE_MONTHLY_BUDGET_USD=50`, `GROWTH_AI_VISIBILITY_REGRADE_SCHEDULER_PAUSED=false` en staging / `true` en production. **Rollout staging aplicado:** migración TASK-1270 OK; ops-worker `ops-worker-00417-m86` (`GIT_SHA=b5cf815d7…`) Ready con flag ON; Scheduler `ENABLED`; smoke manual `ops-growth-grader-regrade` → `claimed=0 enqueued=0 failed=0 skipped=no_due_profiles` porque `opt_in_profiles=0`, `due_profiles=0` (cero costo). **Prod pendiente:** budget sign-off + release control plane. **E2E pendiente:** opt-in gobernado de 1 perfil contratado para verificar run recurrente `pending→terminal`. | Default OFF en prod → `/growth/grader/regrade` responde `disabled`, cero claim de perfiles, cero costo. ON staging → claim `FOR UPDATE SKIP LOCKED` de perfiles due, enqueue `full` idempotente con key `growth-ai-visibility-regrade:*`, retry corto si falla el enqueue, y ejecución real queda a cargo de `/growth/grader/drain`. Revert staging (<5 min): `GROWTH_AI_VISIBILITY_REGRADE_ENABLED=false` + pausar `ops-growth-grader-regrade` o redeploy con `GROWTH_AI_VISIBILITY_REGRADE_SCHEDULER_PAUSED=true`. |
| `GROWTH_AI_VISIBILITY_PROBES_ENABLED` (Vercel **+ ops-worker**) + `GROWTH_AI_VISIBILITY_AGENTIC_READINESS_ENABLED` (Vercel **+ ops-worker**) | TASK-1266 | **staging: ON (2026-06-28, ops-worker rev deploy `83927197756` + Vercel staging, smoke E2E real verde)** · prod: OFF | Site Readiness Probe Layer (probes técnicos read-only del sitio analizado → ejes `structural` + `agentic` ortogonales al de percepción). `PROBES` (gateado además por `isGraderEnabled`) es el flag MAESTRO: habilita el eje structural (robots IA, JSON-LD, llms.txt, sitemap; CWV degrada a skipped sin Chromium). `AGENTIC_READINESS` (requiere `PROBES` ON) suma el eje agentic (.well-known/mcp, API discoverability, potentialAction, DOM semántico; WebMCP degrada a skipped). **⚠️ DUAL-LOCATION:** el probe gatherer corre dentro de `executeClaimedGraderRun` → en **ops-worker** (path async `full`) y en **Vercel** (path inline `light`); ambos necesitan el flag. **Rollout (pendiente):** (1) migración `task-1266-grader-probe-results` aplicada en staging (ya en develop) → (2) flip `PROBES` staging (Vercel + deploy.sh ops-worker) + run real sobre dominio de prueba → verificar `grader_probe_results` + `structural_readiness` + honest degradation de una señal ausente → (3) flip `AGENTIC_READINESS` staging + verificar `agentic_readiness` → (4) revisar costo (fetches read-only, sin Lighthouse aún) + signals `probe_failure_rate`/`probe_headless_coverage` steady → (5) prod vía release control plane + EPIC-020. **Headless (CWV/WebMCP) queda degradado a `skipped/no_headless`** hasta cablear Chromium en el worker (follow-up; out-of-band coordination de la task). | Site readiness (EPIC-020). Default OFF → el run-engine no invoca el gatherer (run de percepción idéntico; readiness omitido del report). ON → tras cada run, probes read-only del dominio → readiness lado a lado del de percepción (NUNCA blended). Revert (<5 min): flags a false → el report omite los ejes de readiness y sigue con percepción intacta. |
| `GROWTH_AI_VISIBILITY_ENTITY_PROBES_ENABLED` (Vercel **+ ops-worker**) | TASK-1267 | **staging: ON** (2026-06-28 — Vercel `env add` env `staging` + `deploy.sh` ENTITY ON; toma efecto en el redeploy del push develop) · prod: OFF | Entity Infrastructure Probes — tercer eje ortogonal `entity` del probe layer (Google Knowledge Graph + Wikidata/Wikipedia + Reddit-UGC). Requiere `GROWTH_AI_VISIBILITY_PROBES_ENABLED` ON (aditivo, igual que `AGENTIC_READINESS`). **⚠️ DUAL-LOCATION** (mismo patrón que TASK-1266): el gatherer corre en **ops-worker** (async `full`) y **Vercel** (inline `light`); ambos tienen el flag ON en staging. **KG api key publicada (2026-06-28):** secret `greenhouse-google-knowledge-graph-api-key` en Secret Manager (key GCP restringida a `kgsearch.googleapis.com`, probada live contra la API) + grant `secretAccessor` a `greenhouse-portal@` + `GOOGLE_KNOWLEDGE_GRAPH_API_KEY_SECRET_REF` wired en `deploy.sh` (staging) y Vercel `staging` → los **3 probes de entidad activos** en staging. **Rollout:** (1) migración `task-1267-grader-probe-axis-entity` aplicada dev (✓) → (2) ✅ flip `ENTITY_PROBES` staging (Vercel + `deploy.sh`) → (3) ✅ KG api key publicada + wired → (4) ✅ **verificado E2E** (run `EO-GRUN-00026`, vercel.com): `grader_probe_results` eje entity → `wikidata` 100 (P856 + 38 wiki), `knowledge_graph` 75 (KG key funcional, homónimo sin dominio), `reddit_ugc` failed/null por 403 (honest degradation correcta) → (5) prod vía release control plane + EPIC-020. **Reddit** vía búsqueda pública read-only (sin secret); si la quota la bloquea, degrada honesto. | Entity backbone (EPIC-020). Default OFF → el gatherer no registra los probes de entidad (run + readiness structural/agentic idénticos). ON → suma el eje `entity` lado a lado (NUNCA blended). Revert (<5 min): flag a false → el report omite el eje entity. El render del detalle de entidad lo gobierna TASK-1252 (out of scope acá). |
| `GROWTH_AI_VISIBILITY_FIX_IT_ENABLED` (Vercel) | TASK-1269 | **staging: ON (2026-06-28, Vercel env + deploy develop `greenhouse-fyoycag0t` Ready, CI/Playwright smoke verde)** · prod: OFF | Fix-It Artifacts del AI Visibility Grader: genera on-demand artefactos public-safe deterministas (`Organization`/`Service` JSON-LD starter, `llms.txt`, content brief AEO y brief de entidad cuando hay gaps KG/Wikidata/Reddit) desde el snapshot/report + probe findings existentes. Gateado por `GROWTH_AI_VISIBILITY_GRADER_ENABLED`; no llama LLM, no escribe en el sitio del prospecto y no persiste cache. **Rollout:** ~~(1) flip staging + redeploy develop~~ HECHO (2026-06-28); (2) smoke funcional por token público y run admin con reporte real; (3) revisión copy/legal de claims y disclaimers + validar JSON-LD con schema.org/Rich Results y revisar `llms.txt` formado; (4) prod vía TASK-1246/EPIC-020 release control plane. | Default OFF → endpoints `/fix-it` responden no disponible/404. ON → entrega archivos public-safe por token público o capability interna `growth.ai_visibility.fix_it.generate`. Revert (<5 min): flag a false. |
| `TURNSTILE_SECRET` + `NEXT_PUBLIC_TURNSTILE_SITE_KEY` (Vercel) | TASK-1240/1251/TASK-1367/TASK-354 | **staging + Production: SET (2026-07-09, verdad live `vercel env ls`; valores no versionados).** | Mantener ambos lados publicados antes de prender intakes públicos: el site key se hornea en el cliente (`NEXT_PUBLIC_*`, requiere build fresco) y el secret verifica server-side. Sin secret con el intake ON, el verifier hace **fail-closed** (rechaza) — comportamiento correcto. | Secret/site key de Turnstile. Lo consume el port compartido `turnstileCaptchaVerifier` del motor + grader y el apply público de Careers. `NODE_ENV=production` en todo deploy Vercel (staging incluido) → el bypass dev NO aplica en staging. |
| `GROWTH_AI_VISIBILITY_ASYNC_EXECUTION_ENABLED` (Vercel) + flags del worker `GROWTH_AI_VISIBILITY_*` (deploy.sh ENV-branch) | TASK-1234 | **Vercel: OFF en todos (verdad live `vercel env ls`)** · **Worker ops-worker (Cloud Run, deploy.sh): staging ON / prod OFF** (ENV-branch) · _en rollout 2026-06-24_ | ~~(1) worker deploy~~ vía CI `ops-worker-deploy.yml` on push develop (ENV=staging) → crea Cloud Scheduler `ops-growth-grader-drain` (*/5) + monta flags **staging ON** (GRADER+OpenAI+Anthropic+Gemini) / **prod OFF** + `OPENAI/ANTHROPIC_API_KEY_SECRET_REF` + TIMEOUT 3600s; el handler hace **no-op prod-safe** con `isGraderEnabled()` OFF (cero queries — el schema `greenhouse_growth` no está migrado en prod). (2) `vercel env add GROWTH_AI_VISIBILITY_ASYNC_EXECUTION_ENABLED=true` (staging/develop) + redeploy → endpoint admin pasa a enqueue+poll (202+runId). (3) smoke real `full` multi-provider (Gemini 3) sin timeout + observations incrementales + signals `run_execution_lag`/`run_stuck_running` en steady. (4) **prod fuera de scope** (migración `greenhouse_growth` + capabilities + env vía release control plane). | Cutover inline → async del AI Visibility Grader (TASK-1234). Vercel flag OFF → endpoint ejecuta inline (sólo `light`/OpenAI cabe en el timeout Vercel); ON → encola y el worker Cloud Run drena sin límite de duración (única vía para `full` multi-provider). Worker compartido staging+prod: flags ramificados por ENV + gate `isGraderEnabled()` en el drain → prod no-op seguro. Revert (<5 min): Vercel flag a false → endpoint vuelve a inline. |
| `GROWTH_SEARCH_CONSOLE_ENABLED` (Vercel) | TASK-1282 | **staging: ON (2026-06-29)** · **prod: ON** (verificado con `vercel env pull` el 2026-08-06; la celda decía OFF y estaba stale desde el flip masivo del 2026-06-30). **Delta 2026-08-07 (TASK-1655/1307): el flag prod estaba ON pero el FLUJO estaba muerto — clase "flag declarado sin cablear": `GOOGLE_SEARCH_CONSOLE_OAUTH_CLIENT_ID` + `_CLIENT_SECRET_SECRET_REF` existían solo en staging, así que `resolveSearchConsoleOAuthConfig` devolvía null y `oauth/start` respondía `search_console_disabled (not_configured)` en producción (reportado por el operador con captura). Ambas vars agregadas a Vercel Production + redeploy. ✅ Redirect URI de prod VERIFICADO por el operador (2026-08-07): el flujo oauth/start en producción llega al consent de Google sin mismatch — producción 100% operativa.** | Conexión multi-tenant a Google Search Console (**flujo property-picker** estilo Semrush: token de OPERADOR + desplegable de propiedades). **Rollout staging APLICADO (2026-06-29):** OAuth client web "Greenhouse Search Console" en `efeonce-group` + secret `greenhouse-search-console-oauth-client-secret` + Vercel env; consent screen pasado a **External + En producción** (Internal bloquea cuentas externas; Testing expira refresh tokens a 7 días) — sin verificación de Google (aviso "app no verificada"); grant IAM secret-write `search-console-token-*` (condición `client-integration-tokens`); flag staging ON; wiring smoke verde. **Pendiente:** smoke humano del flujo nuevo (consent → desplegable → elegir) + prod vía release control plane. **Rollout original (out-of-band, pesado):** (1) crear OAuth 2.0 Client ID (web) en `efeonce-group` + branding consent screen + redirect URIs staging/prod → secrets `GOOGLE_SEARCH_CONSOLE_OAUTH_CLIENT_ID(_SECRET_REF)` / `_CLIENT_SECRET(_SECRET_REF)`; (2) **verificación de Google del scope sensible `webmasters.readonly`** (external user type, toma días) — mientras tanto, test users; (3) **ampliar el grant IAM de secret-write** del SA `greenhouse-portal@` al prefijo `search-console-token-*` (hoy sólo cubre `notion-integration-token-greenhouse-*`; sin esto `createOrAddSecretVersion` degrada `permission_denied`); (4) flip flag staging + OAuth round-trip real con propiedad de prueba → token en Secret Manager + fila `active` + reader devuelve filas; (5) prod vía release control plane (migración `greenhouse_growth` + capability seed + env) tras sign-off. | Default OFF → entrypoint OAuth no expuesto, reader retorna `disabled`. ON → operador conecta la propiedad GSC de un cliente (capability `growth.search_console.connect`). Revert (<5 min): flag a false. |

| `GROWTH_AI_VISIBILITY_SHORT_LINKS_ENABLED` (Vercel) | TASK-1330 | **Production: ON (2026-07-04)** · staging/dev: OFF · code+rollout complete | ✅ RESUELTO 2026-07-04: código promovido a `main` (release `9818f47d3`, manifest `released`) + `vercel env add …=true Production` + redeploy prod `greenhouse-k679lwhtg` (alias `greenhouse.efeoncepro.com`, health 200). Think `/s/[code].astro` verificado deployado (commit `681f1e4`, ancestro del HEAD live de `efeonce-think`). Consumers (token route, correo, HubSpot) prefieren el short URL `think.efeoncepro.com/s/<code>` con fallback al largo. Revert <5 min: `vercel env rm` + redeploy. Pendiente menor: prender en staging/dev para paridad de QA. | Short links gobernados del informe público (EPIC-020, follow-up de TASK-1329/1331). Default OFF → `resolvePreferredReportUrl`/`resolveReportShareUrlForRun` devuelven el URL LARGO sin tocar la DB (short-circuit); el resolve endpoint + auto-create en publish operan igual (deploy con tráfico cero). ON → los consumers prefieren el corto con fallback al largo. Revert (<5 min): flag a false → todos vuelven al largo. |

_(Agrega acá cualquier flag que dejes code-complete sin prender. Si está vacío, ¡no hay deuda pendiente!)_

---

| `PUBLIC_SITE_COMPARISON_TABLE_WRITES_ENABLED` | TASK-1225 | **staging: ON (2026-06-23)** — write path activado y probado end-to-end (WordPress `writes_enabled=true` + secreto canónico + bridge v0.5.0 desplegado + grant `secretAccessor` a greenhouse-portal@ + smoke `execute`→draft real) · prod: OFF ~~(1) secret + writes-enabled WordPress~~ HECHO · ~~(2) flag staging + smoke execute~~ HECHO (draft real verificado). **(3) prod pendiente:** `vercel env add PUBLIC_SITE_COMPARISON_TABLE_WRITES_ENABLED + los 2 refs Production` + redeploy, tras sign-off del operador (la escritura es draft-only; publish siempre humano). | Write gobernado del widget `greenhouse_comparison_table` (TASK-1225). Default OFF → el command corre solo `dry_run` (propose, sin red); `execute` lanza `comparison_table_writes_disabled` (409). El LLM nunca muta directo. |
| `GROWTH_FORMS_PUBLIC_API_ENABLED` (Vercel) | TASK-1229 | **staging: ON (2026-06-25, redeploy d6szlyp11 + GET público verificado live → render contract)** · prod: OFF | (1) la fundación (schema/contracts/commands/compiler/fake adapter/APIs) está code-complete + smoke e2e verde en dev; el público nace **disabled by design** (sin forms `published`). (2) Prender requiere: un form real publicado (TASK-1232 first migration) + el adapter HubSpot real (TASK-1230) + el dispatcher productivo (ops-worker drain `POST /growth/forms/dispatch` + Cloud Scheduler — **ROLLOUT pendiente**, hoy el dispatch se ejercita por el endpoint admin `POST /api/admin/growth/forms/dispatch`). (3) `vercel env add GROWTH_FORMS_PUBLIC_API_ENABLED=true` por environment + redeploy → render/submit público abiertos (igual gated por ausencia de forms publicados). | Motor Growth Forms (TASK-1229). Default OFF → `GET/POST /api/public/growth/forms/*` responden 404 `disabled`. Transversal (no grader-céntrico): 11 `form_kind`, destinos genéricos, port compartido abuse-guard/captcha. Revert (<5 min): flag a false → 404. |
| `GROWTH_CTA_ENGINE_ENABLED` (Vercel) | TASK-1339 | **staging: ON (2026-07-18, redeploy con flag horneado)** · **Production: ON (2026-07-18, redeploy del target `4a1cd11e2` + release orchestrator; smoke live: bundle 200 + render contract arbitrado + forja 403 + ingest accepted + CTA visible en el reporte Think prod con `greenhouse_cta_viewed` en dataLayer)** | (1) Foundation code-complete + smoke e2e verde contra PG dev (CTA `ai-visibility-report-followup` published + 2 surfaces con embed key + render arbitrado + ingest/forja). (2) El flip (staging primero) se coordina con **TASK-1340** (renderer portable): sin renderer la foundation es shadow — prenderlo antes no aporta valor de usuario. (3) `vercel env add GROWTH_CTA_ENGINE_ENABLED=true` por environment + redeploy. | Motor Growth CTA & Popup (TASK-1339). Default OFF → `GET/POST /api/public/growth/ctas/*` y `/api/admin/growth/ctas/*` responden 404 `disabled`. **Runtime: Vercel ÚNICAMENTE** (route handlers Next; ningún Cloud Run lo lee — verificado con grep). Revert (<5 min): flag a false → 404. |
| `GROWTH_CTA_SUPPRESSION_ENFORCEMENT_ENABLED` (Vercel) | TASK-1428 | **staging: ON (2026-07-18, verificado E2E live)** · **Production: ON (2026-07-18, horneado por el release `d5db8b568`; smoke post-release verificado 16:26Z: dismiss → exclusión por visitor, fresco ve el CTA, `engineState: ok`)** | Rollout COMPLETO 2026-07-18 (staging + Production ON, ambos verificados E2E). Ventana de monitor 7d de `growth.cta.*` en `/admin/operations` (comparte calendario con TASK-1427, hasta 2026-07-25). | Enforcement de suppression/frequency capping del motor CTA (TASK-1428). Default OFF = **shadow**: la decisión (dismiss cooldown, already_converted verificado, caps per-CTA y global) se computa y registra en `cta_exposure_rollup` sin cambiar el render. ON: candidatos suprimidos se excluyen + claim atómico de impresión interruptiva. **Runtime: Vercel ÚNICAMENTE** (render/ingest routes; ningún Cloud Run lo lee). Tunables hermanos (defaults en código, no son `*_ENABLED`): `GROWTH_CTA_EXPOSURE_SAMPLE_RATE` (Tier B, 1; `0` apaga el adapter = rollback exposición <5 min), `GROWTH_CTA_INTERRUPTIVE_PER_SUBJECT_PER_DAY` (3). El kill switch NO usa env var: es estado operativo en DB (`cta_kill_switch_event`). Revert (<5 min): flag a false → vuelve a shadow. |
| `GROWTH_FORMS_CATALOG_API_ENABLED` (Vercel) | TASK-1258 | **staging: ON (2026-06-26, `vercel env add` + push develop `97957029b` + smoke live verde)** · dev: OFF · prod: OFF | **Smoke live staging 2026-06-26 (deploy `greenhouse-iswpmjnmy`):** embed key minteada para surface `fhsf-efeonce-lead-gen-web` (`ehk_ee65ad44c942`); `GET /catalog` con key+origin `efeoncepro.com` → **200** + 1 form (`efeonce-lead-gen-web` v1 published, `destinationReadiness:"destination_disabled"` honesto — destino TASK-1261 sigue `disabled`); origin no permitido → **401**; key inválida → **401**; sin key → **400**. Prod = release control plane develop→main + provisionar embed key prod cuando el plugin WordPress (TASK-1259) la consuma. | (1) **code-complete**: reader `listInsertableFormCatalog` + endpoint `GET /api/public/growth/forms/catalog` + auth per-site (embed key + origin allowlist, Opción A) + 12 tests verde + full test/build verde. Es la precondición backend de TASK-1259. (2) Prender requiere: **provisionar la embed key** de la(s) surface(s) que consumirán el catálogo (`pnpm growth:forms:embed-key --surface-id <id>` → guardar el secreto server-side en el plugin WordPress) + `vercel env add GROWTH_FORMS_CATALOG_API_ENABLED=true` por environment + redeploy. Flag **separado** de `GROWTH_FORMS_PUBLIC_API_ENABLED` a propósito: el editor lista forms insertables ANTES de abrir render/submit público. (3) Sin embed key provisionada, el endpoint responde 401 aunque el flag esté ON (fail-closed). | Catálogo externo de forms insertables (TASK-1258, precondición TASK-1259). Default OFF → `GET /api/public/growth/forms/catalog` responde 404 `disabled`. ON → devuelve metadata editor-safe (displayName/slug/version/surfaceIds/destinationReadiness) autorizando por credencial per-site server-side; NUNCA GUIDs/mapping/secretos. Revert (<5 min): flag a false → 404. |
| `GROWTH_SEO_ENABLED` (multi-runtime: Cloud Run ops-worker deploy.sh declarativo + Vercel) | TASK-1302 · Vercel por TASK-1645/1647 | **ops-worker: ON (2026-08-05, rollout)** — el ops-worker es un servicio **ÚNICO compartido staging+prod**, así que no existe un flip "sólo staging" · **Vercel staging: ON (2026-08-05, `vercel env add` + redeploy aplicado)** · **Vercel Production: ON (2026-08-06 00:10Z)** — `vercel env add GROWTH_SEO_ENABLED production` + **redeploy** `dpl_GyGkdEQQTk65qkCs1S3TEH6Jquy9` (Vercel congela env vars al crear el build: sin redeploy el flag no entra), aliased a `greenhouse.efeoncepro.com` sobre el release `70e912056273` | Rollout ejecutado 2026-08-05: flag ON + scheduler `ops-seo-gsc-snapshot` **ACTIVO** (`0 9 * * *` CLT), ambos **declarativos en `deploy.sh`** para que sobrevivan al próximo deploy (`upsert_scheduler_job` re-aplica el estado de pausa en cada deploy: despausar out-of-band se revierte solo). **Hallazgo del rollout:** el worker NO tenía ninguna variable de Search Console — TASK-1302 introdujo el primer consumer del reader GSC en ese runtime y su entorno nunca se provisionó; sin `GROWTH_SEARCH_CONSOLE_ENABLED` + `GOOGLE_SEARCH_CONSOLE_OAUTH_CLIENT_ID` el reader resolvía `disabled` y el batch habría degradado TODAS las orgs **en silencio** (misma bug class que ISSUE-113). Ambas quedaron cableadas en `deploy.sh` + GH secret. Rollback (<5 min): flag a `false` en `deploy.sh` + redeploy, o pausar el scheduler y poner su 5º arg en `"true"` para que el pause sobreviva. **Delta 2026-08-05 (noche, TASK-1645/1647):** desde TASK-1645 el flag también lo lee **Vercel** (gatea el lane ecosystem SEO `/api/platform/ecosystem/seo/*`); se agregó en Vercel **staging** con redeploy aplicado y el canary e2e del gateway MCP (TASK-1647) lo verificó por HTTPS real. **Delta 2026-08-06 (cutover a producción, cerrado):** release `70e912056273` (`release_id=70e912056273-03c36b47-eb75-469c-886f-51c691cd7c34`, run `31058032196`, manifest `released`) → flag ON en Vercel Production → redeploy. **Verificado con tráfico real, no con `env ls`:** el canary del provider (`scripts/greenhouse-seo-canary.mjs` en `efeonce-mcp`) contra `https://greenhouse.efeoncepro.com` devolvió Berel `domainQuadrant=riesgo` (50 keywords, AEO 44.5), Efeonce `entitlement ok` + degradación honesta `no_seo_data`, y deny anti-oracle `404 greenhouse_seo_lane_404`. Con eso se habilitó el provider en el gateway MCP (revisión `efeonce-mcp-gateway-00012-dkj`). | Gatea el batch diario que materializa Google Search Console a la serie propia `greenhouse_growth.seo_gsc_daily` (TASK-1302) **y, desde TASK-1645, el lane ecosystem SEO en Vercel**. ⚠️ **Multi-runtime real**: lo leen el ops-worker (materializer) Y Vercel (lane) — prenderlo en uno solo deja el otro camino muerto. Distinto de `GROWTH_SEARCH_CONSOLE_ENABLED` (TASK-1282), que gatea la conexión GSC en sí — sin aquél este reader degrada a `disabled`. Los env-knobs `GROWTH_SEO_*_PER_MONTH` (TASK-1301) son config de allowance/budget, NO flags. **Delta 2026-08-06 (TASK-1303):** el flag además gatea el batch de rank capture (`POST /seo/rank/capture-batch` en ops-worker) y el reader/lane/MCP `rank-evolution` en Vercel. El cron `ops-seo-rank-capture` (05:00 CLT) nació pausado y fue **DESPAUSADO el 2026-08-06** (autorización del operador, declarativo en `deploy.sh`, deploy verificado `ENABLED`) tras el smoke real E2E: captura Berel 8/8 USD 0.03, ledger, idempotencia USD 0, mirror BQ, reader y signal — evidencia en el runbook `docs/manual-de-uso/growth/operar-captura-rankings-seo.md` §Verificación ejecutada. El control de gasto es el assignment per-org + gate `enforceSeoRunEntitlement` + spend fence. **Delta 2026-08-06 (TASK-1306):** el flag ahora gatea además la **SUPERFICIE UI** `/admin/growth/seo` (cockpit Overview): con el módulo apagado la page hace `notFound()` y los 3 readers de overview devuelven vacío. ⚠️ Como el flag YA está ON en Vercel Production, la ruta queda **viva apenas se despliegue** — el control de exposición restante son el viewCode `administracion.growth_seo` (sembrado sólo a `efeonce_admin` + `ai_tooling_admin`) y el `module_assignment` per-org, no el flag. **Delta 2026-08-06 (TASK-1304):** el flag además gatea los 3 handlers nuevos del ops-worker (`/seo/audit/enqueue-batch`, `/seo/audit/collect`, `/seo/backlinks/capture-batch`) y los lanes/MCP `site-audit-report` + `backlink-profile` en Vercel. Los 3 Cloud Scheduler (`ops-seo-audit-enqueue` `0 6 * * 1` · `ops-seo-audit-collect` `*/30` · `ops-seo-backlink-capture` `0 7 * * 1`) nacieron PAUSADOS y quedaron **ACTIVOS el 2026-08-06** (autorización del operador "termina todo lo que falte"; deploy break-glass local del worker por outage mayor de GitHub Actions — revisión `ops-worker-00528-zgr`, `GIT_SHA=26005a619`; los 3 verificados `ENABLED` + handlers ejercitados vía Scheduler→OIDC real: primer audit de Berel encolado USD 0.015 + backlink snapshot Berel USD 0.048). **Delta 2026-08-08 (TASK-1310):** el flag gatea además las **rutas del portal cliente** `/growth/seo` (dashboard) y `/growth/seo/report`: con el módulo apagado ambas hacen `notFound()`. Igual que con TASK-1306, el flag YA está ON en Production, así que el control de exposición real de la superficie cliente es el `module_assignment` per-org (`seo_v2`) más el viewCode del catálogo. **Delta 2026-08-14:** ambas migraciones (`20260808131441444` de TASK-1310 y `20260809163352129` de TASK-1677) están **aplicadas** y las dos tasks quedaron `complete`: `seo_v2` es la única clave leída (`SEO_MODULE_KEYS_READ`) y el portal ya compone las rutas cliente en el menú. |
| `GROWTH_SEARCH_CONSOLE_ENABLED` (Cloud Run ops-worker) | TASK-1282 · cableado en este runtime por TASK-1302 | **ON (2026-08-05)** en el ops-worker | Ya estaba ON en Vercel staging y prod desde 2026-06-30; lo que faltaba era **este runtime**, y nadie lo había notado porque hasta TASK-1302 el reader GSC sólo corría en rutas Vercel. Declarativo en `deploy.sh` junto con `GOOGLE_SEARCH_CONSOLE_OAUTH_CLIENT_ID` (GH secret, patrón `DATAFORSEO_API_LOGIN`) y `GOOGLE_SEARCH_CONSOLE_OAUTH_CLIENT_SECRET_SECRET_REF`. | Mismo nombre de flag que en Vercel, **otro runtime**: es el caso de manual de por qué prender un flag es multi-runtime. Sin él, el materializer GSC no lee nada y degrada honesto. |
| `GROWTH_FORMS_DISPATCH_ENABLED` (Cloud Run ops-worker, deploy.sh ENV-branch) | TASK-1229 | **staging: ON (2026-06-25, deploy.sh ENV-branch)** · prod: OFF | (1) el dispatcher productivo (ops-worker drain `POST /growth/forms/dispatch` + Cloud Scheduler `ops-growth-forms-dispatch` */2) está **code-complete**: el handler + el job se crean en el próximo deploy del ops-worker (CI `ops-worker-deploy.yml` on push develop). Con el flag OFF el handler hace **no-op prod-safe** (cero queries — el schema `greenhouse_growth` puede no existir en prod). (2) Prender (`gcloud run services update ops-worker --update-env-vars GROWTH_FORMS_DISPATCH_ENABLED=true` o ENV-branch en deploy.sh) cuando haya submissions reales que entregar (junto al adapter HubSpot real TASK-1230 + primer form TASK-1232). | Dispatcher async del motor Growth Forms (TASK-1229). Entrega submissions aceptadas (fake/echo en 1229; HubSpot real TASK-1230) fuera del request Vercel, vía Cloud Scheduler (mismo motivo que el outbox publisher TASK-773: staging no corre crons Vercel). Default OFF → el job dispara pero el handler no-opea. Hoy el dispatch también es operable manual por el endpoint admin `POST /api/admin/growth/forms/dispatch`. |
| `GROWTH_FORMS_HUBSPOT_SECURE_SUBMIT_ENABLED` (Cloud Run ops-worker, deploy.sh ENV-branch) | TASK-1230 | **staging: ON (2026-06-25, deploy.sh ENV-branch + live smoke verde)** · prod: OFF | (1) el adapter HubSpot Forms secure-submit está **code-complete + unit-tested** (9 tests fetch-mockeado) + state-machine de retry/dead-letter verificada (smoke e2e). (2) **Rollout pendiente del LIVE smoke** (out-of-band): un **HubSpot test form GUID** + verificar que el private app token (`hubspot-access-token`) tiene scope `forms` (secure-submit lo exige). (3) Prender (`gcloud run services update ops-worker --update-env-vars GROWTH_FORMS_HUBSPOT_SECURE_SUBMIT_ENABLED=true`) tras el live smoke + junto al primer form real (TASK-1232). | Adapter HubSpot Forms secure-submit del motor (TASK-1230). Default OFF → el adapter resuelve `skipped` (cero writes a HubSpot, la submission queda `accepted` esperando). ON → POST a `api.hsforms.com/.../secure/submit/{portalId}/{formGuid}` desde el dispatcher async (NUNCA inline). At-most-once vía state machine de attempts (solo reintenta fallas retryables; nunca re-entrega `delivered`). Revert (<5 min): flag a false → skipped. |
| `GROWTH_FORMS_EMAIL_VERIFICATION_ENABLED` (Vercel) | TASK-1254 | **staging: ON (2026-06-26 — `vercel env add` al env `staging` + push develop + redeploy + smoke live verde:** `ana@acme.com`→corporate pasa, `gmail`→`email_not_corporate`/suspect, `mailinator`→`email_disposable`/suspect, typo-suggest `gmial→gmail`**)** · **prod: OFF — diferido al cutover de forms (TASK-1258):** el master `GROWTH_FORMS_PUBLIC_API_ENABLED` está OFF en prod y el endpoint `verify-email` ahora exige AMBOS flags → flipear el email flag solo en prod es 404/no-op hasta el primer submit prod (mismo patrón que `GROWTH_FORMS_SERVER_VALIDATION_ENABLED`) | (1) **code-complete (scaffold sin provider real)**: Tier 1 local (gratis) + validador `corporate_email` + puerto `EmailVerificationProvider` con adapter **noop** (`deliverable:'unknown'`) + orquestador Tier1→Tier2 + cache (`email_verification_cache`) + circuit breaker + endpoint público `verify-email` + gate de política en `submitForm` (`block_field`/`warn`/`tag_only`) + 2 reliability signals. (2) Prender requiere: **dar de alta el provider económico** (Abstract API u otro) + publicar el secreto en GCP Secret Manager + grant `secretAccessor` a `greenhouse-portal@` + agregar su adapter en `resolveVerificationProvider()`; **definir la política por form** (`validation_schema_json.emailPolicy`); shadow en staging (observar `growth.forms.email_rejection_rate`). (3) `vercel env add GROWTH_FORMS_EMAIL_VERIFICATION_ENABLED=true` por environment + redeploy. **Cutover gradual:** arrancar en `warn` (etiqueta `suspect`, no rechaza), observar rejection rate, subir a `block_field` donde el negocio lo pida. **TASK-1263 (2026-06-28) — gate cableado en el grader:** el gate de TASK-1254 vivía solo en `submitForm`, pero el intake del grader NUNCA pasa por ahí → se extrajo el helper canónico `evaluateFormEmailGate` (un primitive, 3 consumers) y se cableó en las DOS fachadas del grader (`createPublicGraderRun` + `createPublicGraderRunViaFormsEngine`). Activación gobernada del grader-form = `npx tsx --require ./scripts/lib/server-only-shim.cjs scripts/growth/activate-grader-email-gate.ts --apply` (dry-run default; publica v3 con `emailPolicy.block_field` + `destination_policy`/`retention_policy`, deprecando v2). **Rollout staging APLICADO + verificado (2026-06-28):** deploy staging READY → `--apply` publicó v3 (`fver-9475a038…`) con `emailPolicy.block_field`, deprecó v2; smoke funcional contra config real (gmail/temporal rechazados, corporativo pasa); captcha fail-closed confirmado. **Prod pendiente:** cutover TASK-1246 (flag ya OFF en prod). | Gate de correo corporativo del motor Growth Forms (TASK-1254). Default OFF → `submitForm` NO aplica política de email (legacy) y `POST /api/public/growth/forms/{slug}/verify-email` responde 404 `disabled`. ON → Tier1-first (gratis) + Tier2 (provider, hoy noop=unknown) + cache + política por form. Autoridad server-side (el botón es UX). Revert (<5 min): flag a false → legacy. Follow-up: enchufar provider real + signals `email_provider_error_rate`/`email_verification_cache_hit_rate` (requieren contadores runtime). |
| `GROWTH_FORMS_SERVER_VALIDATION_ENABLED` (Vercel) | TASK-1253 | **staging: ON (2026-06-26, shadow TASK-1261 → 0 falsos rechazos + flip; redeploy al push)** · prod: OFF (flip prod **diferido a propósito** al cutover de TASK-1258, bundle con `GROWTH_FORMS_PUBLIC_API_ENABLED` — decisión operador 2026-06-26: en prod ambos están OFF, sin tráfico → flipear validación sola sería no-op y no smoke-verificable; debe nacer ON junto con el primer submit prod) | (1) la autoridad de validación server-side está **code-complete** (validator registry canónico compartido renderer+`submitForm`, `national_id` multi-país, normalización, test de paridad). Nace **OFF by design** (patrón flag default-OFF + shadow + flip): una re-validación que RECHAZA podría bloquear submissions legítimas si un validador queda muy estricto. (2) Prender requiere shadow en staging (observar tasa de rechazo con un form real publicado) + sign-off. (3) `vercel env add GROWTH_FORMS_SERVER_VALIDATION_ENABLED=true` por environment + redeploy → `submitForm` re-valida por tipo + normaliza + rechaza basura; sin el flag, comportamiento legacy (cliente valida por UX, server no re-valida). | Autoridad de validación del motor (TASK-1253). Default OFF → `submitForm` persiste `{...input.fields}` sin re-validar por tipo (legacy). ON → re-valida cada campo presente con el registry canónico (mismo core que el renderer → paridad), normaliza (email lowercased / E.164 / RUT / número), hashea el email YA normalizado para dedupe, y rechaza con `invalid` el primer campo con formato inválido. Degradación honesta: `field_schema_json` no parseable → señal Sentry + sigue con raw (no rompe el form). Revert (<5 min): flag a false → legacy. Follow-up: reliability signals `server_validation_rejected` / `validation_fallback_used` + flip post-shadow. |
| `GROWTH_FORMS_PII_ENCRYPTION_ENABLED` (Vercel) | TASK-1255 | **staging: ON (2026-06-26 — `vercel env add` flag=true al env `staging` + `GROWTH_FORMS_PII_ENCRYPTION_KEY_SECRET_REF=greenhouse-growth-forms-pii-encryption-key` a staging+production + push develop + redeploy)** · **prod: OFF (SECRET_REF ya seteado en production; el flip del flag va por el release control plane develop→main, no unilateral — el cifrado en prod es seguro/recomendado, no requiere sign-off legal: lo legal es la ventana de retención de Slice 4)** · dev local: SECRET_REF en `.env.local`, flag OFF | (1) **code-complete (Slices 1-3)**: cifrado at-rest de `national_id` (AES-256-GCM, key `greenhouse-growth-forms-pii-encryption-key` en GCP Secret Manager + grant `secretAccessor` a `greenhouse-portal@` — ya creada) + masked-reader por default + reveal gobernado (capability `growth.forms.lead_pii.reveal` + reason ≥10 + audit append-only + outbox + signal). Nace **OFF by design** (patrón flag default-OFF + shadow + flip): el cifrado mueve `national_id` fuera de `normalized_fields_json` → con flag ON la cédula deja de viajar al dispatcher (el boundary de redacción es incondicional, igual aplica con flag OFF). (2) Prender requiere: la key publicada por environment vía `GROWTH_FORMS_PII_ENCRYPTION_KEY_SECRET_REF` (Vercel env → secret name) + redeploy; verificar con un lead de prueba que el RUT queda cifrado en DB y el log redactado. **Fail-closed**: con el flag ON sin key resuelta, `submitForm` devuelve `error` (503) y NO persiste la cédula en claro. (3) `vercel env add GROWTH_FORMS_PII_ENCRYPTION_ENABLED=true` + `GROWTH_FORMS_PII_ENCRYPTION_KEY_SECRET_REF=greenhouse-growth-forms-pii-encryption-key` por environment + redeploy. Prerrequisito del cutover público que capture RUT (TASK-1246). | Cifrado at-rest de national_id del motor Growth Forms (TASK-1255, Ley 21.719). Default OFF → la cédula queda en claro en `normalized_fields_json` (legacy), pero el masked-reader igual la enmascara en lectura y el dispatcher igual la redacta (boundary incondicional). ON → `submitForm` separa national_id del blob, lo cifra → `encrypted_fields_json`. Reveal gateado por capability (no por flag). Revert (<10 min): flag a false → escribe en claro; la key se conserva → descifrar siempre posible. **Pendiente Slice 4 (diferido):** `GROWTH_FORMS_RETENTION_PURGE_ENABLED` (purga de retención, ventana default 24m) — aún NO declarado en código, espera sign-off legal sobre la ventana. |
| `HIRING_ASSESSMENT_PUBLIC_SESSION_LINKS_ENABLED` (**ops-worker / Cloud Run**, NO Vercel — worker ÚNICO compartido staging+prod) | TASK-1746 | **Code-complete (Slices 1–4) y OFF en staging y Production** (`deploy.sh:411` default `false`; **verdad live re-confirmada 2026-08-26**: hoy la env var **sí está seteada, explícitamente a `false`**, en la revisión activa `ops-worker-00594-2tp` (antes era `<NO SET>` en `ops-worker-00586-bww` — mismo efecto), así que el runtime aplica el default `false`). Cambia el link del correo de assessment: OFF conserva `/public/assessment/{token}`; ON emite `/public/assessment/access#access=…` (bearer en el **fragmento**, que el navegador no manda al servidor) y lo canjea por sesión `__Host-` HttpOnly. El recovery en sí NO lo gatea este flag: su puerta son las capabilities `hiring.assessment.recover_access_email` / `hiring.assessment.reveal_access_link`, sembradas por la migración `20260819072130586`, **aplicada el 2026-08-19 13:00 UTC** y confirmadas vivas en `capabilities_registry`. | ~~(1) aplicar y hacer readback de la migración y del índice~~ **HECHO 2026-08-19**: migración aplicada 13:00 UTC, índice `uq_email_deliveries_token_intent_v2` creado y validado (`unique=true, valid=true, ready=true`) y CONTRACT de credencial aplicado (`convalidated=true` + trigger activo); (2) verificar las cuatro routes públicas live; (3) confirmar **por API/readback** que el dominio tiene `click_tracking=false` en Resend — el rewrite de links de un proveedor **descarta el fragmento** y dejaría al candidato con un link sin bearer; (4) smoke que verifique el href **recibido** con el fragmento intacto; (5) confirmar que el scheduler `ops-hiring-assessment-public-access-retention` (04:17) existe y queda `steady`. Orden completo en [`docs/operations/runbooks/resend-email-lifecycle-rollout.md`](runbooks/resend-email-lifecycle-rollout.md). | **Runtime: ops-worker ÚNICAMENTE** — lo lee `src/lib/hiring/notifications/config.ts`, consumido por `send.ts`; ningún route handler de Vercel lo lee (prenderlo ahí no hace nada). ⚠️ **Worker único compartido: prenderlo afecta staging Y producción a la vez.** Depende de TASK-1745 (el lifecycle de entrega por webhook de Resend) para saber si el correo llegó de verdad — sin eso, `sent` sólo significa "aceptado para despacho". Rollback: `false` + redeploy del ops-worker; el link legacy vuelve a servir. |

## § Snapshot de estado por environment

> ### 🔴 Cómo leer esta tabla (corregido 2026-08-26)
>
> Snapshot **2026-06-18** generado con `vercel env ls`. **Esa procedencia la vuelve inválida para
> todo flag que no se lea en Vercel**, y buena parte de este ledger no se lee en Vercel.
>
> **La regla «ausente = OFF» sólo aplica a flags de Vercel.** Para un flag del **ops-worker**, su
> ausencia de `vercel env ls` no significa nada: su fuente de verdad declarativa es
> `services/ops-worker/deploy.sh` y su verdad live es
> `gcloud run services describe ops-worker --region us-east4 --project efeonce-group`. Además el
> ops-worker es **un servicio único compartido staging+prod**: para sus flags no existe «prod vs
> staging», así que una celda `Production = —` sobre un flag suyo es literalmente un error de
> categoría, no un dato.
>
> **Esto ya causó daño.** `ISSUE-164` (SSRF en el fetcher de probes) se clasificó como *«no es un
> incidente vivo, ningún consumer está prendido en producción»* leyendo estas celdas. Los tres flags
> de probes están `true` en la revisión activa del ops-worker, verificado el 2026-08-26. El defecto
> documental degradó la severidad de un problema de seguridad.
>
> **Antes de tomar una decisión de rollout con esta tabla:** identifica primero **dónde se lee** el
> flag (`grep -rn "<FLAG>" src/ services/`) y consulta la fuente que corresponda. Toda celda sin una
> nota de «verificado con <comando> el <fecha>» es una conjetura de junio.

| Flag | Production | staging | Preview/dev | Owner |
|---|---|---|---|---|
| `HIRING_OPENING_CAPACITY_CLOSURE_ENABLED` + `HIRING_CAPACITY_FILLED_EMAIL_ENABLED` | ✅ **AMBOS ON (2026-08-27, autorización expresa del CEO; revisión `ops-worker-00607-bdn`)** — runtime **ops-worker únicamente** (en Vercel son inertes). ⚠️ **Queda UNA sola capa entre el trabajo normal de un reclutador y N correos: la fila `hiring_decision_not_selected` sembrada `enabled=false` en `email_type_config`. NO habilitarla hasta cerrar `TASK-1764`** — hoy el correo saldría con footer legacy en silencio. Un cierre notifica a la cohorte de golpe (36 y 14 personas en las vacantes vivas) y un correo emitido no se retira. | ❌ OFF | — | TASK-1762 |
| `GROWTH_SEO_PROSPECT_DIAGNOSTIC_ENABLED` | ✅ **ON (2026-08-27, `vercel env add` + redeploy `greenhouse-if2u2c8ys`)** — runtime **Vercel únicamente**; la corrida es inline en el command, sin batch ni scheduler. Sign-off comercial del operador otorgado ese día. Topes: `..._CEILING_USD` 1,00 por diagnóstico + `..._DAILY_ACTOR_CAP` 10 por actor | ❌ OFF | — | TASK-1709 |
| `GROWTH_PROBE_FETCH_STRICT_NETWORK_ENABLED` | ✅ **ON (2026-08-27)** — **dual-runtime**: ops-worker desde temprano ese día (declarativo `:-true` en `deploy.sh`), Vercel Production con el release `cc73c74789ce` + redeploy `greenhouse-if2u2c8ys` | ✅ ON (env agregada 2026-08-27) | — | TASK-1778 / ISSUE-164 |
| `GROWTH_AI_VISIBILITY_BUDGET_GATE_ENABLED` + `GROWTH_AI_VISIBILITY_BUDGET_GATE_ENFORCED` | ❌ **AMBOS OFF** (default; declarativos `:-false` en `deploy.sh`) | ❌ OFF | ❌ OFF | TASK-1696 (gate en shadow; el flip a ENFORCED exige un ciclo mensual de medición + decisión del operador) |
| `GROWTH_SEO_BACKLINK_DETAIL_ENABLED` | ✅ **ON (2026-08-27, declarativo en `deploy.sh` + revisión activa verificada)** — runtime **ops-worker únicamente**; paso del batch semanal `ops-seo-backlink-capture` (sin scheduler propio) | (mismo servicio único; ON) | — | TASK-1777 |
| `GROWTH_SEO_URL_VISIBILITY_ENABLED` | ✅ **ON (2026-08-27, declarativo en `deploy.sh` + revisión activa verificada)** — runtime **ops-worker únicamente**. Scheduler `ops-seo-url-visibility` (`0 9 17 * *`) **ENABLED** | (mismo servicio único; ON) | — | TASK-1776 |
| `GROWTH_SEO_WORK_QUEUE_ENABLED` | ⛔ **OFF en los DOS runtimes (nace así, 2026-08-28)** — **ops-worker** (`deploy.sh` declarativo `:-false`): gatea el materializador; sin él NO se escribe ningún snapshot. **Vercel**: gatea reader, ruta app, lane ecosystem, tool MCP interna y el cutover del consumer; sin él el worker acumularía snapshots que nadie lee. Scheduler `ops-seo-work-queue-materialize` (`0 10 * * *`, después de `ops-seo-gsc-snapshot`) **PAUSADO** — tercer freno independiente. 🔴 **No compromete gasto de proveedor**: la cola lee tablas ya pagadas. Lo que compromete es la AUTORIDAD DE ORDEN — el operador ve un orden que manda otra cosa y aparecen filas de orígenes nuevos, así que el flip se AVISA antes aunque cueste cero | ⛔ OFF | Despausar el scheduler y prender en ambos runtimes SOLO tras: (1) corrida shadow verificada sobre un target con inspección fila por fila; (2) aviso al operador de SEO; (3) 🔴 `TASK-1792` Slices 1–2 en `main` antes del cutover del consumer — el rollback aterriza en el reader legacy | TASK-1700 |
| `GROWTH_SEO_COMPETITOR_GAP_ENABLED` | ✅ **ON y VIVO (2026-08-28)** — el Ops Worker Deploy del push a develop (`fdfdedbe5`) dejó la revisión activa `ops-worker-00610-kc8` con el flag y el endpoint; verificado con dry-run real (`eligible:0` por frescura — cero gasto). Scheduler `ops-seo-competitor-coverage` (`0 9 18 * *`) **ENABLED** (despausado tras la verificación). Runtime **ops-worker únicamente** (en Vercel es inerte). Primera corrida real medida USD 0,1076; próximo ciclo con gasto ~oct (18-sep cae en frescura) | (mismo servicio único; ON) | — | TASK-1662 |
| `GROWTH_SEO_SERP_TOP_RESULTS_ENABLED` | ✅ **ON en los DOS runtimes (2026-08-28)** — ops-worker (escritura) revisión `ops-worker-00610-kc8`; **Vercel Production ON con el release `c983be7f18e6` + redeploy `greenhouse-aj0ng1mfw`**, verificado en runtime con el canary de producción (`serp-top-results` → `ok:true`, no `disabled`). Día 1 de la serie: 2026-08-29 (cron 05:00 CLT). Sin scheduler propio (viaja en `ops-seo-rank-capture`) | ✅ ON (custom env `staging`, 2026-08-28) | — | TASK-1699 |
| `GROWTH_SEO_DOMAIN_OVERVIEW_ENABLED` | ✅ **ON (2026-08-27, declarativo en `deploy.sh` + revisión activa verificada)** — runtime **ops-worker únicamente**. Scheduler `ops-seo-domain-overview` (`0 9 16 * *`) **ENABLED** | (mismo servicio único; ON) | — | TASK-1775 |
| `GROWTH_SEO_KEYWORD_DISCOVERY_ENABLED` | ✅ **ON (2026-08-14, declarativo en `deploy.sh` + Vercel Production/staging)** — runtimes: **Vercel (enqueue/lanes) + ops-worker (drain)**. Scheduler `ops-seo-keyword-discovery-drain` **`*/2 * * * *` ACTIVO** — verificado live 2026-08-28 (`userUpdateTime` 22:48:20Z, `lastAttemptTime` 00:02:00Z con `scheduleTime` 00:04, o sea corriendo cada 2 min). Bajado de `*/10` ese día. ⚠️ **Drift declarativo abierto: `develop` declara `*/2`, `origin/main` todavía `*/10`** — ver fila en § Pendientes de acción. Sin enqueue automático: costo sólo cuando alguien encola (preview + entitlement por corrida) | (mismo servicio único; ON) | — | TASK-1664 |
| `GROWTH_SEO_KEYWORD_MARKET_DATA_ENABLED` | ✅ **ON (2026-08-13, declarativo en `deploy.sh`)** — runtime **ops-worker únicamente** (servicio único compartido staging+prod), no Vercel. Scheduler `ops-seo-keyword-market-data` (`0 8 15 * *`) **ACTIVO**. Alcance efectivo: orgs con assignment + keywords — hoy sólo Berel, ~USD 0.016/mes | (mismo servicio único) | — | TASK-1661 |
| `NOTION_WEBHOOK_ASYNC_INGESTION_ENABLED` | ✅ ON (2026-07-17; `dpl_DkdnLEUFwY3MvxyD9VncYwqzQNj1`) | OFF | — | Webhooks / release ops |
| `NEXA_KNOWLEDGE_RETRIEVAL_ENABLED` (+`NEXT_PUBLIC`) | ✅ | ✅ | — | TASK-1085 |
| `NEXA_SYSTEM_PROMPT_V2_ENABLED` | ✅ | ✅ | — | TASK-1124 |
| `NEXA_ACTION_RUNTIME_ENABLED` | ✅ | ✅ | — | TASK-1137 |
| `NEXA_AUTO_ROUTER_ENABLED` | ✅ | ✅ | — | TASK-1091 |
| `NEXA_SUGGESTED_PROMPTS_DATA_AWARE_ENABLED` (`NEXT_PUBLIC`) | ✅ | ✅ | — | TASK-1087 |
| `NEXA_FORCE_KNOWLEDGE_RETRIEVAL_ENABLED` | — | ✅ | — | TASK-1156 |
| `NEXA_KNOWLEDGE_SYNTHESIS_BRIEF_ENABLED` | — | ✅ | — | TASK-1124 |
| `NEXA_ANSWERS_CANVAS_LENS_ENABLED` | — | ✅ | — | TASK-1101 |
| `NEXA_INTERACTION_LANE_ENABLED` (+`NEXT_PUBLIC`) | — | ✅ (redeploy pend.) | — | TASK-1079 |
| `NEXA_QUOTE_AUTHOR_ACTION_ENABLED` | — | — | — | TASK-1212 |
| `NEXA_PROPOSAL_ACTIONS_ENABLED` | — | ✅ (2026-07-12, verificado con el LLM real) | — | TASK-1399 |
| `COMMERCIAL_Q2C_CANONICAL_CLOSE_ENABLED` | ✅ (2026-06-29, release `056c2dde8` + redeploy `9mxqphay0`) | ✅ (2026-06-22, smoke HTTP PASS) | — | TASK-1206 |
| `COMMERCIAL_Q2C_CONTRACT_ONLY_ENABLED` | — | — | — | TASK-1206 |
| `TENDER_CHAPTER_AUTHOR_ENABLED` | — | — | — | TASK-1415 |
| `PUBLIC_SITE_COMPARISON_TABLE_WRITES_ENABLED` | — | ✅ (2026-06-23, write path live + proven) | — | TASK-1225 |
| ~~`NEXT_PUBLIC_NEXA_FLOATING_EXPANDABLE_ENABLED`~~ | 🗑️ retirado 2026-08-05 | 🗑️ | 🗑️ | TASK-1078 — rollout completado; el panel ampliable es el comportamiento base incondicional. El código ya no lee el flag (ni el server var ni el mirror). **Env vars borradas de Vercel el 2026-08-05** (Production/staging/Preview develop; solo existía el mirror `NEXT_PUBLIC_*`, verificado con `vercel env ls` antes y después). Salió junto al modo `dock` ("Compacto"), el único fallback que gateaba. |
| `KNOWLEDGE_SEARCH_HYBRID_ENABLED` | — | ✅ | — | TASK-1151 |
| `KNOWLEDGE_SEARCH_RERANK_ENABLED` | — | ✅ | — | TASK-1140 |
| `NOTION_KNOWLEDGE_WEBHOOK_ENABLED` | ✅ | — | — | TASK-1094 |
| `NOTION_STATUS_TRANSITIONS_WEBHOOK_ENABLED` | ✅ | — | — | TASK-913 |
| `HOME_V2_ENABLED` | — | ✅ | — | TASK-696/780 |
| `CLIENT_LIFECYCLE_ONBOARDING_ENABLED` (+`NEXT_PUBLIC`) | ✅ | ✅ | Preview | TASK-1001/1009 |
| `CLIENT_LIFECYCLE_HUBSPOT_DEAL_TRIGGER_ENABLED` | ✅ | — | — | TASK-1001 |
| `ONBOARDING_ITEM_EVIDENCE_AUTOCOMPLETE_ENABLED` | ✅ | ✅ | Preview | TASK-1017 |
| `PAYROLL_WORKFORCE_INTAKE_GATE_ENABLED` | ✅ | ✅ | Preview | TASK-872 |
| `PAYROLL_PARTICIPATION_WINDOW_ENABLED` | ✅ | ✅ | — | TASK-890 |
| `PAYROLL_EXIT_ELIGIBILITY_WINDOW_ENABLED` | ✅ | ✅ | — | TASK-891 |
| `LEAVE_PARTICIPATION_AWARE_ENABLED` | ✅ | ✅ | — | TASK-892 |
| `SCIM_INTERNAL_COLLABORATOR_PRIMITIVE_ENABLED` | ✅ | ✅ | Preview | TASK-872 |
| `CONTRACTOR_PAYABLE_SETTLEMENT_ENABLED` | ✅ | ✅ | — | EPIC-013 |
| `RETENTION_POSITION_ENABLED` | ✅ (2026-06-29, release `056c2dde8` + redeploy `9mxqphay0`) | ✅ (2026-06-20, redeploy via push) | — | TASK-1188 |
| `PPM_POSITION_ENABLED` | — | — | — | TASK-1189 |
| `FINANCE_BIGQUERY_WRITE_ENABLED` | ✅ | — | — | Finance |
| `FINANCE_RECONCILIATION_AI_ENABLED` | ✅ | — | — | TASK-934 |
| `FINANCE_DISTRIBUTION_AI_ENABLED` | ✅ | — | — | Finance |
| `FINANCE_CORE_MXN_ENABLED` | — | ✅ | Preview | Finance MXN |
| `FINANCE_MULTI_CURRENCY_REPORTING_ENABLED` | — | ✅ | Preview | Finance MXN |
| `FINANCE_MXN_PAYMENT_ORDERS_ENABLED` | — | ✅ | Preview | Finance MXN |
| `NUBOX_EXPORT_FOREIGN_CURRENCY_ENABLED` | — | ✅ | Preview | Finance/Nubox |
| `HIRING_HANDOFF_BRIDGES_ENABLED` | TASK-356 | **ON en Vercel staging desde 2026-07-13 y Production desde 2026-07-14; OFF/default en development salvo env explícita** (verdad live: `vercel env ls`). El **materializer reactivo NO va gateado** (corre siempre — un no-op sería terminal en `outbox_reactive_log` y perdería eventos); el flag gatea SOLO los readers/bridges: `listInternalHireReadyForOnboarding` (cola para TASK-770) y `listStaffAugmentationHandoffIntents` (bridge Staff Aug), ambos retornan `enabled:false` explícito con flag OFF. | ✅ Vercel Production corregido a valor exacto `"true"` (primer add quedó `"true\n"` y el smoke devolvió `enabled:false`), redeploy `dpl_Grm71rLhwyyURq9ar7jf87i7DGzF` Ready con alias `greenhouse.efeoncepro.com`; smoke autenticado `GET /api/hr/hiring-activation?limit=5` → `200 { enabled:true, items:[] }`; smoke no-mutante `resolve-blocker` inexistente → `404 hiring_activation_not_found`, no disabled. ✅ ops-worker actualizado 2026-07-14: revision `ops-worker-00488-fvl`, env `HIRING_HANDOFF_BRIDGES_ENABLED=true`; `services/ops-worker/deploy.sh` lo declara para que no lo borre `--set-env-vars`. Staging: `greenhouse-ird9fygnf` 2026-07-13. | **Runtime: Vercel** (readers los consume la UI/API de 770) **+ ops-worker** para reliability AI Observer (`hiring.internal_hire_awaiting_onboarding`). Config: `src/lib/hiring/handoff/config.ts`; rollback Vercel: remover/apagar env + redeploy; rollback Cloud Run: `gcloud run services update ops-worker --update-env-vars HIRING_HANDOFF_BRIDGES_ENABLED=false`. |
| `HIRING_ACTIVATION_ENABLED` | TASK-770 | **ON en Vercel staging desde 2026-07-13 y Production desde 2026-07-14; OFF/default en development salvo env explícita**. Gatea los commands + readers del bridge hiring→HRIS (`/api/hr/hiring-activation/**`, cola merged, detail). Se apila sobre `HIRING_HANDOFF_BRIDGES_ENABLED` (356): sin la cola, el queue reader retorna `enabled:false` aunque este flag esté ON. | ✅ Vercel Production corregido a valor exacto `"true"` (primer add quedó `"true\n"`), redeploy `dpl_Grm71rLhwyyURq9ar7jf87i7DGzF` Ready; smoke autenticado `GET /api/hr/hiring-activation?limit=5` → `200 { enabled:true, items:[] }`; smoke no-mutante `resolve-blocker` con ID inexistente → `404 hiring_activation_not_found`, confirmando que el bridge no está disabled. Staging: `greenhouse-ird9fygnf` 2026-07-13. | **Runtime: Vercel** (API/UI; ningún worker lo lee — verificado con grep). Config: `src/lib/workforce/hiring-activation/config.ts`. La ACTIVACIÓN del colaborador no depende del flag (pasa por completeWorkforceMemberIntake, siempre ON). Rollback: remover/apagar env + redeploy. |
| `GREENHOUSE_SISTER_PLATFORM_OAUTH_ENABLED` | — | ✅ | — | Kortex bridge |
| `KORTEX_COMMAND_*` / `KORTEX_GITHUB_*` (varios) | — | ✅ | — | Kortex bridge |
| `WORKFORCE_CONTRACTING_AI_ENABLED` | — | — | Dev/Preview | TASK-1019 |
| `HIRING_ASSESSMENT_AI_ENABLED` | ✅ (2026-07-10, env SET + redeploy; eval verde Pearson 0.99) | — | — | TASK-1361 (prod: tras sign-off HR/Legal) |
| `HIRING_VACANCY_AI_ENABLED` | 🟡 SET `true` 2026-08-26, **frío hasta el próximo push de código a `develop`** | ✅ **ON desde 2026-07-16** (verificado live 2026-08-26) | — | TASK-1385 (copy público de vacantes propose→confirm). **Smoke CUMPLIDO, en el ambiente equivocado**: el 2026-07-18 el operador ejercitó el propose dos veces (`hiring_assessment_ai_proposal`, kind `opening_public_copy`) — una sobre `EO-OPN-0057` (`data_origin=smoke_test`) y otra sobre `EO-OPN-0009` (real, `active`), ambas `anthropic/claude-sonnet-5`. **Ninguna se confirmó, y no hubo un solo uso en los 39 días siguientes** — el pendiente real no es de gobernanza sino de ADOPCIÓN |
| `HIRING_EVALUATION_DOSSIER_AI_ENABLED` / `_AUTO_PROPOSE_ENABLED` | ✅ (generación desde 2026-08-16) | ✅ (**Production ON 2026-08-18**; auto-propose worker + App API, Google `gemini-2.5-flash`, prompt v2). El smoke post-fix preservó 8240 caracteres y supersede append-only; una propuesta automática nunca se confirma sola. | — | TASK-1735/1737: packet allowlisted, confirm/reject humano, nunca candidate-facing. |
| `HIRING_CANDIDATE_IDENTITY_NORMALIZATION_ENABLED` | ✅ (2026-08-16) | ✅ (**Production ON 2026-08-18** por autorización del CEO — release `fa54670470c1`, redeploy `greenhouse-4qu4swddd`. **Canary EJECUTADO 2026-08-18**: 5/5 puntos del Paso 2 verdes contra PG real vía `HIRING_CANARY_T1736=1 pnpm vitest run src/lib/hiring/candidate-intake/canary.live.test.ts`, cero correos emitidos. ⚠️ Queda el **residuo del canary sin purgar** — evidencia/audit son append-only por grant y pinnean la cadena por FK; requiere perfil `ops`, bloqueado por la MISMA credencial que `ISSUE-159`) | — | TASK-1736 (evidencia de intake + reconciliación de display del candidato; Vercel-only; Slices 1-4 code-complete, staging ON, prod gated — runbook `candidate-identity-rollout.md`; remediación histórica **ejecutada** 2026-08-16 por allowlist humana, independiente del flag: 3 personas reales + 2 QA podados) |
| `HIRING_ASSESSMENT_AI_RUN_ENQUEUE_ENABLED` / `_EXCEPTION_POLICY_ENABLED` / `_RUN_CONFIRM_ENABLED` | — | `RUN_ENQUEUE` ✅ ON en ops-worker; `EXCEPTION_POLICY` ⛔ OFF; `RUN_CONFIRM` ⛔ OFF. `RUN_MODE=global_provisional`, concurrencia 1, cap 1000, scheduler activo cada 2 min (`00584-r4x`, release `7e7a474217eb`). | — | TASK-1734/1742: provisional operator-only; no score efectivo ni resultado al postulante. Batch/calibrated sigue bloqueado. |
| `HIRING_PUBLIC_APPLICATIONS_ENABLED` | ✅ (2026-07-09, env SET; route live; opening real `EO-OPN-0009` publicado; submit smoke pendiente) | ✅ (2026-07-09, env SET; release `433cfa2b0fd3` + follow-up `915be02a86ab`, route live; submit smoke pendiente) | — | TASK-1367/TASK-354 |
| `CAREERS_DETAIL_EDITORIAL_V2_ENABLED` | ✅ (2026-08-18, staging + redeploy; GVC 1440/390) | ✅ (**Production ON 2026-08-18** — release `fa54670470c1`, redeploy `greenhouse-4qu4swddd`; hoja editorial sirviéndose en greenhouse.efeoncepro.com) | — | TASK-1741 (renderer editorial del detalle; Vercel-only, default OFF; prerequisite técnico del schema; ver § Pendientes) |
| `HIRING_PUBLIC_JOBPOSTING_SCHEMA_ENABLED` | ✅ (2026-08-18, staging + redeploy) | ✅ (**Production ON 2026-08-18** — release `fa54670470c1`, redeploy `greenhouse-4qu4swddd`; verificado en greenhouse.efeoncepro.com: JobPosting/TELECOMMUTE/20 países/Efeonce, sin directApply/validThrough/baseSalary, canonical correcto, vacante cerrada → 404) | — | TASK-1740 (JSON-LD `JobPosting` del detalle público; Vercel-only, default OFF; requiere además `CAREERS_DETAIL_EDITORIAL_V2_ENABLED`; ver § Pendientes) |
| `HIRING_SYNTHETIC_DATA_FILTER_ENABLED` | ✅ **Production ON 2026-08-19** (`vercel env add` + redeploy, tras el release `30301816955f`) | ✅ **staging ON 2026-08-18** (efecto medido en `/api/hiring/desk`: 2 vacantes / 47 postulaciones con ON, contra 24 / 79 con OFF) | — | TASK-1739 (filtro de procedencia del desk y del talent pool; **Vercel-only**, ningún Cloud Run lo lee; default OFF; rollback = poner la env var en `false` + redeploy) |
| `CAREERS_NATIVE_GROWTH_FORM_ENABLED` | ✅ (2026-07-14, release `a3b5ea3adb30-afed291d-c084-4192-aed9-5de9905b8a64`, deployment `dpl_7Wpv3vSPoDXnTQq8Za2Xfw2ZHkt2`, HTTP/API/visual smoke PASS) | ✅ (2026-07-13, deployment `greenhouse-ldqkedyia`, GVC/API/submit smoke PASS) | — | TASK-1373 (cutover visual de apply a `<greenhouse-form>`; rollback: apagar/remover flag + redeploy para volver al custom form) |
| `HIRING_HANDOFF_BRIDGES_ENABLED` | ✅ (2026-07-14, Vercel `dpl_Grm71rLhwyyURq9ar7jf87i7DGzF` + ops-worker `00488-fvl`; smoke API) | ✅ (2026-07-13, redeploy `greenhouse-ird9fygnf`) | — | TASK-356 (materializer siempre-ON sin flag; el flag gatea solo readers/bridges) |
| `HIRING_ACTIVATION_ENABLED` | ✅ (2026-07-14, Vercel `dpl_Grm71rLhwyyURq9ar7jf87i7DGzF`; smoke API) | ✅ (2026-07-13, redeploy `greenhouse-ird9fygnf`; smoke API) | — | TASK-770/TASK-1400 (bridge hiring→HRIS; se apila sobre el flag de 356) |
| 🔴 **BLOQUE GRADER — las celdas `Production = —` de aquí abajo son FALSAS.** Estos flags se leen en el **ops-worker**, cuya rama `production` de `deploy.sh:607-631` los declara `"true"` con el comentario *"PRODUCTION — espeja staging … todo activo en prod"*. Verificado contra la revisión activa el 2026-08-26 para `PROBES`, `ENTITY_PROBES` y `BRAND_INTELLIGENCE`: los tres `true`. **No planifiques un rollout AEO con esta columna.** | 🔴 | 🔴 | 🔴 | corrección 2026-08-26 |
| `GROWTH_AI_VISIBILITY_GRADER_ENABLED` | — | ✅ (2026-06-24) | — | TASK-1226 |
| `GROWTH_AI_VISIBILITY_OPENAI_ENABLED` | — | ✅ (2026-06-24, smoke real local OK) | — | TASK-1226 |
| `GROWTH_AI_VISIBILITY_ANTHROPIC_ENABLED` | — | ✅ (2026-06-24, smoke real local OK) | — | TASK-1226 |
| `GROWTH_AI_VISIBILITY_PERPLEXITY_ENABLED` | — | ✅ (2026-06-29, ops-worker `00418-2m6`; Vercel staging ya estaba registrado) | — | TASK-1226/TASK-1249 (prod OFF; smoke async post-flip recomendado) |
| `GROWTH_AI_VISIBILITY_GEMINI_ENABLED` | — | ✅ (2026-06-24, TASK-1233, smoke real local OK) | — | TASK-1226/1233 |
| `GROWTH_AI_VISIBILITY_GOOGLE_AIO_ENABLED` | — | ✅ (2026-06-28, ops-worker + Vercel staging, smoke real DataForSEO OK) | — | TASK-1265 (prod OFF; rotar credencial antes de prod) |
| `GROWTH_AI_VISIBILITY_PUBLIC_INTAKE_ENABLED` | — | ✅ (2026-06-25, smoke staging OK) | — | TASK-1240 (prod OFF; sign-off legal + captcha prod) |
| `GROWTH_AI_VISIBILITY_REPORT_EMAIL_ENABLED` (ops-worker + Vercel) | — | ✅ (2026-06-27, smoke E2E sent) | — | TASK-1250 (prod OFF; gated TASK-1246) |
| `GROWTH_AI_VISIBILITY_OPERATOR_SEND_ENABLED` (ops-worker + Vercel) | — | ⚠️ **CORREGIDO 2026-06-29 (TASK-1291): ABSENT en TODOS los envs Vercel** (`vercel env ls` = SoT; el "staging ON" anterior quedó stale desde el smoke de TASK-1279). La cross-sell SEND está gateada OFF (command rechaza `aeo_send_disabled`). | — | TASK-1279/1291 (re-enable gateado por eval TASK-1292 + sign-off) |
| `GROWTH_AI_VISIBILITY_LLM_EXTRACTION_ENABLED` | — | — | — | TASK-1227 (gatea el extractor de prosa; el proveedor lo elige `_PROSE_EXTRACTION_PROVIDER`, TASK-1271, default `anthropic`) |
| `HIRING_LIFECYCLE_EMAILS_ENABLED` (**ops-worker / Cloud Run**, NO Vercel — worker ÚNICO compartido staging+prod) | ✅ ON (2026-08-12, `deploy.sh` default true; revisión activa `ops-worker-00557-hfp`). Los 6 correos originales tienen E2E real EO-APP-0090 `sent`, incluido `selected` por supersede controlado. El séptimo, `hiring_assessment_submitted_internal`, está desplegado/configurado desde 2026-08-15, pero espera su primera entrega real: no hay backfill del único evento anterior al consumer. | ✅ ON (mismo worker compartido) | People/Operations: confirmar la primera entrega posterior a un `candidate_test` nuevo en `email_deliveries` y `outbox_reactive_log`; si falla, pausar sólo el tipo antes de desactivar el flag general. | TASK-1689 — 7 emails transaccionales del ciclo de Hiring. El tipo `hiring_assessment_submitted_internal` avisa a `HIRING_INTERNAL_NOTIFICATIONS_EMAIL` (default `people@efeoncepro.com`) sin score, respuestas, cambio de etapa ni decisión; cada tipo mantiene kill-switch propio. |
| `HIRING_ASSESSMENT_PUBLIC_SESSION_LINKS_ENABLED` (**ops-worker / Cloud Run**, NO Vercel — worker ÚNICO compartido staging+prod) | ⛔ OFF (`deploy.sh` default `false`) | ⛔ OFF (mismo worker compartido) | TASK-1746: ~~migración e índice~~ **aplicados 2026-08-19**; rutas live verificadas; **falta** confirmar por API/readback `click_tracking=false` en Resend (hoy el apex `efeoncepro.com` lo tiene **ON** — llegó un `email.clicked` real, así que el rewrite de links está activo y descartaría el fragmento; `mail.efeoncepro.com` nace con tracking OFF y es la salida limpia) y ejecutar smoke que preserve el href fragmentado antes del flip. | Cambia sólo el link del email inicial `hiring_assessment_assigned`: OFF conserva `/public/assessment/{token}`; ON usa `/public/assessment/access#access=...`. Owner TASK-1746. Rollback: `false` + redeploy del ops-worker. |
| `HIRING_STAGE_TEST_ASSIGNMENT_ENABLED` (**ops-worker / Cloud Run**, NO Vercel — worker ÚNICO compartido staging+prod) | ✅ ON (mismo worker compartido) | ✅ **ON desde 2026-08-18** (rev `ops-worker-00576-7zz`) | Ninguna: el rollout está cerrado (ver la fila de § Pendientes de acción, conservada como historia). ⚠️ **Corregido 2026-08-19:** esta fila decía `⛔ OFF (default deploy.sh)` y contradecía a la de § Pendientes. La afirmación era falsa por dos vías: `services/ops-worker/deploy.sh:493` declara `${HIRING_STAGE_TEST_ASSIGNMENT_ENABLED:-true}`, es decir el default del SoT es **`true`**, no `false`; y las tres precondiciones del flip ya estaban cumplidas y documentadas. Fuentes usadas: `deploy.sh:493` (SoT declarado) **y la verdad live de runtime**, confirmada el 2026-08-19 contra la revisión activa `ops-worker-00586-bww` (`gcloud run services describe ops-worker --region us-east4`): la env var está presente con valor **`true`**. Nota: la revisión activa ya no es la `ops-worker-00576-7zz` del flip original — hubo deploys posteriores y el flag sobrevivió, que es justo lo que garantiza declararlo en `deploy.sh` (los deploy.sh usan `--set-env-vars` destructivo). | TASK-1719 — asignación automática del test al entrar a la etapa. **Con el flag OFF el candidato SIGUE recibiendo el aviso de avance**: el consumer que absorbió al de TASK-1689 corre igual. El flag decide sólo si además se asigna el test. |
| `HIRING_TALENT_POOL_PROJECTION_ENABLED` / `HIRING_TALENT_POOL_SEARCH_ENABLED` / `HIRING_TALENT_POOL_INVITE_ENABLED` / `HIRING_TALENT_POOL_SELF_SERVICE_ENABLED` / `HIRING_TALENT_POOL_MCP_ENABLED` | ✅ Producción 2026-08-16: los cinco flags Vercel `true`; worker `HIRING_TALENT_POOL_SELF_SERVICE_ENABLED=true` en revisión `ops-worker-00563-ghv` (Ready=True, change-gated; consumer ya contenía el código). 52 memberships (50 `active_process`, 2 `needs_reconsent`); sin backfill de consentimiento futuro. Release `20245888625b8dc979cf2f747f5ef9d7999df6e5` / run `31953851353` terminó `released`; redeploy Vercel `dpl_CTxG3tx66S159tazMSyNiGSmqzHJ` quedó `READY`, health 200, watchdog `aggregateSeverity=ok`, `drift_count=0`. Smoke read-only: token inválido responde `404 talent_pool_link_unavailable`; App API sin sesión `401`. | Mismo schema Cloud SQL; búsqueda interna y flujo de confirmación futuro activos, contacto sólo después de grant explícito del candidato. | Rollback: Vercel `HIRING_TALENT_POOL_SELF_SERVICE_ENABLED=false` e `HIRING_TALENT_POOL_INVITE_ENABLED=false` + redeploy; worker `HIRING_TALENT_POOL_SELF_SERVICE_ENABLED=false` por `deploy.sh`. No se envió correo a un candidato real durante el flip; la primera entrega real debe verificarse con un candidato de prueba controlado antes de campañas. | TASK-1723–1726. Vercel consume search/invite/self-service/MCP; ops-worker consume projection y el consumer `talent_pool.consent_requested`; gateway `efeonce-mcp-gateway` consume provider Hiring. CEO autorizó el encendido operativo el 2026-08-16; la revisión jurídica formal, si la política la exige, permanece como sign-off residual. |
| `HIRING_CANDIDATE_REVIEW_READER_ENABLED` / `HIRING_CANDIDATE_REVIEW_PROJECTION_ENABLED` / gateway review | ✅ **Producción internal-only 2026-08-18.** App API exacta por `applicationId` y tools `hiring.applications.review.list` / `hiring.application.review_packet.get`; canary OAuth/MCP 200, una chunk y hash ligado; acceso sin auth 401. | Mismo schema Cloud SQL; Vercel consume reader, worker materializa proyección limpia y gateway federa sin almacenar. | Completar firmas Privacy/Legal, Security, Talent, Identity, MCP y Platform; probar revoked/base-only y rollback/revocación. B2B permanece bloqueado. | TASK-1718; sin PDF crudo, contacto, ranking, decisiones, stage move, tests ni correo. |
| `GROWTH_EBOOK_EMAIL_DELIVERY_ENABLED` (**ops-worker / Cloud Run**, NO Vercel) | ✅ (2026-07-10, rev `ops-worker-00474-jrf` + declarado en `deploy.sh`) | — | — | TASK-1375 (flujo crítico: la success card promete el email). El flip vía sólo `--update-env-vars` fue borrado por el deploy siguiente |
| `GROWTH_AI_VISIBILITY_PROSE_EXTRACTION_SHADOW_ENABLED` | — | — | — | TASK-1271 (eval-only; default OFF; NO afecta el path de runs — lo consume sólo el harness/CLI de eval) |
| `GROWTH_AI_VISIBILITY_ASYNC_EXECUTION_ENABLED` | — | ✅ (2026-06-24, smoke real `full` EO-GRUN-00011 OK) | — | TASK-1234 |
| `GROWTH_AI_VISIBILITY_PORTAL_RUN_ENABLED` | — | ✅ (2026-06-29, smoke real: Berel contratado accepted + run `portal_contracted` drenado; org sin entitlement denied) | — | TASK-1277 (prod OFF; gated EPIC-020 + sign-off comercial) |
| `GROWTH_AI_VISIBILITY_TRIAL_ENABLED` | — | ✅ (2026-06-29, inerte sin orgs trial provisionadas; provisión PLG = decisión comercial) | — | TASK-1277 (prod OFF; tier trial PLG) |
| `GROWTH_AI_VISIBILITY_PROBES_ENABLED` | — | ✅ (2026-06-28, smoke E2E real EO-GRUN-00025 — structural 78.8, 8/8 probes sin fallo) | — | TASK-1266 (prod OFF, gated EPIC-020) |
| `GROWTH_AI_VISIBILITY_AGENTIC_READINESS_ENABLED` | — | ✅ (2026-06-28, agentic 43.8 en el mismo run) | — | TASK-1266 (prod OFF; requiere `PROBES` ON) |
| `GROWTH_AI_VISIBILITY_ENTITY_PROBES_ENABLED` | — | ✅ (2026-06-28, flip staging Vercel+ops-worker; KG api key publicada+wired → 3 probes activos) | — | TASK-1267 (prod OFF; eje `entity` KG/Wikidata/Reddit; requiere `PROBES` ON; secret `greenhouse-google-knowledge-graph-api-key`) |
| `GROWTH_AI_VISIBILITY_FIX_IT_ENABLED` | — | ✅ (2026-06-28, Vercel staging + deploy `greenhouse-fyoycag0t` Ready) | — | TASK-1269 (artifacts JSON-LD/llms/briefs, gated copy/legal; prod OFF) |
| `GROWTH_AI_VISIBILITY_REGRADE_ENABLED` (ops-worker) | — | ✅ (2026-06-29, scheduler enabled; no-op smoke `no_due_profiles`) | — | TASK-1270 (prod OFF; E2E opt-in pendiente) |
| `GROWTH_SEARCH_CONSOLE_ENABLED` | — | ✅ (2026-06-29, wiring smoke verde) | — | TASK-1282 (staging ON; flujo property-picker; OAuth External+Producción; prod pendiente release control plane) |

---

## § Cómo prender un env-var flag

> ⚠️ **Un flag no es "el flag de Vercel".** Es una env var leída por **cada runtime que ejecuta el código gateado**. Greenhouse tiene **5 runtimes con env vars totalmente independientes**. Prenderlo en el runtime equivocado = no pasa nada (y peor: parece prendido en el ledger). **El paso 0 no es opcional.**

### Los 5 runtimes y sus env vars

| Runtime | Qué ejecuta | Cómo se prende | Cómo se verifica |
|---|---|---|---|
| **Vercel** (app Next.js) | API routes, server components, UI, crons de `vercel.json` | `vercel env add` + redeploy | `vercel env ls` |
| **`ops-worker`** (Cloud Run, `us-east4`) | **projections reactivas, consumers del outbox, crons de Cloud Scheduler**, emails async | `gcloud run services update ops-worker` | `gcloud run revisions describe` |
| **`commercial-cost-worker`** (Cloud Run, `us-east4`) | jobs de costo comercial | `gcloud run services update commercial-cost-worker` | idem |
| **`ico-batch-worker`** (Cloud Run, `us-east4`) | batches ICO / delivery metrics | `gcloud run services update ico-batch-worker` | idem |
| **`hubspot-greenhouse-integration`** (Cloud Run, `us-central1`) | bridge + webhooks HubSpot | `gcloud run services update hubspot-greenhouse-integration --region us-central1` | idem |

### Paso 0 — Mapear en QUÉ runtime(s) vive el flag (ANTES de prender)

```bash
# Encontrar dónde se LEE el flag (no dónde se declara)
grep -rn "<FLAG_NAME>\|is<Feature>Enabled" src/ services/ | grep -v __tests__
```

Traduce cada path a su runtime:

| Path donde se lee | Runtime real |
|---|---|
| `src/app/**`, `src/views/**`, `src/components/**` | **Vercel** |
| `src/lib/sync/projections/**` (projection reactiva) | **ops-worker** (la drena `ops-reactive-*`) |
| `services/ops-worker/**` | **ops-worker** |
| `services/<otro-worker>/**` | ese worker |
| `src/lib/**` compartido | **depende de los callers** → seguir la cadena hasta un `src/app/**` (Vercel) y/o una projection/worker (Cloud Run). Puede ser **los dos**. |

**Heurística de bolsillo:** ¿el flag gatea algo **async** (email de respaldo, projection, consumer del outbox, cron de Scheduler, materializer)? → **ops-worker, NO Vercel.** ¿Gatea una **ruta/superficie visible**? → **Vercel.** ¿Ambas cosas? → **ambos** (ej. `GROWTH_AI_VISIBILITY_REPORT_EMAIL_ENABLED`, marcado `(ops-worker + Vercel)` en el snapshot).

### Paso 1 — Prender en CADA runtime del mapeo

> ⚠️ **En Cloud Run, `--update-env-vars` NO es el flip: es un parche efímero.** Los `deploy.sh` de los workers usan **`--set-env-vars` (destructivo)**, que reescribe el set completo de env vars. Una var agregada out-of-band con `--update-env-vars` **sobrevive hasta el siguiente deploy del worker y después desaparece en silencio**. El **SoT es `services/<worker>/deploy.sh`**: ahí se declara el flag. Por eso ese archivo está lleno de comentarios "Declarativo acá para que `--set-env-vars` (destructivo) NO lo borre".

```bash
# --- Vercel (si aplica) --------------------------------------------------
printf %s "true" | vercel env add <FLAG_NAME> <environment> --scope efeonce-7670142f
#    environments: Production | staging | preview | development  (staging = custom env)
#    Si el flag tiene mirror NEXT_PUBLIC_*, agregar AMBAS.
```

**Cloud Run (si aplica) — SIEMPRE los dos pasos, en este orden:**

**(a) Declararlo en `services/<worker>/deploy.sh`** (SoT, sobrevive deploys) siguiendo la convención del archivo:

```bash
# <TASK-###> — <qué gatea>. Declarativo acá para que `--set-env-vars` (destructivo) NO lo
# borre en cada redeploy. Rollback (<5min): gcloud run services update <svc> --update-env-vars <FLAG>=false
<FLAG_NAME>="${<FLAG_NAME>:-true}"
ENV_VARS="${ENV_VARS},<FLAG_NAME>=${<FLAG_NAME>}"
```

**(b) Aplicarlo en vivo** para que tome efecto ya, sin esperar un deploy del worker:

```bash
gcloud run services update <servicio> \
  --region <us-east4|us-central1> --project efeonce-group \
  --update-env-vars <FLAG_NAME>=true
#    Crea la revisión y enruta tráfico. Pero SIN el paso (a), el próximo deploy lo borra.
```

Hacer sólo (b) es el bug class del 2026-07-10: `GROWTH_EBOOK_EMAIL_DELIVERY_ENABLED` se prendió en la revisión `00470`, tres deploys después (`00473`) ya no estaba, y el consumer reactivo registró `skip: flag OFF` mientras la success card del form le prometía el email al usuario. **Fallo silencioso: el ledger decía ON, la realidad era OFF.**

### Paso 2 — Redeploy donde haga falta

> ⚠️ **En Vercel, el orden importa: `env add` ANTES del `push`.** Vercel **congela las env vars cuando CREA el build**, no cuando lo termina. Si empujas primero, el deploy que arranca con tu push **no tiene la var** — y queda `● Ready`, verde, con el flag OFF. Es un fallo silencioso: `vercel env ls` te dice que la var existe (existe, en la CONFIG del environment) mientras la revisión que sirve el tráfico no la tiene.

- **Vercel server var** (`*_ENABLED`): aplica **en el deploy que se cree DESPUÉS** de agregar la var (las env vars NO se toman en caliente, y **un deploy ya en vuelo nunca la va a tener**).
  - Si el código ya está empujado (el caso normal cuando prendes un flag de algo recién mergeado), **redeploya**:

    ```bash
    # 1. Cuál es el deploy activo del environment
    vercel list greenhouse-eo --scope efeonce-7670142f --meta githubCommitRef=develop | head -3

    # 2. Redeploy (reusa el build; re-aplica las env vars ACTUALES)
    vercel redeploy <deployment-url> --scope efeonce-7670142f

    # 3. Confirmar que los ALIAS quedaron apuntando a la revisión nueva
    vercel inspect <nueva-url> --scope efeonce-7670142f | grep -A3 Aliases
    #    staging → dev-greenhouse.efeoncepro.com + greenhouse-eo-env-staging-*.vercel.app
    ```

- **Vercel `NEXT_PUBLIC_*`**: se hornea en build → requiere **BUILD FRESCO** (push a la rama del env, o Redeploy con "Use existing Build Cache" DESMARCADO). Un `vercel redeploy` normal **no alcanza**.
- **Cloud Run**: el `services update` ya crea la revisión. Nada más.

### Paso 3 — Verificar en CADA runtime (que la var exista Y que el consumer se comporte)

> ⚠️ **`vercel env ls` NO prueba nada.** Te dice que la var existe en la **config del environment**, no que la **revisión que sirve el tráfico** la tenga (ver la trampa del Paso 2). En Vercel la única verificación honesta es **ejercitar el flujo real contra el deploy activo**.

```bash
# Vercel — necesario, NO suficiente (dice que la var existe, no que el runtime la vea)
vercel env ls | grep <FLAG_NAME>

# Vercel — la verificación que SÍ vale: ejercitar el flujo contra el deploy activo.
#   staging tiene SSO → usar el camino canónico (bypass + sesión de agente, en memoria):
pnpm staging:request /api/<ruta-gateada-por-el-flag>
#   Si el flag gatea una capability de Nexa, el smoke es PREGUNTARLE y ver si el tool dispara:
pnpm staging:request POST /api/home/nexa '{"prompt":"<la pregunta real>","history":[]}'
#   (si `staging:request` no encuentra el bypass, se resuelve vía la API de Vercel con el token de
#    Secret Manager `greenhouse-vercel-api-token` — canalizado en memoria, NUNCA escrito a disco)

# Cloud Run — leer la env de la REVISIÓN ACTIVA (no del servicio "en general")
REV=$(gcloud run services describe <servicio> --region <region> --project efeonce-group \
        --format='value(status.latestReadyRevisionName)')
gcloud run revisions describe "$REV" --region <region> --project efeonce-group \
  --format="json(spec.containers[0].env)" | grep -A1 <FLAG_NAME>
```

Y además: **ejercitar el flujo real**. Que la var exista no prueba que el consumer haga lo suyo. Si el flag gatea un email async, el smoke es "llega el email", no "la var está".

### Paso 4 — Actualizar este ledger

Registrar en la fila **el/los runtime(s)** donde quedó prendido + fecha + revisión (Cloud Run) — igual que
`GROWTH_EBOOK_EMAIL_DELIVERY_ENABLED` (**ops-worker**, rev `ops-worker-00470-898`). Sin el runtime explícito, el próximo agente asume Vercel y se equivoca.

### Apagar (rollback)

```bash
vercel env rm <FLAG_NAME> <environment> --scope efeonce-7670142f   # + redeploy
gcloud run services update <servicio> --region <region> --project efeonce-group \
  --remove-env-vars <FLAG_NAME>
```

(Quitar la var = OFF/default.) **Apagar también es multi-runtime**: quitarla de Vercel no la apaga en el worker.

Para los **PG rollout flags** (`home_rollout_flags`): se prenden vía admin endpoint sin redeploy — ver el platform doc.

---

## § Inventario completo (referencia por dominio)

> Todos los `*_ENABLED` referenciados en código. **Default = OFF** salvo nota. Owner = task ancla. El estado live por env está arriba (§ Snapshot) o en `vercel env ls`.

**Nexa / Knowledge** (`src/lib/nexa/flags.ts`, `src/lib/knowledge/search/flags.ts`):
~~`NEXA_FLOATING_EXPANDABLE_ENABLED`~~ (panel B, TASK-1078 — **retirado 2026-08-05**, ya no se lee) · `NEXA_INTERACTION_LANE_ENABLED` (lane C, TASK-1079) · `NEXA_KNOWLEDGE_RETRIEVAL_ENABLED` (TASK-1085) · `NEXA_FORCE_KNOWLEDGE_RETRIEVAL_ENABLED` (TASK-1156) · `NEXA_AUTO_ROUTER_ENABLED` (TASK-1091) · `NEXA_SYSTEM_PROMPT_V2_ENABLED` (TASK-1124) · `NEXA_KNOWLEDGE_SYNTHESIS_BRIEF_ENABLED` (TASK-1124) · `NEXA_ACTION_RUNTIME_ENABLED` (TASK-1137) · `NEXA_QUOTE_AUTHOR_ACTION_ENABLED` (TASK-1212) · `NEXA_PROPOSAL_ACTIONS_ENABLED` (TASK-1399) · `NEXA_ANSWERS_CANVAS_LENS_ENABLED` (TASK-1101) · `NEXA_SUGGESTED_PROMPTS_DATA_AWARE_ENABLED` (TASK-1087) · `KNOWLEDGE_SEARCH_HYBRID_ENABLED` (TASK-1151) · `KNOWLEDGE_SEARCH_RERANK_ENABLED` (TASK-1140) · `KNOWLEDGE_REACTIVE_EMBEDDING_ENABLED` (TASK-1155) · `NOTION_KNOWLEDGE_WEBHOOK_ENABLED` (TASK-1094). Provider pin (no `*_ENABLED`): `NEXA_PROVIDER`.

**Payroll / Workforce:** `PAYROLL_PARTICIPATION_WINDOW_ENABLED` (TASK-890) · `PAYROLL_EXIT_ELIGIBILITY_WINDOW_ENABLED` (TASK-891) · `LEAVE_PARTICIPATION_AWARE_ENABLED` (TASK-892) · `PAYROLL_CONTRACTOR_ENGAGEMENT_EXCLUSION_ENABLED` · `PAYROLL_WORKFORCE_INTAKE_GATE_ENABLED` (TASK-872) · `WORKFORCE_ACTIVATION_READINESS_GUARD_ENABLED` · `SCIM_INTERNAL_COLLABORATOR_PRIMITIVE_ENABLED` (TASK-872) · `WORKFORCE_CONTRACTING_AI_ENABLED` (TASK-1019).

**Hiring / ATS (EPIC-011):** evaluación provisional activa en producción mediante `HIRING_ASSESSMENT_AI_ENABLED` + `HIRING_ASSESSMENT_AI_RUN_ENQUEUE_ENABLED` y `RUN_MODE=global_provisional`; exception policy y run confirm permanecen OFF. El expediente usa `HIRING_EVALUATION_DOSSIER_AI_ENABLED` + `_AUTO_PROPOSE_ENABLED` ON en producción, con confirmación humana. Candidate review internal-only usa reader/projection + gate del gateway; no habilita B2B ni writes. `HIRING_SYNTHETIC_DATA_FILTER_ENABLED` (TASK-1739) filtra por procedencia el desk y el talent pool y está ON en staging y en producción; es Vercel-only y no gatea la guarda de publicación, la exclusión del gold set ni los commands de marcado/purga. El inventario exacto, owners y rollback están en las filas anteriores y en los runbooks de Hiring/MCP.

**Storage / Assets (TASK-1362/TASK-1378):** `ASSET_MALWARE_SCAN_ENABLED` — **ON en staging Y Production (Production desde 2026-08-12, redeploy `greenhouse-aivcug5f5`)**. Suma el adapter `clamav-http` (Cloud Run `clamav` us-east4, IAM-only) encima del scanner estructural en `scanAssetBytes`; `src/lib/storage/asset-scan/config.ts`; **runtime: Vercel** — el upload público corre en el route handler de Next. Verificado post-flip desde el runtime (`GET /api/internal/health/scanner-auth?probe=scan`: `flagEnabled=true`, `mint.ok`, `probe.ok`) + postulación real por el formulario público (`scanner=structural+clamav-http`, `clean`, `attached`). Producción resuelve credencial por `service_account_key` (TASK-800); staging por WIF — ambas ramas verificadas en su runtime. Acompañantes (no `*_ENABLED`): `ASSET_MALWARE_SCAN_ENDPOINT`, `ASSET_MALWARE_SCAN_TIMEOUT_MS`.

**Finance:** `FINANCE_BIGQUERY_WRITE_ENABLED` · `FINANCE_RECONCILIATION_AI_ENABLED` (TASK-934) · `FINANCE_DISTRIBUTION_AI_ENABLED` · `FINANCE_CORE_MXN_ENABLED` · `FINANCE_MULTI_CURRENCY_REPORTING_ENABLED` · `FINANCE_MXN_PAYMENT_ORDERS_ENABLED` · `FINANCE_MXN_BEREL_BACKFILL_APPLY_ENABLED` · `NUBOX_EXPORT_FOREIGN_CURRENCY_ENABLED` · `RETENTION_POSITION_ENABLED` (TASK-1188, línea retenciones del F29 — code-complete + shadow, ver § Pendientes; `src/lib/finance/retention/flags.ts`) · `PPM_POSITION_ENABLED` (TASK-1189, línea PPM del F29 — code-complete + shadow; `src/lib/finance/ppm/flags.ts`). **Contractor:** `CONTRACTOR_PAYABLE_SETTLEMENT_ENABLED` · `CONTRACTOR_AGREED_AMOUNT_GUARDRAIL_ENABLED` (EPIC-013).

**ICO / Delivery metrics:** `ICO_MATERIALIZER_FRESHNESS_GATE_ENABLED` · `ICO_MATERIALIZER_INCREMENTAL_DELTA_ENABLED` · `ICO_MATERIALIZER_MERGE_PATTERN_ENABLED` · `ATTRIBUTABLE_LATENESS_OTD_ENABLED` · `OTD_CLASSIFIER_GH_SHADOW_ENABLED` · `CT_DAYS_CANONICAL_FORMULA_ENABLED` · `CT_SLO_PCT_METRIC_ENABLED` · `NOTION_RPA_WRITEBACK_ENABLED` · `NOTION_FTR_WRITEBACK_ENABLED` · `NOTION_OTD_WRITEBACK_ENABLED` (+ `_EFEONCE`/`_SKY`, TASK-927) · `NOTION_DUE_DATE_CAPTURE_ENABLED` · `NOTION_STATUS_TRANSITIONS_WEBHOOK_ENABLED` (TASK-900…943).

**Client lifecycle / Onboarding:** `CLIENT_LIFECYCLE_ONBOARDING_ENABLED` (+`NEXT_PUBLIC`) · `CLIENT_LIFECYCLE_HUBSPOT_DEAL_TRIGGER_ENABLED` · `CLIENT_BIRTH_CANONICAL_WRITE_ENABLED` · `ONBOARDING_ITEM_EVIDENCE_AUTOCOMPLETE_ENABLED` · `GREENHOUSE_NOTION_PG_PROJECTION_ENABLED` (TASK-991…1017).

**UI / Design tokens:** `HOME_V2_ENABLED` (TASK-696/780) · `NEXT_PUBLIC_AXIS_NEUTRALS_ENABLED` · `NEXT_PUBLIC_GREENHOUSE_SECONDARY_TEAL_ENABLED` (default ON; emergency revert to legacy azure; 2026-07-18). `NEXT_PUBLIC_AXIS_SECONDARY_LIME_ENABLED` is retired and no longer read by runtime.

**Kortex bridge / sister platform:** `GREENHOUSE_SISTER_PLATFORM_OAUTH_ENABLED` · `KORTEX_COMMAND_ADAPTER_ENABLED` · `KORTEX_COMMAND_ADMIN_ENABLED` · `KORTEX_COMMAND_LIVE_EXECUTE_ENABLED` · `KORTEX_GITHUB_COMMANDS_ENABLED` · `KORTEX_GITHUB_WORKFLOW_DISPATCH_ENABLED`.

**Commercial / Proposal Studio:** `ARTIFACT_RENDER_JOBS_ENABLED` (TASK-1391 — pipeline de render gobernado; multi-runtime ×3) · `NEXA_PROPOSAL_ACTIONS_ENABLED` (TASK-1399 — acciones gobernadas de Nexa; Vercel only) · `TENDER_CHAPTER_AUTHOR_ENABLED` (TASK-1415 — motor de chapter-authors, gatea `proposeChapter`; Vercel/CLI only) · `COMMERCIAL_Q2C_CANONICAL_CLOSE_ENABLED` / `COMMERCIAL_Q2C_CONTRACT_ONLY_ENABLED` (TASK-1206 — cierre Quote-to-Cash).

**Growth / AI Visibility Grader** (`src/lib/growth/ai-visibility/flags.ts`): `GROWTH_AI_VISIBILITY_GRADER_ENABLED` (kill switch global) · `GROWTH_AI_VISIBILITY_OPENAI_ENABLED` · `GROWTH_AI_VISIBILITY_ANTHROPIC_ENABLED` · `GROWTH_AI_VISIBILITY_PERPLEXITY_ENABLED` · `GROWTH_AI_VISIBILITY_GEMINI_ENABLED` · `GROWTH_AI_VISIBILITY_GOOGLE_AIO_ENABLED` (TASK-1226/TASK-1265 — todos default OFF; sin flag/secret el adapter resuelve skip limpio) · `GROWTH_AI_VISIBILITY_LLM_EXTRACTION_ENABLED` (TASK-1227 — fallback LLM de extracción de prosa para el normalizer; default OFF → determinista-first preserva `unknown`) · **Prose extraction router (TASK-1271):** `GROWTH_AI_VISIBILITY_PROSE_EXTRACTION_PROVIDER` (no `*_ENABLED`; `anthropic`|`gemini`|`openai`, default `anthropic` behavior-preserving — elige el proveedor del extractor cuando `LLM_EXTRACTION` está ON) · `GROWTH_AI_VISIBILITY_PROSE_EXTRACTION_SHADOW_ENABLED` (eval-only, default OFF) · config no-`*_ENABLED`: `_PROSE_EXTRACTION_MAX_TOKENS` (default 1024), `_PROSE_EXTRACTION_MAX_COST_USD` (default 0.02), `_PROSE_EXTRACTION_MODEL_{ANTHROPIC,GEMINI,OPENAI}` (override de modelo por proveedor, sin deploy) · `GROWTH_AI_VISIBILITY_ASYNC_EXECUTION_ENABLED` (TASK-1234 — cutover inline→async; default OFF → endpoint ejecuta inline; con ON encola y el worker Cloud Run `ops-growth-grader-drain` ejecuta sin límite de duración). Pin de modelo (no `*_ENABLED`): `GREENHOUSE_GEMINI_GROUNDED_MODEL` (TASK-1233 — override del modelo Gemini de grounding; default `gemini-3-flash-preview`, la última generación disponible en Vertex; bumpear a 3.1/3-pro apenas lleguen, sin deploy). **Site Readiness (TASK-1266):** `GROWTH_AI_VISIBILITY_PROBES_ENABLED` (flag maestro del probe gatherer + eje structural; gateado por `isGraderEnabled`) · `GROWTH_AI_VISIBILITY_AGENTIC_READINESS_ENABLED` (eje agentic; requiere `PROBES` ON) · `GROWTH_AI_VISIBILITY_ENTITY_PROBES_ENABLED` (TASK-1267 — eje `entity`: Google Knowledge Graph + Wikidata/Wikipedia + Reddit-UGC; requiere `PROBES` ON; KG api key vía `GOOGLE_KNOWLEDGE_GRAPH_API_KEY[_SECRET_REF]`, sino degrada honesto) — todos default OFF; readiness es ortogonal al de percepción (NUNCA blended). **Endurecimiento del fetcher (TASK-1778):** `GROWTH_PROBE_FETCH_STRICT_NETWORK_ENABLED` (lector canónico en `src/lib/growth/site-substrate/site-fetch.ts` desde TASK-1697, 2026-08-27 — viajó con el fetcher; `ai-visibility/flags.ts` lo re-exporta) (**ON desde 2026-08-27** en ops-worker —default `true` en `deploy.sh`— y Vercel staging; Vercel Production con el release que lleve el código a `main`; gatea SÓLO redirect containment + guarda DNS — el stream cap/`truncated`/robots obedecido viajan sin flag; ver § Pendientes). **Fix-It Artifacts (TASK-1269):** `GROWTH_AI_VISIBILITY_FIX_IT_ENABLED` (default OFF; genera artifacts deterministas public-safe desde report/probes, sin LLM ni writes al sitio). **Run gobernado de portal (TASK-1277):** `GROWTH_AI_VISIBILITY_PORTAL_RUN_ENABLED` · `GROWTH_AI_VISIBILITY_TRIAL_ENABLED` (default OFF; ver § Pendientes). **Gate de presupuesto per-org (TASK-1696):** `GROWTH_AI_VISIBILITY_BUDGET_GATE_ENABLED` (shadow: computa `resolveAeoBudget` y registra `wouldBlock`, NO bloquea) · `GROWTH_AI_VISIBILITY_BUDGET_GATE_ENFORCED` (subordinado al anterior; sólo con los dos en ON bloquea con `budget_exhausted`) — ambos default OFF, **dual-runtime Vercel + ops-worker**; knobs asociados NO-flag `GROWTH_AI_VISIBILITY_{CONTRACTED,PILOT,TRIAL}_MONTHLY_BUDGET_USD` (60/10/3, espejo de los `GROWTH_SEO_*_MONTHLY_BUDGET_USD`). **Recurring re-grade (TASK-1270):** `GROWTH_AI_VISIBILITY_REGRADE_ENABLED` (ops-worker; staging ON, prod OFF; Scheduler active staging/paused prod; opt-in por perfil contratado). **Search Console connection (TASK-1282)** (`src/lib/growth/search-console/flags.ts`): `GROWTH_SEARCH_CONSOLE_ENABLED` (default OFF → `oauth/start` y el reader resuelven disabled; ON tras verificación del consent screen de Google + grant IAM secret-write al prefijo `search-console-token-*`). **AEO brand-aware (EPIC-021):** `GROWTH_AI_VISIBILITY_CATEGORY_GUARD_ENABLED` (TASK-1288 — guard de categoría no resuelta) · `GROWTH_AI_VISIBILITY_BRAND_INTELLIGENCE_ENABLED` (TASK-1288 — lectura grounded) · `GROWTH_AI_VISIBILITY_ARCHETYPE_PROMPTS_ENABLED` (TASK-1290 — prompts por arquetipo/`business_model`; default OFF → pack agencia v1 bit-for-bit; ON → baseline del arquetipo, `unknown`→genérico) · `GROWTH_AI_VISIBILITY_PROMPT_AUTHORING_ENABLED` (TASK-1290 Slice 3 — autoría LLM del set por marca, gateado además por `isGraderEnabled`; default OFF → baseline determinista; ON → el LLM propone el Query Fan-Out brand-specific, 1×/marca, se congela vía approve; smoke live gemini-2.5-flash-lite SKY ok) — todos default OFF, ver § Pendientes. **Host del hub público (TASK-1324, no `*_ENABLED`):** `PUBLIC_GRADER_HUB_URL` — base del hub headless `efeonce-think` que renderiza el informe público (`buildPublicReportUrl` en `hubspot/report-link.ts`, fuente única del link del correo + HubSpot `report_url`; también el redirect puente `next.config.ts`). **Default seguro = `https://think.efeoncepro.com` (hub prod)** → funciona sin setearla; override solo para apuntar a staging del hub. NO reusar `NEXT_PUBLIC_APP_URL` (portal).

**Mirrors `NEXT_PUBLIC_*` (client-readable)** — pares de un flag server que la UI necesita leer client-side: ~~`NEXT_PUBLIC_NEXA_FLOATING_EXPANDABLE_ENABLED`~~ (retirado 2026-08-05) · `NEXT_PUBLIC_NEXA_INTERACTION_LANE_ENABLED` · `NEXT_PUBLIC_NEXA_KNOWLEDGE_RETRIEVAL_ENABLED` · `NEXT_PUBLIC_NEXA_SUGGESTED_PROMPTS_DATA_AWARE_ENABLED` · `NEXT_PUBLIC_CLIENT_LIFECYCLE_ONBOARDING_ENABLED` · `NEXT_PUBLIC_AXIS_NEUTRALS_ENABLED` · `NEXT_PUBLIC_GREENHOUSE_SECONDARY_TEAL_ENABLED`. Recuerda: se hornean en build → prenderlos requiere build fresco.

> Para regenerar/auditar el inventario desde código + cruzarlo contra Vercel y este ledger:
> **`pnpm flags:audit`** (resalta: en código sin registrar · ON en staging pero no prod · OFF everywhere · en Vercel sin código). `--strict` falla si hay flags en código sin registrar acá. Script: `scripts/ci/feature-flags-audit.mjs`.

---

## § Mantenimiento

Este ledger es **doc viva**. Al cerrar una task con flag:

1. Agrega el flag al **§ Inventario** (dominio correcto) en el PR de la feature.
2. Si queda code-complete sin prender → fila en **§ Pendientes de acción**.
3. Al prender/apagar en cualquier env → actualiza **§ Snapshot** con fecha y, si cerraste un pendiente, remueve su fila.
4. Refresca el snapshot completo periódicamente con `vercel env ls` (la verdad live).

**Idea de follow-up (no implementada):** un `pnpm flags:audit` que cruce los flags de código vs `vercel env ls` y resalte "en staging pero no en prod" / "en código pero sin registrar acá" — automatizaría la detección de deuda. Si se materializa, este doc se vuelve el output humano de ese script.
