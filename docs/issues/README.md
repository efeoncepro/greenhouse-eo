# Issue Tracker

Pipeline de incidentes operativos del portal.

## Convención

- Los issues usan `ISSUE-###` como ID estable.
- Los archivos viven en `docs/issues/{open,resolved}/`.
- Un issue se mueve a `resolved/` cuando se confirma la solución en el ambiente afectado.
- A diferencia de las tasks (`TASK-###`), los issues son reactivos: documentan un problema encontrado en runtime, no trabajo planificado.

## Estados

- `open`: incidente detectado, en diagnóstico o pendiente de solución.
- `resolved`: solución aplicada y verificada en el ambiente afectado.

## Protocolo operativo

- El modelo operativo canónico de issues vive en `docs/operations/ISSUE_OPERATING_MODEL_V1.md`.
- Un issue puede resolverse directamente sin task si el fix es localizado y verificable.
- Al resolverse, el archivo debe moverse de `docs/issues/open/` a `docs/issues/resolved/` y el tracker debe actualizarse en el mismo lote.

## Plantilla

```markdown
# ISSUE-### — Título breve

## Ambiente

production | staging | preview

## Detectado

Fecha, canal de detección (Slack alert, Sentry, Admin Center, usuario)

## Síntoma

Qué se observa desde fuera.

## Causa raíz

Qué lo provoca a nivel técnico.

## Impacto

Qué funcionalidad está afectada y para quién.

## Solución

Qué se hizo o hay que hacer para resolverlo.

## Verificación

Cómo confirmar que se resolvió.

## Estado

open | resolved

## Relacionado

Tasks, docs de arquitectura, o commits relacionados.
```

## Siguiente ID disponible

`ISSUE-121`

## Open

| ID          | Título                                                                                                                       | Ambiente             | Detectado  | Estado |
| ----------- | ---------------------------------------------------------------------------------------------------------------------------- | -------------------- | ---------- | ------ |
| `ISSUE-118` | [Los readers del watchdog usan un resolver PAT-only e ignoran el GitHub App ya provisionado](open/ISSUE-118-watchdog-readers-sync-resolver-ignore-github-app.md) | local + Vercel (no afecta GH Actions) | 2026-07-10 | open |
| `ISSUE-117` | [Offboarding ejecutado nunca desactiva `greenhouse_core.members` (active/status) → desvinculados filtran a rosters/nómina/360](open/ISSUE-117-offboarding-executed-never-deactivates-member-canonical.md) | production + staging (identity canónico) | 2026-07-06 | open |
| `ISSUE-114` | [Preflight batch-policy: falso positivo `requires_break_glass` por divergencia de squash-merge](open/ISSUE-114-release-batch-policy-classifier-squash-divergence-false-positive.md) | CI/local (release preflight) | 2026-07-03 | open (mitigado con bypass + sync merge-base; fix de raíz pendiente) |
| `ISSUE-113` | [Brand-intelligence: el provider Gemini errora (Vertex) y bloqueaba toda la lectura](open/ISSUE-113-brand-intelligence-gemini-provider-error.md) | local + staging (ops-worker) | 2026-07-02 | open (mitigado: router fallthrough) |
| `ISSUE-108` | [El color `primary` falla contraste WCAG 4.5:1 como texto (breadcrumb/botón/links) portal-wide](open/ISSUE-108-primary-palette-text-contrast-wcag-fail.md) | staging + production (paleta) | 2026-06-26 | open   |
| `ISSUE-106` | [CHECK product_catalog_hubspot_trace_consistent bloquea productos inbound de quotes HubSpot](open/ISSUE-106-product-catalog-hubspot-trace-check-blocks-inbound-quote-products.md) | dev/staging data plane | 2026-06-22 | open   |
| `ISSUE-044` | [Dashboard pages 500 via agent headless](open/ISSUE-044-dashboard-ssr-500-agent-headless.md)                                  | staging              | 2026-04-11 | open   |
| `ISSUE-046` | [Deliverability monitor siempre devuelve 0 bounces y 0 complaints](open/ISSUE-046-deliverability-monitor-query-rota.md) | production + staging | 2026-04-13 | open   |
| `ISSUE-047` | [Kill switch de email sin endpoint CRUD — solo operable por SQL directo](open/ISSUE-047-email-kill-switch-sin-endpoint-crud.md) | production + staging | 2026-04-13 | open   |
| `ISSUE-050` | [Staging salta correos de permisos porque el runtime reactivo no tiene Resend configurado](open/ISSUE-050-staging-leave-emails-skipped-resend-not-configured-in-reactive-runtime.md) | staging | 2026-04-15 | open |
| `ISSUE-058` | [Teams Finance Alerts webhook not provisioned in GCP Secret Manager](open/ISSUE-058-teams-finance-alerts-webhook-not-provisioned.md) | staging + production | 2026-04-26 | open (mitigated via `pending_setup`, awaiting Azure Logic App deploy from TASK-669) |
| `ISSUE-067` | [Vitest jsdom warning por Lottie en `ServicesListView` empty state](open/ISSUE-067-jsdom-canvas-warning-lottie-empty-state-fetch-mock.md) | local test suite + CI logs | 2026-05-07 | open |
| `ISSUE-081` | ["Días de retraso" no congela tiempo no imputable (freeze roto: `frozenDays`=0) → penaliza OTD% y bonus](open/ISSUE-081-dias-retraso-freeze-roto-frozendays-cero-penaliza-otd-bonus.md) | production (Efeonce) | 2026-05-23 | open |
| `ISSUE-087` | [Account 360 delivery facet falla por drift de columnas RpA en serving cache](open/ISSUE-087-account-360-delivery-rpa-median-schema-drift.md) | preview | 2026-06-12 | open |
| `ISSUE-101` | [Posición IVA mensual mal scopeada por `space_id` excluye todo el crédito fiscal](open/ISSUE-101-vat-monthly-position-mis-scoped-by-space-excludes-credito-fiscal.md) | production + staging | 2026-06-20 | open |

## Resolved

| ID          | Título                                                                                                                                                            | Ambiente                       | Detectado  | Resuelto   | Causa                                                                                                                    |
| ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ | ---------- | ---------- | ------------------------------------------------------------------------------------------------------------------------ |
| `ISSUE-120` | [AI Visibility Grader: `citation_quality` estructuralmente 0 + degradación silenciosa de la extracción de prosa](resolved/ISSUE-120-ai-visibility-grader-citation-quality-structural-zero-and-silent-prose-degradation.md) | pipeline normalización/scoring | 2026-07-11 | 2026-07-11 (TASK-1390: clasificador sourceType + same-site + prose outcome + backoff; re-score live verificado) |
| `ISSUE-119` | [Saturación de conexiones PG por scripts tsx locales zombies](resolved/ISSUE-119-pg-connection-saturation-zombie-local-scripts.md) | Cloud SQL única instancia (afectaba prod) | 2026-07-10 | 2026-07-10 | Dos scripts `tsx` locales de una sesión previa quedaron colgados 4+ horas con pools vía Cloud SQL Connector (invisibles a `lsof :15432`) y agotaron los 100 slots de `greenhouse-pg-dev`. Kill de los zombies → 3/100 conexiones al instante. Lección: scripts con pool deben cerrar con `process.exit()`; diagnóstico debe revisar `ps aux | grep tsx` además del proxy. |
| `ISSUE-116` | [El deadline de cálculo de nómina se anclaba al último día hábil del mes del período, no al close-window del mes siguiente](resolved/ISSUE-116-payroll-calculation-deadline-anchored-on-wrong-month.md) | staging + production (payroll) | 2026-07-06 | 2026-07-06 | El período de junio aparecía "Fuera de plazo operativo" con deadline `2026-06-30`, pero Efeonce paga dentro de los primeros 5 días hábiles POSTERIORES al cierre → el deadline correcto es el 5.º día hábil de julio (`2026-07-07`). `resolvePayrollCalculationDeadline` anclaba en `getLastBusinessDayOfMonth(period.month)` (mes equivocado). No bloqueaba el cálculo (`blocksCalculation:false`), pero la etiqueta + `overdue_allowed` + `calculatedOnTime` eran incorrectos para TODOS los períodos. Fix: primitiva canónica `getNthBusinessDayOfMonth` + deadline = N.º día hábil del mes siguiente (reusa `closeWindowBusinessDays`) + rename semántico `lastBusinessDay→deadlineDate`. Code-only. Verificado: readiness 2026-06 → `deadlineDate:2026-07-07, state:pending, isOverdue:false`; 596 tests + typecheck verdes. |
| `ISSUE-115` | [Nómina bloqueada permanentemente por `daily_required` incoherente con el régimen de contrato](resolved/ISSUE-115-payroll-blocked-daily-required-incoherent-with-regime.md) | staging + production (payroll) | 2026-07-06 | 2026-07-06 | El readiness exigía señal de asistencia diaria a un `international_internal` con `members.daily_required=true` (Maria Fernanda Gonzalez) → **Calcular** deshabilitado, sin recuperación (`attendance_daily` vacía). Root cause: `requiresPayrollAttendanceSignal` era denylist (`!=honorarios && !=deel && scheduleRequired!==false`) → régimen internacional se colaba; y el read mapper bypaseaba `resolveScheduleRequired`. Resuelto por TASK-1347: predicado **allowlist régimen-scoped** (`isChileDependentContract`) + read mapper que honra `SCHEDULE_DEFAULTS.overridable`. El régimen es autoritativo sobre `daily_required`. Sin migración/backfill (la data internacional con `daily_required=true` es válida, `overridable`). Verificado: `getPayrollPeriodReadiness('2026-06')` → `blockingIssues=[]`; 8806 tests + build verdes. |
| `ISSUE-111` | [Flake de tests en CI/ci-deep bloqueó el preflight de un release productivo (pdf-parse bajo Node 24)](resolved/ISSUE-111-ci-test-flake-blocked-production-release-twice.md) | CI (GitHub Actions) | 2026-06-30 | 2026-07-02 | Los 3 tests de `generate-payroll-pdf.test.ts` (TASK-782) re-parseaban el PDF binario con `pdf-parse@1.1.1`, cuyo pdf.js ~2016 lanza `Illegal character: 41` de forma no-determinista **bajo Node 24** (destapado por el cutover CI Node 20→24 del 2026-06-22, TASK-845). Bloqueó el preflight `ci_green` de 2 releases. Resuelto eliminando `pdf-parse`: helper compartido `src/test/react-pdf-text.ts` (`extractReactPdfText`) aserta sobre el árbol de elementos react-pdf sin binario, vía builders SSOT (`build*DocumentElement()`) que producción renderiza y los tests inspeccionan. Suite completa 8639 passed / 0 failed. |
| `ISSUE-112` | [Form público de AEO shipeado ROTO a prod: gate de aserciones sin verificación visual + parche por-host + cutover live prematuro](resolved/ISSUE-112-aeo-form-shipped-broken-assertion-gate-no-visual.md) | production (`/aeo-2/`, tema Ohio) | 2026-06-30 | 2026-06-30 | La migración TASK-1298 (bridge → renderer `<greenhouse-form>`) shipeó el form roto (inputs grises, `<select>` chevron-wall, botón oscuro) porque el tema Ohio le pisó el CSS al renderer. El agente declaró "verificado" confiando en un gate de aserciones que **no miraba el render**, parchó por-host sobre el bundle viejo (overrides no aplicaban) y tocó prod temprano. Detectado por el operador. Resuelto: restaurar backup → bridge (form pulido, `heroans` estable), verificado mirando el frame. Fix de fondo (Codex, en `develop`): endurecer el renderer + gates que muestrean píxeles + verificación en memoria pre-live. Regla durable: mirar el frame real antes de "listo", arreglar el componente no parchar por host, no tocar prod hasta verificar pre-live (memoria `feedback_verify_visual_root_cause_prelive`). |
| `ISSUE-110` | [AEO Grader: diagnóstico falso (score 0) para marcas no-agencia/consumo — prompt pack ICP-Efeonce + bypass de taxonomía](resolved/ISSUE-110-aeo-grader-false-zero-non-agency-brands-taxonomy-bypass.md) | staging + production | 2026-06-29 | 2026-06-30 | Resuelto por EPIC-021 (5 tasks): el grader estaba calibrado solo para el ICP agencia/B2B-svc (prompt pack único + bypass de taxonomía que inyectaba el HubSpot industry enum crudo) → falso-0 a marcas de consumo (SKY). Fix: TASK-1288 (categoría canónica, deja de inyectar el enum) + TASK-1289 (eje `business_model`/arquetipo) + TASK-1290 (packs de prompts por arquetipo × buyer-intent, scoring INTACTO) + TASK-1291 (guard `assertSubjectGradeable` + reabilitar cross-sell con seguridad) + TASK-1292 (eval de cobertura por arquetipo + drift signal). Motor + flags ON en prod (release `056c2dde8`) y staging (parity flip). Smoke staging: signal `operator_gate_blocking`=ok, 0 prospectos no graduables; SKY scorea con prompts de consumo (0 agency leak). |
| `ISSUE-109` | [`next build` local congela la máquina (concurrencia de static-generation sin cap en local)](resolved/ISSUE-109-local-next-build-static-gen-concurrency-freezes-machine.md) | local | 2026-06-28 | 2026-06-28 | El cap `experimental.cpus: 4` (TASK-1157) se aplicaba SOLO en Vercel; en local `next build` usaba `os.cpus().length` (10) workers de static-generation, cada uno heredando heap de 8 GB + Turbopack → sobre-suscribe 16 GB de RAM → swap → freeze. No es Turbopack vs Webpack (dev ya usa webpack). Fix: cap aplicado también en local (default 4, env `NEXT_BUILD_CPUS` override) + script `build:fast` (cpus=2). |
| `ISSUE-107` | [ops-worker path filter excluye `src/lib/nubox` → Nubox sync con código stale](resolved/ISSUE-107-ops-worker-path-filter-excludes-nubox-stale-sync-code.md) | production | 2026-06-22 | 2026-06-22 | El ops-worker corre el Nubox sync (bundlea `src/lib/nubox`) pero el path filter de `ops-worker-deploy.yml` no incluía `src/lib/nubox/**` → el deploy se salteó y el worker quedó stale (sin el fix TASK-1210). Fix: nubox en las 3 listas de paths + 8 flags MXN/CLF en `deploy.sh` + redeploy break-glass → `GIT_SHA=3a39c68ba`, watchdog drift=ok (4/4 synced). |
| `ISSUE-045` | [Registrar OC falla por referencia ambigua a `client_id` en el resolver canónico de Finance](resolved/ISSUE-045-purchase-order-create-ambiguous-client-id.md) | staging | 2026-04-13 | 2026-06-20 | `resolveFinanceClientContext()` usaba columnas sin alias en el lookup de `client_profiles` unido a `spaces`, provocando `column reference "client_id" is ambiguous`. Fix aliasó `cp.*`; verificado con tests focales y `POST /api/finance/purchase-orders` en staging respondiendo HTTP 201. |
| `ISSUE-055` | [Quote builder no puede cotizar `ECG-004` por gap canónico en cost basis del rol](resolved/ISSUE-055-quote-builder-role-sku-missing-cost-basis.md) | staging | 2026-04-19 | 2026-06-20 | `ECG-004` existía en el catálogo y tenía pricing por moneda, pero no tenía `role_employment_compatibility` ni `sellable_role_cost_components` porque TASK-464a lo dejó `needs_review` por mezclar Fee Deel + gastos previsionales. Migración `20260620190000000` inserta cost basis revisado `indefinido_clp` para `ECG-004/017/018` con provenance `admin_manual`; staging simulate devuelve HTTP 200 y `role_modeled`. |
| `ISSUE-105` | [Boleta de honorarios anulada en SII seguía contando en la posición F29 de retención](resolved/ISSUE-105-annulled-honorario-counted-in-f29-retention.md) | production + staging | 2026-06-20 | 2026-06-20 | Validación contable real del F29 mayo 2026 vs SII: una boleta anulada en SII/Nubox (`sii_document_status='Anulada'`, folio 40) quedó con `is_annulled=false` en `expenses` porque el sync Nubox derivaba `is_annulled` solo del booleano (no del `document_status_name='Anulada'`) y el materializador de retención no excluía anulados → +107.970 sobre-declarado. Resuelto por TASK-1204: helper `isNuboxPurchaseAnnulled` (sync), guard `is_annulled` en `retention-ledger`, backfill (2 docs) + re-materialización. Confirmó además la tasa PPM real (0,125%, no placeholder 0,25%). Mayo cuadra al peso con el SII (retención 134.653, PPM 7.250, total 1.222.308). |
| `ISSUE-104` | [`pnpm build` hace OOM sistemático bajo el Node 20 al que Volta ata pnpm](resolved/ISSUE-104-pnpm-build-oom-node20-volta-heap.md) | local + agentes (gate de build) | 2026-06-20 | 2026-06-20 | Volta ata `pnpm` a Node 20.20.1 (old-space chico) y el build runner `scripts/run-next-build.mjs` no pasaba por el flag de heap que `local:check` ya aplica a tsc → `pnpm build` hacía OOM determinista en la fase post-compile. Resuelto inyectando `--max-old-space-size=8192` en NODE_OPTIONS del `next build` (commit `11ac6adf9`); verificado bajo el toolchain default sin workaround manual. |
| `ISSUE-103` | [Docs con IVA sincronizados desde Nubox sin período fiscal → excluidos del F29](resolved/ISSUE-103-nubox-synced-vat-docs-missing-fiscal-period.md) | production + staging | 2026-06-20 | 2026-06-20 | El sync de Nubox no estampaba `period_year`/`period_month`, así que 165 docs con IVA (53 income + 112 expense) quedaban sin período y nunca entraban al F29. Resuelto por TASK-1191: helper canónico `getOperationalFiscalPeriod()`, sync estampa el período + self-heal, backfill idempotente source-agnostic + re-materialización de 30 posiciones VAT. Signals `finance.vat.eligible_without_period` y `finance.vat.position_drift` en `ok`. |
| `ISSUE-102` | [Cost Intelligence publicaba margen falso por Cost Attribution degradado](resolved/ISSUE-102-cost-intelligence-commercial-cost-attribution-deadletters-and-false-margin.md) | dev/staging data plane | 2026-06-20 | 2026-06-20 | Dead letters históricas `commercial_cost_attribution:*` por DDL runtime sobre `greenhouse_serving` + `materializeOperationalPl()` ocultaba fallos técnicos y no tenía gate para revenue con costo `0` por upstream faltante. Resuelto por TASK-1190 con replay focal, `handler_health` healthy, dead letters activas 0 y signal `finance.operational_pl.cost_coverage_degraded`. |
| `ISSUE-100` | [Local dev queda en skeleton/`Compiling...` en Nexa Insight por Turbopack](resolved/ISSUE-100-local-dev-turbopack-nexa-insight-skeleton-stuck.md) | local dev (`/nexa/insights/[id]`) | 2026-06-20 | 2026-06-20 | Turbopack local dejaba `next-server` con CPU alto y el fallback `loading.tsx` de Nexa Insight pegado aunque GCP/Cloud SQL estuvieran sanos. No se quitaron skeletons: siguen siendo el estado honesto de carga. Resuelto cambiando `pnpm dev` a `next dev --webpack`, dejando `pnpm dev:turbo` como opt-in, reiniciando el server y verificando que la ruta reemplaza skeletons por contenido real en local. |
| `ISSUE-099` | [Design Handoff inspector no cerraba y regresión visual del cockpit](resolved/ISSUE-099-design-handoff-inspector-ui-regression.md) | local dev (`/design-system/handoff`) | 2026-06-20 | 2026-06-20 | El shell heredaba el affordance AXIS/Figma en una ruta que ya es handoff; el inspector mostraba todas las secciones abiertas y su `onClose` limpiaba `selectedId`, pero el fallback de `selectedEntry` re-seleccionaba el primer entry. Resuelto ocultando el affordance por ruta, compactando la lane con accordions, moviendo acciones al workbench y separando `inspectorOpen` de `selectedId`. El `Compiling...` local correspondía a degradación dev server/PG pool tipo ISSUE-094, no a JSX. |
| `ISSUE-098` | [Cálculo por-tarea de atraso imputable (M2 shadow) congelado como "abierta" en tareas completadas → bloquea writeback `[GH] OTD`](resolved/ISSUE-098-attributable-lateness-shadow-stale-terminal-tasks.md) | production + staging | 2026-06-19 | 2026-06-19 | El compute M2 event-driven re-leía `greenhouse_delivery.tasks` (`task_status`/`completed_at`), pero ese row sync laggea la transición terminal que **dispara** el compute (`task_status_transitions` es otro pipeline). Como `Aprobado`/`Archivado` son terminales (sin transición futura), el bucket abierto quedaba congelado en 250/337 filas. Resuelto por TASK-1174 sin migración: `resolveEffectiveTaskState` (estado efectivo desde el log de transiciones, `completed_at` de respaldo = `transitioned_at` terminal) + core extraído + barrido idempotente (backfill 250→0 terminal-open) + signal `delivery.attributable_lateness.shadow_terminal_open` (steady=0, gate del writeback TASK-927). Invariante nuevo: tarea terminal NUNCA con bucket abierto. ADR §16.12. |
| `ISSUE-097` | [OTD de Daniela salia cerca de 5% por `metrics_by_member` stale](resolved/ISSUE-097-ico-member-metrics-stale-daniela-otd-rpa.md) | production (Efeonce) | 2026-06-17 | 2026-06-17 | `metrics_by_member` de junio 2026 estaba materializado desde `2026-06-01` mientras las fuentes base estaban frescas al `2026-06-16`; los readers aceptaban cualquier fila existente y no hacian fallback live. Resuelto por TASK-1163: freshness guard current-period + fallback live canonico + `sourceMode` real en payroll + health script + rematerializacion member-only junio 2026 (`7` filas; Daniela OTD `99.1`, RpA `1.14`). |
| `ISSUE-096` | [Build de Vercel OOM-ea flaky y bloquea deploys](resolved/ISSUE-096-vercel-build-oom-flaky.md) | staging + production | 2026-06-16 | 2026-06-16 | `next build` (Turbopack) OOM-eaba flaky en el builder default (~8 GB): **1106 entrypoints** + workers de static-gen (~9 = `os.cpus().length`) → pico de RAM al borde del techo → SIGKILL silencioso a los 45 min (colgado en "Creating an optimized production build"). Flaky: mismo commit Ready u OOM en redeploys; bloqueaba cualquier deploy. NO era código (build local + CI verdes). Resuelto $0 en `next.config.ts` gateado a Vercel: `experimental.cpus: 4` (static-gen ~9→4) + sourcemaps de Sentry solo en producción (`VERCEL_ENV==='production'`; off en staging/preview). `turbopackMemoryLimit: 6 GiB` descartado por medición live (bajo el working set → thrashing 25 min+, peor que el OOM). Verificado: deploy Ready en 7 min ("Compiled in 3.1min") vs 3 deploys sin fix OOM a 45-46 min. Enhanced Builds = no-go. Cero runtime. Hermano de ISSUE-095. |
| `ISSUE-095` | [Sentry source-map upload degrada por token sin permisos de release/source maps (`403`)](resolved/ISSUE-095-sentry-sourcemap-upload-token-403.md) | staging + production | 2026-06-13 | 2026-06-14 | El `SENTRY_AUTH_TOKEN` cargado en Vercel `staging` Y `Production` (42d, mismo token) era un token personal read-only (`event:read`+`project:read`, `org:read=403`, sin `project:releases`) → `sentry-cli releases new`/source-map upload daban `403 You do not have permission`. El token correcto ya existía en `.env.vercel-staging`. Resuelto: `vercel env add --force` del token bien scopeado en ambos entornos (verificado `org:read=200`+`project:releases`) + subir `SENTRY_SOURCEMAP_UPLOAD_TIMEOUT_MS` a 180000ms (el upload legítimo tardaba >30s y lo cortaba el guardrail de ISSUE-093). Verificado en build real `greenhouse-4wzmp4oj9`: `runAfterProductionCompile` completó en 40.7s sin 403 ni timeout. Sin cambio de código. |
| `ISSUE-094` | [Dev server: render SSR 12-66s en TODOS los routes por PG pool cerrado tras HMR](resolved/ISSUE-094-dev-server-pg-pool-closed-after-hmr-render-degradation.md) | local dev (Turbopack + GVC) | 2026-06-13 | 2026-06-13 | Tras sesión larga (muchos HMR + `pnpm build` + GVC), el pool PG singleton (`src/lib/postgres/client.ts`) queda cerrado (`Cannot use a pool after calling end on the pool`); cada query reintenta 3×1s → render SSR de 12-66s en TODOS los routes (home/border-beam incluidos). Afecta GVC `--env=local` (falsos timeouts) y confunde A/B de render-timing. NO afecta prod/staging (sin HMR). Detectado en TASK-1108 (el código era correcto). Resuelto: restart del dev server (pool fresco) + diagnóstico canónico (`grep "Cannot use a pool"` + comparar render de un route ajeno ANTES de culpar al código). Hermano de ISSUE-085. Fix de fondo opcional: pool resiliente a `end()` accidental en dev. |
| `ISSUE-093` | [Builds de staging fallan por timeout (45m) colgados en la subida de source-maps a Sentry (`runAfterProductionCompile`)](resolved/ISSUE-093-vercel-build-timeout-sentry-sourcemap-upload-hang.md) | staging | 2026-06-13 | 2026-06-13 | `withSentryConfig` subía sourcemaps en `runAfterProductionCompile` sin presupuesto defensivo; si Sentry/source-map upload se colgaba, Vercel quemaba 45-46m y fallaba. Resuelto con guardrail `runSentrySourcemapUploadWithTimeout`, kill de `sentry-cli`, `errorHandler` fail-open y `SENTRY_SOURCEMAP_UPLOAD_TIMEOUT_MS=30000` en staging. Deploy `greenhouse-clbkbt7o6` (`a9dcb389c`) quedó Ready en 7m; hook completó en 13686ms pese a 403 de Sentry. |
| `ISSUE-092` | [Nexa tool-calling roto: `id` huérfano en `functionResponse` (Gemini 2.5)](resolved/ISSUE-092-nexa-tool-calling-broken-function-response-id.md) | Production runtime (Nexa chat) | 2026-06-12 | 2026-06-12 | Cualquier respuesta de Nexa que invocara un tool reventaba con 400 `Unknown name "id" at function_response`. El `functionCall` part se arma sin `id` pero el `functionResponse` (`createPartFromFunctionResponse` de `@google/genai@1.45.0`) inyectaba un UUID random; Gemini 2.5 no acepta `id` en `functionResponse`. Afectaba TODOS los tools (`check_payroll` etc.). Destapado por el smoke de TASK-1085. Resuelto: construir el part según el schema real (`{name, response}`, sin id; match por nombre/orden) en el path compartido `nexa-service.ts`. Smoke live: Nexa responde con citas del corpus real; 16 tests verdes. |
| `ISSUE-091` | [`/api/my/performance` filtraba campos de compensación/costo a colaboradores](resolved/ISSUE-091-my-performance-self-service-pii-leak.md) | Production runtime (self-service) | 2026-06-10 | 2026-06-10 | El endpoint self-service devolvía `intelligence`/`intelligenceTrend` crudos (`PersonIntelligenceSnapshot` con `cost.{monthlyBaseSalary,monthlyTotalComp,compensationVersionId,loadedCostTarget,costPerHourTarget,suggestedBillRateTarget}`). Fuga de shape de respuesta (anti-IDOR ya era correcto). Resuelto (TASK-1027 Slice 1): DTO con **redacción por construcción** — el composer `src/lib/my-performance/dto.ts` nunca importa los readers con costo; solo readers cost-free. Tests prohíben las keys + cubren anti-IDOR/validación. |
| `ISSUE-090` | [ops-worker crash por paquete runtime (`pngjs`) en `devDependencies`](resolved/ISSUE-090-ops-worker-pngjs-devdep-startup-crash.md) | `ops-worker` (Cloud Run, deploy) | 2026-06-09 | 2026-06-09 | `services/ops-worker/server.ts` importa estático `organization-brand-assets-discovery` (TASK-999) que usa `pngjs` en runtime, pero `pngjs` estaba en `devDependencies`. El runtime stage del Dockerfile hace `pnpm install --prod` (solo `dependencies`) + esbuild `--packages=external` → `pngjs` externalizado y no instalado → `ERR_MODULE_NOT_FOUND` en startup → no escucha en 8080 → deploy failed (silent startup crash; revisión vieja seguía sirviendo). Hermano de ISSUE-086 (`@core` boundary). Resuelto: `pngjs`→`dependencies` (+lockfile) + guard canónico `pnpm worker:runtime-deps-gate` (replica el bundle de los 3 workers, falla loud si un externalizado no está en deps; wired en ci.yml) + regla "Worker runtime npm deps" en CLAUDE.md/AGENTS.md. ops-worker deploy SUCCESS (`edffa61f4`). |
| `ISSUE-086` | [ICO batch worker crash por import `@core/theme` en código de dominio worker-bundled](resolved/ISSUE-086-ico-batch-worker-core-theme-import-crash.md) | `ico-batch-worker` (Cloud Run, deploy) | 2026-06-08 | 2026-06-08 | TASK-1053 (charts) agregó `import @core/theme/axis-chart` a `metric-registry` (dominio que el worker bundlea) para un mapa de color de UI; el `.dockerignore` de los workers excluye `src/@core` y esbuild usa `--packages=external` → `@core/theme` externalizado → `ERR_MODULE_NOT_FOUND` en startup → deploy failed. Resuelto (TASK-1048): layering (`CSC_CHART_COLORS`→`src/components/`), SoT de tokens runtime-agnóstico (`src/lib/design-tokens`), guard esbuild `--alias:@core` en los 3 workers (crash→loud-build-fail), regla canónica "Worker @core boundary" en CLAUDE.md/AGENTS.md. ico-batch+ops-worker deploy SUCCESS. |
| `ISSUE-083` | [session_360.route_groups filtra route groups de roles revocados (over-exposure de navegación)](resolved/ISSUE-083-session-route-groups-leak-from-revoked-roles.md) | dev/staging + producción | 2026-06-01 | 2026-06-01 | El view `session_360` derivaba `route_groups` sin el predicado de lifecycle que sí aplica a `role_codes` → roles revocados seguían aportando su `route_group_scope` (Valentina collaborator veía Personas/Comercial; Humberly Finanzas+HR). Resuelto con TASK-987: FILTER lifecycle en el agregado + re-grant canónico de Humberly (finance_admin+hr_manager) + signal `identity.session.route_group_drift` (steady=0). |
| `ISSUE-084` | [Invitación al portal cliente rota (INSERT client_users sin user_id + auth_mode inválido)](resolved/ISSUE-084-client-portal-invite-insert-broken.md) | producción + staging + local | 2026-06-04 | 2026-06-04 | `inviteClientPortalUser` (onboarding TASK-1001 + `/api/admin/invite`) fallaba al crear usuario: el INSERT no proveía `user_id` (NOT NULL sin default) y usaba `auth_mode='credentials'` (CHECK TASK-742 exige password_hash NOT NULL → rechaza un invitado sin password). Latente: el INSERT pre-data el CHECK; unit tests mockeaban la DB. Fix robusto del lifecycle completo: invite genera `user_id`=randomUUID + `auth_mode='invited'`; `accept-invite` flipea a `credentials`/`both` al setear password. + guard de regresión. Verificado e2e contra DB real (invite+activación) + `emailSent:true`. |
| `ISSUE-082` | [Nexa Insights en falso-sano: timestamp NULL en BQ DML + DELETE+INSERT destructivo + runs succeeded con 0 señales](resolved/ISSUE-082-nexa-insights-false-healthy-destructive-replace-null-timestamps.md) | production (Efeonce) + staging | 2026-05-27 | 2026-05-28 | BQ DML `ARRAY<STRUCT<TIMESTAMP>>` recibía ISO strings y escribía NULL; el enriquecedor descartaba señales y marcaba runs `succeeded` con 0. Resuelto con STRING+CAST + helper/lint, invariante anti-falso-sano, guard no-destructivo, signal `nexa.insights.stale_with_eligible_signals`, self-heal live y remediación acotada de 40 `ai_prediction_log.predicted_at` NULL. |
| `ISSUE-080` | [Nubox /sales timeout transitorio paginea quotes-hot](resolved/ISSUE-080-nubox-sales-transient-timeout-pages.md) | production | 2026-05-22 | 2026-05-22 | `listNuboxSales` usaba timeout default 15s; cuando Nubox `/sales` está transitoriamente lento (corrida legítima de 45s observada) una página agotaba los 4 reintentos y `syncNuboxQuotesHot` re-lanzaba → `wrapCronHandler` lo capturaba como error duro (502) y paginaba. Sentry `JAVASCRIPT-NEXTJS-6E`. Resuelto de fondo: timeout propio env-tunable `NUBOX_SALES_LIST_TIMEOUT_MS` (default 30s) + `NuboxApiError` tipado (transient vs real) + degradación honesta (transient → run failed + WARNING + cron 200, recupera próximo ciclo */15min; real → paginea) + per-period isolation. Backstop: freshness signal Nubox (TASK-841). Commits `c7eadf2c` + `e50c4811`. |
| `ISSUE-079` | [Notion conformed → PG drain viola FK sync_run_id](resolved/ISSUE-079-notion-conformed-drain-fk-violation.md) | production | 2026-05-22 | 2026-05-22 | `syncBqConformedToPostgres` estampaba `sync_run_id` en `greenhouse_delivery.{tasks,projects,sprints}` (FK → `source_sync_runs`) pero nunca creaba el row parent. El handler usaba un fallback sintético `pg-drain-<timestamp>` cuando la orquestación devolvía `syncRunId=null` al hacer skip → id sin parent → violación de FK en cada drain. Sentry `JAVASCRIPT-NEXTJS-6C`. Resuelto de fondo: el drain posee su propio `source_sync_runs` run (open `running` antes de estampar → FK siempre satisfecho; finalize `succeeded`/`failed`) + orchestration id solo como lineage. Backstop: signal `sync.notion_conformed_drain.freshness`. Commits `0521c236` + `e50c4811`. |
| `ISSUE-078` | [SQL reader schema mismatch task_source_id canonical drift](resolved/ISSUE-078-sql-reader-schema-mismatch-task-source-id-canonical-drift.md) | preview + staging + production (`/admin` page) | 2026-05-18 | 2026-05-18 | Reader signal `notion.correction_transitions.source_availability` (shipped TASK-908 Slice 3.5 commit `ca62969a` 2026-05-18) usaba `t.task_source_id` en query SQL contra `greenhouse_delivery.tasks` donde el column canonical real es `t.notion_task_id` (NOT NULL). Sentry alert `JAVASCRIPT-NEXTJS-65` continuo en cada render `/admin`. Causa raíz: schema naming drift entre 2 tablas — `tasks.notion_task_id` ⇄ `task_status_transitions.task_source_id` (mismo Notion page UUID, nombres distintos). Yo violé CLAUDE.md "SQL Signal Reader Schema Validation Gate" (TASK-893 hotfix #3) al shipear sin live PG smoke verify. Resuelto canonical V1: alias `t.notion_task_id AS task_source_id` en CTE (pattern fuente `get-project-detail.ts:484`) + JSDoc canonical documenta el cross-table naming drift + live PG smoke verified pre-merge (1927 tasks, severity=error 100% steady state pre-TASK-912 esperado). Architectural rename de column `task_source_id → notion_task_id` deferred a evaluation post-mortem sin urgencia (drift residual documented). |
| `ISSUE-077` | [Nexa Person Insights empty state por runtime DDL](resolved/ISSUE-077-nexa-person-insights-empty-state-runtime-ddl.md) | staging | 2026-05-18 | 2026-05-18 | `/api/people/[memberId]/intelligence` intentaba `CREATE TABLE IF NOT EXISTS` en runtime sobre `greenhouse_serving`; `greenhouse_app` tiene lectura pero no `CREATE`, la API devolvía 500 y la UI caía a `Sin datos`. Resuelto quitando DDL del read path y usando probes read-only `SELECT 1 ... LIMIT 0`. |
| `ISSUE-075` | [Entra webhook validation handshake rejects POST (subscription create/renew fails)](resolved/ISSUE-075-entra-webhook-validation-handshake-rejects-post.md) | production (Vercel + ops-worker) | 2026-05-13 | 2026-05-13 | Cron `ops-entra-webhook-renew` fallaba `ValidationError 400` porque el notification endpoint `POST /api/webhooks/entra-user-change` NO respondía al handshake POST de Microsoft Graph (la spec V1 decía GET, pero Microsoft envía POST). Resuelto via fix immediate (helper `respondToValidationToken` shared GET+POST, commit `86890bae`) + hardening 3-layer (env-aware notification URL resolver + persistSubscriptionState completo + reliability signal `identity.entra.webhook_subscription_health` con state machine 6-estados). Promovido a producción 2026-05-13 via canonical orchestrator release run `25825280928` manifest `e02cb32e9c30-5acb894c-f164-486c-99c0-074d42aefbeb`. Pattern canonizado: webhooks externos con validation handshake deben soportar todos los métodos HTTP del contract + responder al handshake ANTES de validation de payload + tests anti-regresión + reliability signal subscription health + audit trail estructurado. |
| `ISSUE-076` | [Vercel CLI duplicate project recurrent bug class](resolved/ISSUE-076-vercel-cli-duplicate-project-recurrent-bug-class.md) | production (Vercel cloud) + local agents/devs | 2026-05-13 | 2026-05-13 | Bug class recurrente (2do incidente — ISSUE-013 fue el 1ro). Agente "Kortex Agent" corrió `vercel` CLI 2026-04-14 sin `--scope efeonce-7670142f`, Vercel CLI default scope = cuenta personal del operador → creó proyecto duplicado `prj_FKsbIbQfUHp8OlNgnWp5j7RHnYsL` auto-vinculado al repo. 29 días generando email burst de preview deploys fallidos hasta detección. Hard rule canonical existía (commit `c03a23a5` 2026-04-05) pero era prosa NO enforced. Resuelto con defense-in-depth de 3 capas: (a) delete duplicado con verify-then-delete defensive pattern, (b) `.vercel/project.json` checked-in al repo (structural enforcement — pinea scope canonical para Vercel CLI), (c) `.gitignore` ajustado `.vercel/*` + `!.vercel/project.json` preservando `.env*.local` secrets ignored, (d) CLAUDE.md sección "Vercel CLI Scope Discipline" canonizada con 5 reglas duras + verify-then-delete pattern reusable. Lección operativa canonizada: cuando una hard rule se viola más de una vez, la siguiente iteración del fix debe ser estructural (lint rule / checked-in file / CI gate), no más prosa. |
| `ISSUE-074` | [ops-worker missing `@sentry/nextjs` bundle bloquea reactive projections](resolved/ISSUE-074-ops-worker-missing-sentry-bundle-blocks-projections.md) | production (Cloud Run ops-worker) | 2026-05-09 | 2026-05-09 | El wrapper canónico `captureWithDomain` importaba `@sentry/nextjs` (Next.js-específico) cuyo shape variaba en runtime Cloud Run, causando `Sentry.captureException is not a function` en projections. TASK-813b nunca funcionó end-to-end en producción. Resuelto via TASK-844: switch a `@sentry/node` runtime-portable + helper canónico `initSentryForService` invocado por cada Cloud Run service en server.ts + lint rule + reliability signal anti-regresión + CLAUDE.md hard rule. Verificado live 2026-05-09 19:30:04 con cycle PATCH→outbox→reactive→PG sync completo (materialized=1/1 failures=0). |
| `ISSUE-073` | [Playwright flaky smoke-lane semantics and transient navigation timeouts](resolved/ISSUE-073-playwright-flaky-smoke-lane-semantics-and-navigation.md) | GitHub Actions / Playwright E2E smoke | 2026-05-09 | 2026-05-09 | El publisher leia solo `test.results[0]`, por lo que un retry `failed -> passed` quedaba contado como falla final aunque Playwright reportara `flaky`. Ademas, `page.goto` directo en smoke tests trasladaba cold-start/timeouts transitorios al retry global. Resuelto con parser canonico `scripts/lib/smoke-lane-report.ts` (`flaky != failed`), tests anti-regresion y fixture `gotoWithTransientRetries` reutilizada por navegaciones autenticadas. |
| `ISSUE-072` | [CI smoke-lane publisher fallaba con warning non-blocking](resolved/ISSUE-072-ci-smoke-lane-publisher-failed-non-blocking.md) | GitHub Actions / Playwright E2E smoke | 2026-05-09 | 2026-05-09 | Cadena de contrato incompleto: `sync:smoke-lane` sin `server-only` shim; Secret Manager ref `secret:version` inválido; WIF deployer sin `roles/cloudsql.client`; y Postgres bajo presión transitoria (`53300`) sin retry/backoff. Resuelto en primitives compartidas: script con shim, workflow con secret ref canónico + pool 1, IAM versionado en `scripts/setup-github-actions-wif.sh`, y `src/lib/postgres/client.ts` con retry/backoff acotado para errores transitorios. Playwright run `25605103310` publicó `finance.web`, `delivery.web` e `identity.web` sin annotations `sync:smoke-lane failed`; CI run `25605103305` verde. |
| `ISSUE-071` | [Workspace projection relationship resolver throw silencioso por type mismatch en COALESCE](resolved/ISSUE-071-workspace-relationship-resolver-coalesce-type-mismatch.md) | staging + production | 2026-05-08 | 2026-05-08 | El CTE `subject_admin` retornaba `SELECT 1 AS is_admin` (integer) pero el `COALESCE` downstream lo combinaba con `FALSE` (boolean). PG rechaza con "COALESCE types integer and boolean cannot be matched". El catch silencioso en `resolveSubjectOrganizationRelation` convertía el throw a `degradedMode=true, reason='relationship_lookup_failed'`. Bloqueaba TASK-613 V1.1: usuario veía banner "Workspace en modo degradado — No pudimos resolver tu relación con esta organización". Latente desde TASK-611 merge — los unit tests usaban mocks, no ejercitaban SQL crudo contra PG. Resuelto cambiando `SELECT 1` a `SELECT TRUE` (1 línea). Lección operativa documentada: cualquier query SQL embebido con uniones de tipos (COALESCE de subqueries, CASE WHEN, NULL coalescing) debe ejercitarse contra PG real ANTES de mergear. |
| `ISSUE-070` | [Finance Clients page caía silenciosamente a legacy por validación estricta del resolver](resolved/ISSUE-070-finance-clients-page-strict-validation-pass-all-shapes.md) | staging + production | 2026-05-08 | 2026-05-08 | El page server `/finance/clients/[id]/page.tsx` (TASK-613 Slice 2) pasaba el URL `[id]` como los 4 shapes simultáneamente a `resolveFinanceClientContext`. El resolver canónico (write-path) valida estrictamente cada shape — si pasás `hubspotCompanyId="hubspot-company-X"` pero la columna real tiene `"X"` (sin prefix), throw `FinanceValidationError`. El catch silencioso en page caía a legacy. Bloqueaba TASK-613 V1.1 pilot rollout (Julio veía legacy aunque el flag estaba activo). Resuelto via helper canónico nuevo `findFinanceClientContextByLookupId` (read-path: prefix-aware, no-throw, separa contratos READ vs WRITE). 8 tests anti-regresión cubren los 4 prefijos + miss + propagación selectiva de errores. NO se modificó el resolver canónico (load-bearing en write paths). |
| `ISSUE-069` | [Cron rematerialize-balances deja un día ciego en el límite del lookback](resolved/ISSUE-069-finance-cron-rematerialize-seed-day-blind-spot.md) | staging + production | 2026-05-08 | 2026-05-08 | Cron `ops-finance-rematerialize-balances` calculaba `seedDate = today − lookbackDays`. El contrato canónico de `rematerializeAccountBalanceRange` (account-balances-rematerialize.ts:258) NO materializa el día seed (itera desde `seedDate + 1`) — el seed es ancla muda para preservar reconciliation snapshots TASK-721 + OTB anchor TASK-703. Como el window rota cada día, el día `today − lookbackDays` quedaba como ancla muda diaria, perdiendo cualquier `settlement_leg` / `expense_payment` / `income_payment` retroactivo con esa `transaction_date`. Bug determinístico que afectaba TODAS las cuentas. Detectado 2026-05-08 por reliability signal `finance.account_balances.fx_drift` (TASK-774) + Playwright smoke. Caso real: santander-clp 2026-05-01 con $402,562.50 CLP en outflows no contabilizados. Resuelto via helper canónico `computeRematerializeSeedDate(today, lookbackDays)` que resta `lookbackDays + 1`, 7 tests anti-regresión, backfill one-shot del registro roto, y regla canónica documentada en CLAUDE.md/AGENTS.md para que ningún cron futuro re-introduzca el bug. |
| `ISSUE-068` | [TASK-404 governance tables nunca creadas (pre-up-marker bug)](resolved/ISSUE-068-task-404-pre-up-marker-bug-governance-tables-never-created.md) | dev + staging + production | 2026-05-08 | 2026-05-08 | Migration `20260417044741101_task-404-entitlements-governance.sql` tenía las 3 `CREATE TABLE` bajo `-- Down Migration` (markers invertidos). node-pg-migrate parseó la sección Up vacía, registró la migration como aplicada en pgmigrations, pero nunca ejecutó el SQL. Las 3 governance tables (`role_entitlement_defaults`, `user_entitlement_overrides`, `entitlement_governance_audit_log`) nunca existieron. No bloqueante en runtime (Greenhouse compone capabilities como pure-function, no lee de las tablas), bloqueaba writes Admin Center > Gobernanza. Resuelto via TASK-838 (Fases 1-4): forward-fix migration + CI gate `migration-marker-gate.mjs` mode strict (anti regression de la clase de bug) + reliability signal `infrastructure.critical_tables.missing` (runtime guard) + FK enforcement NOT VALID + VALIDATE atomic de `role_entitlement_defaults.capability` y `user_entitlement_overrides.capability` → `capabilities_registry.capability_key`. Fase 5 (wire Admin Center mutations) deferida a TASK-839. 3592/3592 tests verdes. |
| `ISSUE-066` | [Microsoft SSO callback invalid_client por secret rechazado](resolved/ISSUE-066-microsoft-sso-invalid-client-secret.md) | production + staging | 2026-05-03 | 2026-05-03 | Entra ID rechazaba el `AZURE_AD_CLIENT_SECRET` efectivo publicado en Vercel con `AADSTS7000215`. La App seguía multi-tenant y con redirect URIs sanas; no era el drift de ISSUE-061. Resuelto con rotación segura `--append`, publicación en Vercel production/staging/preview develop y GCP Secret Manager, más hardening en `/api/auth/health` con token probe que degrada Azure ante `invalid_client` pero acepta `invalid_grant` por Conditional Access como prueba de secret válido. |
| `ISSUE-065` | [KPI Nómina sub-counted por mis-clasificación expense_type (dimensión conflate)](resolved/ISSUE-065-kpi-nomina-mis-classification-by-expense-type-conflate.md) | production / staging / dev | 2026-05-03 | 2026-05-03 | `expense_type` mezclaba dimensiones legítimamente ortogonales (taxonomía fiscal/SII vs categoría económica/operativa). Bank reconciler defaulteaba `expense_type='supplier'` cuando creaba expenses desde transacciones bancarias, sesgando KPIs Nómina/Proveedores en ~$3M abril 2026 (Daniela España, Andrés Colombia, Valentina, Humberly, Previred caían en buckets equivocados). Resuelto via TASK-768 (9 slices): nueva columna persistida `economic_category` separada + 11 valores enum canonical + lookup tables `known_regulators`/`known_payroll_vendors` + resolver canónico TS con 10 rules + backfill defensivo + CHECK NOT VALID + canonical_values VALIDATED + trigger BEFORE INSERT default + reclassification endpoints + capabilities granulares + outbox events v1 + audit log append-only + manual queue + 2 reliability signals + lint rule mode `error` + VIEWs canónicas extendidas + cash-out byEconomicCategory exposure. 5 migrations + 108 tests verdes. Cero impacto en saldos / P&L histórico / SII (preserved en expense_type legacy). KPI Nómina canónico post-fix ≈ $4M (vs $1.03M pre-fix). |
| `ISSUE-064` | [/finance/cash-out KPIs inflados 88× por anti-patrón sistémico CLP × rate](resolved/ISSUE-064-cash-out-kpi-inflated-clp-currency-anti-pattern.md) | production / staging / dev | 2026-05-02 | 2026-05-03 | Anti-patrón sistémico `SUM(ep.amount × COALESCE(e.exchange_rate_to_clp, 1))` en SQL embebidos. Cuando un payment CLP nativo toca un expense USD (caso CCA TASK-714c — HubSpot $1.106.321 CLP × rate USD 910.55), multiplicar el monto por el rate del documento original infla los KPIs en mil millones por payment. Resuelto end-to-end vía TASK-766 (5 slices: VIEW canónica `expense_payments_normalized` + helper TS canónico + lint rule `no-untokenized-fx-math` mode `error` + 2 reliability signals + backfill + CHECK constraint + repair admin endpoint con capability granular). 4 migrations + 79 tests verdes + 8 endpoints migrados (4 con bonus fix de leak supersede). KPI canónico post-fix: $11.546.493 (real). |
| `ISSUE-063` | [Payment orders marcadas como pagadas sin afectar el banco origen](resolved/ISSUE-063-payment-orders-paid-without-bank-impact.md) | production / staging / dev | 2026-05-01 | 2026-05-02 | Cadena de 3 fallas estructurales dejó 2 `payment_orders` (Luis $148,312.50 + Humberly $254,250.00) en `state='paid'` sin afectar Santander CLP. F1: wizard permitió `source_account_id=NULL`. F2: materializer `finance_expense_reactive_intake` dead-letter con "INSERT has more target columns than expressions" → 0 filas en `expenses` para 2026-04. F3: proyector `record_expense_payment_from_order` skipea silencioso (`recorded=0 skipped=1`) sin error_class ni alerta. Resuelto end-to-end via TASK-765 (8 slices: hard-gate triple `source_account_id` + parity test universal + admin endpoints rematerialize/recover + resolver loud + atomicidad transaccional + state machine triggers + audit log append-only + 3 reliability signals). Recovery ejecutado: Santander CLP outflow $402,562.50 reflejado, closing $3,750,478.50 al 2026-05-02. |
| `ISSUE-062` | [Payslip on `payment_paid` no envía mail si receipt ya tenía `status='email_sent'` del modo legacy](resolved/ISSUE-062-payslip-idempotency-blocked-by-legacy-receipt-status.md) | staging + production | 2026-05-01 | 2026-05-01 | El helper `sendPayslipForEntry` chequeaba idempotency contra `payroll_receipts.status` (flag global por receipt) en vez de `payslip_deliveries` (granular por `delivery_kind` que es el modelo canónico V2). Tras switch del flag `GREENHOUSE_PAYSLIP_DELIVERY_MODE` a `on_payment_paid`, los receipts ya marcados `email_sent` por el legacy export bloqueaban el envío del mail nuevo de pago ejecutado. Fix vía granular check `hasActivePayslipDelivery(entryId, kind)` alineado al UNIQUE partial index del schema V2. Recovery operativo de Humberly Henriquez via endpoint canónico `/resend-payslips` con `trigger='manual_resend'`. |
| `ISSUE-061` | [Microsoft SSO callback rejection (multi-tenant drift)](resolved/ISSUE-061-microsoft-sso-callback-rejection-multitenant-drift.md) | production + staging | 2026-04-30 | 2026-05-01 | Azure App Registration `3626642f-...` tenía `signInAudience=AzureADMyOrg` (single-tenant). NextAuth dispatchea via `tenantId='common'` (multi-tenant) y Azure rechazaba con `AADSTS50194/9002313`; NextAuth swallow-eaba el error como `?error=Callback`. Greenhouse es multi-tenant por arquitectura. Fix vía Azure CLI: `az ad app update --sign-in-audience AzureADMultipleOrgs`. Hardening con TASK-742: smoke lane sintética cada 5min al endpoint `/common/authorize`, `pnpm auth:audit-azure-app` (7 invariantes), wrappers structured de `signIn/jwt` callbacks, magic-link self-recovery, schema CHECK `client_users_auth_mode_invariant`. |
| `ISSUE-057` | [Build noise: `[DashboardLayout] getServerAuthSession failed: Dynamic server usage`](resolved/ISSUE-057-build-noise-getserverauthsession-dynamic-server-usage.md) | dev + preview (build only — runtime intacto) | 2026-04-26 | 2026-04-26 | Antipatrón en 4 server components que tragaba el `DYNAMIC_SERVER_USAGE` del framework como error de auth. Resuelto con helpers canónicos `requireServerSession` + `getOptionalServerSession` en `src/lib/auth/require-server-session.ts` que detectan el error por `digest` y lo re-lanzan; `force-dynamic` en cada page/layout que consume sesión; convención obligatoria en CLAUDE.md + `GREENHOUSE_AUTH_SERVER_SESSION_V1.md`. |
| `ISSUE-059` | [ICO AI predictions depend on runner clock instead of `generatedAt`](resolved/ISSUE-059-ico-ai-predictions-depend-on-runner-clock-instead-of-generated-at.md) | CI (`main` + `develop`) + runtime general | 2026-04-30 | 2026-04-30 | `buildAiPredictions()` calculaba el progreso del mes con `new Date()` en Santiago en vez del `generatedAt` explícito, rompiendo reproducibilidad entre CI, replays y materialización. Se resolvió con helper temporal reusable y contexto temporal explícito compartido por predictor + materializer. |
| `ISSUE-056` | [TASK-466 migration referenced phantom schema (`quotation_defaults` + `approval_policies.default_currency`)](resolved/ISSUE-056-missing-quotation-defaults-ddl-task-466.md) | dev + staging + production | 2026-04-21 | 2026-04-21 | Hotfix idempotente en migración 466 (`ALTER TABLE IF EXISTS` + `DO` block con chequeo a `information_schema`). Auditoría empírica confirmó cero consumers en `src/`, entonces no se crea DDL especulativo. |
| `ISSUE-054` | [`/my/profile` returns HTTP 500 in staging (sólo esa página, resto `/my/*` OK)](resolved/ISSUE-054-my-profile-500-staging.md) | staging | 2026-04-19 | 2026-04-25 | La vista migró al reader canónico `GET /api/person/me/360` y quedó validada manualmente en staging con datos reales visibles. |
| `ISSUE-049` | [Leave review puede fallar con acción stale y policy frágil de aprobación](resolved/ISSUE-049-leave-review-modal-stale-actions-and-brittle-approval-policy.md) | staging + runtime general | 2026-04-15 | 2026-04-25 | Se corrigió el override SQL con casts explícitos, se centralizó la policy de review y la UI ya envía la acción clickeada explícitamente. |
| `ISSUE-048` | [Payroll compensation overview puede fallar o degradar por referencia ambigua a `member_id`](resolved/ISSUE-048-payroll-compensation-member-id-ambiguous-silent-degradation.md) | staging + runtime general | 2026-04-13 | 2026-04-25 | El reader canónico de compensaciones ya aliasa `cv.member_id` y quedó cubierto por regresión focalizada en Vitest. |
| `ISSUE-002` | [Nubox sync conformed: data integrity (period mixing, identity loss)](resolved/ISSUE-002-nubox-sync-conformed-data-integrity.md) | staging + production | 2026-03-30 | 2026-04-13 | El writer conformed dependía de `DELETE` sobre filas calientes de BigQuery y la frescura visible se maquillaba con `NOW()`. Se resolvió con sync plan histórico + snapshots append-only + latest-snapshot readers. |
| `ISSUE-023` | [Tablas de email sin migracion formal](resolved/ISSUE-023-email-tables-no-formal-migration.md)                                   | production + staging | 2026-04-06 | 2026-04-13 | Migraciones formales creadas en TASK-382: `20260413162215719_email-notifications-schema-foundation.sql` + `20260413162238855_email-delivery-enterprise-v2.sql` |
| `ISSUE-020` | [3 endpoints duplicados de retry batch sin error handling](resolved/ISSUE-020-duplicate-email-retry-endpoints.md)                | staging + production | 2026-04-06 | 2026-04-13 | STALE — duplicados ya habian sido removidos. Solo existe `/api/admin/ops/email-delivery-retry` con try-catch. Verificado en auditoria TASK-382. |
| `ISSUE-046` | [Reactive pipeline silent-skip backlog (~11k eventos sin procesar)](resolved/ISSUE-046-reactive-pipeline-silent-skip-backlog.md) | staging + production (single-instance) | 2026-04-13 | 2026-04-13 | Consumer V1 silent-skip path + fan-out explosion. Resuelto por TASK-379 (PR #53) + audit-only sweep follow-up (PR #54). Backlog drenado de 11,495 → 0. |
| `ISSUE-052` | [CI `test:coverage` flake: HrLeaveView backfill dialog times out](resolved/ISSUE-052-ci-coverage-flake-hr-leave-timeout.md) | CI (GitHub Actions) | 2026-04-17 | 2026-04-17 | Vitest `testTimeout` default de 5s quedaba corto bajo instrumentación v8 de coverage en GitHub runners. Fix: bump global a 15s en `vitest.config.ts`. Desbloquea 5 commits de `develop` con CI rojo por flake no-reproducible localmente. |
| `ISSUE-053` | [`client_users.password_hash` reescrito por batch sync, bloqueando login con credentials](resolved/ISSUE-053-password-hash-overwritten-by-batch-sync.md) | staging (dev DB) | 2026-04-17 | 2026-04-17 | TASK-451: trigger DB `client_users_password_guard` + helper `withPasswordChangeAuthorization` + remoción de escritura desde backfill. Writes a `password_hash` requieren ahora session var explícita; emite outbox `identity.password_hash.rotated`. |
| `ISSUE-031` | [Vercel Preview falla en build por drift de `NEXTAUTH_SECRET`](resolved/ISSUE-031-vercel-preview-build-fails-missing-nextauth-secret.md)                        | preview                        | 2026-04-08 | 2026-04-08 | `authOptions` se resolvía en import-time y Preview no tenía `NEXTAUTH_SECRET`, por lo que el build caía en page-data collection |
| `ISSUE-032` | [Secret Manager payload contamination breaks runtime secrets](resolved/ISSUE-032-secret-manager-payload-contamination-breaks-runtime-secrets.md)                 | staging + production           | 2026-04-08 | 2026-04-09 | Payloads de Secret Manager publicados con comillas/`\\n` literal llegaban sucios al runtime y rompían auth/integraciones |
| `ISSUE-027` | [My Profile vacío tras migración a Person 360: resolución "me" retorna 404](resolved/ISSUE-027-my-profile-360-me-resolution-404.md)                               | staging                        | 2026-04-07 | 2026-04-07 | `resolvePersonIdentifier` no buscaba por `identity_profile_id` — WHERE clause solo tenía `member_id OR user_id`          |
| `ISSUE-025` | [sendEmail() reporta 'sent' cuando todos los recipients fueron skipped](resolved/ISSUE-025-sendmail-status-aggregation-skipped-as-sent.md)                        | production + staging + preview | 2026-04-06 | 2026-04-07 | `sendEmail()` no trackeaba `sawSkipped`; 18 registros históricos corregidos via backfill                                 |
| `ISSUE-024` | [Admin Notifications: errores silenciosos ocultan estado real](resolved/ISSUE-024-admin-notifications-silent-failures-zero-kpis.md)                               | staging + preview              | 2026-04-06 | 2026-04-07 | Catch blocks silenciosos + logDispatch vacío + schema validation faltante + diagnostics UI ausente                       |
| `ISSUE-022` | [Adjuntos de email base64 pueden corromper PDFs](resolved/ISSUE-022-email-attachments-base64-corruption.md)                                                       | production + staging           | 2026-04-06 | 2026-04-07 | Attachments re-encoded a base64 string; corregido a `Buffer` directo                                                     |
| `ISSUE-021` | [Ventana de retry de emails limitada a 1 hora](resolved/ISSUE-021-email-retry-window-too-narrow.md)                                                               | production + staging           | 2026-04-06 | 2026-04-07 | Retry window expandida de 1h a 24h                                                                                       |
| `ISSUE-019` | [ensureEmailSchema() ejecuta DDL en cada envio de email](resolved/ISSUE-019-email-schema-runtime-ddl-on-every-send.md)                                            | production + staging           | 2026-04-06 | 2026-04-07 | `ensureEmailSchema()` eliminada; DDL runtime removido                                                                    |
| `ISSUE-018` | [Usuario invitado se crea con status 'pending' en vez de 'invited'](resolved/ISSUE-018-invite-user-status-pending-not-invited.md)                                 | production + staging           | 2026-04-06 | 2026-04-07 | INSERT usaba `'pending'`; corregido a `'invited'`                                                                        |
| `ISSUE-017` | [invite/route.ts consulta columna inexistente `display_name`](resolved/ISSUE-017-invite-route-display-name-column-missing.md)                                     | production + staging           | 2026-04-06 | 2026-04-07 | Query referenciaba `display_name` inexistente; corregida a `client_name`                                                 |
| `ISSUE-005` | [Payroll close route drains global notification backlog](resolved/ISSUE-005-payroll-close-route-drains-global-notification-backlog.md)                            | preview + production           | 2026-04-05 | 2026-04-05 | Payroll close drenaba backlog de notificaciones globales                                                                 |
| `ISSUE-004` | [column "organization_id" does not exist en finance route](resolved/ISSUE-004-finance-organization-id-column-missing.md)                                          | preview                        | 2026-04-02 | 2026-04-02 | Columna inexistente referenciada en query de finance                                                                     |
| `ISSUE-003` | [Permission denied for schema greenhouse_notifications](resolved/ISSUE-003-notification-schema-permission-denied.md)                                              | preview                        | 2026-04-01 | 2026-04-01 | Runtime user sin permisos DDL para crear schema notifications                                                            |
| `ISSUE-016` | [Microsoft SSO roto en produccion (secret rotado + member_id faltante en BQ)](resolved/ISSUE-016-microsoft-sso-broken-production-secret-rotation.md)              | production                     | 2026-04-05 | 2026-04-05 | Azure AD client secret rotado sin sincronizar produccion + `cu.member_id` inexistente en BQ query                        |
| `ISSUE-006` | [Payroll leave fallback silently zeroes unpaid leave](resolved/ISSUE-006-payroll-leave-fallback-silently-zeroes-unpaid-leave.md)                                  | preview + production           | 2026-04-05 | 2026-04-05 | Fallback silencioso de `leave_requests` colapsaba `dato no disponible` a `0` y permitía cálculo oficial incorrecto       |
| `ISSUE-015` | [Scroll horizontal en AgencyWorkspace (CustomTabList sin wrapper overflow)](resolved/ISSUE-015-agency-workspace-horizontal-scroll-overflow.md)                    | staging                        | 2026-04-05 | 2026-04-05 | `CustomTabList variant='scrollable'` en flex container sin `minWidth: 0` + `overflow: hidden`                            |
| `ISSUE-014` | [person_360 VIEW faltaba columnas enriched (avatar, job_title, phone)](resolved/ISSUE-014-person-360-view-missing-enriched-columns.md)                            | staging                        | 2026-04-05 | 2026-04-05 | VIEW era version antigua sin `resolved_avatar_url`, `resolved_job_title`, etc. — datos existian pero no se exponian      |
| `ISSUE-013` | [Staging deploy failures: proyecto duplicado, variables faltantes y bypass secret](resolved/ISSUE-013-staging-deploy-failures-duplicate-project-bypass-secret.md) | staging + preview              | 2026-04-05 | 2026-04-05 | Proyecto Vercel duplicado + `AGENT_AUTH_SECRET` faltante + `VERCEL_AUTOMATION_BYPASS_SECRET` manual con valor incorrecto |
| `ISSUE-012` | [Reactive cron routes fail closed without CRON_SECRET](resolved/ISSUE-012-reactive-cron-routes-fail-closed-without-cron-secret.md)                                | develop runtime                | 2026-04-05 | 2026-04-05 | `requireCronAuth()` validaba `CRON_SECRET` antes de reconocer requests legítimas de Vercel Cron                          |
| `ISSUE-011` | [Pre-merge conflict check false positives from SQL](resolved/ISSUE-011-merge-tree-conflict-check-false-positives.md)                                              | develop (tooling)              | 2026-04-05 | 2026-04-05 | `git merge-tree \| grep CONFLICT` matchea SQL `ON CONFLICT`; documentado comando correcto en AGENTS.md                   |
| `ISSUE-009` | [Reactive event backlog can accumulate without Ops visibility](resolved/ISSUE-009-reactive-event-backlog-can-accumulate-without-ops-visibility.md)                | develop runtime                | 2026-04-05 | 2026-04-05 | Admin Ops no contaba backlog reactivo oculto entre `published` y `outbox_reactive_log`                                   |
| `ISSUE-010` | [Finance schema drift logger type mismatch](resolved/ISSUE-010-finance-schema-drift-logger-type-mismatch.md)                                                      | staging + production           | 2026-04-05 | 2026-04-05 | `logFinanceSchemaDrift` tipaba `Error` pero catch da `unknown`                                                           |
| `ISSUE-007` | [Finance fallback writes can duplicate income and expenses](resolved/ISSUE-007-finance-fallback-writes-can-duplicate-income-and-expenses.md)                      | preview + production           | 2026-04-05 | 2026-04-05 | Fallback recalculaba un segundo ID en `income` y `expenses`                                                              |
| `ISSUE-008` | [Finance routes mask schema drift as empty success](resolved/ISSUE-008-finance-routes-mask-schema-drift-as-empty-success.md)                                      | preview + production           | 2026-04-05 | 2026-04-05 | Routes devolvían vacío ambiguo ante drift de schema                                                                      |
| `ISSUE-026` | [Mi Perfil crash: leave.requests nested object + auth redirect](resolved/ISSUE-026-my-profile-crash-leave-requests-nested-object.md)                              | production + staging           | 2026-04-07 | 2026-04-07 | `leave?.requests` es objeto anidado no array; fallback auth a `/dashboard` en vez de `/home`                             |
| `ISSUE-029` | [HubSpot sync falla por columnas incorrectas en identity_profiles](resolved/ISSUE-029-hubspot-sync-identity-profiles-column-mismatch.md)                          | staging + production           | 2026-04-07 | 2026-04-07 | `source_system` → `primary_source_system` + `profile_type` NOT NULL faltante en INSERT                                   |
| `ISSUE-028` | [HubSpot Cloud Run service 401: Private App Token expirado](resolved/ISSUE-028-hubspot-cloud-run-token-expired.md)                                                | staging + production           | 2026-04-07 | 2026-04-07 | Private App Token en Secret Manager revocado; rotado a version 2 + Cloud Run service update                              |
| `ISSUE-051` | [Emails de recibo de nómina no se envían: INSERT column mismatch](resolved/ISSUE-051-payroll-receipt-emails-insert-column-mismatch.md) | staging + production | 2026-04-16 | 2026-04-16 | `buildSavePayrollReceiptStatement()` tenía 2 `CURRENT_TIMESTAMP` extra en VALUES sin columna correspondiente; ops-worker no auto-desplegaba cambios en `src/lib/payroll/` |
| `ISSUE-001` | [SSL bad certificate en webhook-dispatch](resolved/ISSUE-001-ssl-bad-certificate-production.md)                                                                   | production                     | 2026-03-30 | 2026-03-30 | `GREENHOUSE_POSTGRES_IP_TYPE` faltante en production                                                                     |
| `ISSUE-030` | [CI: test stale de OrganizationPeopleTab bloquea PRs no relacionados](resolved/ISSUE-030-ci-stale-organization-people-tab-test-blocks-unrelated-prs.md)           | preview + GitHub Actions CI    | 2026-04-08 | 2026-04-08 | El test asumía 1 `fetch()` y un solo `1.0`, pero el componente ya cargaba memberships + faceta `team` y renderizaba KPI  |
| `ISSUE-033` | [Emails duplicados a HR cuando permiso salta supervisor](resolved/ISSUE-033-leave-email-duplicate-hr-when-no-supervisor.md)                                       | production + staging           | 2026-04-09 | 2026-04-09 | `leave_request.created` sin supervisor duplicaba notificacion de `escalated_to_hr` a HR                                  |
| `ISSUE-034` | [Email decision permiso guarda userId en actor_email](resolved/ISSUE-034-leave-decision-email-actor-email-stores-userid.md)                                       | production + staging           | 2026-04-09 | 2026-04-09 | `actorUserId` se pasaba como `actorEmail` en `leave_request_decision` — datos de audit incorrectos                       |
| `ISSUE-035` | [Emails de permisos sin dedup por source_event_id](resolved/ISSUE-035-leave-emails-no-source-event-dedup.md)                                                      | production + staging           | 2026-04-09 | 2026-04-09 | Emails de leave no verificaban `email_deliveries` antes de enviar — sin segunda capa de dedup                            |
| `ISSUE-036` | [HR member profile queda stale tras cambio de supervisor en Jerarquía](resolved/ISSUE-036-hr-hierarchy-profile-reader-stale-reports-to.md) | staging | 2026-04-10 | 2026-04-10 | La ficha HR seguía leyendo `reports_to` desde BigQuery; ahora resuelve la línea vigente desde la jerarquía canónica en Postgres |
| `ISSUE-037` | [Historial de jerarquía pierde `effectiveTo` en líneas cerradas](resolved/ISSUE-037-hr-hierarchy-history-drops-effective-to.md) | staging | 2026-04-10 | 2026-04-10 | El mapper colapsaba timestamps `Date` a `null`; ahora normaliza `effective_from` y `effective_to` antes de serializar |
| `ISSUE-038` | [Organigrama y Jerarquía no reflejan departamento cuando solo se asigna responsable del área](resolved/ISSUE-038-org-chart-departments-head-vs-member-assignment-drift.md) | staging | 2026-04-10 | 2026-04-10 | `head_member_id` y `members.department_id` se habían desacoplado; el write lane ahora sincroniza ambos y el organigrama tiene fallback estructural |
| `ISSUE-039` | [Modal de “Cambiar supervisor” bloquea guardar sin validación visible](resolved/ISSUE-039-hr-hierarchy-change-supervisor-modal-silent-disabled-save.md) | staging | 2026-04-10 | 2026-04-10 | La UI bloqueaba el submit sin feedback; ahora muestra validación visible, estado `error` y alerta contextual |
| `ISSUE-040` | [Crear nueva delegación puede dejar al supervisor sin delegación activa](resolved/ISSUE-040-hierarchy-delegation-revoke-before-create.md) | staging + runtime general | 2026-04-10 | 2026-04-10 | El reemplazo revocaba antes de crear; ahora corre en una única transacción con rollback completo |
| `ISSUE-041` | [Reasignación masiva ignora la fecha efectiva al resolver reportes directos](resolved/ISSUE-041-hierarchy-bulk-reassign-ignores-effective-date-for-scope.md) | staging + runtime general | 2026-04-10 | 2026-04-10 | El bulk usaba el estado actual del request; ahora resuelve los direct reports con la fecha efectiva seleccionada |
| `ISSUE-042` | [Organigrama visual no representa la jerarquía estructural de departamentos](resolved/ISSUE-042-org-chart-uses-reporting-lines-instead-of-structural-hierarchy.md) | staging | 2026-04-10 | 2026-04-10 | El grafo usaba reporting lines como edges; ahora materializa `departments.parent_department_id` + adscripción de miembros |
| `ISSUE-043` | [Organigrama puede estar permitido por permisos pero oculto en el menú](resolved/ISSUE-043-org-chart-menu-hidden-for-supervisor-limited-access.md) | runtime general | 2026-04-10 | 2026-04-10 | La navegación lateral no seguía el mismo contrato que page/API; ahora comparte criterio supervisor-aware |
