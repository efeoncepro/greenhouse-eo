# Architecture Decisions

| ID | Status | Decision | Canonical document |
| --- | --- | --- | --- |
| ADR-018 | Proposed — validación requerida; sin cambios nativos ni de runtime autorizados | **Continuidad móvil y aplicación companion.** Globe es continuity-first: mobile web/PWA primero para capturar intención, drafts, deep links, inbox, estados asíncronos, review y handoff; desktop conserva la composición profunda y Globe cloud conserva la autoridad. Una app nativa sólo se evalúa con evidencia de uso, contrato de identidad/media/notifications y gates enterprise. | [`EFEONCE_GLOBE_MOBILE_CONTINUITY_APPLICATION_DECISION_V1.md`](EFEONCE_GLOBE_MOBILE_CONTINUITY_APPLICATION_DECISION_V1.md) |
| ADR-001 | Accepted design; rollout pending | Separate human federation from keyless workload federation between Greenhouse and Globe. | [`GREENHOUSE_CONNECTIVITY_V1.md`](GREENHOUSE_CONNECTIVITY_V1.md) |
| SPEC-001 | Accepted and implemented (TASK-1481) | Full API Parity spine: server-derived branded trusted context vs untrusted payload, transport-neutral capability registry, versioned schemas, three-state coverage manifest (no `missing`), canonical error vocabulary and a manifest-driven conformance harness. | [`EFEONCE_GLOBE_API_CONTRACT_SPINE_V1.md`](EFEONCE_GLOBE_API_CONTRACT_SPINE_V1.md) |
| SPEC-002 | Accepted and implemented — fake canary (TASK-1457) | Model Lab: the first business capability on the spine (`globe.lab.experiment.run`). Experiment aggregate + state machine (`prepared → estimated → reserved → running → candidate_ready \| failed \| cancelled`), estimate→cap→reserve→run→settle flow, hard spend fence (safety, not the credit ledger), private-ingest (hash+rights, never bytes), fail-closed kill switch, and a single provider seam (`LabRunnerPort`) proven with a deterministic fake — the real adapter is deferred. | [`EFEONCE_GLOBE_MODEL_LAB_V1.md`](EFEONCE_GLOBE_MODEL_LAB_V1.md) |
| SPEC-003 | Accepted and implemented — fake canary (TASK-1458) | Golden Briefs & Evaluation Harness: a second capability (`globe.lab.evaluation.run`) that consumes the Model Lab to turn an attempt into repeatable, comparable evidence PER FIDELITY CONTRACT. Versioned rights-declared fixtures (still/motion/audio) + rubrics (objective automatic checks vs declared human criteria) + an `evaluate` command that runs a golden brief through `runModelLabExperiment` and scores the manifest. The verdict never auto-marks a creative pass (`objective_fail` \| `objective_pass_pending_human`); it never declares a model globally better; reports are versioned, workspace-scoped and declare their limitations. | [`EFEONCE_GLOBE_EVALUATION_HARNESS_V1.md`](EFEONCE_GLOBE_EVALUATION_HARNESS_V1.md) |
| SPEC-004 | Accepted and implemented (TASK-1500) | Governed Producer route catalog: the keystone of the Creative Producer cluster. `PRODUCER_ROUTE_CATALOG` is versioned DATA (adding a route edits the array + `PRODUCER_CATALOG_VERSION`; the reader engine has no per-route `switch`), exposed by two governed readers (`globe.producer.catalog.list`/`.get`, capability `globe.producer.catalog.read`; `ui`/`mcp` `policy-blocked` until the TASK-1505 gate). Each route declares its `CreativeCapability`, output-shape `constraints` discriminated by modality (image/video/audio, a compile-time fail-closed), specialty, `audioCapable`, input modes and naming. In-process helpers `resolveRouteConstraints`/`getProducerRoute`/`listProducerRoutes` are the SSOT that TASK-1501 (fail-closed pre-spend shape validation) and TASK-1502 (`cost = f(route, shape)`) reuse WITHOUT re-dispatch. Load-time drift guards abort on a duplicate routeId, unknown capability, modality mismatch, incoherent `audioCapable`, or a leaked provider slug. | [`EFEONCE_GLOBE_CREATIVE_PRODUCER_ARCHITECTURE_V1.md`](EFEONCE_GLOBE_CREATIVE_PRODUCER_ARCHITECTURE_V1.md) |
| ADR-003 | Accepted and implemented (TASK-1500, decision inverted post-ship 2026-07-20) | Naming on the Producer route catalog: the real model identity (name + optional version, e.g. "Seedance" · "2.0") is PUBLIC/client-facing — a legible quality signal and suite positioning anchor (the enterprise ICP knows which model is best-in-class), NOT the provider slug. This corrects the initial ship, which had the model operator-only and conflated "model name" with "wire slug": they are split. The internal `house` taxonomy is operator-only, gated by a DEDICATED capability `globe.producer.route.reveal_house` (never reuses the Lab authority); `resolveRouteAudience` fail-closes to `client`, which omits `house`. The provider wire slug, vendor cost and margin are forbidden on every surface — a drift guard aborts catalog load if a slug leaks into `routeId`/`model`/`house`. `actualRoute` is a fidelity contract, never a slug. | [`EFEONCE_GLOBE_CREATIVE_PRODUCER_ARCHITECTURE_V1.md`](EFEONCE_GLOBE_CREATIVE_PRODUCER_ARCHITECTURE_V1.md) (§ Alternativas rechazadas + § Boundary) |
| SPEC-005 | Accepted and implemented (TASK-1501) | Modality-discriminated run contract. `PrepareExperimentPayloadV1` gains `output?: OutputShapeV1`, a discriminated union by `modality` (image/video/audio) designed for all three from day one (image implemented end to end; video/audio declared + structurally validated, their adapter execution deferred to TASK-1504). Every output-shape param is validated FAIL-CLOSED in `prepare` (`validateOutputShape` inside `validatePreparePayload`) against the selected route's governed catalog constraints (TASK-1500, consumed in-process via a `RouteCatalogPort.getRoute` seam whose production impl is `getProducerRoute` — no re-dispatch) — strictly before `executeExperiment` reaches the spend fence. Unknown route, capability/route mismatch, modality incoherence, out-of-range/enum param, unsupported input mode, malformed shape, or a missing catalog all map to `invalid_request`, never silent coercion. Additive-optional (a shapeless caller is unchanged); the shape is threaded to the provider seam image-first (`CreativeProviderRequestV1` gains quality/aspectRatio/count), absorbing TASK-1495's aspect ratio for image. Selectors carry no bytes (references via `authorizedInputs`, edit base via `editFrom`); credit unit stays route × shape; coverage unchanged (`ui`/`mcp` policy-blocked). | [`EFEONCE_GLOBE_CREATIVE_PRODUCER_ARCHITECTURE_V1.md`](EFEONCE_GLOBE_CREATIVE_PRODUCER_ARCHITECTURE_V1.md) (§ Contratos reales del run contract) |
| SPEC-006 | Accepted and implemented (TASK-1502) | Previewable estimate reader. The credit estimate is extracted from inside the `execute` command to a governed, read-only reader `globe.lab.experiment.estimate` that returns `credits = f(route × output-shape)` BEFORE any spend — no experiment created, no credit reserved, no state transitioned. `LabRunnerPort.estimate` now takes a prospective `LabQuoteInputV1` (not a stored experiment); `execute` derives the quote from its stored experiment (`quoteInputFromStored`), so the preview and the command share the exact same estimate computation, never duplicated. The output shape is validated fail-closed against the route's catalog constraints reusing TASK-1501's validator (`invalid_request`); an over-cap estimate is NOT rejected — it returns `withinHardCap:false`, a budget signal (a preview tells the truth, it never spends). The curated `LabEstimatePreviewV1` exposes the credit number, the fidelity route and budget signals and OMITS provider/model slug, vendor cost and margin. Credit unit is route × output-shape, never the model. Read-only/idempotent; kill switch OFF ⇒ `policy_blocked`; coverage unchanged (`ui`/`mcp` policy-blocked). It is the advance slice of TASK-1469, which consumes the same estimate path. | [`EFEONCE_GLOBE_CREATIVE_PRODUCER_ARCHITECTURE_V1.md`](EFEONCE_GLOBE_CREATIVE_PRODUCER_ARCHITECTURE_V1.md) (§ Contratos reales del estimate) |
| SPEC-007 | Accepted and implemented — deployed + live-verified (TASK-1465) | Durable persistence layer: Globe's first durable datastore. A Globe-owned Cloud SQL `globe-pg` (Postgres 16, `southamerica-west1`, `db-g1-small`, ZONAL, keyless IAM auth over the Cloud SQL connector, connector-only with no authorized networks, PITR+backups, deletion protection) provisioned in Terraform (`cloud_sql.tf`, plan `12 added / 0 destroyed`). `packages/database` exposes `createGlobePool` (connector + pg pool + transaction) and a migration runner (`globe._migrations`, `SET ROLE globe_owner`), keyless IAM (a password exists only for the one-time bootstrap). Role model: `globe_owner` NOLOGIN owns every object, migrators are members, runtime SAs get DML via default privileges — no standing superuser after bootstrap (PG16 restricted-superuser gotcha: `postgres` joins `globe_owner` before it can `CREATE SCHEMA AUTHORIZATION`). Six tenant-scoped tables + append-only `audit_log` back five durable stores behind the existing ports (experiments, evaluation reports, spend fence [a SAFETY fence, NOT the TASK-1468 commercial ledger], sessions/OAuth) plus a durable audit log. DI wired in `app.ts`/`main.ts` (durable when `GLOBE_POSTGRES_*` is set; in-memory only in `internal_smoke`). Deployed live: both Cloud Run services deployed durable (per-service `web_runtime`/`api_runtime` IAM DB users); the `maxScale=3` reported here was the REVISION ceiling — the service ceiling was still 1 and Cloud Run applies the lower, so the effective ceiling was 1 until TASK-1508 corrected it to 3/3 under Terraform (hence the cross-replica spend fence was never exercised → TASK-1512). Rich workspace/members/grants tenancy deferred. | [`EFEONCE_GLOBE_DURABLE_PERSISTENCE_V1.md`](EFEONCE_GLOBE_DURABLE_PERSISTENCE_V1.md) |
| SPEC-008 | Accepted, deployed and verified internal-only (TASK-1466) | Operating responsibility contract for `client-operated`, `co-operated` and `efeonce-managed`: append-only workspace defaults/run overrides, eight explicit accountable roles, immutable non-pricing commercial classification, assign/change commands and get/effective/history readers on the API Contract Spine. Trusted workspace/actor context, tenant scope, stable replay, optimistic concurrency and assignment+audit atomicity are enforced. Assignments are accountability only and never grant membership, roles, entitlements or capabilities. Migration `0002`, Cloud SQL readback and authenticated deployed smoke passed; external clients and production remain blocked. | [`EFEONCE_GLOBE_OPERATING_RESPONSIBILITY_V1.md`](EFEONCE_GLOBE_OPERATING_RESPONSIBILITY_V1.md) |
| ADR-004 | Accepted for the internal-only release; commercial frontend host deferred (TASK-1506) | Globe frontend hosting + front door. For the internal-only release the web/BFF/SSO shell stays on Cloud Run (native Node server; the Next.js target is superseded for this shell) — migrating to Vercel is rejected because it does not fix the in-memory/`maxScale=1` HA ceiling. The commercial frontend remains a separate deferred decision. `TASK-1507` delivered `globe.efeoncepro.com` through Global External ALB + serverless NEG (applied + live-verified 2026-07-21 — see SPEC-009 and the ADR's Delta); `TASK-1508` adopted both services into Terraform and reconciled deploy ownership (complete 2026-07-21). `globe-api-internal` stays IAM-private with `run.app` audience. Internal URL, HA (`TASK-1465`) and external Production (`TASK-1480`) remain separate gates. | [`EFEONCE_GLOBE_FRONTEND_HOSTING_FRONT_DOOR_DECISION_V1.md`](EFEONCE_GLOBE_FRONTEND_HOSTING_FRONT_DOOR_DECISION_V1.md) |
| ADR-005 | Accepted; implementation distributed across TASK-1519/1469/1505/1520/1521 | The approved Claude Design source is the complete Producer product target and cannot be reduced to match current backend coverage. Human execution uses browser → same-origin `studio-web` BFF → workload-authenticated IAM-private API with server-derived actor/workspace/surface and preserved correlation/idempotency. Coverage metadata does not replace fail-closed surface enforcement. Expensive execution becomes a durable job/outbox lifecycle, and every approved library/budget/provenance/collaboration capability receives a domain owner rather than browser-only state. | [`EFEONCE_GLOBE_PRODUCER_HUMAN_EXECUTION_DECISION_V1.md`](EFEONCE_GLOBE_PRODUCER_HUMAN_EXECUTION_DECISION_V1.md) |
| ADR-006 | Accepted with V2 reconciliation correction; implementation and rollout gated (TASK-1521) | Greenhouse remains authority for ecosystem subjects, desired access and workspace/client bindings. V2 makes reconciliation workspace-complete (`members[]`), separates semantic revision from lease freshness, suspends omitted members fail-closed and preserves bounded grant/revocation history append-only. No parallel identity or organizational role model. | [`EFEONCE_GLOBE_PERSISTED_TENANCY_PROJECTION_DECISION_V1.md`](EFEONCE_GLOBE_PERSISTED_TENANCY_PROJECTION_DECISION_V1.md#delta-2026-07-23--v2-workspace-complete-reconciliation) |
| ADR-007 | Implemented internal-only; private ingest and commercial rollout gated (TASK-1467/TASK-1505) | Private ingest and generated outputs share a durable asynchronous governance pipeline. The request validates and enqueues; a keyless one-shot Cloud Run Job performs malware → C2PA → rights in strict order, with bounded streaming, tenant-scoped leases/evidence and fail-closed workspace discovery from the persisted tenancy projection. | [`EFEONCE_GLOBE_ASSET_GOVERNANCE_WORKER_DECISION_V1.md`](EFEONCE_GLOBE_ASSET_GOVERNANCE_WORKER_DECISION_V1.md) |
| ADR-008 | Accepted; build units 1-3 implemented internal-only (SPEC-010, TASK-1528); feed projection (TASK-1526) + GC (TASK-1529) + commercial rollout (TASK-1521/1480) gated | Media delivery separates immutable private originals, versioned derivatives, authorized Range streaming, governance-aware feed projection and audited mark-and-sweep GC into independently scalable responsibilities. PostgreSQL/domain state remains authority; GCS stores bytes only. | [`EFEONCE_GLOBE_MEDIA_DELIVERY_LIFECYCLE_DECISION_V1.md`](EFEONCE_GLOBE_MEDIA_DELIVERY_LIFECYCLE_DECISION_V1.md) |
| ADR-009 | Accepted; implementation and rollout gated (TASK-1527) | Route promotion is a durable, recoverable saga over existing authorities. Generated-rights application is workspace-scoped; every phase has fixed authority and exact server-side readback; recovery can reconcile or roll back but never promote, activate or attest. | [`EFEONCE_GLOBE_ROUTE_PROMOTION_OPERATION_DECISION_V1.md`](EFEONCE_GLOBE_ROUTE_PROMOTION_OPERATION_DECISION_V1.md) |
| SPEC-009 | Accepted and implemented — applied + live-verified internal-only (TASK-1507) | Internal-only front door: `globe.efeoncepro.com` on a Global External Application Load Balancer (Google-managed certificate + HTTP→HTTPS redirect) through a serverless NEG in `southamerica-west1` to `globe-studio-internal`. **Ten** Terraform resources in `front_door.tf` (plan `11 to add, 0 to change, 0 to destroy` — the eleventh addition is enabling `compute.googleapis.com`, which lives in `locals.tf`, not in `front_door.tf`; zero destroy/replace, zero Cloud Run in the diff), global IP `8.233.189.79`, `enable_cdn = false` because the surface is per-session SSO, and the NEG referencing the Cloud Run service as a **string literal** so a front-door plan never touches the services (they are governed by their own Terraform resources since TASK-1508). A serverless NEG is chosen over a Cloud Run domain mapping because mappings are Preview/region-limited (`southamerica-west1` unsupported) and because only the ALB enables `ingress internal-and-cloud-load-balancing`. Hard boundary: `globe-api-internal` never enters the NEG, never gets a custom domain, stays IAM-private (anonymous → `403`) with its audience derived from `run.app` and never from the browser domain; domain mappings in the project: 0. Its ingress was **not** touched and stays `all` deliberately — its perimeter is exclusively IAM + in-app ID-token verification, never ingress, because its caller reaches it over the internet from Vercel. Cutover order is a contract — **allowlist → three-way broker verification → `GLOBE_PUBLIC_BASE_URL` → smoke → ingress hardening → re-smoke** — because adding a redirect URI is inert while flipping the base URL first breaks SSO; the allowlist is operated by the new additive/subtractive primitive `updateSisterPlatformOAuthRedirectUris` (CLI `pnpm sister-platform:redirect`, dry-run by default), never by the seed script, which would truncate the array and rotate the client secret. Acceptance is the human-federation SSO smoke, calibrated against the previous origin before the cutover. Cost: ~US$18.25/month fixed + ~US$0.024/GiB (Cloud Billing Catalog API, "Networking" `E505-1604-58F8`, 2026-07-21). Rollback is an ordered sequence, not a menu (ingress → env var → DNS → ALB). `maxScale` untouched by this slice; the ingress value and both ceiling levels are governed by IaC since TASK-1508, which corrected the effective ceiling to 3 and left `deploy-internal.yml` passing only `--image`. Internal-only ≠ Production/HA/external access → TASK-1480. | [`EFEONCE_GLOBE_INTERNAL_FRONT_DOOR_V1.md`](EFEONCE_GLOBE_INTERNAL_FRONT_DOOR_V1.md) |
| SPEC-010 | Accepted and implemented — deployed + canary-verified internal-only (TASK-1528) | Media derivatives + Range delivery: implements ADR-008 build units 1-3. Six governed profile DATA entries (image thumb/preview WebP, video poster + MP4/H.264/AAC 720p transcode, audio waveform-peaks JSON + AAC preview), each parameter explicit (no ffmpeg defaults); exact immutable identity `(workspace, sourceSha256, sourceObjectGeneration, profileId, profileVersion, transformerVersion, outputMime)` — changing any component creates a new record, never an overwrite. Migration `0029`: intents queue (leases + fencing) + append-only records/attempts + workspace RLS + worker scan lane. A dedicated keyless Cloud Run Job (`apps/media-derivatives`, debian + version-pinned ffmpeg; bumping the pin requires bumping `MEDIA_TRANSFORMER_VERSION`) claims fenced intents `SKIP LOCKED`, downloads the source PINNED to the identity generation (drift = permanent failure), transforms deterministically per profile, and uploads content-addressed to a SEPARATE governed bucket with `ifGenerationMatch=0` + same-key 412 readback reconciliation. The `GET /v1/media/:sha256` gateway is an authority gateway: authenticate → verify principal-bound HMAC media ticket (own secret, 120s TTL, bound to workspace+experiment+sourceSha256+representation+disposition+principal) → re-run `authorizeOwnedOutput` NOW → resolve the READY representation (original or `profileId@version`) → pass ONE validated Range to GCS and pipe with backpressure (200/206/416 native, no arrayBuffer/Blob/base64, multipart 400). Request command + ticket mint are governed (capabilities `globe.media.derivative.read`/`.operate`); flags `GLOBE_MEDIA_DERIVATIVES_ENABLED`/`GLOBE_MEDIA_RANGE_GATEWAY_ENABLED` default OFF then flipped ON after canary. Feed/viewer consumption (TASK-1526) and orphan GC (TASK-1529, unblocked) are separate build units; external/commercial gated by TASK-1480. | [`EFEONCE_GLOBE_MEDIA_DERIVATIVES_V1.md`](EFEONCE_GLOBE_MEDIA_DERIVATIVES_V1.md) |
| ADR-010 | Accepted and implemented — deployed + canary-verified live (TASK-1535, 2026-07-24) | Commercial promotion via rights attestation. The readiness signature sat on the wrong unit (route × workspace, O(routes×workspaces)) — the fix relocates human judgment to the two facts that carry liability and removes it from the toil. (1) A new `requireHuman` **Model Commercial Rights Attestation**, recorded **once per model**, anchored to durable license evidence (`providerTermsRef` + `providerTermsDigest` + reviewer + the exact grant: commercial-use / client-delivery / sublicensable or their absence) — O(models), effectively O(providers). (2) An **automated route-promotion lane** (service principal distinct from attestor/promoter/checker) that, given a valid attestation + a passing objective eval, proposes the route, **derives the rights posture from the attestation** (never fabricates it) and drives the ADR-009 saga — zero per-route human keystroke. (3) A **per-workspace promotion ceiling** in `tenancy_workspaces.projection`, fail-closed by `kind`: an internal-eval-only route can NEVER be promoted to a `client` workspace. Two safety valves: **promotion ≠ delivery** (a promoted route is available, not auto-approved — every client artifact still passes candidate→human approval), and **the attestation is SSOT, every promotion a derivation**. Does NOT relax `recordModelReadinessReview` (stays human), does NOT fabricate rights (posture always mirrors the attested license), reuses the ADR-009 saga unchanged. | [`EFEONCE_GLOBE_COMMERCIAL_PROMOTION_ATTESTATION_DECISION_V1.md`](EFEONCE_GLOBE_COMMERCIAL_PROMOTION_ATTESTATION_DECISION_V1.md) |
| ADR-011 | Accepted; implementation and commercial rollout gated | Video Effectiveness Agent as one commercial, cross-product Globe capability with a standalone analysis surface. Generated outputs, canonical private uploads and assets from any authorized Globe domain converge on one governed `assetRef`; the analyzer creates no parallel uploader or asset store. Gemini through the governed Google Cloud/Vertex boundary is the first video-native provider family, but the domain remains vendor-neutral and exact routes require a blinded expert bake-off. Deterministic media evidence and timestamp/frame verification remain separate from model interpretation. Creative/channel fit can operate without history; numeric performance forecasts are forbidden unless a versioned calibration policy proves sufficient comparable first-party data. The agent recommends but never approves, publishes, spends, changes rights or promotes itself. | [`EFEONCE_GLOBE_VIDEO_EFFECTIVENESS_AGENT_DECISION_V1.md`](EFEONCE_GLOBE_VIDEO_EFFECTIVENESS_AGENT_DECISION_V1.md) |
| SPEC-011 | Accepted design; implementation and commercial rollout pending | Executable architecture for the Video Effectiveness Agent: canonical governed-asset intake over TASK-1467/ADR-007, cross-domain `assetRef`, standalone surface plus embedded entry points, explicit brief intake, durable state machine, deterministic preflight, dense evidence through `apps/media-derivatives`, provider-neutral `VideoAnalysisPort`, Gemini/Vertex primary candidate plus Claude/OpenAI evidence-bundle challengers, versioned creative/channel rubrics, immutable timestamp-addressable reports, human disposition, Level 0/1/2 forecast eligibility, tenant-isolated outcome feedback, Full API Parity and six compact implementation slices. | [`EFEONCE_GLOBE_VIDEO_EFFECTIVENESS_AGENT_V1.md`](EFEONCE_GLOBE_VIDEO_EFFECTIVENESS_AGENT_V1.md) |
| ADR-012 | Accepted; implementation and commercial rollout gated | Storyboard Studio is a first-class Globe surface backed by Narrative Preproduction, not a Producer or Video Effectiveness subfeature. Script and Storyboard are sibling revisioned aggregates; collaboration is revision-addressed; visual markup is non-destructive; masked edits hand off to Producer; AI proposes structured diffs; per-shot mixed-origin realization composes captured, recorded, generative, licensed, archival and deterministic contributions without reusing commercial `Hybrid`. | [`EFEONCE_GLOBE_STORYBOARD_STUDIO_DECISION_V1.md`](EFEONCE_GLOBE_STORYBOARD_STUDIO_DECISION_V1.md) |
| SPEC-012 | Accepted design; implementation and commercial rollout pending | Executable Narrative Preproduction architecture: aggregates/revisions, approval and concurrency invariants, typed mentions/anchors/vector annotations/masks, bounded proposal workflow, ShotRealizationPlan, Producer and Video Effectiveness handoffs, access/privacy/retention, Structured Sequence Canvas, deterministic exports, multi-shot realization orchestration and TASK-1543…1550 delivery sequence. | [`EFEONCE_GLOBE_STORYBOARD_STUDIO_V1.md`](EFEONCE_GLOBE_STORYBOARD_STUDIO_V1.md) |
| ADR-013 | Accepted; route-based foundation implemented, per-route rollout governed (TASK-1553) | Route-based model resolution + extensible multi-model catalog. `estimate.model` is a function of the exact `routeId`, so N models per provider coexist; a declared route without an adapter entry fails closed. The adapter table owns executable identity + slug, the binding owns promotion state + a derived snapshot, and the endpoint allowlist owns the production wire; `producer.route.binding_model_mismatch` watches the equality chain. Update = version bump within one route; add = a new route; lineage is immutable. The public catalog remains slug-free. As of 2026-07-30, Seedream 5 Pro, Nano Banana Pro/2, GPT Image 2/1.5 and Recraft v4.1 are simultaneously available and exercised through the Producer; every future exact identity still requires its own evaluation, rights, binding/readiness/circuit and real UI canary. | [`EFEONCE_GLOBE_ROUTE_BASED_MODEL_RESOLUTION_DECISION_V1.md`](EFEONCE_GLOBE_ROUTE_BASED_MODEL_RESOLUTION_DECISION_V1.md) |
| ADR-002 | Accepted and implemented — live-verified through the seam (TASK-1490); service rollout tracked in `Handoff.md` | Cross-model edit/refine in the Model Lab. One transport-neutral semantics, `editFrom = { experimentId }`, for every editable model instead of provider vocabulary (`previousInteractionId` deprecated and mutually exclusive with it — sending both is `invalid_request`, never resolved by precedence). NO dedicated `edit` command: an edit IS an experiment, on the same authority (`globe.lab.experiment.run`), spend fence, state machine and manifest. The caller declares the edit's own `capability`/`referenceRoute`/`hardCapCredits` — precisely what makes cross-model refinement possible (inheriting them from the parent would forbid it). The edit base never enters `authorizedInputs`, which stays pure caller declaration, and travels as `CreativeProviderRequestV1.editReference`, so an adapter tells "the thing being edited" apart from "additional references" instead of hoping it lands at index 0. A derivative carries the platform-certified `derived-internal` rights posture plus the parent's inherited constraints, rather than being laundered as `internal-owned`. And the chainability of a provider run reference is certified by the ADAPTER (`providerRunChainable`), never inferred by the domain, so a doomed chain is refused before the fence reserves. | [`EFEONCE_GLOBE_MODEL_LAB_V1.md`](EFEONCE_GLOBE_MODEL_LAB_V1.md) (§ Edit / refine cross-model) |
| ADR-014 | Accepted; **foundation implemented (TASK-1556 slices 1-3, 2026-07-25) — no surface ported yet**; share board blocked on approved visual direction; commercial host still deferred by ADR-004 | Globe client application: the entire browser payload of **every** Globe human surface migrates from server-concatenated string templates + `Function.prototype.toString()` serialization to a typed, componentized React + Vite client served as static assets by the SAME `studio-web` Node server. Exercises ADR-004's framework revisit trigger (TASK-1505's client UI IS being built) while leaving its **host** half deliberately deferred — React components port to a future host, string templates get rewritten, so staying on the current pattern is what forecloses ADR-004's open door. Scope is all five HTML surfaces (`launch`, `studio`, `error`, `producer`, `share`), not Producer alone: verified that Producer is internal-only by hard identity gate (`isEligibleInternalIdentity`) and the ONLY client-facing surface is the unauthenticated `/shares/:shareId` board — 15 lines with 3,071 chars of CSS on one line and brand tokens re-typed by hand that have ALREADY drifted from Producer's. Four parallel `:root` token blocks across three files (two inside `producer-ui.ts`) collapse into one SSOT with a drift guard; 184 raw hex occurrences (63 unique colors) bypass the ~30 existing semantic tokens with nothing able to stop them, because Globe has ZERO of Greenhouse's five UI quality gates and a `String.raw` cannot be linted — so the migration ships the gate machinery, not just the framework. Trust boundary untouched (ADR-005 §3/§4 normative), CSP posture preserved exactly (nonce already allows external script/style — verified), no Greenhouse primitives imported (TASK-1540). Strangler behind `GLOBE_CLIENT_APP_ENABLED` default-OFF, client-facing surfaces first; Storyboard (TASK-1547), Video Effectiveness (TASK-1540) and delivery (TASK-1472) are BORN on the new payload, never ported. | [`EFEONCE_GLOBE_CLIENT_APPLICATION_DECISION_V1.md`](EFEONCE_GLOBE_CLIENT_APPLICATION_DECISION_V1.md) |
| ADR-015 | **Partially implemented; funding lane live, agent-native extension in TASK-1629, convergence in TASK-1630** | **Greenhouse administra Globe; Globe conserva la autoridad financiera.** El fondeo usa plan durable `propose → confirm`, firma/mutación server-side y evidencia append-only. Una instrucción atribuida del CEO puede autorizar a un humano o usuario agente autenticado para ejecutar el flujo completo; el segundo humano es policy opcional y queda OFF en el workspace owner-operated. Scopes, entitlements, instrucción/delegación y límites se aplican server-side; workloads, service principals, API keys y modos desconocidos nunca confirman. Confirmaciones ambiguas se recuperan por status/readback con la misma operation key. | [`EFEONCE_GLOBE_GREENHOUSE_ADMINISTRATION_DECISION_V1.md`](EFEONCE_GLOBE_GREENHOUSE_ADMINISTRATION_DECISION_V1.md) |
| ADR-017 | Accepted v2.0 (2026-07-30); implementación pendiente (dueño `TASK-1485`) | **Color scheme y arquitectura de superficie del payload cliente.** Las v1.0/v1.1 razonaron sobre una premisa falsa —que `--action #4db8ff` era la marca de Globe— y el **Delta v2 la invierte con el design system en la mano**: el theme vigente define **primary `#FF6500` (naranja)** y **secondary `#4A108C` (morado)**; el azul **no ocupa ningún rol** y venía de un prototipo de Claude Design que inventó su paleta. El naranja, que el runtime usa como acento menor (`--warm`), **es el primary**. Consecuencia: el chasis **se ADOPTA, no se desatura** — las superficies son tokens globales (`Misc`: `body-bg` `#F8F7FA`/`#25293C`, `paper` `#FFFFFF`/`#2F3349`), ya presentes en `@efeoncepro/axis-tokens` byte por byte, y **`#25293C` es hue 230 / sat 24 %: exactamente el neutro al que la v1.1 llegó por cálculo** — el equipo ya lo había resuelto y el runtime nunca adoptó su theme (los valores de la v1.1 quedan **retirados**). Se decide además la **arquitectura de superficie**: dos planos no alcanzan, así que la escala se extiende a **hundido** (`#181B28`/`#ECEAF1`) y **elevado** (`#3B405C`/`paper+sombra`), y la asignación es la **variante C — el contenedor del contenido se hunde y las piezas SUBEN a `paper`** (separación card↔fondo **1,379 dark / 1,193 claro**, contra 1,157/1,067 de A y 1,192/**1,118** de B). **C es la única que funciona en los dos modos**, porque en el extremo claro los planos se comprimen y bajar el fondo no alcanza: hay que subir el contenido a blanco puro. **B descartada** — sólo sirve en oscuro, invierte la elevación de Material (contenido por debajo del fondo) y responde al patrón de app de **lienzo** (Figma/Runway) cuando el Producer es un **feed** (Behance/Dribbble): error de categoría del autor, corregido por el operador. El viewer y el share board **sí** usan lienzo hundido con la pieza sola. 🔴 El contenedor hundido **NO es una card** (sin sombra propia, o es card-on-card). Se declara el **presupuesto del naranja** (CTA, estado activo, foco; nunca superficie, nunca estado —está a 21–27° de `warning`/`error`—, nunca identidad ni etiquetas), que **el CTA lleva texto oscuro** (blanco da 2,95:1 y falla; el oscuro del theme 4,64:1), que **las dos familias de sombra no son intercambiables** (`Primary/Globe` teñida = acción; `gray` = superficie: elevar un header con la de marca produce un halo naranja), y que **el morado no es usable en dark** — ningún escalón de `secondary` llega al piso de texto (1,19 / 2,00 / 3,59) y el único que pasa es **`orchid-300` 4,80:1**, lo que reencuadra a orchid como la versión operable de secondary en oscuro. Corrige el argumento del dark-only: se apoyaba en que el modo claro no existía, y **existe**; la decisión se mantiene sólo por la razón colorimétrica. | [`EFEONCE_GLOBE_CLIENT_COLOR_SCHEME_DECISION_V1.md`](./EFEONCE_GLOBE_CLIENT_COLOR_SCHEME_DECISION_V1.md) |
| ADR-016 | Accepted (2026-07-27); motor implementado — retiro del legacy gated por `TASK-1560` (dueño `TASK-1485`) | Motor de estilos del payload cliente: **Tailwind v4 con el SSOT `tokens.ts` como theme**. Reemplaza el CSS global plano, cuyo modo de falla era estructural y no de criterio: seis colisiones medidas en una sesión, todas consecuencia de que cada clase compite en un único espacio de nombres con dos hojas conviviendo. Supersede parcialmente ADR-014 **sólo** en la elección de motor. Cinco condiciones lo separan de «sumar una herramienta»: el SSOT manda (un token se declara una vez), los gates se **reescriben** —un gate que deja de morder al cambiar de motor no era un gate sino un accidente de ubicación—, migración por superficie nunca big-bang, cero reescritura sin diff de referencia, y CSP por nonce intacta. **El idiom de alias de la documentación de Tailwind NO funciona acá y está medido:** en Globe los nombres del SSOT ya coinciden con los namespaces de v4, así que `@theme inline { --text-xs: var(--text-xs) }` es una referencia circular que rinde `text-xs` a 16px y `rounded-sm` a 0 **con el build verde**; el theme se **genera** con valores (`pnpm theme:generate`). Tres modos de falla silenciosa documentados: alias circular, **namespace vaciado y no repoblado** (los seis `backdrop-blur-*` computando `none`) y —Delta 2026-07-29— la **frontera token↔uso**, donde una combinación de dos valores ambos legítimos (`--weight-display: 700` sobre una familia que no carga 700) se sintetiza, renderiza y pasa los cinco gates; su contrato de uso vive en [`EFEONCE_GLOBE_CLIENT_TYPOGRAPHY_CONTRACT_V1.md`](./EFEONCE_GLOBE_CLIENT_TYPOGRAPHY_CONTRACT_V1.md). Preflight queda **fuera** hasta que migre la última superficie (adoptarlo es el big-bang que el ADR prohíbe) — y ése es el origen del `bolder` del UA que ningún gate puede ver. Regla que se desprende y aplica a todo el repo del payload: **documentar un anti-patrón dentro del árbol que Tailwind escanea lo materializa como clase real**. | [`EFEONCE_GLOBE_CLIENT_STYLING_ENGINE_DECISION_V1.md`](./EFEONCE_GLOBE_CLIENT_STYLING_ENGINE_DECISION_V1.md) |
