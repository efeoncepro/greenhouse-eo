# Greenhouse UI Platform — Internationalization (i18n)

> Parte de **Greenhouse UI Platform**. Índice: [README.md](./README.md). Historial: [HISTORIAL.md](./HISTORIAL.md).

## Estado canónico

- **Librería:** `next-intl` en App Router. Portal privado **state-only sin locale prefix** por defecto.
- **Resolución de locale runtime:** cookie `gh_locale` + `Accept-Language` + fallback `es-CL` (`src/i18n/*`); App Router envuelto por `NextIntlClientProvider`; `<html lang>` usa el locale efectivo.
- **Locales:** `es-CL` (default) · `en-US` (activado: shell navigation + namespaces shared) · `pt-BR` (planned).
- **Tipografía Latin-first + RTL-ready** vía CSS logical properties; CJK diferido. Ver la skill `typography-design` (`references/i18n-typography.md`).

**Fuente canónica:** [GREENHOUSE_I18N_ARCHITECTURE_V1.md](../GREENHOUSE_I18N_ARCHITECTURE_V1.md) (decisión TASK-428) + runtime TASK-430.

Deltas de origen: [HISTORIAL.md](./HISTORIAL.md) → Deltas 2026-05-06b (arquitectura, TASK-428) y 2026-05-06c (runtime, TASK-430).

## Microcopy (es-CL) — frontera con el contenido

El *texto* (palabras, tono es-CL tuteo) es propiedad de la skill `greenhouse-ux-writing` + `src/lib/copy/*` + `src/config/greenhouse-nomenclature.ts` (Copy System Contract TASK-265, HISTORIAL Delta 2026-05-02). Este doc cubre la *infraestructura* i18n, no las cadenas.
