# Greenhouse Architecture Decisions Index

Este indice lista decisiones arquitectonicas aceptadas o formalizadas en Greenhouse. No reemplaza las specs canonicas: apunta a ellas para que los agentes encuentren rapido el contrato vigente.

Regla canonica: `docs/operations/ARCHITECTURE_DECISION_RECORD_OPERATING_MODEL_V1.md`.

## Como usar este indice

- Antes de crear una task o plan que toque arquitectura, buscar aqui una decision vigente.
- Si la decision vive en una spec de dominio, actualizar esa spec y este indice.
- Si una decision cambia, crear una nueva decision y marcar la anterior como superseded/deprecated donde vive.
- No duplicar el contenido completo del ADR en esta tabla.

## Decisiones vigentes

| Decision | Status | Scope | Canonical doc | Source / notes |
| --- | --- | --- | --- | --- |
| Adoptar ADRs distribuidos con indice maestro | Accepted | Documentacion / arquitectura / multi-agente | [ARCHITECTURE_DECISION_RECORD_OPERATING_MODEL_V1.md](../operations/ARCHITECTURE_DECISION_RECORD_OPERATING_MODEL_V1.md) | Formalizado 2026-05-08. |
| Full API parity: toda capacidad Greenhouse debe tener o planificar un equivalente programatico gobernado | Accepted | Platform / API / Product / UI / MCP / Integrations / Agents | [GREENHOUSE_FULL_API_PARITY_DECISION_V1.md](GREENHOUSE_FULL_API_PARITY_DECISION_V1.md) | Aceptado 2026-06-03; la UI no es source of truth de logica de negocio, debe consumir primitives server-side y contratos API/app/MCP/CLI cuando el dominio requiera automatizacion o integracion. La spec API Platform consume esta decision. |
| Journey Intelligence + Touchpoint Ledger como capa transversal read-only para trazabilidad causal cross-domain | Accepted | Platform / Data / Product Operations / Notifications / HR / Finance / Commercial / AI context | [GREENHOUSE_JOURNEY_INTELLIGENCE_LAYER_V1.md](GREENHOUSE_JOURNEY_INTELLIGENCE_LAYER_V1.md) | 2026-05-31; observa outbox/audit/notification/email/Teams/webhook events y compone journeys + touchpoints sin reemplazar sources of truth ni ejecutar workflows en V1. |
| CI se optimiza por costo por señal útil, no apagando gates | Accepted | Platform / CI / GitHub Actions / Reliability / FinOps | [GREENHOUSE_CI_COST_SIGNAL_GUARDRAILS_V1.md](GREENHOUSE_CI_COST_SIGNAL_GUARDRAILS_V1.md) | TASK-931; define lanes `fast/full/smoke/release/scheduled`, latest-only PR/develop, reporte `pnpm actions:cost:audit` y frontera con TASK-859. |
| Spec-Driven Development es práctica explícita; invariantes se promueven de prosa (L0) a check ejecutable (L2) por criterio, no por reflejo | Accepted (modelo) | Documentacion / arquitectura / governance / CI / multi-agente | [GREENHOUSE_SPEC_DRIVEN_DEVELOPMENT_V1.md](GREENHOUSE_SPEC_DRIVEN_DEVELOPMENT_V1.md) | 2026-05-24; escalera de promoción L0→L1→L2 + patrón canónico (regla → check declarativo → rollout warn-first → legacy-exempt → gate CI). Generaliza `design:lint`/lint rules/parity tests. Primera implementación TASK-926 complete (`pnpm task:lint`, `--active`, `--changed`, `--task`); follow-ups: collision detector Files owned, parity tipos↔OpenAPI. |
| i18n usa `next-intl`, portal privado sin locale prefix por defecto | Accepted | Platform UI / Identity / copy / routing | [GREENHOUSE_I18N_ARCHITECTURE_V1.md](GREENHOUSE_I18N_ARCHITECTURE_V1.md) | ADR de TASK-428; `TASK-430` y `TASK-431` lo consumen. |
| Runtime theme es source of truth para tokens visibles | Accepted | UI platform / design tokens | [GREENHOUSE_THEME_TOKEN_CONTRACT_V1.md](GREENHOUSE_THEME_TOKEN_CONTRACT_V1.md) | TASK-368; heredado por `GREENHOUSE_DESIGN_TOKENS_V1.md` y DESIGN.md. |
| `DESIGN.md` refleja runtime, no lo genera | Accepted | UI platform / agent-facing visual contract | [GREENHOUSE_DESIGN_TOKENS_V1.md](GREENHOUSE_DESIGN_TOKENS_V1.md) | TASK-764; `pnpm design:lint` es gate. |
| GSAP queda adoptado como carril especializado de motion avanzado, no como reemplazo de Framer Motion | Accepted | UI platform / motion system / animation libraries | [GREENHOUSE_GSAP_ADOPTION_DECISION_V1.md](GREENHOUSE_GSAP_ADOPTION_DECISION_V1.md) | 2026-05-17; wrappers canonicos `src/libs/GSAP.tsx` y `src/libs/GSAPScrollTrigger.tsx`; microinteracciones normales siguen Framer/CSS/auto-animate. |
| Sample Sprint no es entidad nueva; es `services.engagement_kind != 'regular'` | Accepted | Commercial / Agency / Sample Sprints | [GREENHOUSE_PILOT_ENGAGEMENT_ARCHITECTURE_V1.md](GREENHOUSE_PILOT_ENGAGEMENT_ARCHITECTURE_V1.md) | EPIC-014; rechaza tabla `sample_sprints`. |
| Commercial Health lee primitives canonicas y degradea a `unknown` ante error | Accepted | Commercial reliability / Ops Health | [GREENHOUSE_PILOT_ENGAGEMENT_ARCHITECTURE_V1.md](GREENHOUSE_PILOT_ENGAGEMENT_ARCHITECTURE_V1.md) | TASK-807; helper `src/lib/commercial/sample-sprints/health.ts`. |
| Anti-zombie de engagements vive como trigger, no CHECK | Accepted | Commercial / PostgreSQL / data quality | [GREENHOUSE_PILOT_ENGAGEMENT_ARCHITECTURE_V1.md](GREENHOUSE_PILOT_ENGAGEMENT_ARCHITECTURE_V1.md) | TASK-810; PostgreSQL no admite subqueries en CHECK. |
| Organization Workspace projection separa `views` visibles y `entitlements` finos | Accepted | Identity / Access / Organization Workspace | [GREENHOUSE_ORGANIZATION_WORKSPACE_PROJECTION_V1.md](GREENHOUSE_ORGANIZATION_WORKSPACE_PROJECTION_V1.md) | TASK-611; projection server-only + cache TTL 30s. |
| External identity links se leen Postgres-first desde `identity_profile_source_links` | Accepted | Identity / Workforce / Notion / BigQuery projections | [GREENHOUSE_PERSON_IDENTITY_CONSUMPTION_V1.md](GREENHOUSE_PERSON_IDENTITY_CONSUMPTION_V1.md#external-identity-source-links) | TASK-877; `members.notion_user_id` y BigQuery `team_members.notion_user_id` quedan como proyecciones/fallback, no source of truth. |
| Organization Workspace shell separa chrome compartido de facet content | Accepted | UI platform / Agency / Finance / Organization details | [GREENHOUSE_ORGANIZATION_WORKSPACE_PROJECTION_V1.md](GREENHOUSE_ORGANIZATION_WORKSPACE_PROJECTION_V1.md) | TASK-612/TASK-613; entrypoints gated por rollout flags. |
| Payment Orders pertenece a Finance/Tesoreria, no Payroll | Accepted | Finance / Payroll boundary / Treasury | [GREENHOUSE_PAYMENT_ORDERS_ARCHITECTURE_V1.md](GREENHOUSE_PAYMENT_ORDERS_ARCHITECTURE_V1.md) | TASK-747..751; Payroll calcula/exporta obligaciones, Finance paga. |
| Processor no es necesariamente instrumento de salida | Accepted | Finance / Treasury / Payment Orders | [GREENHOUSE_PAYMENT_ORDERS_ARCHITECTURE_V1.md](GREENHOUSE_PAYMENT_ORDERS_ARCHITECTURE_V1.md) | TASK-799; Deel es rail/counterparty, no source account. |
| FX drift en saldos bancarios se remedia con control plane acotado, no SQL/backfills ad hoc | Accepted | Finance / Treasury / Reliability / Ops Worker | [GREENHOUSE_FINANCE_ARCHITECTURE_V1.md](GREENHOUSE_FINANCE_ARCHITECTURE_V1.md#task-842--fx-drift-remediation-control-plane) | TASK-842; reader detallado + planner con policies + scheduler `ops-finance-fx-drift-remediate` + auditoria en `source_sync_runs`. |
| Postgres/Kysely TLS recovery se resuelve en primitive comun | Accepted | PostgreSQL / runtime resilience | [project_context.md](../../project_context.md) | 2026-05-03; no parchear endpoints aislados ante TLS retryable. |
| MCP remoto Greenhouse V1 es stateless + bearer token | Accepted | MCP / API Platform / Vercel runtime | [project_context.md](../../project_context.md) | TASK-741; OAuth multiusuario queda en TASK-659. |
| Contractor/honorarios es relacion nueva, no mutacion de `members.contract_type` | Accepted | Workforce / Payroll boundary / People 360 | [GREENHOUSE_PERSON_LEGAL_ENTITY_RELATIONSHIPS_V1.md](GREENHOUSE_PERSON_LEGAL_ENTITY_RELATIONSHIPS_V1.md) | TASK-789; employee closed + contractor separate relationship. |
| Professional Presence es faceta gobernada de Person 360, no identidad legal ni cargo laboral | Accepted | People / HR / Identity / client-safe profiles | [GREENHOUSE_PERSON_COMPLETE_360_V1.md](GREENHOUSE_PERSON_COMPLETE_360_V1.md) | Formaliza TASK-786 antes de implementacion; phone/contact internal-only por defecto. |
| Compliance exports Chile son proyecciones payroll versionadas, no source of truth ni calculo paralelo | Accepted | Payroll / HR compliance / Previred / LRE | [GREENHOUSE_HR_PAYROLL_ARCHITECTURE_V1.md](GREENHOUSE_HR_PAYROLL_ARCHITECTURE_V1.md) | TASK-812; exige discovery oficial antes de generadores y corrige supuestos Previred/LRE stale. |
| International Internal Contract Type es un perfil operacional controlado, no Deel/EOR ni employer-of-record local | Accepted (SHIPPED 2026-05-16) | HR / Payroll / Workforce / Data / Reliability | [GREENHOUSE_HR_PAYROLL_ARCHITECTURE_V1.md](GREENHOUSE_HR_PAYROLL_ARCHITECTURE_V1.md#delta-2026-05-16--task-894-international-internal-contract-type-foundation) | TASK-894 agrega `international_internal` como `ContractType` first-class (`payRegime='international'`, `payrollVia='internal'`) para pagos internacionales internos. Defense in depth: CHECK tuple en `members`, CHECK `(contract_type,pay_regime)` en `compensation_versions`, capability `payroll.contract.use_international_internal`, `legalReviewReference`, audit log append-only, outbox `member.contract_type.changed v1` sin reference cruda y 3 reliability signals. Sin backfill automatico de miembros reales. |
| Direct service expense allocation requiere ancla explicita expense -> service | Accepted | Finance / Commercial cost attribution | [GREENHOUSE_COMMERCIAL_COST_ATTRIBUTION_V1.md](GREENHOUSE_COMMERCIAL_COST_ATTRIBUTION_V1.md) | TASK-815; no inferir por cliente/nombre/linea. |
| Payroll se paga en arrears (sueldo mes N → primeros ~5 días del mes N+1); conciliacion bank↔payroll usa el lag | Accepted | Payroll / Finance / bank reconciliation | [GREENHOUSE_HR_PAYROLL_ARCHITECTURE_V1.md](GREENHOUSE_HR_PAYROLL_ARCHITECTURE_V1.md) | TASK-936; la descripcion del giro refiere al mes de devengo, no al egreso; giro en N+1 settlea nomina de N; pago sin nomina registrada del mes de devengo = nomina faltante, no adelanto por defecto. |
| Governance tables de capabilities tienen FK a registry + CI migration marker gate | Accepted | Identity / Access / DB migrations / CI | [Handoff.md](../../Handoff.md) | TASK-838/ISSUE-068; runtime guard + CI gate anti pre-up-marker bug. |
| Smoke-lane publisher usa WIF + Cloud SQL Connector + Postgres primitive resiliente | Accepted | CI / Reliability / PostgreSQL / GCP IAM | [GREENHOUSE_RELIABILITY_CONTROL_PLANE_V1.md](GREENHOUSE_RELIABILITY_CONTROL_PLANE_V1.md#delta-2026-05-09--issue-072-smoke-lane-publisher-reliability) | ISSUE-072; no silenciar warnings `sync:smoke-lane failed`, corregir shim/secrets/IAM/pool/backoff. |
| Atraso imputable y trazabilidad de reprogramación son dos conceptos separados (event logs distintos), no un número | Accepted (modelo) | Delivery / ICO / OTD / Payroll bonus / Notion | [GREENHOUSE_ATTRIBUTABLE_LATENESS_V1.md](GREENHOUSE_ATTRIBUTABLE_LATENESS_V1.md) | 2026-05-23; freeze de {Listo para revisión, Bloqueado, En pausa} desde `task_status_transitions`; reprogramación desde nuevo log `task_due_date_changes` con motivo; el motivo decide contra qué fecha mide el OTD (cliente→vigente, interno→original); anti-doble-descuento por partición disjunta; cutover del bono gated (estrangulador + HR sign-off). Implementa TASK-921 + TASK-922 + TASK-923 (clasificador ownership). Redefine la familia OTD (§17). Cierra ISSUE-081. |
| Onboarding de tenant Notion = control plane idempotente "adopt", no setup manual de fórmulas per-cliente | Accepted (modelo) | Delivery / ICO / Integrations / Platform / multi-tenant | [GREENHOUSE_CLIENT_ONBOARDING_PROVISIONING_V1.md](GREENHOUSE_CLIENT_ONBOARDING_PROVISIONING_V1.md) | 2026-05-23; híbrido (humano duplica Golden Template = clone Demo Greenhouse per `GREENHOUSE_TASK_STATUS_LIFECYCLE_V1`; Greenhouse `provision-tenant` adopta: verify schema → install `[GH]` props vía API → register → subscribe webhooks → smoke readiness). Habilitado por y secuenciado DESPUÉS de la migración de cómputo a Greenhouse (cómputo tenant-agnóstico → onboarding NO instala fórmulas). Reusa discover/register/`space_notion_sources`. Enforce vocabulary canónico al onboarding (NO aliases per-cliente). 3 reliability signals (schema_drift, unprovisioned, not_ready). Tasks follow-up no creadas. |
| Smoke-lane flaky no es failed final | Accepted | CI / Reliability / Playwright E2E | [GREENHOUSE_RELIABILITY_CONTROL_PLANE_V1.md](GREENHOUSE_RELIABILITY_CONTROL_PLANE_V1.md#delta-2026-05-09--issue-073--task-607-flaky-semantics--github-actions-runtime) | ISSUE-073/TASK-607; parser canónico usa último intento, `failed -> passed` queda `flaky`, no incrementa `failed_tests`. |
| Smoke specs navegan via helper resiliente, no `page.goto` crudo | Accepted | CI / Reliability / Playwright E2E / Staging probes | [GREENHOUSE_RELIABILITY_CONTROL_PLANE_V1.md](GREENHOUSE_RELIABILITY_CONTROL_PLANE_V1.md#delta-2026-05-09--issue-073-follow-up-smoke-navigation-contract) | Follow-up ISSUE-073; `gotoWithTransientRetries` / `gotoAuthenticated` son primitives canonicas y `scripts/lib/e2e-smoke-navigation-contract.test.ts` bloquea regresion. |
| Cross-runtime observability via `@sentry/node` + helper `initSentryForService` | Accepted | Observability / Cloud Run / Vercel | [CLAUDE.md](../../CLAUDE.md#cross-runtime-observability--sentry-init-invariant-task-844) | TASK-844; cierra ISSUE-074 — `src/lib/observability/capture.ts` usa `@sentry/node` runtime-portable, cada Cloud Run service inicializa Sentry hub explícitamente antes de aceptar HTTP. Lint rule + reliability signal anti-regresión. |
| Greenhouse Visual Capture (`pnpm fe:capture`) reemplaza scripts Playwright ad-hoc per-task | Accepted | Frontend tooling / Visual QA / Microinteractions / Agent loops | [GREENHOUSE_FRONTEND_CAPTURE_HELPER_V1.md](GREENHOUSE_FRONTEND_CAPTURE_HELPER_V1.md) | 2026-05-12; herramienta canónica Playwright + agent auth + scenario DSL tipado bajo `scripts/frontend/`. V1.1: GCS upload, mobile viewport, diff structural, health, audit JSONL, review dossier y capability `platform.frontend.capture_prod` para Triple Gate. V1.2: hook obligatorio de verificación visual UI. V1.3: nombre `Greenhouse Visual Capture` (`GVC`) + scroll resiliente con `scroll selector`, `scrollTo`, `mark fullPage` y `mark clipSelector`. Reusa `playwright-auth-setup.mjs`; output gitignored bajo `.captures/`. |
| Connection pooling Postgres V1 data-driven + V2 PgBouncer GKE Autopilot contingente | Accepted | PostgreSQL / Cloud SQL / Vercel + Cloud Run runtimes | [GREENHOUSE_POSTGRES_CONNECTION_POOLING_V1.md](GREENHOUSE_POSTGRES_CONNECTION_POOLING_V1.md) | TASK-846 (V1 deployed) + TASK-847 (V2 contingente). V1: defense-in-depth 3 capas sin multiplexer (ALTER ROLE idle_session_timeout + runtime-aware pool sizing + reliability signal data-driven). V2: PgBouncer GKE Autopilot deploy si signal `runtime.postgres.connection_saturation` sustained > 60%. Pivot crítico: Cloud Run NO soporta TCP raw → multiplexer va en GKE. Trigger: PG saturation 103% + Sentry weekly report 7 errors NEW. |
| Production release control plane vive en `greenhouse_sync.release_manifests` + state machine append-only + 4 reliability signals separados | Accepted | Platform / DevOps / Cloud / Reliability | [GREENHOUSE_RELEASE_CONTROL_PLANE_V1.md](GREENHOUSE_RELEASE_CONTROL_PLANE_V1.md) | TASK-848 V1; concurrency fix Opcion A kills deadlock 2026-04-26 → 2026-05-09, 3 capabilities granulares least-privilege, 7 outbox events `platform.release.* v1`, anti-UPDATE/DELETE triggers. Rollback automatico Vercel + Cloud Run; Azure manual gated V1. |
| Production release watchdog usa GitHub App installation token canonico, NO PAT personal | Accepted | Platform / DevOps / Auth / Reliability | [GREENHOUSE_RELEASE_CONTROL_PLANE_V1.md](GREENHOUSE_RELEASE_CONTROL_PLANE_V1.md#delta-2026-05-10--v11-github-app-shipped-live) | TASK-849 V1.1 SHIPPED LIVE 2026-05-10. App ID `3665723`, Installation ID `131127026` en `efeoncepro` org, private key en GCP Secret Manager `greenhouse-github-app-private-key`. Beneficios: token NO ligado a usuario individual + rate limit 15K vs 5K req/h + auditoria per-installation. Costo: $0 GitHub side + ~$0.72/anio GCP. Resolver `src/lib/release/github-app-token-resolver.ts` mintea installation token con cache 1h, fallback a PAT como degraded mode. Setup orquestado por `pnpm release:setup-github-app` (~5 min, 2 clicks browser). Bugs corregidos durante setup live: race condition `/start`↔`/callback`, `hook_attributes` validation, PKCS#1 vs PKCS#8. |
| Production preflight CLI usa composer pattern TASK-672, NO single-CLI monolithic | Accepted | Platform / DevOps / Reliability | [CLAUDE.md](../../CLAUDE.md#production-preflight-cli-invariants-task-850) | TASK-850 (Slices 0-8 SHIPPED). Composer puro `composeFromCheckResults` + runner async `runPreflight` con `Promise.all` + `withSourceTimeout` per-check (TASK-672 mirror). Reusable desde CLI + TASK-851 orchestrator workflow + TASK-855 dashboard. Contract versionado `production-preflight.v1` estabiliza shape para consumers downstream. Code constants para `release_batch_policy` heuristic (YAGNI promote-a-PG). 3 capabilities sub-granulares least-privilege (`platform.release.preflight.{execute,read_results,override_batch_policy}`); override solo EFEONCE_ADMIN con audit. Severity differentiated per check: STRICT (Sentry, GCP, Postgres, batch_policy) vs DEGRADED (Vercel, Azure WIF). 69 tests verdes anti-regresion. |
| Production release orchestrator workflow + worker SHA verification compactados en TASK-851 | Accepted | Platform / DevOps / CI | [CLAUDE.md](../../CLAUDE.md#production-release-orchestrator-invariants-task-851) | TASK-851 (Slices 0-4 SHIPPED). Compactacion arch-architect del scope original TASK-851 + TASK-852: orquestador y SHA verification son arquitecturalmente acoplados (workflow_call invoca workers + verifica GIT_SHA matches). production-release.yml workflow_dispatch con 8 jobs canonicos (preflight + record-started + approval-gate + 4 workers parallel + wait-vercel + post-release-health + transition-released + summary). Worker workflows aceptan workflow_call canonico (environment + expected_sha + GCP_WORKLOAD_IDENTITY_PROVIDER) preservando push/dispatch existing. Worker deploy.sh aceptan EXPECTED_SHA env var + post-deploy verify gcloud run revisions describe. CLI scripts orchestrator-record-started + orchestrator-transition-state invocables desde workflow YAML (no API admin endpoint). State machine TS↔SQL parity test live skipea sin DB. Concurrency canonica `production-release-${{ inputs.target_sha }}` cancel-in-progress=false (distinct SHAs no race; partial UNIQUE INDEX V1.0 enforce 1 release activo per branch). Tests anti-regresion concurrency-fix-verification + state-machine parity verdes. |
| Azure infra release gating: health-check siempre + Bicep apply gated por diff o force flag | Accepted | Platform / DevOps / Cloud / Azure | [CLAUDE.md](../../CLAUDE.md#azure-infra-release-gating-invariants-task-853) | TASK-853 (Slices 0-3 SHIPPED). 2 workflows Azure (teams-notifications + teams-bot) refactored a 5 jobs canonicos (health-check + validate + diff-detection + deploy + skip-deploy-summary). workflow_call canonico con inputs.{environment, target_sha, force_infra_deploy} + secrets.AZURE_*. Health check (WIF + providers + RG) corre SIEMPRE preflight-style. Bicep apply gated por `force_infra_deploy=true` (operator override) o push event con path filter o git diff `origin/main~1...target_sha -- 'infra/azure/<sub>/**'`. Orquestador (TASK-851) wires los 2 jobs Azure en paralelo con workers Cloud Run via `secrets: inherit` (patron canonico GH Actions para environment-scoped secrets — AZURE_* viven en environment production scope, no repo-level). 11 tests anti-regresion verdes (workflow_call contracts + push trigger preserved + 5 jobs canonicos + orchestrator wiring). Runbook ampliado §6.1 gating + §6.2 WIF subjects + §6.3 rollback V2 contingente. Cero outbox events nuevos, cero capabilities nuevas, cero reliability signals nuevos. Rollback Azure NO automatizado V1 (Bicep reapply destructivo); V2 contingente con `what-if` mandatory. |
| Release observability completion: 2 signals (deploy_duration_p95 + last_status) + dashboard /admin/releases | Accepted | Reliability / Platform / DevOps | [CLAUDE.md](../../CLAUDE.md#release-observability-completion-invariants-task-854) | TASK-854 (Slices 0-2 SHIPPED). Cierra subsystem `Platform Release` con 5 of 5 signals canonicos. `platform.release.deploy_duration_p95` (kind=lag, ventana 30d, p95 sobre releases released solamente, severity ok <30min/warning 30-60/error >=60). `platform.release.last_status` (kind=drift, ultimo release main, severity per estado + ventana incidente 24h/7d). Wire-up canonico en `getReliabilityOverview.productionRelease[]` 5 readers en paralelo. Dashboard `/admin/releases` V1 read-only con cursor pagination keyset on started_at DESC + tabla TanStack 6 cols + drawer manifest viewer anchor='right' 480px + banner Alert condicional + EmptyState + comando rollback copy-to-clipboard via sonner. Auth via `platform.release.execute` (read-equivalent V1). Skills invocadas pre-implementacion (per usuario): greenhouse-ux + greenhouse-microinteractions-auditor + greenhouse-ux-writing — plan UX explicito impreso ANTES de codigo. Microcopy canonical en `src/lib/copy/release-admin.ts` (GH_RELEASE_ADMIN). 16 tests anti-regresion verdes (deploy_duration + last_status). Cero outbox events nuevos, cero capabilities nuevas, cero migrations. |
| Firmas digitales de representantes legales viven como recurso canónico reutilizable, NO ad-hoc por flow | Accepted | Identity / Documents / Legal artifacts | [GREENHOUSE_LEGAL_SIGNATURES_PLATFORM_V1.md](GREENHOUSE_LEGAL_SIGNATURES_PLATFORM_V1.md) | TASK-863 V1.4. Helper canónico `@/lib/legal-signatures` con `buildSignatureFilenameForTaxId` + `resolveLegalRepresentativeSignaturePath` + `getLegalRepresentativeSignatureAbsolutePath`. Convención de filename `{taxId_normalizado}.png` en `src/assets/signatures/`. Path-safe protection (rechaza `..`, paths absolutos, extensiones no png/jpg/jpeg). Graceful fallback `null` cuando archivo no existe → línea queda vacía para firma manual post-impresión. Reusable desde cualquier flow legal del repo (finiquitos hoy; contratos, addenda, cartas formales mañana) sin reimplementación inline. V2 path-forward: migrar storage a asset privado canónico (`greenhouse_core.assets` + FK desde `organizations.legal_representative_signature_asset_id`) preservando signature pública del helper. 11 tests anti-regresión verdes (path traversal, RUT cleaning, file-not-found graceful, real file resolution). |
| Cláusulas legales del finiquito separan hitos temporales distintos y usan verbo performativo state-conditional | Accepted | Payroll / HR / Legal documents | [GREENHOUSE_FINAL_SETTLEMENT_V1_SPEC.md](GREENHOUSE_FINAL_SETTLEMENT_V1_SPEC.md#delta-2026-05-11-v15--5-bloqueantes-legales-cerrados) | TASK-863 V1.5. (a) Cláusula PRIMERO separa `resignationNoticeSignedAt` (firma trabajador) de `resignationNoticeRatifiedAt` (ratificación notarial art. 177 CT) — mezclarlas es vicio defendible en demanda chilena. Pre-ratificación renderiza "cuya ratificación ante ministro de fe se efectuará..."; post-ratificación renderiza "...ratificada ante ministro de fe el {ratifiedAt} conforme al artículo 177 del Código del Trabajo". (b) Cláusula SEGUNDO con verbo performativo `isRatified`-conditional: pre-ratificación → "declara que recibirá, al momento de la ratificación..." (futuro condicional); post-ratificación → "declara haber recibido en este acto..." (perfecto consumado). Evita vicio de consentimiento en documentos draft. (c) Cláusula CUARTO cita artículo operativo `art. 13 de la Ley N° 14.908 sobre Abandono de Familia y Pago de Pensiones Alimenticias, en su texto modificado por la Ley N° 21.389 de 2021` — citar solo la ley modificatoria es jurídicamente débil. Auditado por skill `greenhouse-payroll-auditor` + UX writing es-CL formal-legal + modern-ui en review comprehensive enterprise. |
| PDF de documentos legales regenera automáticamente al transicionar a issued/signed_or_ratified | Accepted | Payroll / HR / Documents / State machine | [GREENHOUSE_FINAL_SETTLEMENT_V1_SPEC.md](GREENHOUSE_FINAL_SETTLEMENT_V1_SPEC.md#delta-2026-05-11-v11--auto-regeneracion-canonica-al-transicionar) | TASK-863 V1.1. Helper privado `regenerateDocumentPdfForStatus` en `document-store.ts` reemplaza `pdf_asset_id` del mismo documento (sin bump versión, sin alterar state machine, sin reissue) cuando transita a `issued` o `signed_or_ratified`. Idempotente: si render falla, transition de estado ya commiteo y operador puede usar reissue para recovery. Wire en `issueFinalSettlementDocumentForCase` + `markFinalSettlementDocumentSignedOrRatifiedForCase`. `renderFinalSettlementDocumentPdf(snapshot, options)` acepta `documentStatus` explícito; matriz canónica de watermark: `rendered/in_review/approved → PROYECTO`, `issued/signed_or_ratified → CLEAN`, `blocked/rejected/voided → tonal error`, `superseded → REEMPLAZADO`. Operador descarga PDF emitido SIN watermark sin click extra ("Reemitir" crea v+1; auto-regen refresca v actual). **Delta V1.5.2 extendió el invariante a las 7 transiciones del state machine con defense-in-depth de 5 capas — ver ADR específico.** |
| Finiquito PDF lifecycle invariant: regen canónico en TODAS las transiciones + defense-in-depth | Accepted | Payroll / HR / Documents / Reliability | [GREENHOUSE_FINAL_SETTLEMENT_V1_SPEC.md](GREENHOUSE_FINAL_SETTLEMENT_V1_SPEC.md#delta-2026-05-11-v152--lifecycle-pdf-defense-in-depth-regen-canonico-en-todas-las-transiciones) | TASK-863 V1.5.2. Extiende V1.1 a las 7 transiciones del state machine (`in_review`, `approved`, `issued`, `signed_or_ratified`, `voided`, `rejected`, `superseded`). Defense-in-depth de 5 capas: (a) helper canónico atómico `regenerateDocumentPdfForStatus` con type union cerrado `DocumentStatusForRegen`, (b) asset metadata canónica `metadata_json.documentStatusAtRender`, (c) `captureWithDomain('payroll', err, { tags: { source, stage } })` reemplaza `console.warn` raw, (d) reliability signal nuevo `payroll.final_settlement_document.pdf_status_drift` (kind=drift, warning si count>0, error si drift>24h, wireup en `getReliabilityOverview.finalSettlementPdfStatusDrift`), (e) test anti-regresión `document-status-regen-invariant.test.ts` parsea source y verifica que TODA `SET document_status = 'X'` tiene call matchedo a helper (rompe build si emerge transition nueva sin regen). Bug detectado por usuario en re-emisión real (Valentina Hoyos d15): doc aprobado mostraba "Borrador HR" + watermark PROYECTO porque solo `issued` + `signed_or_ratified` regeneraban — las 5 transitions restantes dejaban PDF stale. Degradación honesta: si render falla, transition de DB commitea (estado legal source of truth) + Sentry alerta + signal detecta drift hasta reissue. Recovery: operador hace reissue (idempotente). 12 tests verde. |
| Secret-ref normalizer canónico V2 con shape regex en el boundary + reliability signal upstream | Accepted | Secret resolution / Cloud / Reliability / Identity | [CLAUDE.md](../../CLAUDE.md#secret-manager-hygiene) | TASK-870 (2026-05-12). Cierra bug class detectada live durante intentos de Codex de promover `develop → main` (~3h, 4 fix commits sin atacar root cause). `GREENHOUSE_GITHUB_APP_PRIVATE_KEY_SECRET_REF` quedó persistido en Vercel production como `"greenhouse-github-app-private-key\n"` (quotes envolventes + LF literal); `normalizeSecretRefValue` legacy NO stripaba quotes → GCP NOT_FOUND silencioso → `Sentry "is not valid PEM"` burst recurrente → preflight check `sentry_critical_issues` bloqueaba orchestrator. Fix V2 unifica higiene en helper privado `stripEnvVarContamination` (single-source-of-truth aplicado por `normalizeSecretValue` y `normalizeSecretRefValue`) + `SECRET_REF_SHAPE` regex en boundary rechaza shapes inválidos antes de invocar GCP. Reliability signal nuevo `secrets.env_ref_format_drift` (kind=drift, severity=error si count>0, module `cloud`, steady=0) detecta drift activo upstream sin GCP round-trip. `github-app-token-resolver` diferencia "ref corruption" (silent degrade a PAT, sin Sentry spam) de "content corruption" (Sentry capture legítimo). Predicate exportable `isCanonicalSecretRefShape` para auditores externos. 52 tests anti-regresión verde (7 secret-manager incluyendo bug class exacto + 7 signal + 4 resolver). Audit pasivo S0 pre-merge: 21/21 canonical refs (17 production + variantes) pasan V2 regex. Arch verdict M1-M5 obligatorias aplicadas. |
| Client Portal Composition Layer materializa resolver TASK-825 en UI cliente con menú dinámico + page guards resolver-based + 5 empty states honestos | Accepted | Client Portal / UI / Identity / Reliability | [GREENHOUSE_CLIENT_PORTAL_DOMAIN_V1.md](GREENHOUSE_CLIENT_PORTAL_DOMAIN_V1.md#delta-2026-05-13--task-827-composition-layer-cerrada) | TASK-827 (2026-05-13, EPIC-015 child 6/8). 13 commits sin PR ceremony en `develop`. **Patterns canonizados**: (a) Slice 0 — parity TS↔DB para `view_codes` (`VIEW_REGISTRY` + seed migration `role_view_assignments` + parity test live `skipIf(!hasPgConfig)`), (b) Slice 4 — `requireViewCodeAccess` page guard server-only con D1 bypass `tenantType==='efeonce_internal'` + redirect canonical `/home?denied=<slug>` con slug mapping + degradación honesta `?error=resolver_unavailable` via `captureWithDomain('client_portal',...)`, (c) Slice 5 — anatomía 5-elementos canonical empty state (icon + title + body + primary CTA + secondary CTA) consumiendo `<EmptyState>` primitive + microcopy dictionary `GH_CLIENT_PORTAL_COMPOSITION`, (d) Slice 6 — refactor light + audit grep + override marker `// client-portal-allowed:` para lint rule, (e) Slice 8 — reliability signal scaffold V1.0 returns 'unknown' preservando shape canonical para V1.1 telemetry adapter (TASK-829). **7 Open Questions cerradas D1-D7** lens 4-pilar pre-execution. **Regla canónica nueva en CLAUDE.md "View Registry Governance Pattern"**: cualquier viewCode agregado a `VIEW_REGISTRY` TS requiere migration acompañante seed `role_view_assignments` por CADA role que deba acceder. Telemetría `role_view_fallback_used` es el detector canonical de gobernanza pendiente — NUNCA desactivar. **Incident hardening** (commit `2fd8a60c`): Sentry alerts `role_view_fallback_used` (JAVASCRIPT-NEXTJS-4X, 10 warnings) resuelto via seed migration 44 filas (4 roles × 11 viewCodes), NO parche del fallback. 38/38 tests verde + tsc + lint full repo verde (4 warnings esperadas son target sweep V1.1). Migration aplicada live a `greenhouse-pg-dev`. Desbloquea TASK-828 cascade + TASK-829 reliability signals. Skills invocadas: arch-architect (Greenhouse overlay 4-pilar pre-execution + incident resolution), info-architecture, state-design, greenhouse-ux/ux-writing/dev/backend/mockup-builder. |
| Rolling rematerialize usa primitive tipada + integrity check + policy `rolling_window_repair` con escalación honesta | Accepted | Finance / Treasury / Reliability / Ops Worker | [GREENHOUSE_FINANCE_ARCHITECTURE_V1.md](GREENHOUSE_FINANCE_ARCHITECTURE_V1.md#delta-2026-05-13--task-871-rolling-rematerialization-anchor-contract) | TASK-871 (2026-05-13). Cierra el bug class que ISSUE-069 dejó parcial. Nuevas primitives canónicas: `computeRollingRematerializationWindow(today, lookbackDays) → {targetStartDate, seedDate, materializeStartDate, materializeEndDate, lookbackDays, policy}` y `resolveCleanSeedDate({client, accountId, candidateSeedDate, maxExpandDays=30})` que walks backward hasta ancla movement-free o devuelve `ok=false, reason='exceeded_max_expand'` para escalar honestamente a `historical_restatement`. El cron `ops-finance-rematerialize-balances` consume ambas en sequence per-account: protected snapshot? → anchor on it; else integrity check → clean anchor or skip + Sentry. `FxDriftRemediationPolicy` extendido con 5to value `rolling_window_repair` (narrow-scope opposite del `historical_restatement`) usando `seedMode='explicit'` + `block_on_reconciled_drift`. Helper `resolveCleanSeedDate` vive en `src/lib/finance/` (dual-import: cron + remediator). 7 invariantes anti-regresión cubren shape de window, integridad del clean seed, escalation cuando exceed maxExpand, y composición end-to-end para el 2026-05-13 incident shape exacto. Hard rules: nunca `seedDate` con movements canonicos; nunca `seedMode='active_otb'` en rolling; nunca `warn_only` en rolling repair. Reusa contratos TASK-703 OTB + TASK-721 reconciliation snapshots + TASK-766 normalized VIEWs sin modificarlos. ISSUE-069 marcado como "fix parcial; TASK-871 cierra contrato completo". |
| Workforce Exit Payroll Eligibility resolver canonico TS-only + `exitLane` DB-aligned + cutoff `COALESCE(LWD, effective_date)` | Accepted | HR / Payroll / Identity / Reliability | [GREENHOUSE_WORKFORCE_EXIT_PAYROLL_ELIGIBILITY_V1.md](GREENHOUSE_WORKFORCE_EXIT_PAYROLL_ELIGIBILITY_V1.md) | TASK-890 V1 spec entregada (2026-05-15). Nace del caso Maria Camila Hoyos `EO-OFF-2026-0609A520`: lane `external_payroll`/Deel `last_working_day=2026-05-14` status `draft` apareciendo full-month en nomina proyectada porque el gate inline legacy (`pgGetApplicableCompensationVersionsForPeriod`) solo excluia `status='executed' AND last_working_day < periodStart`. **5 decisiones canonicas**: (1) Resolver TS-only `resolveExitEligibilityForMembers` server-only + thin predicate `isMemberInPayrollScope` (pattern TASK-720 `aggregateBankKpis` + TASK-672 safeModes), bulk-first `Map<memberId, Window>`, NO SQL function PG. (2) Asymmetric threshold por lane: `internal_payroll` excluye desde `executed` (preserva contract finiquito Chile TASK-862/863 que requiere doc emitido+ratificado); `external_payroll`/`non_payroll` excluyen desde `approved` (Greenhouse no paga, decision firmada es suficiente). (3) Cutoff canonico = `COALESCE(last_working_day, effective_date)` respeta invariantes CHECK schema TASK-760 (`effective_date_required_check` en approved, `last_working_day_required_check` en scheduled). (4) `exitLane` enum del resolver es **1:1 con `rule_lane` persistido** (NO inventar enum #3; closureLane de derivation.ts queda como UI-display layer). (5) Feature flag `PAYROLL_EXIT_ELIGIBILITY_WINDOW_ENABLED=false` default V1.0 con shadow compare obligatorio pre-activacion. **6 reliability signals nuevos** (`payroll.exit_window.{projection_delta_anomaly, full_month_projection_drift}`, `payroll.exit_eligibility.bq_fallback_invoked`, `hr.offboarding.external_provider_close_failed`, `identity.relationship.member_contract_drift`, `payroll.projected_snapshot.exit_window_stale`). **Lint rule `greenhouse/no-inline-payroll-scope-gate`** modo warn ship Slice 3 (patron TASK-766/TASK-825). **BQ fallback degraded mode honesto** con `captureWithDomain('payroll', warn, { source: 'bq_fallback_no_exit_gate' })` + signal upstream — single source of truth en TS, BQ no replica logica compleja para evitar drift. **Validacion arch-architect Greenhouse overlay**: 2 correcciones criticas aplicadas pre-implementation (cutoff COALESCE en lugar de `last_working_day` solo; enum DB-aligned en lugar de inventar uno nuevo). 13 hard rules anti-regresion. Slices 1-3 ship esta sesion (ADR + resolver + integration); Slices 4-7 (provider closure command + UI + drift signal + docs) sesiones siguientes. |
| Payroll Participation Window compone ingreso, vigencia de compensacion y salida TASK-890 para definir monto elegible por periodo | Accepted (V1 SHIPPED 2026-05-16) | HR / Payroll / Workforce / Reliability | [GREENHOUSE_PAYROLL_PARTICIPATION_WINDOW_V1.md](GREENHOUSE_PAYROLL_PARTICIPATION_WINDOW_V1.md) | TASK-893 V1 SHIPPED 2026-05-16 (Slices 1-6, 9 commits a develop). Cierra el bug class Felipe Zurita: colaborador con inicio mid-month aparecia como full-month (`baseSalary=650000`, `grossTotal=650000`, `prorationFactor=1`). Primitive server-only bulk-first `resolvePayrollParticipationWindowsForMembers()` calcula `eligibleFrom/eligibleTo/policy/reasonCodes/prorationFactor` componiendo `periodStart/periodEnd`, `compensation.effective_from/effective_to`, start de relacion/onboarding cuando exista, y TASK-890 exit cutoff. **Pattern canonical BL-1**: escalar compensation inputs ANTES de `buildPayrollEntry` (no rescale post-hoc) — el calculator canonico recomputa deducciones Chile + gratificacion legal cap mensual (jurisprudencia chilena Opcion A, Art 50 CT + Dictamen DT 2937/050 2002) + retencion SII desde gross prorrateado correctamente. **Helper compartido cross-path** `prorateCompensationForParticipationWindow` consumido por projection (`projectPayrollForPeriod`) Y official (`calculatePayroll`). Regla critica: entry proration NO es attendance; no modelar dias previos al inicio como ausencias. **5 blockers cerrados** convergentes con payroll-auditor + finance-accounting-operator: BL-1 recompute deductions, BL-2 integracion calculatePayroll oficial + `recalculate-entry` single-member bypass guard + skip exclude policy, BL-3 flag dependency TASK-893→TASK-890 enforcement, BL-4 RESOLVED NON-ISSUE (finiquito ya consume `compensation.baseSalary` nominal cross-spec invariant lift), BL-5 reopened recompute guard bajo flag ON. **3 reliability signals** canonicos bajo subsystem `Finance Data Quality`. Feature flag `PAYROLL_PARTICIPATION_WINDOW_ENABLED=false` default; activacion productiva requiere capability `payroll.period.force_recompute` (reclassified V1.1→pre-flag-ON gate), 5 Open Questions HR/Finance/Legal signoff, staging shadow compare ≥7d verde, allowlist explicita members. V1 prorratea por `countWeekdays`; V2 puede migrar a operational calendar. 499/499 payroll + 266/266 reliability tests verde. Cero regresion. |
| Person 360 Relationship Drift Reconciliation Write Path: helper canonico atomic + capability EFEONCE_ADMIN-only + reuse `.deactivated` + `.created` events (NO `.reconciled` nuevo) | Accepted | Identity / HR / Person 360 / Reliability | [GREENHOUSE_PERSON_LEGAL_RELATIONSHIP_RECONCILIATION_V1.md](GREENHOUSE_PERSON_LEGAL_RELATIONSHIP_RECONCILIATION_V1.md) | TASK-891 V1 spec entregada (2026-05-15). Cierra el follow-up declarado en TASK-890 ADR §7. Reusa `endPersonLegalEntityRelationship` + `createContractorLegalEntityRelationship` existentes (TASK-337 helpers) envueltos en `withGreenhousePostgresTransaction` atomic. **6 decisiones canonicas**: (1) Helper canonico atomic REUSE > CREATE — compone primitives existentes en lugar de SQL inline. (2) Capability granular `person.legal_entity_relationships.reconcile_drift` EFEONCE_ADMIN-only V1.0 (delegacion a HR queda V1.1 post 30d steady). (3) Reason >=20 chars (bar mas alto que TASK-890 close_external_provider >=10 porque blast Person 360 cross-domain es mayor). (4) Outbox events: NO crear `.reconciled` v1 nuevo — reusar `.deactivated` + `.created` existentes con `metadata_json.reconciliationContext` para correlation forensic. (5) UI operator-initiated dialog (NUNCA cron / auto / batch en V1.0; V2 contingente con ADR nuevo + HR approval). (6) Auto-escalation severity signal `warning` → `error` post 30d data-driven (lógica condicional `firstDetectedAt + 30d < NOW()`). **13 hard rules** anti-regresion incluyendo NEVER DELETE filas relationship (supersede via `effective_to + status='ended'`), NEVER auto-mutate Person 360 from read path, NEVER expose error.message raw, SIEMPRE atomic tx wrap + metadata_json correlation + notes marker append-only. **No outbox events nuevos. No reliability signals nuevos** (auto-escalation extiende `identity.relationship.member_contract_drift` existente TASK-890 Slice 6). **No migration schema nueva** — solo capability registry seed. Caso fuente disparador: Maria Camila Hoyos. Maria NO mutada dentro de TASK-891 — recovery espera staging synthetic fixture + HR approval + ejecucion via dialog. Slices 1-6 entregan V1.0 fin a fin: ADR + helper + capability + endpoint + UI dialog + severity bump + docs. |
| Leave Accrual Participation-Aware: primitive year-scope canonical compone TASK-893 month-scope + filtra dependent CL para evitar sobreacumulacion feriado legal Art 67 CT | Accepted (V1.1a SHIPPED 2026-05-16) | HR / Leave / Payroll / Reliability | [GREENHOUSE_PAYROLL_PARTICIPATION_WINDOW_V1.md](GREENHOUSE_PAYROLL_PARTICIPATION_WINDOW_V1.md#delta-2026-05-16--task-895-v11a-s0--leave-accrual-participation-aware-adr) | TASK-895 V1.1a SHIPPED 2026-05-16 (Slices 0-5, 5 commits a develop). Cierra bug class regulatorio CL: transiciones contractor → dependent mid-year acumulan feriado legal desde `members.hire_date` (legacy) ignorando que Art 67 CT solo aplica durante vinculo dependent activo, generando sobrepago al finiquito. **6 decisiones canonicas**: (1) NUNCA reescribir helper puro `calculateAccruedLeaveAllowanceDays` legacy — integration ocurre en call site `postgres-leave-store.ts:computeBalanceSeedForYear` via helper local `tryComputeParticipationAwareAllowanceDays` que valida 4 pre-condiciones (flag + accrualType + payRegime + degraded) antes de aplicar fórmula canonical `(annualDays × eligibleDays) / firstServiceCycleDays`. Preserva 7 tests pure verdes + SRP. (2) Boundary semantic: Leave year-scope aggregator compone Payroll month-scope (TASK-893) + Workforce Exit (TASK-890). DAG direction: Leave → Payroll, NUNCA reverse. Anti-corruption layer enforced en barrel `import 'server-only'`. (3) NUNCA extender `PayrollParticipationWindow` con contractType/payRegime para servir a Leave — Leave hace su propia query independiente a `compensation_versions` JOIN `members` + filtra `contract_type IN ('indefinido','plazo_fijo') AND pay_regime='chile' AND payroll_via='internal'`. Compose TASK-893/890 solo para exit cutoff via `WorkforceExitPayrollEligibilityWindow.eligibleTo`. (4) Triple flag dependency canonical: `LEAVE_PARTICIPATION_AWARE_ENABLED=true` REQUIERE `PAYROLL_PARTICIPATION_WINDOW_ENABLED=true` AND `PAYROLL_EXIT_ELIGIBILITY_WINDOW_ENABLED=true`. Helper `isLeaveAccrualParticipationAwareEnabled()` enforce dependency al boundary; sin pre-condicion retorna false → legacy fallback bit-for-bit. (5) `firstServiceCycleDays` preserva denominador legacy exactly (`addDays(addYears(firstDependentEffectiveFrom, 1), -1) - firstDependentEffectiveFrom + 1`) — cuando policy=`full_year_dependent` sin transiciones, formula canonical reproduce legacy bit-for-bit. (6) Mutation auditada queda V1.2 con capability `leave.balances.reconcile` — V1.1a es read-only (resolver + signal + audit script dry-run). **1 reliability signal nuevo** `hr.leave.accrual_overshoot_drift` (kind=drift, severity warning >0, steady=0 post-flag-ON + re-seed). Subsystem rollup `Payroll Data Quality` unificado con TASK-893. Reader pattern SHAPE detector (NO recompute exacto) — identifica miembros con `hire_date` >30 dias antes del MIN(qualifying dependent CL `effective_from`). **Lección canonical hotfix Sentry** aplicada: SQL queries de signal readers DEBEN validar contra PG real via proxy (`pnpm pg:connect:shell` + smoke script) ANTES de mergear. NO confiar en `db.d.ts` TS shapes inferred como ground truth — bug class concreto detectado: `compensation_versions.payroll_via` NO existe en PG real (Kysely codegen drift); `payroll_via` vive en `members`. Y `compensation_versions.effective_from` es `date` no `timestamp` (`date - date = integer`, no `interval`). **Audit script canonical** `scripts/leave/audit-accrual-drift.ts` read-only dry-run reporta drift exacto por miembro. Validacion live staging: 4 miembros CL con drift detectado (3 honorarios con sobreacumulación de 15 días cada uno, total 46.52 días). **33 tests pure verde** + 60 integration verde + 266 reliability verde. Hard rules en CLAUDE.md TASK-895 section. Runbook operativo en `docs/operations/runbooks/leave-accrual-drift-audit.md`. Pre-flag-ON gates: las 3 flags ON + staging audit ≥30d + signal count=0 sustained + HR/Legal signoff. Open questions V1.2: write-path reconciliation auditada, honorarios proporcional opcional, tracking historical de `members.payroll_via`. Zero behavioral change post-merge (flag OFF default preserva legacy bit-for-bit). |
| ICO metrics progressive migration: strangler pattern obligatorio + 8 stop-gates canonical + demo teamspace pre-prod + recovery primitives + 12-14 meses timeline NO acelerable | Accepted | Delivery / ICO / Integrations / Payroll downstream / Reliability | [GREENHOUSE_ICO_METRICS_PROGRESSIVE_MIGRATION_V1.md](GREENHOUSE_ICO_METRICS_PROGRESSIVE_MIGRATION_V1.md) | 2026-05-17. Migración del compute canonical de las 14 métricas ICO desde fórmulas Notion legacy (frágiles) hacia compute canonical Greenhouse con writeback (TASK-901+). **NO big-bang** — strangler obligatorio. Bug class motivador: TASK-877 follow-up (3,168 tareas Sky con `rpa=null` 10 meses, nómina perdía bonus silenciosamente). **3 capas risk surface** distinct velocidades: bonus (RpA+OTD) más lenta 90+ días/métrica, operational visible (FTR/CT/CT SLO%/Throughput/Pipeline Velocity/CSC/Stuck/OCF) media 30-60d/métrica, narrative-level (BCS/TTM/Iteration Velocity) deferred V2 hasta Frame.io+ad platforms. **8 stop-gates obligatorios per flip**: (1) foundation TASK-908 completa, (2) demo teamspace TASK-910 verde 4 semanas runtime, (3) shadow mode prod verde 30d bonus/7d operational, (4) pilot ≤1 cliente (Efeonce primero), (5) HR/Finance written sign-off bonus, (6) snapshot BQ pre-flip restorable, (7) kill switch testeado staging, (8) runbook + cliente sign-off. **Ramp canonical 6 fases**: Fase 0 foundation (T+6 semanas) → Fase 0.5 demo setup paralelo (T+4 semanas TASK-910) → Fase 1 RpA pilot Efeonce (T+10→22 semanas) → Fase 2 RpA Sky (T+22→30) → Fase 3 OTD% (T+30→42) → Fase 4 operational rapid (T+42→50) → Fase 5 resto operational (T+50→58). **Defense in depth 8-layer per flip**: feature flag per-cliente per-métrica + kill switch + reliability signal paridad + HR reconciliation script + snapshot BQ + Sentry alerts + runbook + cliente sign-off. **Recovery primitives pre-instaladas** (4 scripts + dashboard + runbook). **Backward compatibility 90+ días**: formulas Notion legacy NO se borran. **15 hard rules** incluyendo NUNCA acelerar timeline + NUNCA flip global directo + NUNCA rollback parcial per-member. Demo teamspace canonical identificado live 2026-05-17 vía Notion MCP: `Demo Greenhouse` teamspace ID `36339c2f-efe7-814c-a0f5-0042863dbb5a` + 3 databases (Tareas DS `36339c2f-efe7-81a6-980c-000b0056bba8`, Proyectos DS `36339c2f-efe7-8116-8c15-000be81c5538`, Sprints DS `36339c2f-efe7-81cc-8f2f-000b112ee87c`) con cross-references confirmadas + formulas legacy preservadas (RpA Promedio, % On-Time, Client Change Round Final). IDs distintos vs Efeonce/Sky productivos — cero overlap, cero risk cross-contamination. |
| Payroll bonus calculation: RpA + OTD inputs canonical con proration declarativa + thresholds per-tenant + reglas honorarios discrecional | Accepted (V1 SHIPPED desde TASK-758 era; canonical doc 2026-05-17) | HR / Payroll / Delivery / ICO downstream consumer | [GREENHOUSE_PAYROLL_BONUS_CALCULATION_V1.md](GREENHOUSE_PAYROLL_BONUS_CALCULATION_V1.md) | 2026-05-17. Spec canonical de cómo Payroll consume métricas ICO (RpA + OTD%) per-member-month para calcular bonificaciones variables. Las únicas 2 métricas ICO inputs directos bonus V1 son RpA + OTD. **`calculateOtdBonus`** (graduated linear proration 3 zonas: 100% pago si OTD% ≥89%, lineal entre 70-89%, $0 bajo 70%). **`calculateRpaBonus`** (banded inverse proration 4 zonas: 100% pago si RpA ≤1.7, soft band linear 1.7-2.0 baja a 80%, hard band linear 2.0-3.0 baja a 0%, $0 sobre 3.0). Thresholds configurables per-tenant via BQ `payroll_bonus_config` con vigencia temporal; defaults canonical en `bonus-config.ts`. Bridge canonical `fetchKpisForPeriod` strategy `materialized_first_with_live_fallback` (reusa agregados SQL del registry, NO recompute paralelo). Per-member topes en `compensation_versions.bonus_otd_max/bonus_rpa_max`. Reglas especiales declarativas: `honorarios` → `usesDiscretionaryBonuses=true` ($0 automático, manual via `bonus_other_amount`); `deel` → mismas fórmulas suman a `deelGrossTotal`; resto flow canonical normal. Persistencia auditable: `bonus_{otd,rpa}_{amount,proration_factor,min,max}` + `kpi_{otd_percent,rpa_avg}` snapshots reproducibles. Bug class TASK-877 follow-up documentado (3,168 tareas Sky con `rpa=null` 10 meses → toda nómina Sky proyectada perdía bonus RpA silenciosamente; TASK-901 elimina dependencia sync legacy). Lista canonical de 12 métricas NO-input bonus V1 con razones (FTR=double-counting con RpA; CT/CT SLO%=velocidad absoluta no promise; Throughput=quality conflict; Pipeline Velocity/Stuck/OCF/CSC=causa externa al member; Iteration Velocity/BCS/TTM=narrative-level mostly proxy). 11 hard rules anti-regresión incluyendo NUNCA recomputar `otd_pct`/`rpa` en Payroll, NUNCA hardcodear thresholds, NUNCA agregar métrica nueva sin 7 pasos canonical extensión, SIEMPRE persistir snapshot KPI + proration factor reproducible. Sección 13 "Downstream consumers" agregada a 14 specs de métricas (`metrics/<METRIC>_V1.md` §13) — RpA + OTD documentan input bonus completo; 12 restantes documentan razón canonical de NO entrar a bonus V1. |
| 1 métrica crítica = 1 spec canonical en `docs/architecture/metrics/<METRIC>_V1.md` (12 secciones obligatorias) | Accepted | Documentation / Metrics / Delivery / ICO | [GREENHOUSE_METRIC_SPEC_PATTERN_V1.md](GREENHOUSE_METRIC_SPEC_PATTERN_V1.md) | 2026-05-17. Pre-decisión, entender una métrica como RpA requería leer 6 fuentes distintas (Contrato + Delta + Engine doc + código + 2 tasks) — fragmentación insostenible para métricas contractuales con clientes/equipo/management. Pattern canonical: cada métrica crítica tiene spec dedicado con 12 secciones obligatorias (definición / fórmula / inputs / helper canonical / agregado canonical / semántica edge / estados / threshold / writeback / histórico / cross-refs / open questions). Template canonical: `docs/architecture/metrics/_TEMPLATE.md`. Índice maestro: `docs/architecture/metrics/METRICS_INDEX.md` (equivalente a DECISIONS_INDEX para métricas). Migración progresiva (strangler pattern): cuando una task toca métrica sin spec, primer slice crea el spec antes de tocar código. Contrato + Engine doc quedan como narrativa de negocio + framework conceptual (referencian specs, NO redefinen). Código = source of truth canonical; specs reflejan código (drift → actualizar spec inmediatamente). Hard rules: NUNCA crear métrica crítica sin spec desde día 1, NUNCA duplicar definición cross-doc, NUNCA dejar drift conocido sin actualizar spec. Primeras 2 specs creadas V1 simultáneamente: `RPA_V1.md` + `FTR_V1.md`. 9 specs pendientes documentados con status Pending para migración progresiva. Plus 2 specs adicionales planeados en TASK-909 (Throughput + Pipeline Velocity). Defense in depth: convención (este ADR) + CI gate futuro `scripts/ci/metric-spec-coverage-gate.mjs` (warn V1, error post Phase 11) + lint rule futura `greenhouse/no-metric-definition-outside-spec` + code review humano. |
| Delivery Metrics Ownership Boundary: Notion = task OS, Greenhouse ICO Engine = motor exclusivo de métricas con writeback canonical | Accepted | Delivery / ICO / Integrations / Reliability | [GREENHOUSE_DELIVERY_METRICS_OWNERSHIP_BOUNDARY_V1.md](GREENHOUSE_DELIVERY_METRICS_OWNERSHIP_BOUNDARY_V1.md) | 2026-05-17. Cierra el bug class TASK-877 follow-up donde fórmulas ICO viven como propiedades formula Notion editables por cualquier operador (sin git history, tests, code review, observabilidad). Notion conserva ownership de datos operativos primitivos (asignación, fechas, estado, tipo de entregable, archivos) y UI de gestión; Greenhouse computa TODAS las métricas (RpA, OTD, FTR, Cumplimiento, Cycle Time, Throughput, Pipeline Velocity, BCS, TTM, Iteration Velocity) desde eventos canonical y escribe los valores de vuelta a Notion vía bulk PATCH a propiedades `[GH] <métrica>` read-only. **Semántica canonical de corrección**: 1 corrección = 1 transición `Listo para revisión → En Feedback` (no rondas internas, no comentarios). RpA canonical = `countCorrectionTransitions(taskId)` que lee `greenhouse_delivery.task_status_transitions` (TASK-908 foundation). FTR canonical delega a `calculateRpa(taskId) === 0`. **Migración progresiva canonical** (strangler pattern): TASK-908 foundation → TASK-901 RpA V1 → TASK-902 OTD V2 → TASK-903 FTR V3 → TASK-904 Cumplimiento V4 → TASK-905+ resto progresivamente. Cada Vn ship con shadow mode 7d verde antes de activar writeback. Hard rules: NUNCA introducir formula nueva en Notion para métrica ICO, NUNCA computar métrica leyendo property Notion derivable de eventos canonical, NUNCA consumer downstream recomputa inline. Cross-refs: `Contrato_Metricas_ICO_v1.md` Delta 2026-05-17 sección F+G; CLAUDE.md sección "Delivery Metrics Ownership Boundary invariants". |
| Task Status Lifecycle canonical: 11 estados universales en property `Estado` compartida cross-tenant, sin per-tenant mapping ni adapter layer | Accepted | Delivery / ICO / Notion / cross-tenant operativo | [GREENHOUSE_TASK_STATUS_LIFECYCLE_V1.md](GREENHOUSE_TASK_STATUS_LIFECYCLE_V1.md) | 2026-05-17. Cierra **Bomba 1** detectada en sesión live: schemas Notion divergían cross-tenant (Efeonce `Estado` con `Cambios Solicitados`/`Listo`/`Cancelada`/`Archivadas`/`Listo para diseñar`/`Pendiente Dir. Arte`/`Detenido` vs Sky `Estado 1` con `En feedback`/`Aprobado`/`Archivado`/`Tomado` sin `Cancelado` ni los 3 Efeonce-only). El "evento canonical de corrección" (`Listo para revisión → En Feedback`) solo existía en Sky — rompía estructuralmente la premisa del ADR RpA V2 firmado horas antes. **Path B canonical adoptado**: templates Notion unificados con vocabulary canonical universal en lugar de adapter layer Greenhouse-side. **11 estados canonical**: `Sin empezar`, `Brief listo`, `En curso`, `Listo para revisión`, `Cambios solicitados`, `Aprobado`, `Pendiente aprobación interna`, `En pausa`, `Bloqueado`, `Cancelado`, `Archivado`. Property unificada `Estado` (Sky renombra de `Estado 1`). **Evento canonical de corrección universal**: transición `Listo para revisión → Cambios solicitados` funciona idéntico en todos los tenants. **Estado `Tomado` Sky eliminado** (era tag de responsable mal puesto como status; vive en campo `Responsable`). **Onboarding cliente nuevo** = clone del template canonical Notion (Demo Greenhouse), sin customización vocabulary. **8 hard rules anti-regresión** incluyendo NUNCA introducir estado fuera de los 11 canonical, NUNCA usar property name distinto de `Estado`, NUNCA usar status como tag de responsable/tipo/prioridad (esos viven en otros campos). Migration Notion (operador-side, ~1-2 días): Efeonce hace 7 renames; Sky hace 1 rename de property + 1 rename de status + agregar 4 estados nuevos + eliminar `Tomado`. Skills invocadas pre-decisión: `arch-architect` (DDD anti-corruption layer evaluation) + `greenhouse-ico` (boundary canonical) + `notion-platform` (schema inspection live via Notion MCP). 8 open questions deliberadamente NO decididas (signal drift, localización futura, `Brief listo` adoption real, etc.). Es precondición canonical de: ADR RpA V2 strangler (Delta aplicado), TASK-908 (foundation simplificado sin columnas dobles canonical), TASK-901 (Fase 0 nueva pre-A), TASK-910 (demo teamspace template post-cleanup). |
| RpA V2 strangler migration: V1 (formula Notion) y V2 (helper canonical Greenhouse) coexisten en paralelo durante 5-7 meses; cutover bonus gated por feature flag per-tenant reversible <5min; cleanup V1 OPCIONAL post 90+d stable | Accepted (con Delta 2026-05-17 post-Bomba-1) | Delivery / ICO / Integrations / Payroll downstream / Reliability | [GREENHOUSE_RPA_V2_STRANGLER_MIGRATION_V1.md](GREENHOUSE_RPA_V2_STRANGLER_MIGRATION_V1.md) | 2026-05-17. ADR canonical que formaliza el strangler fig pattern (Fowler 2004) para mover compute de métrica RpA desde Notion formula → Greenhouse helper canonical sin tocar bonus payroll productivo. **5 Fases canonical**: Fase A build paralelo V2 invisible → Fase B shadow mode + paridad signal ≥95% sustained 7d → Fase C writeback `[GH] RpA v2` visible operador (bonus sigue V1) → Fase D cutover bonus gradual per-tenant (Efeonce primero 30d observación + HR reconciliation, después Sky) → Fase E cleanup V1 OPCIONAL post 90+d stable (puede deferirse indefinidamente como defensive depth permanente). **Naming canonical V2**: helper `calculateRpaV2`, BQ column `rpa_avg_v2`, Notion property `[GH] RpA v2`, raw BigQuery echo `notion_ops.tareas.gh_rpa_v2` (audit/parity only, not compute source), signals sufijo `_v2`, feature flags `NOTION_RPA_V2_COMPUTE_ENABLED`/`NOTION_RPA_V2_WRITEBACK_ENABLED`/`BONUS_USE_RPA_V2`. **Garantía operativa central**: cutover bonus = una línea de código gated por flag + reversible <5min via env var. V1 path (formula Notion + sync legacy + `metrics_by_member.rpa_avg` + `calculateRpaBonus`) NUNCA se toca durante toda la migration. **Signal canonical paridad cross-version** `notion.metrics.rpa_v2_vs_v1_paridad` mide V1 vs V2 per-member-month durante shadow + cutover — gate ≥95% sustained 7d pre-flip bonus. **10 hard rules anti-regresión** incluyendo NEVER flip global directo, NEVER cleanup pre-Fase D + 6m, ALWAYS HR reconciliation mes 1 post-flip antes de extender tenant. Bug class fuente: TASK-877 follow-up (3,168 tareas Sky con `rpa=null` 10 meses, nómina perdía bonus silenciosamente). **Template canonical reusable** por TASK-902 (OTD V2 strangler) / TASK-903 (FTR V2) / TASK-904 (Cumplimiento V2) / TASK-905+ progresivamente — heredan naming + 5 fases + 5-pillar ICO scoring + hard rules. Extiende ADRs `GREENHOUSE_DELIVERY_METRICS_OWNERSHIP_BOUNDARY_V1` + `GREENHOUSE_ICO_METRICS_PROGRESSIVE_MIGRATION_V1` + `GREENHOUSE_METRIC_SPEC_PATTERN_V1` + `GREENHOUSE_PAYROLL_BONUS_CALCULATION_V1`. Skills invocadas pre-decisión: `arch-architect` Greenhouse overlay (4-pillar) + `greenhouse-ico` (5-pillar + strangler mandate) + `notion-platform` (5-pillar + boundary). Implementation owner: TASK-901; bloqueada por TASK-908 + TASK-910. |
| Ecosystem Access Control Plane: Greenhouse owns desired state, sister platforms own applied state, drift cierra el loop | Accepted | Platform / Identity / Ecosystem (Kortex / Verk / Sitio publico) | [GREENHOUSE_ECOSYSTEM_ACCESS_CONTROL_PLANE_V1.md](GREENHOUSE_ECOSYSTEM_ACCESS_CONTROL_PLANE_V1.md) | TASK-884 V1 spec entregada (2026-05-15). Doctrina central: Greenhouse owns ecosystem **desired** access state; sister platforms may own local provisioning mechanics, but must converge or report explicit drift. Tres estados ortogonales `desired`/`observed`/`applied`. Cuatro platform modes por binding (`greenhouse_managed`, `hybrid_approval`, `platform_managed_observed`, `read_only_observed`) — modo es per binding, no per platform global. Siete drift types canonicos (`pending_provisioning`, `pending_deprovisioning`, `unauthorized_local_access`, `missing_identity_link`, `scope_mismatch`, `capability_mismatch`, `platform_apply_failed`) con severity matriz que depende del modo (helper `resolveDriftSeverity`). Subject types: internal_collaborator + client_user + service_account + external_partner, siempre anclados a `identity_profile_id`. Scope types extendidos con `platform_workspace` y `platform_installation`. Registries ortogonales: `ecosystem_platform_capabilities` (catalogo plataforma con prefix `kortex.*` / `verk.*` / `public_website.*`) NUNCA se mezcla con `capabilities_registry` interno (lint rule canonica). 11 capabilities Greenhouse-internas least-privilege para gobernar el control plane. 14 outbox events versionados v1. 9 reliability signals bajo subsystem nuevo `Ecosystem Access`. Defense in depth 7 capas (patron TASK-742). Hosting dispatcher: Cloud Scheduler + ops-worker (async_critical TASK-775). Sister platforms reciben commands via webhook outbound (subscriptions filtradas por `platform_key`) o polling endpoint. Tasks downstream: TASK-885 (registry + catalog) → TASK-886 (desired state + 5 capabilities granulares + UNIQUE PARTIAL INDEX active + idempotency_key SHA256) → TASK-887 (observed + drift + `resolveDriftSeverity` + 7 signals) → TASK-888 (commands + 2 signals + 5 events v1 + Cloud Scheduler) → TASK-889 (Admin Center + Kortex pilot). Doctrina extiende `GREENHOUSE_SISTER_PLATFORMS_INTEGRATION_CONTRACT_V1.md` §9 (action boundary write) sin paralelizar identidades canonicas. |
| Greenhouse acts as identity broker for Kortex SSO via authorization-code lane | Accepted | Platform / Identity / Ecosystem / Kortex | [GREENHOUSE_KORTEX_INTEGRATION_ARCHITECTURE_V1.md](GREENHOUSE_KORTEX_INTEGRATION_ARCHITECTURE_V1.md#17-architecture-decision-2026-05-28--greenhouse-identity-broker-for-kortex-sso) | TASK-948; validated with `software-architect-2026` and official Microsoft/Auth.js OAuth guidance. Greenhouse authenticates via existing providers (Microsoft SSO/Google/credentials), issues a one-time authorization code, and Kortex exchanges it server-to-server for a minimal identity/access payload. No passwords, password hashes, Greenhouse cookies, Microsoft tokens or upstream refresh tokens leave Greenhouse. Explicit no-regression boundary: no changes to existing SSO providers, SCIM, Entra provisioning, Graph sync, global session callbacks or Greenhouse identity lifecycle without a separate ADR/task. |
| Nexa Insights detail routing canonical: top-level `/nexa/insights/[id]` (NO bajo route_group de dominio) + dispatch prefix signal-anchored / enrichment-anchored + 12 UI states honest-degradation + capability dedicada `nexa.insights.read` | Accepted (V1 spec, en TASK-947 to-do) | UI / Delivery / Identity / Cross-domain | [GREENHOUSE_NEXA_INSIGHTS_LAYER_V1.md](GREENHOUSE_NEXA_INSIGHTS_LAYER_V1.md#delta-2026-05-28--task-947-detail-page-canonical-nexainsightsid) | TASK-947 (2026-05-28). **Decisión arquitectónica cross-domain** post arch-architect Greenhouse 4-pillar + greenhouse-ico 5-pillar eval del bug class live 2026-05-28 (404 sistemático `/agency/insights/EO-AIE-*` cuando operador clickea "Ver causa raíz" en Home Nexa Insights bento). **Causa raíz**: drift TASK-696 (Smart Home v2 redesign construyó el CTA `drillHref` apuntando a página que nunca se materializó). **Decisiones canonical**: (1) Routing top-level `/nexa/insights/[id]` (NO `/agency/insights/*` — Nexa es lane cross-domain como `/admin/...`, no dominio); aliases `/finance/insights/[id]` reusan el mismo shell. (2) Drill key con dispatch prefix: `EO-AIS-*` signal-anchored (default cards "Ver causa raíz" — estable cross-period TASK-943 append-only-aware) + `EO-AIE-*` enrichment-anchored (TASK-449 share permalinks — semántica "esto vio el operador") + `EO-FAIE-*` Finance sibling. (3) Capability dedicada `nexa.insights.read` (module `delivery`, scope `tenant`) NO heredada de route_group; grant dual-plane TASK-873 mismo PR. (4) Helper canonical `readNexaInsightDrill(id, subject)` server-only con 3-tier lookup current→history→notFound + subject-aware filter + discriminated union 5 states. (5) `notFound()` cuando subject sin acceso (anti-oracle existencia, mirror TASK-872 RCA). (6) 12 UI states honest-degradation canonical mapeados (TASK-946 framework canonized aquí, propaga a 5 surfaces post). (7) Banner amber explícito cuando enrichment superseded — NUNCA redirect transparente (preserva share semantics). **14 hard rules** anti-regresión incluyendo NUNCA `/agency/insights/*`, NUNCA drawer per-surface, NUNCA `403` (anti-oracle), NUNCA colapsar a "Sin datos" ambiguo (cierra último gap UX de ISSUE-082). **Composabilidad**: desbloquea TASK-449 (Read/Pin/Dismiss/Share inline) + TASK-944 (Finance toggle alias) + TASK-945 (lifecycle timeline embebido + time-travel `?at=`) + TASK-946 (propagation phase post-canonization) + Weekly Executive Digest deep-links (funcionales por primera vez) + Teams notifications. **Reliability signal V1.1** `home.insights.drill_404_rate` (kind=drift, warning >0.5% / error >5%, steady=0). Spec output: Delta a `GREENHOUSE_NEXA_INSIGHTS_LAYER_V1.md` §9-18 + Deltas a TASK-449/944/945/946 marcando convergencia canonical. Skills invocadas pre-decisión: `arch-architect` Greenhouse overlay (4-pillar Safety/Robustness/Resilience/Scalability) + `greenhouse-ico` (5-pillar canonical dichotomy + append-only-aware reasoning). |
| Dicotomía canonical de persistencia: estado mutable → MERGE; observación histórica → append-only event log (NO "estable vs volátil") | Accepted | Data / Cross-domain (ICO / Delivery / Outbox / Audit / Finance) | [GREENHOUSE_ICO_MATERIALIZER_HARDENING_V1.md](GREENHOUSE_ICO_MATERIALIZER_HARDENING_V1.md#delta-2026-05-28--task-943-rectificación-append-only-event-log-para-observaciones-históricas) | TASK-943 (2026-05-28). **Decisión cross-domain reutilizable** canonizada al rectificar el modelo erróneo del Delta TASK-942 2026-05-27 ("set volátil → full-replace") sobre `ico_engine.ai_signals`. La pregunta canónica al decidir patrón de persistencia para una entidad nueva NO es "¿el set cambia entre runs?" — es **"¿el dato responde a `cuál es el valor actual` o a `qué se vio cuando`?"**. Si responde a "valor actual" (`metrics_by_member.otd_pct`, `account_balances.closing_balance`, `compensation_versions`) → **MERGE upsert por key** (TASK-900 ICO Materializer Hardening). Si responde a "qué se vio cuando" (`ai_signals`, `ai_prediction_log`, `task_status_transitions` TASK-908, outbox `*_events` TASK-773, audit logs TASK-742, `task_due_date_changes` TASK-921, `task_status_transitions_demo` TASK-910, `release_state_transitions` TASK-848, `member_role_title_audit_log` TASK-785) → **Append-only event log + VIEW canonical para current state** (latest-per-key via `ROW_NUMBER() OVER PARTITION BY key ORDER BY timestamp DESC = 1`). **3 invariantes canonical**: (1) writers append-only INSERT-only sin DELETE; (2) consumers que necesitan "current state" leen VIEW canonical; consumers que necesitan historia evolutiva leen raw; (3) excepciones mutables se documentan explícitamente con guards (e.g. `hydratePredictionActuals` permite UPDATE de `actual_value`+`actual_recorded_at`+`error_pct` SOLO con `WHERE IS NULL` + período pasado). **Beneficios canonical**: auditoría reproducible desde cualquier punto temporal, análisis evolutivo intra-período (sprints 15d), pipeline 100% no-destructivo by construction, eliminación de la complejidad MERGE+generation-stamp+GC cuando el modelo es event-sourced. **Bug class fuente** (BUG-CLASS-004 en `~/.claude/skills/greenhouse-ico/reference/bug-class-catalog.md`): TASK-942 modeló `ai_signals` como "set volátil → full-replace" y aplicó cron rolling 3m con DELETE+INSERT — borraba evidencia ya capturada Y recomputaba meses cerrados. Operador identificó el error de framing: las anomalías son observaciones temporales con valor histórico. **Heartbeat signal canonical** para reemplazar la observabilidad implícita del DELETE+INSERT (run vacío = visible): cuando un set migra a append-only, agregar un signal `<dominio>.no_new_<entity>_in_24h` (kind=lag, warning >24h, error >48h) para detectar cron-down — el silencio no es default-OK en append-only. **Hard rules adicionales canonical** (CLAUDE.md "Nexa AI Signals append-only event log invariants"): NUNCA DELETE sobre raw event-log tables (excepto cleanup manual auditado con capability); SIEMPRE leer current state via VIEW; cron scope solo período actual; única excepción mutable documentada con guards. **Patrón replicable** para futuros downstream (Frame.io ingestion, HubSpot webhook events history, Nubox transaction observations): si la entidad nueva responde "qué se vio cuando", nacer como append-only + VIEW current desde día 1, NO usar replace semantics aunque "el set parezca volátil intra-run". Skills invocadas pre-decisión: `arch-architect` Greenhouse overlay (4-pillar) + `greenhouse-ico` (5-pillar canonical dichotomy) + `greenhouse-backend` (BQ schema patterns) + `state-design` + `dataviz-design` (UI consequences habilitadas — task derivada TASK-945 timeline). Implementation: TASK-943 V1 SHIPPED 2026-05-28 (7 slices + closing gate canonical + 4 Cloud Run workers redeployed verificados). |

## Decisiones propuestas / en discusion

Estas decisiones todavia no gobiernan runtime. Sirven para discovery, research y review antes de abrir tasks de implementacion.

| Decision | Status | Scope | Canonical doc | Source / notes |
| --- | --- | --- | --- | --- |
| Creative Video Studio como control plane Greenhouse sobre HyperFrames para generar, revisar y exportar videos de marca | Proposed | Platform / AI Tooling / Creative Operations / Content / Asset Library / Render Jobs / UI | [GREENHOUSE_CREATIVE_VIDEO_STUDIO_V1.md](GREENHOUSE_CREATIVE_VIDEO_STUDIO_V1.md) | Abierto 2026-06-02 desde piloto local `videos/efeoncepro-promo/`: website capture + DESIGN/SCRIPT/STORYBOARD + HTML animado + VO + snapshots + HyperFrames preview. Implementacion futura: [TASK-996](../tasks/to-do/TASK-996-creative-video-studio.md). |
| Workforce Payables Control Plane como capa read-only/projection-first para operar pagos de workforce sin fusionar rails legales | Proposed | Finance / Treasury / Payroll / HR / Contractors / Notifications / Reliability / UI | [GREENHOUSE_WORKFORCE_PAYABLES_CONTROL_PLANE_V1.md](GREENHOUSE_WORKFORCE_PAYABLES_CONTROL_PLANE_V1.md) | Abierto 2026-06-02 desde auditoria de sinergias Payroll + Contractor Payables. Converge en `payment_obligations -> payment_orders -> settlement -> artifacts/notifications`; no reemplaza Payroll, Contractors ni Payment Orders. Implementacion futura: [TASK-994](../tasks/to-do/TASK-994-workforce-payables-control-plane.md). |
| Greenhouse debe promover MXN a finance_core con native/functional/reporting/settlement planes y FX snapshots auditables, no como enum edit | Accepted | Finance / Treasury / Commercial / Nubox / Payment Orders / Reporting / Data | [GREENHOUSE_MULTI_CURRENCY_FINANCE_CORE_V1.md](GREENHOUSE_MULTI_CURRENCY_FINANCE_CORE_V1.md) | Aceptado 2026-06-02 (operador) tras review arch+finance. Caso Grupo Berel/Nubox export `28800562` (`MXN 89.960`, `CLP 4.617.647`). Decisiones clave: USD reporting via CLP funcional (IAS 21, §8.4); functional CLP = valor legal Nubox para income, computado para gastos (§5.4); settlement Berel = cuenta Global66 MXN nativa (§0/§9.1, distinta de la Global66 CLP→USD de payroll). Implementacion: [TASK-990](../tasks/in-progress/TASK-990-mxn-multi-currency-finance-core.md). |
| Greenhouse debe converger a una fundacion workforce persona-centrica: una persona, multiples rails legales/payroll/finance/compliance | Proposed | HR / Payroll / Finance / Identity / Platform / Person 360 / Workforce reporting / AI context | [GREENHOUSE_UNIFIED_WORKFORCE_FOUNDATION_DECISION_V1.md](GREENHOUSE_UNIFIED_WORKFORCE_FOUNDATION_DECISION_V1.md) | Abierto 2026-05-31 desde [RESEARCH-008](../research/RESEARCH-008-unified-workforce-foundation.md) + [GREENHOUSE_UNIFIED_WORKFORCE_FOUNDATION_V1.md](GREENHOUSE_UNIFIED_WORKFORCE_FOUNDATION_V1.md). No autoriza tasks ni runtime changes hasta aceptacion explicita. |

## Pendientes de formalizacion retroactiva

Estas decisiones existen en runtime o docs, pero pueden merecer ADR mas explicito cuando una task futura las toque:

| Decision candidate | Why it matters | Suggested canonical home |
| --- | --- | --- |
| Runtime projections como capa server-side canonica para surfaces complejas | Evita heuristicas React y drift entre UI, reliability y backend | Spec de cada dominio o doc transversal de runtime projections |
| Event/outbox compatibility entre eventos genericos y eventos granulares | Define contratos de idempotencia y fan-out | `GREENHOUSE_EVENT_CATALOG_V1.md` |
| Preview generic env baseline vs branch overrides | Evita deploys rojos por secrets solo en `Preview (develop)` | `RELEASE_CHANNELS_OPERATING_MODEL_V1.md` o cloud governance |
| Access design dual-plane obligatorio | Muchas tasks deben distinguir `views` vs `entitlements` | `GREENHOUSE_ENTITLEMENTS_AUTHORIZATION_ARCHITECTURE_V1.md` |
