# TASK-1485 — Globe Pattern Lab Wireframe

```text
┌ Globe Pattern Lab ─ version ─ theme ─ viewport ─ motion ─ audience ──┐
│ Registry: candidate | trial | stable | deprecated                    │
├──────────────────────┬──────────────────────────────────────────────┤
│ Pattern navigator     │ Live specimen / anatomy / slots          │
│ globe.shell           │ states + variants + responsive controls  │
│ globe.context-rail    │ keyboard/a11y/content/motion evidence    │
├──────────────────────┴──────────────────────────────────────────────┤
│ Consumers | version history | evidence | do / don't | migration     │
└──────────────────────────────────────────────────────────────────────┘
```

Mobile usa navigator->specimen->evidence sin tabla comprimida. Fixtures mínimos: default, dense, empty,
loading, partial, error, denied, long content, 200% zoom, RTL-readiness cuando corresponda, reduced motion.
