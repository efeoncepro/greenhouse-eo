# TASK-1852 — Rollout autorizado, 2026-09-09

## Resultado vigente — Production

**Rollout técnico completado; apertura cliente pendiente.** PR #231/main `5726ce9d90d7a0bb3e9ed6aaab011b50351430d2`, orquestador `34416904936` success y manifest `5726ce9d90d7-61ae6901-a225-46bc-b993-2b671a12ce1d` **released** a las **23:38:36.029Z**. [Readback final](TASK-1852_PRODUCTION_RELEASE_READBACK_2026-09-09.json): Vercel READY, cinco servicios Ready con 100 % de tráfico, health 200, watchdog ok sin drift/datos ausentes y siete casos HTTP passed.

Altas nuevas OFF por instrucción del operador. Se preservaron las cinco filas de asignación, incluida una histórica con vigencia terminada; no se confunde status con vigencia. El servicio real de Berel sigue activo con importes ausentes NULL. El operador ya eligió tres personas Berel y confirmó tres personas Sky; las primeras aún no existen como usuarios Greenhouse y ninguna de las seis tiene login certificado en esta task. Mapping comercial, login/rutas/canales y paridad de escritura delegada siguen pendientes; TASK-1852 permanece in-progress. Las secciones siguientes conservan la cronología, incluidos los bloqueos resueltos.

## Alcance confirmado

El operador confirmó el alcance de las dos cuentas y ordenó «Haz el rollout». La evidencia completa de entregables, cantidades, responsabilidades de publicación y equipo está en `.captures/task-1852/private-scope/berel-sky.v1.json`, ignorada por Git y con permisos 0600. El repositorio público conserva únicamente el contrato técnico y los blockers del preview.

El [manifiesto de cohorte](../../operations/client-service-enablement/berel-sky.v1.json) mantiene IDs verificados y referencia local al alcance. La solicitud autoriza el despliegue necesario; no autoriza envíos, invitaciones ni publicación de contenido. La implementación y el rollout se hicieron secuencialmente en develop. Una instrucción posterior autorizó tres subagentes read-only sólo para consolidar esta documentación.

## Conciliación real antes de publicar

Lectura PostgreSQL read-only a las **22:08:16Z**:

- Berel tiene organización y space activos, pero cero servicios y cero términos. Sólo aparece el usuario técnico de diagnóstico. Su Company HubSpot es `55405407542`.
- Sky conserva `SVC-HS-551519372424`, regular/activo, sin términos; los servicios archivados y el piloto finalizado no se reutilizan como cobertura.
- Los tres integrantes del equipo confirmado ya tienen asignaciones activas a la cuenta de Sky; IDs en la evidencia local. No se crean asignaciones duplicadas ni se confunden con usuarios cliente.
- Las tres personas cliente activas de Sky conservan su pertenencia; no hay último login registrado. Esto no prueba un login fallido ni uno exitoso.

La confirmación del operador resuelve qué se presta; **no materializa por sí misma** los registros Commercial/HubSpot ni certifica acceso humano o canales. El conector HubSpot informa `SERVICE` pendiente de reautorización. El helper legado tenía una referencia de credencial inválida; el resolver canónico de TASK-1230 permitió verificar el servicio existente en HubSpot a las 22:16:11Z. Se corrigió el helper para reutilizar ese resolver, sin nueva credencial. No se usan escrituras SQL ad hoc de servicios o términos. El writer existente de términos no acepta `bundled_modules`: ese gap de la superficie comercial debe resolverse antes de usarlo para abrir acceso.

## Corrección necesaria para compensar

`GREENHOUSE_CLIENT_PORTAL_DOMAIN_V1.md` §capabilities ya concede `client_portal.module.pause:update` a EFEONCE_ADMIN. El registro runtime omitía ese permiso aunque concedía altas. Se alinea únicamente el default de EFEONCE_ADMIN, sin nuevos roles/personas ni grants cliente. Prueba conductual: antes, alta permitida y compensación 403; después ambas pasan para el mismo administrador vigente. Inactivos, invitados, clientes y actores sin privilegios siguen denegados en las tres operaciones; App/Nexa mantienen la denegación de escrituras agent.

## Verificación de la versión

- 312 tests / 30 archivos: passed, cero omitidos; incluye 14 pruebas en PostgreSQL local real.
- Permiso de compensación: prueba roja reproducida y corregida; suite runtime/authority/parity verde.
- Build local del dominio: passed. Los últimos cambios de intake pasaron sus 80 tests, lint y TypeScript; el build Vercel del SHA final también pasó. Gates de task y cierre documental: exit 0; warnings documentales preexistentes. Context strict: cero errores/warnings antes del commit. `project_context.md` revisado: el router vigente cubre el dominio; no requiere agregar estado de sesión.
- Commit publicado en develop: `68e18fe0be8ea40085d3904d9ddfa4730a57a2ba`; [PR 231](https://github.com/efeoncepro/greenhouse-eo/pull/231), draft. Staging READY: `dpl_LiNfR32CGUm75A9QwmWT6iwM6dDm` en ese SHA. Production conserva `45f6910e3ae35cad5d775eaabf9ce6e15d156d6e`; no se ha promovido main ni iniciado un manifest nuevo.
- Preflight configurado: PG/migraciones/WIF GCP+Azure/Sentry verdes. `release_batch_policy=requires_break_glass` por `src/lib/entitlements/runtime.ts`; no se usa override. El cambio completa el permiso de compensación ya declarado por el canon, pero el classifier trata esa ruta como irreversible. El runbook §2.2 limita break-glass a incidente productivo activo: esta promoción requiere decisión explícita sobre esa excepción, no una repetición de la autorización de rollout.
- El diff previo develop/main es documental; `services/auth-server/README.md` cae bajo el trigger real de Auth Server Deploy. Variables GitHub de multi-org verificadas: enabled=true, cohorte limitada a Julio. No se amplía la cohorte ni se modifica la configuración.

## Estado y siguiente verificación

Preparación de despliegue de la capacidad común. Berel ya tiene el servicio canónico reflejado; el primer baseline de esta auditoría se conserva fechado. `CLIENT_SERVICE_ENABLEMENT_WRITES_ENABLED` conserva su default OFF hasta certificar contrato, sesión humana y compensación en destino. Apply cliente no ejecutado; no se declara la task completa. Persisten registro/mapping comercial, destinatarios Berel, login/rutas y canales; TASK-1687 conserva el destino de Sky y TASK-1834 la convergencia de entrada. No se sustituye la certificación de personas por la sesión técnica.

El primer push se detuvo antes de publicar al confirmar la visibilidad pública del repositorio. Remote develop seguía en `e1375df19`; el commit local se saneó antes de reintentar. Los detalles nuevos del cliente no se publicaron.

## Conciliación del servicio Berel — 22:20:31Z

El reader canónico verificó `554261764224` asociado a Company `55405407542`. El command existente `upsertServiceFromHubSpot` materializó **una** fila `SVC-HS-554261764224` en su organización y space verificados, con audit/outbox, `regular`, `onboarding`, `active` y `synced`. No se creó un servicio nuevo en HubSpot ni se duplicó el contrato. No se modificó Sky.

El source no informa importes. El normalizador anterior los convertía en cero: siete casos negativos reprodujeron ese defecto. Se corrige a NULL para ausencia/blank/NaN/infinito, preservando el cero explícito y valores numéricos válidos. Schema real acepta NULL en ambos campos. Readback PostgreSQL: `total_cost IS NULL` y `amount_paid IS NULL`. El default físico de moneda sigue siendo el contrato previo del schema, no prueba una moneda contractual. No se crearon términos ni un precio.

80 tests adicionales del intake/servicios pasan en seis archivos (más los 312 de habilitación); lint focal verde. El primer push publicado no ocurrió: la conexión SSH cayó tras local:check verde. Reintento con keepalive, manteniendo todos los hooks. El rollback del feature conserva el reflejo del servicio real; no borra fuentes comerciales ni historial.

## Canary HTTP de staging — 22:28:32Z

Siete casos pasaron en el deployment exacto `https://greenhouse-7ll4vuiwt-efeonce-7670142f.vercel.app`: preview de cada organización 200 con `canApply=false` y `commercial_mapping_unresolved`; actor suministrado en body 400; personas de otra organización rechazadas sin devolver sus datos; organización inexistente 404; anónimo 401; escritura desde sesión técnica 403 `invalid_delegated_context`. Actor dedicado de diagnóstico: no certifica login humano ni autoridad delegada de escritura. Evidencia local: `.captures/task-1852/staging-http-smoke.json`. El smoke Playwright de GitHub `34411937857` también terminó success.

## Cloud Run compartido y recuperación de tráfico

El push a develop desplegó `ops-worker-00672-jmj`, `commercial-cost-worker-00574-xnf` e `ico-batch-worker-00383-spz`, con Ready=True y 100 % de tráfico, todos en `68e18fe0…`. Sus digests y condiciones están en `.captures/task-1852/staging-workers-readback.json`. Estos servicios son compartidos: el portal Production conserva su SHA previo, pero sí hubo cambio de workers.

Auth Server Deploy `34411937795` falló en el primer intento: construyó `auth-server-00049-pvz`, pero el tráfico seguía fijado a `auth-server-00048-4vq`. El gate de SHA detectó la revisión anterior; no fue un fallo de compilación ni se ignoró el resultado. Se compararon ambas configuraciones: sólo cambia `GIT_SHA`; mismos secretos, service account, flags y cohorte. Se completó el routing autorizado con `update-traffic --to-latest` a las 22:33:04Z. Readback: 100 % en `00049-pvz`, Ready=True, digest `sha256:e2572350dc8a30a3dfd015cdb1248db8fbf58feb32f89ff6297a2dac072edeae`, SHA `68e18fe0…`; `/readyz` 200 con PostgreSQL/KMS/activeKey `ok`. El segundo intento del mismo run terminó success: reconoció SHA/configuración coincidentes y verificó health/Ready sin reconstruir. No se cambió el script. Los otros tres workflows también terminaron success.

## Promoción preparada y límites

La excepción propuesta afecta únicamente el permiso `client_portal.module.pause:update` de EFEONCE_ADMIN necesario para compensar altas propias. Owner humano: Julio Reyes, pendiente de decisión explícita sobre la excepción. Reversión: restaurar el deployment Production `dpl_2uGoEoujQLnsnR8UogFye5x9JjSL` y revisiones previas por el control plane (`ops-worker-00671-jj8`, `commercial-cost-worker-00573-846`, `ico-batch-worker-00382-sn6`, `auth-server-00048-4vq`; digests verificados en `.captures/task-1852/rollback-revisions.json`); como las altas nuevas siguen OFF y no se ejecutó apply, no hay asignaciones nuevas que deshacer. La fila fiel de servicio Berel y su audit/outbox se conservan.

Antes de promover: CI y jobs de staging terminales verdes, preflight actualizado y decisión del operador sobre §2.2. Después: merge del PR, vigilancia del SHA main exacto, CI/CI Deep/Vercel READY, orquestador único, gates, manifest/health/watchdog y canary HTTP en Production. El write flag permanece OFF; contratos/mapping, destinatarios Berel, login/rutas/canales y autoridad delegada siguen siendo pendientes de apertura operativa.

La evidencia de esta fase queda como WIP documental local para conservar el candidato publicado y su staging READY. Publicar estas notas después de resolver la promoción; un push docs-only previo cambia el HEAD y puede cancelar el build de staging por Ignored Build Step (runbook §2.3). No crear otro worktree ni cambiar de branch al retomar.

## Cierre de preparación — 22:51Z

CI push `34411937833` success; CI PR `34412246451` success, con tests terminados 22:44:42Z y build final 22:50:00Z. Los 14 runs asociados al candidato terminaron success (Auth Server Deploy en su segundo intento). Preflight final: 11 checks ok; único error `release_batch_policy=requires_break_glass`, sin override. Production y staging reconsultados conservan los deployments/SHA registrados; main y develop remotos coinciden con sus valores previstos. El write flag sigue ausente en Production tras nuevo env pull (default OFF). No hay orquestador nuevo ni otro run activo en la lista consultada.

Task abierta; siguiente decisión: excepción puntual de §2.2 para el permiso de compensación ya probado. El rollout autorizado sigue vigente. Si se concede la excepción, continuar por el orquestador con razón explícita, sin convertirla en incidente ficticio ni omitir la evidencia del SHA main.

## Excepción autorizada y continuación — 23:00Z

El operador respondió «Ok autorizado» a la excepción puntual para promover el permiso de compensación del administrador manteniendo apagadas las altas nuevas. Owner humano: Julio Reyes. Esta autorización excepciona para TASK-1852 el límite de incidente activo de §2.2; no declara un incidente ni amplía el scope. Se conserva el candidato `68e18fe0`, el rollback documentado y el orquestador canónico. La razón se registra en el override de preflight y en la transición del manifest.

PR #231 integrado por squash a las 23:02:08Z: `5726ce9d90d7a0bb3e9ed6aaab011b50351430d2`. Ambos commits (candidato y main) tienen árbol `aaff5f5b8680d3e9df867503e97e29ad1519075b`; diff de árbol completo vacío. Watch acotado iniciado inmediatamente; CI `34415072772`, CI Deep `34415072635` y smoke manual del SHA main `34415084221`. Production deployment `dpl_mfME7kGqyyyD5tgY2sVsVZVc42ti` en construcción. Sin otro manifest/run activo antes del merge.

### Canary del portal en Production

Vercel READY observado a las 23:12:21Z; alias productivo verificado contra `5726ce9d90` y `dpl_mfME7kGqyyyD5tgY2sVsVZVc42ti`. Nueva sesión técnica por el mecanismo estándar de diagnóstico; siete casos HTTP passed en el runtime productivo. [Readback fechado](TASK-1852_PRODUCTION_HTTP_READBACK_2026-09-09.json): previews 200, actor en body 400, personas ajenas excluidas, organización ausente 404, anónimo 401 y escritura agent 403. Confirma el contrato nuevo servido; no prueba login humano ni aplica asignaciones. Esta observación precede el orquestador; su cierre y los workers se verifican por separado.

### Orquestador iniciado

CI y CI Deep del SHA main, smoke y Vercel READY verificados antes del dispatch. Consulta fresca de manifest/run: ninguno activo; main sigue en el SHA previsto. Orquestador único [34416904936](https://github.com/efeoncepro/greenhouse-eo/actions/runs/34416904936) iniciado a las 23:25:58Z con excepción autorizada, `force_infra_deploy=false` y razón explícita que conserva writes OFF. GitHub ejecuta con el actor autenticado `cesargrowth11`; Julio Reyes es el owner humano de la aprobación. No se atribuye a la sesión técnica una identidad app humana. El preflight del orquestador usa su checkout del SHA exacto; desde 23:15 hay WIP ajeno TASK-1604 en el checkout local que se preserva.

## Verificación final y cierre de la excepción

Preflight fresco: doce checks, once ok y únicamente el warning de batch policy autorizado. Se inspeccionó el artefacto antes de aprobar los dos gates Production, a las 23:29:16Z y 23:29:39Z. La razón y el actor del override están persistidos tanto en el manifest como en su primera transición. No se declaró incidente, no se omitieron otros controles ni se amplió la autoridad cliente.

Lectura de tráfico a las 23:36:56Z: `commercial-cost-worker-00575-qlj`, `ico-batch-worker-00384-6mz` y `hubspot-greenhouse-integration-00180-j24` sirven el SHA main exacto. `ops-worker-00672-jmj` y `auth-server-00049-pvz` conservan `68e18fe0` por change-gate: diff del árbol completo contra main vacío, Ready=True y 100 % de tráfico en las revisiones inspeccionadas. Los cinco digests están en el readback final. Auth conserva flags y cohorte; Azure verificó health y `no_infra_diff`, sin aplicar Bicep.

`/api/auth/health` y `auth.efeonce.org/readyz`: 200/ready; proveedores Azure AD, Google y credenciales ready, PostgreSQL/KMS/activeKey ok. Canary repetido 23:36:57Z: siete casos passed. Health del workflow success 23:37:12Z. Manifest released 23:38:36.029Z y workflow success 23:38:44Z. Readback PostgreSQL posterior 23:39:21Z confirma estado, transiciones, excepción y preservación de asignaciones/servicio. Los campos `workflow_runs`, `worker_revisions`, URL Vercel y health del manifest permanecen vacíos; no se completan por SQL ni se presentan como evidencia que no contienen. La prueba de esos componentes se conserva separada, vinculada al SHA y run.

Watchdog local final 23:39:17Z: tres señales ok, cinco workers verificados, `drift_count=0`, `data_missing_count=0`; sin Teams. Una lectura durante el release abierto comparó correctamente los workers nuevos contra el último release todavía cerrado (`45f6910e`); se conservó esa observación y se repitió después del cierre, sin ejecutar la recomendación de redeploy hacia la versión anterior.

Post-mortem de la excepción: la falta inicial era la omisión runtime de un permiso de compensación ya canónico. Se reprodujo y corrigió con prueba de comportamiento, se mantuvieron los gates del SHA exacto y la excepción quedó acotada a esta promoción. No hay rollback pendiente ni nuevas asignaciones que compensar. Permanece el rollback documentado; futuras aperturas requieren contrato/mapping, destinatarios, sesión humana y canales verificados. Detalles comerciales y equipo del operador continúan únicamente en evidencia local privada.

Documentación de operación, disponibilidad funcional, task/índices, flags, tiempos, changelog y handoff actualizada. La task permanece abierta por la certificación cliente; la capacidad técnica común ya está en producción.

Gates documentales: task lint 0 errores/0 warnings; cierre acotado a los 13 paths propios sin warnings; feature-flag audit strict e índice Creative Studio verdes. `ops:lint --changed` exit 0 con 13 warnings preexistentes de paridad de epics ajenos. Context strict 0 errores/0 warnings; se repite como último gate después de esta edición. WIP TASK-1604, incluidos sus hunks en los índices compartidos, queda fuera del commit de este rollout.

## Consolidación para discovery de Claude — 23:56Z

Tres auditorías read-only autorizadas revisaron la superficie API/MCP, la cohorte y las rutas documentales.
El [dossier canónico](TASK-1852_CLAUDE_DISCOVERY_2026-09-09.md) fija el orden de lectura y el estado vigente.

- Berel: tres contactos elegidos por el operador y verificados en HubSpot; búsqueda exacta por email en
  Greenhouse devuelve cero usuarios. Requieren provisión/invitación, identity link y login. El usuario técnico
  del preview queda excluido como destinatario.
- Sky: tres usuarios cliente confirmados, activos y con identity profile; `password_reset_pending` y sin
  `last_login_at`. El equipo interno de delivery no se confunde con estos destinatarios.
- Paridad: App, CLI, Nexa y MCP interno reutilizan el mismo primitive. Ecosystem/MCP apply y rollback siguen
  en 403 por falta de autoridad humana atribuible; las tools no estaban federadas en el gateway. Full API Parity
  operativa de escritura no está cerrada. *Delta 2026-09-10 ~05:30Z: federadas y canal completo, ver §Canal MCP delegado.*
- PII, emails e IDs CRM permanecen en `.captures/task-1852/private-scope/claude-discovery.v1.md`, ignorado por
  Git y `0600`. La consolidación no activó flags, creó usuarios, cambió asignaciones ni envió mensajes.

## Continuación 2026-09-10 — mapping comercial, provisión Berel y autoridad delegada

Instrucción del operador: completar el mapping servicio → módulos, provisionar a las tres personas Berel, usar sólo los
tres usuarios activos de Sky, completar Full API Parity de apply/rollback con autoridad humana delegada, mantener las
altas OFF hasta un preview limpio, probar compensación, activar y aplicar por cuenta, sin enviar mensajes.
Evidencia saneada: [readback](TASK-1852_MAPPING_PROVISIONING_READBACK_2026-09-10.json); detalle privado en
`.captures/task-1852/cohort-*.json` y `production-*.json` (0600).

### Datos aplicados (shared DB, primitives canónicos, sin SQL ad hoc)

- **Términos** (`declareCommercialTerms`, `declared_by=user-agent-e2e-001` por instrucción del operador, audit
  `declared` + outbox `service.engagement.declared` publicados): Berel `SVC-HS-554261764224` →
  `5bf801c0-7cb3-4ab3-9b8f-f0b702f4a86d`, `bundled_modules=[ai_visibility_v1, seo_v2]`; Sky `SVC-HS-551519372424` →
  `7c26c8df-f409-4e34-bef7-d225aaf9f022`, `bundled_modules=[creative_hub_globe_v1]`. Ambos `committed`, `effective_from
  2026-09-09`, `monthly_amount_clp NULL` (la fuente no informa importes; nunca 0). Marketing de contenidos no tiene módulo en
  el catálogo de 14 módulos: queda declarado en la razón del término, sin módulo, para decisión de Commercial/Product.
- **Berel, tres personas** (`inviteClientPortalUser`, `delivery: 'deferred'`, `onExisting: 'ensure'`, rol
  `client_executive`, actor técnico): creadas `status=invited`/`auth_mode=invited`, roles activos con outbox
  `role.assigned` publicado, **cero tokens y cero correos**. IDs `60418418-…`, `61ad9fd3-…`, `c76bdfb4-…`.
- **Migración** `20260910005222927` aplicada (única pendiente; la de TASK-1604 ya estaba aplicada por su dueño):
  siembra el cliente OAuth confidencial `efeonce-mcp-client-services` (`client_services.enablement.write`), inerte hasta
  allowlist/Entra/gateway (los tres aplicados el 2026-09-10, ver §Canal MCP delegado).

### Preview y certificaciones

- Runtime productivo `5726ce9d90`, actor técnico interno, 2026-09-10T01:03Z: **Sky `canApply=true`**, fingerprint
  `05d8f4b799930567c915ed129245f0caafc68dad76ec361b78261b6a4668d433`, un cambio `preserve` sobre
  `cpma-ec0041f7-…` (no se crearía ninguna asignación nueva). **Berel `canApply=false`** únicamente por las tres personas
  (`person_not_authorized_in_organization` en el runtime desplegado; el candidato local las reporta como
  `person_invitation_pending`), con `preserve` de `seo_v2` y `ai_visibility_v1`. `commercial_mapping_unresolved` desapareció
  en ambas. Apply con la sesión técnica: `403 invalid_delegated_context` (contrato intacto).
- Rutas Berel en producción con la persona técnica de la organización (no una persona cliente): `/growth/seo`,
  `/growth/seo/report`, `/aeo`, `/home` → 200; `/campanas` → 307 `denied`; `/creative-hub` → 404. Certifica el mapping de la
  organización, no el login humano.
- Sky: no existe persona técnica ligada a su cliente y no se impersona a personas reales; `/creative-hub` (bundle
  `cliente.creative_hub`) sigue 404 en producción, propiedad de TASK-1687. Canales: cero `teams_notification_channels` y cero
  preferencias en ambas organizaciones.

### Lo que queda fuera de este corte y por qué

- **Apply, compensación y flag ON:** el contrato exige una persona administradora (`app_session` o `delegated_oauth`);
  la única sesión disponible en el harness es la técnica de diagnóstico, que por diseño no aprueba writes. Comando listo
  para el operador (Sky), con flag `CLIENT_SERVICE_ENABLEMENT_WRITES_ENABLED=true` en Vercel Production y redeploy previo:

  ```sh
  pnpm exec tsx scripts/client-portal/service-enablement.ts --base-url https://greenhouse.efeoncepro.com \
    --operation apply --input .captures/task-1852/private-scope/sky-apply.json --token-env GREENHOUSE_APP_ACCESS_TOKEN
  ```

  donde `sky-apply.json` = `{proposal: <request Sky del manifiesto>, fingerprint: "05d8f4b7…", idempotencyKey: "task1852-sky-apply-1"}`
  tras releer el preview (un cambio de configuración invalida el fingerprint). La compensación usa el `operationId` del
  recibo; como el apply sólo `preserve`, pausará cero asignaciones.
- **Berel:** entregar las invitaciones es un mensaje; requiere autorización explícita (`POST …/portal-users/deliver`).
- **Contrato delegado:** implementado y probado en local (tests de adapter, token exchange y suite PostgreSQL local, 14/14).
  El release `f69b9d32` lo llevó a Production; allowlist del consumer, scope Entra `efeonce.mcp.client_services.write` y
  federación en `efeonce-mcp` se aplicaron después (ver §Canal MCP delegado).
- Sin push, sin mensajes, sin cambios de flags; WIP ajeno de TASK-1604 preservado en el checkout compartido.

### Destinos Teams — 2026-09-10T01:35Z

El operador entregó los chats grupales de Berel y Sky y confirmó que las notificaciones salen por el Teams bot. Se
registraron como `recipient_kind='chat_group'` por Space (`client-teams-chat-<space>`), tras relajar con migración
`20260910013234351` el CHECK legado que exigía `team_id`/`channel_id` a todo `teams_bot`. El writer inspeccionó cada chat
por Graph (GET, sin mensajes) y confirmó que el bot está instalado: ambos `ready`. Preview de Sky en producción tras el
registro: `canApply=true`, fingerprint `a6811c4ce775d5d78dd0ead0ea5536cbd768071669bf3a570ef2ad0f145a0e18` (cambió
porque cambió la configuración; `sky-apply.json` actualizado); `teams_destination_unverified` desapareció en ambas
cuentas. **Decisión del operador:** las invitaciones de Berel no se entregan hasta que las interfaces estén listas.

## Release 2026-09-10 — apertura Berel/Sky en producción

Autorización del operador: «Todo lo que sigue terminalo» y «Si haces release pasa todo develop a main». Preferencias y
cadencia delegadas al agente («algo razonable») → política `client_service_default_v1` aplicada a las seis personas.

- **Secuencia:** V1 = sólo el squash `5726ce9d9` → `git merge origin/main -s ours` (árbol idéntico) → push `develop`
  `6807406bd7` 01:50:28Z → PR #232 (17 checks pass) → squash-merge 02:16:19Z → `main` `f69b9d326a6fe25316b6638a5463f3a19c862758`
  (árbol idéntico al de develop) → smoke `playwright.yml --ref main` `34428819048` success → CI + CI Deep + Vercel Production
  READY (`greenhouse-naxc5guq3-efeonce-7670142f.vercel.app`) → dispatch `34431792218` 03:02:40Z con `bypass_preflight_reason`
  (db_migrations `20260910005222927` y `20260910013234351` ya en `pgmigrations`) → gates `Production` 03:06:59Z y 03:07:47Z →
  manifest `f69b9d326a6f-5616d4ef-0b31-4972-9cc7-527a16ef18aa` **released** 03:16:23.904Z; transiciones
  preflight→ready→deploying→verifying→released con la razón del override persistida.
- **Workers:** `commercial-cost-worker`, `ico-batch-worker`, `hubspot-greenhouse-integration` en `f69b9d32`;
  `ops-worker` y `auth-server` conservan `6807406bd7` con diff de árbol completo = 0 archivos (no-op legítimo). Azure
  `no_infra_diff`. Watchdog: 3 señales ok, 5/5 workers synced.
- **Flag:** `CLIENT_SERVICE_ENABLEMENT_WRITES_ENABLED=true` en Vercel Production desde ~01:52Z (antes del merge), horneada
  en el build del release; `pnpm flags:audit` sin drift. Runtime lector: sólo Vercel.
- **Canary de contrato (producción, 5/5, ~03:20Z):** preview Berel sirve el blocker nuevo `person_invitation_pending`;
  preview Sky `canApply=true` con fingerprint `e2e5d938a6c3221bf514c4305f9dc40eb5d915357dc317a2e4a9c1709779ffcc`, destino
  Teams `ready` y sin `preferences_not_explicit`; `GET /api/platform/app/commercial/services/SVC-HS-551519372424/terms` devuelve
  el término con `bundledModules=[creative_hub_globe_v1]`; declarar términos y aplicar con la sesión técnica → `403
  invalid_delegated_context` aunque la flag esté ON (el contrato de autoridad humana sigue intacto).
- **Fuera del release, con dueño:** el apply de Sky requiere una persona administradora (payload listo en
  `.captures/task-1852/private-scope/sky-apply.json`, fingerprint `e2e5d938…`); entrega de invitaciones Berel bloqueada por
  el operador hasta tener interfaces; canal delegado MCP completado ~05:30Z (ver §Canal MCP delegado); `cliente.creative_hub` sigue 404 y pasa a
  construirse en TASK-1857 (decisión del operador: Creative Hub ES el módulo de Sky; TASK-1687 no supersede el bundle).

## Canal MCP delegado — 2026-09-10 ~05:30Z

- **Entra:** scope `efeonce.mcp.client_services.write` (consentimiento Admin) registrado en la app recurso MCP
  `c5363215-b9a6-4bf1-bb1c-e61963b37dac`; el cliente público PKCE `32617b87-…` no se tocó.
- **Vercel Production:** `GREENHOUSE_SISTER_PLATFORM_OAUTH_ALLOWED_CONSUMERS=efeonce-mcp-hiring,efeonce-mcp-hiring-review,efeonce-mcp-client-services`,
  horneada por el redeploy `greenhouse-naxc5guq3` (READY); `CLIENT_SERVICE_ENABLEMENT_WRITES_ENABLED=true` confirmado con
  `vercel env pull` (valor, no presencia).
- **Gateway `efeonce-mcp`:** PR #9 (`e457c31b`, 1.4.0, superficie 43 tools) federa el provider `greenhouse-client-services`
  con `preview_/apply_/rollback_client_service_enablement` (política de autoridad nativa `unsupported` + scope propio;
  exchange RFC 8693 hacia el lane App), desplegado en `efeonce-mcp-gateway-00051-c7b` (run 34440656341). PR #10
  (`80ea8d74`) corrige `efeonce.gateway.status`, que omitía el provider nuevo, desplegado en `00052-slt` (run 34441487000,
  100 % del tráfico). Repo vars `GREENHOUSE_CLIENT_SERVICES_PROVIDER_ENABLED=true`, `_API_URL`, `_TOKEN_EXCHANGE_URL`; bypass
  secret montado. 174 tests del gateway verdes.
- **Verificación en producción:** `efeonce.gateway.status` (llamada real desde el conector interno tras el deploy) lista
  `greenhouse-client-services` `task-1852-v1` `enabled`; `gcloud run services describe` confirma `00052-slt` sirviendo 100 %.
- **Lo que falta y por qué:** `pnpm client-services:canary` exige un bearer Entra de una persona con el scope nuevo
  (authorization code + PKCE interactivo); no existe forma desatendida de mintearlo y ninguna sesión `agent` aprueba writes.
  La primera ejecución punta a punta (preview de Sky vía gateway y, si el operador lo decide, el apply) la hace una persona
  administradora. Estado honesto: **federado y verificado en superficie, sin canary de escritura**. Ningún apply se ejecutó.

